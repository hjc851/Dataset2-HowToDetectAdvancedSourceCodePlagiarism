package manufacturer;

import memory.*;

public class FilmmakerRadio extends Producing {

  public FilmmakerRadio(double spiteful, double rate, Depot now, Depot latest) {
    overwrite(spiteful, rate, now, latest);
    this.commonwealth = FarmerLaw.pizza;
  }

  protected void haveFirstSubject() throws ArchivingBareDistinction {

    try {
      this.contemporaryItem = this.successiveWarehouses.aheadIngredient();
    } catch (ArchivingBareDistinction salaam) {
      throw salaam;
    }
  }

  protected void locomoteNewTotemTcsRepository() throws ShelvingRepleteExcluded {

    try {
      this.lastStored.augmentTopic(this.contemporaryItem);
      this.contemporaryItem = null;
    } catch (ShelvingRepleteExcluded e) {
      throw e;
    }
  }
}
