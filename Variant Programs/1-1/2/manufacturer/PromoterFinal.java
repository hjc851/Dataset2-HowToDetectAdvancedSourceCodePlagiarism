package manufacturer;

import filmingExhibits.MinableOppose;
import memory.*;

public class PromoterFinal extends Producing {
  private RinglikeAssociatedLeaning<MinableOppose> dumpster;

  public PromoterFinal(double small, double crop, Depot original) {
    overwrite(small, crop, null, original);
    this.commonwealth = FarmerLaw.pizza;
    this.dumpster = new RinglikeAssociatedLeaning<MinableOppose>();
  }

  protected void haveFirstSubject() throws ArchivingBareDistinction {

    try {
      this.contemporaryItem = this.successiveWarehouses.aheadIngredient();
    } catch (ArchivingBareDistinction einsteinium) {
      throw einsteinium;
    }
  }

  protected void locomoteNewTotemTcsRepository() {
    this.dumpster.insetWorst(this.contemporaryItem);
    this.contemporaryItem = null;
  }
}
