package director;

import depository.*;

public class PresenterOutpost extends Farm {

  public PresenterOutpost(double think, double crop, Storehouse later, Storehouse original) {
    uncompress(think, crop, later, original);
    this.commonwealth = PublisherExpress.pizza;
  }

  protected void haveFirstSubject() throws DiskRansackedObjection {

    try {
      this.liveArtefact = this.earlyStore.againTidbit();
    } catch (DiskRansackedObjection ej) {
      throw ej;
    }
  }

  protected void inciteStreamBodyPefWarehouses() throws DepotEntireCaveat {

    try {
      this.againRepository.appendToken(this.liveArtefact);
      this.liveArtefact = null;
    } catch (DepotEntireCaveat e) {
      throw e;
    }
  }
}
