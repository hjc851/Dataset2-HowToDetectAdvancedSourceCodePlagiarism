package director;

import supplyArtefact.PerformableObjection;
import depository.*;

public class FarmRun extends Farm {

  public FarmRun(double poor, double graze, Storehouse third) {
    uncompress(poor, graze, third, null);
    this.commonwealth = PublisherExpress.serving;
  }

  protected void haveFirstSubject() throws DiskRansackedObjection {
    this.liveArtefact = new PerformableObjection();
  }

  protected void inciteStreamBodyPefWarehouses() throws DepotEntireCaveat {

    try {
      this.againRepository.appendToken(this.liveArtefact);
      this.liveArtefact = null;
    } catch (DepotEntireCaveat einsteinium) {
      throw einsteinium;
    }
  }
}
