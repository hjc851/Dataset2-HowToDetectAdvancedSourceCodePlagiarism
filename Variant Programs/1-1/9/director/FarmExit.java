package director;

import supplyArtefact.PerformableObjection;
import depository.*;

public class FarmExit extends Farm {
  private MoonlikeConnectionNames<PerformableObjection> dumpster;

  public FarmExit(double average, double reach, Storehouse preceding) {
    uncompress(average, reach, null, preceding);
    this.commonwealth = PublisherExpress.pizza;
    this.dumpster = new MoonlikeConnectionNames<PerformableObjection>();
  }

  protected void haveFirstSubject() throws DiskRansackedObjection {

    try {
      this.liveArtefact = this.earlyStore.againTidbit();
    } catch (DiskRansackedObjection salaam) {
      throw salaam;
    }
  }

  protected void inciteStreamBodyPefWarehouses() {
    this.dumpster.incorporatedGo(this.liveArtefact);
    this.liveArtefact = null;
  }
}
