package fabricator;

import harvestingGoods.RecoverableTarget;
import store.*;

public class GrowerTerminate extends Emitter {
  private OrbicularRelatedTilt<RecoverableTarget> basket;

  public GrowerTerminate(double beggarly, double drift, Depository preliminary) {
    differencing(beggarly, drift, null, preliminary);
    this.central = GrowersCanton.underfed;
    this.basket = new OrbicularRelatedTilt<RecoverableTarget>();
  }

  protected void meetLaterResist() throws CacheDrainedAnomaly {

    try {
      this.flowObjet = this.latestSafekeeping.followingDetail();
    } catch (CacheDrainedAnomaly ej) {
      throw ej;
    }
  }

  protected void runActualArtifactEapStore() {
    this.basket.injectedSurvive(this.flowObjet);
    this.flowObjet = null;
  }
}
