package fabricator;

import plantCelestial.CalcicPurpose;
import warehouses.*;

public class ManufacturerBegin extends Supplier {

  public ManufacturerBegin(double meanspirited, double reach, Space second) {
    overwrite(meanspirited, reach, second, null);
    this.territory = FilmmakerSay.struggling;
  }

  protected void encounterUpcomingCavil() throws InventoryAbandonRule {
    this.newTotem = new CalcicPurpose();
  }

  protected void propelOngoingAimCoughMemory() throws WarehousesOverfullExempted {

    try {
      this.succeedingStore.totalElement(this.newTotem);
      this.newTotem = null;
    } catch (WarehousesOverfullExempted einsteinium) {
      throw einsteinium;
    }
  }
}
