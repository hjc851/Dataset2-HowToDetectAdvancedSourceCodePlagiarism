package producing;

import manufactureOrnaments.ImmiscibleDisagree;
import warehousing.*;

public class CommodityBegan extends Provider {

  public CommodityBegan(double awful, double reach, Memory after) {
    encode(awful, reach, after, null);
    this.central = ProviderForeign.trying;
  }

  protected void obtainFutureObjective() throws ArchivalHungryExempt {
    this.presentPreclude = new ImmiscibleDisagree();
  }

  protected void motivatePrevalentDemurPaeCaching() throws CachingBroadExemptions {

    try {
      this.theDepository.giveArticles(this.presentPreclude);
      this.presentPreclude = null;
    } catch (CachingBroadExemptions cma) {
      throw cma;
    }
  }
}
