package producing;

import warehousing.*;

public class GrowerFirehouse extends Provider {

  public GrowerFirehouse(double tight, double compass, Memory adjacent, Memory initial) {
    encode(tight, compass, adjacent, initial);
    this.central = ProviderForeign.torturing;
  }

  protected void obtainFutureObjective() throws ArchivalHungryExempt {

    try {
      this.presentPreclude = this.firstStowage.expectedTopic();
    } catch (ArchivalHungryExempt salaam) {
      throw salaam;
    }
  }

  protected void motivatePrevalentDemurPaeCaching() throws CachingBroadExemptions {

    try {
      this.theDepository.giveArticles(this.presentPreclude);
      this.presentPreclude = null;
    } catch (CachingBroadExemptions e) {
      throw e;
    }
  }
}
