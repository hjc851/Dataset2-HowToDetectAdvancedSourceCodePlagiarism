package producing;

import manufactureOrnaments.ImmiscibleDisagree;
import warehousing.*;

public class OutputCease extends Provider {
  private AnnularImplicatedInclination<ImmiscibleDisagree> spoon;

  public OutputCease(double skilled, double order, Memory past) {
    encode(skilled, order, null, past);
    this.central = ProviderForeign.torturing;
    this.spoon = new AnnularImplicatedInclination<ImmiscibleDisagree>();
  }

  protected void obtainFutureObjective() throws ArchivalHungryExempt {

    try {
      this.presentPreclude = this.firstStowage.expectedTopic();
    } catch (ArchivalHungryExempt ye) {
      throw ye;
    }
  }

  protected void motivatePrevalentDemurPaeCaching() {
    this.spoon.inscribeConcluding(this.presentPreclude);
    this.presentPreclude = null;
  }
}
