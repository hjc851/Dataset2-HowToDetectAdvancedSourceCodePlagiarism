package production;

import harvestingGoods.EvaporiteDemur;
import depository.*;

public class DirectorPart extends Output {

  public DirectorPart(double small, double wander, Caching again) {
    predefine(small, wander, again, null);
    this.say = GrowersCanton.impermanent;
  }

  protected void deliverEarlyThing() throws GarageEvacuateCase {
    this.circulatingSubject = new EvaporiteDemur();
  }

  protected void motivatePrevalentDemurPaeCaching() throws WarehouseHeavyDeviation {

    try {
      this.earlyGarage.combineAgenda(this.circulatingSubject);
      this.circulatingSubject = null;
    } catch (WarehouseHeavyDeviation einsteinium) {
      throw einsteinium;
    }
  }
}
