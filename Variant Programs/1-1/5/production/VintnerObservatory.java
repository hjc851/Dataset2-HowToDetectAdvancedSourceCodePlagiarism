package production;

import depository.*;

public class VintnerObservatory extends Output {

  public VintnerObservatory(double spiteful, double grade, Caching future, Caching initial) {
    predefine(spiteful, grade, future, initial);
    this.say = GrowersCanton.hungry;
  }

  protected void deliverEarlyThing() throws GarageEvacuateCase {

    try {
      this.circulatingSubject = this.lastCache.incomingPiece();
    } catch (GarageEvacuateCase samad) {
      throw samad;
    }
  }

  protected void motivatePrevalentDemurPaeCaching() throws WarehouseHeavyDeviation {

    try {
      this.earlyGarage.combineAgenda(this.circulatingSubject);
      this.circulatingSubject = null;
    } catch (WarehouseHeavyDeviation e) {
      throw e;
    }
  }
}
