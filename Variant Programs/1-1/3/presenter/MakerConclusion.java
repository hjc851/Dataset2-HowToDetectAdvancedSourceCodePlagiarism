package presenter;

import producersArtifacts.EvaporiteDemur;
import stored.*;

public class MakerConclusion extends Production {
  private GlobularCorrelatedBlacklist<EvaporiteDemur> pipe;

  public MakerConclusion(double miserly, double browse, Shelving pre) {
    inputted(miserly, browse, null, pre);
    this.central = GrowerTell.orphaned;
    this.pipe = new GlobularCorrelatedBlacklist<EvaporiteDemur>();
  }

  protected void encounterUpcomingCavil() throws DiskRansackedObjection {

    try {
      this.liveArtefact = this.predecessorInventory.laterGoods();
    } catch (DiskRansackedObjection cma) {
      throw cma;
    }
  }

  protected void pushExistingPurposeOfficesArchival() {
    this.pipe.addLatter(this.liveArtefact);
    this.liveArtefact = null;
  }
}
