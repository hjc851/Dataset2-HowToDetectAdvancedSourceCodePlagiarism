public abstract class ReplacingScheme {

  protected abstract boolean isComplete();

  protected abstract boolean ensureAppeals(Negotiations afootSummons);

  protected abstract void supplySummary(Annexes postscript, Negotiations thisMethods);
}
