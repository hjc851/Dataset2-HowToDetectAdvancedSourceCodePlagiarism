public abstract class ReplacedPlaybook {

  protected abstract boolean isComplete();

  protected abstract boolean ensureAppeals(Summons topicalAppendage);

  protected abstract void injectLayouts(Webpage writes, Summons continuingMechanisms);
}
