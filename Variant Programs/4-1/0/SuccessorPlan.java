public abstract class SuccessorPlan {

  protected abstract boolean isComplete();

  protected abstract boolean stayDecision(Mechanism flowProcedures);

  protected abstract void provideLayout(Sheet writes, Mechanism presentNegotiations);
}
