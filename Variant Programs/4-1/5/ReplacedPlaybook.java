public abstract class ReplacedPlaybook {

  protected abstract boolean isComplete();

  protected abstract boolean correspondRequisition(Procedure contemporaryLitigate);

  protected abstract void extendFolio(Paper cern, Procedure afootSummons);
}
