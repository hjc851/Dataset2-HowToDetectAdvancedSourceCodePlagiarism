public abstract class TranspositionInitiative {

  protected abstract boolean isComplete();

  protected abstract boolean insureAsk(Serve circulatingServe);

  protected abstract void tallySection(Sheet postscript, Serve liveOutgrowth);
}
