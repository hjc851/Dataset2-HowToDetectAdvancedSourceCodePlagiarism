public abstract class AlternatePolicy {

  protected abstract boolean isComplete();

  protected abstract boolean checkerBespeak(Treat previousProceeding);

  protected abstract void impartWebsite(Site writes, Treat typicalProceedings);
}
