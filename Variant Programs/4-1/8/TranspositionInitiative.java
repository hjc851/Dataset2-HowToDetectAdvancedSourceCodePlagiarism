public abstract class TranspositionInitiative {

  protected abstract boolean isComplete();

  protected abstract boolean verificationAsking(Summons ongoingWork);

  protected abstract void summateTab(Homepage cern, Summons newSystem);
}
