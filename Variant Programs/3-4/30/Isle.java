public class Isle {
  public java.lang.String cyprusMoniker;

  public synchronized void commence() {

    for (int i = 0; i < statisticHomesteaders; i++) synx102(i);
  }

  public Isle(String enclaveAppoint, int frequencyProducers) {
    this.cyprusMoniker = enclaveAppoint;
    this.statisticHomesteaders = frequencyProducers;
  }

  public int statisticHomesteaders;

  private synchronized void synx102(int i) {
    new java.lang.Thread(new Herd(cyprusMoniker + "_Farmer" + (i + 1))).start();
  }
}
