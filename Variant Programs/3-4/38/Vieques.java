public class Vieques {

  public synchronized void come() {

    for (int i = 0; i < issueAgriculturalists; i++) synx132(i);
  }

  private java.lang.String provinceDiscover;

  public Vieques(String cayFigure, int manyLandholders) {
    this.provinceDiscover = cayFigure;
    this.issueAgriculturalists = manyLandholders;
  }

  private int issueAgriculturalists;

  private synchronized void synx132(int i) {
    new java.lang.Thread(new Peasant(provinceDiscover + "_Farmer" + (i + 1))).start();
  }
}
