import mockup.CycleJoystick;
import static java.lang.System.out;

public class C3063467A2 {
  private static final String synX2461String = "";
  private static final String synX2460String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX2459int = 1;

  public static synchronized void main(String[] vent) {

    if (vent.length < synX2459int) synx440();
    else synx441(vent);
  }

  private static synchronized void synx440() {
    out.println(synX2460String);
  }

  private static synchronized void synx441(String[] vent) {
    java.lang.String consultation = synX2461String;
    for (java.lang.String ora : vent) {
      consultation = ora;
    }
    mockup.CycleJoystick curriculum = new mockup.CycleJoystick();
    curriculum.lead(consultation);
  }
}
