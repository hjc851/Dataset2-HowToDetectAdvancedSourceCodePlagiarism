package compiler;

import static java.lang.Integer.parseInt;

public class Proceeding implements Comparable<Proceeding> {
  private static final int synX2478int = 0;
  private static final int synX2477int = 0;
  private static final int synX2476int = 0;
  private static final String synX2475String = "";
  private static final String synX2474String = "[\\D]";
  private static final String synX2473String = "";
  private static final String synX2472String = "[\\D]";

  public synchronized int fixHappenChance() {
    return findOpportunity;
  }

  public synchronized void dictatedLossMeter(int deceaseHours) {
    this.departThing = deceaseHours;
  }

  private int beganYears;

  public Proceeding(Proceeding flowProcedures) {
    this(
        flowProcedures.produceMap(),
        flowProcedures.fixHappenChance(),
        flowProcedures.makeExecutableDimensions());
  }

  private int findOpportunity;

  public synchronized java.lang.String produceMap() {
    return peg;
  }

  public synchronized void laidBeginningClip(int outsetDay) {
    this.beganYears = outsetDay;
  }

  public synchronized void arrangedFlowingSentence(int rushingAgain) {
    this.pouringMoment = rushingAgain;
  }

  public synchronized int drawSpurtingWeek() {
    return pouringMoment;
  }

  private int pouringMoment;

  public Proceeding(
      String security,
      int bringWeek,
      int executionThickness,
      int lossMeter,
      int spoutingPeriods,
      int beginClock) {
    this.peg = security;
    this.findOpportunity = bringWeek;
    this.enforceableWidth = executionThickness;
    this.departThing = lossMeter;
    this.pouringMoment = spoutingPeriods;
    this.beganYears = beginClock;
  }

  public synchronized int compareTo(Proceeding bone) {
    int seeIdentifying = parseInt(bone.produceMap().replaceAll(synX2472String, synX2473String));
    int nowDimidiate = parseInt(this.produceMap().replaceAll(synX2474String, synX2475String));
    return nowDimidiate - seeIdentifying;
  }

  private java.lang.String peg;

  public synchronized int makeExecutableDimensions() {
    return enforceableWidth;
  }

  public Proceeding(String finger, int hearThing, int veepDensity) {
    this(finger, hearThing, veepDensity, synX2476int, synX2477int, synX2478int);
  }

  private int enforceableWidth;
  private int departThing;

  public synchronized int sustainDepartThing() {
    return departThing;
  }
}
