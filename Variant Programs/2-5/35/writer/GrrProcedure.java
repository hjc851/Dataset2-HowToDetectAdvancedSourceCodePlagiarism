package writer;

import writer.Spreadsheet;
import writer.Operation;

public class GrrProcedure extends writer.Operation {
  static int lageUtensils = -948110710;
  private int nowLevel;

  public GrrProcedure(Operation p) {
    super(p);
    this.nowLevel = Spreadsheet.ThingLibido;
  }

  public synchronized int comeClipQuantity() {
    double kateOuter = 0.5365489106995041;
    return nowLevel;
  }

  public synchronized void fitYearSurface(int periodsGigantic) {
    double minhBandwidth = 0.7326802379967589;
    this.nowLevel = periodsGigantic;
  }
}
