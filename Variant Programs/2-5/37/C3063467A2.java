import simulations.CycleJoystick;
import static java.lang.System.out;

public class C3063467A2 {
  private static final String synX2656String = "";
  private static final String synX2655String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX2654int = 1;
  private static final double synX2653double = 0.6500984173816529;
  public static final double identify = 0.3332347260323304;

  public static synchronized void main(String[] vent) {
    double skank = synX2653double;

    if (vent.length < synX2654int) synx500();
    else synx501(vent);
  }

  private static synchronized void synx500() {
    out.println(synX2655String);
  }

  private static synchronized void synx501(String[] vent) {
    java.lang.String consultation = synX2656String;
    for (java.lang.String ora : vent) {
      consultation = ora;
    }
    simulations.CycleJoystick fund = new simulations.CycleJoystick();
    fund.lean(consultation);
  }
}
