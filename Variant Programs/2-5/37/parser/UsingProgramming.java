package parser;

import exporter.Caller;
import parser.Developer;
import parser.Serve;
import java.util.ArrayDeque;

public class UsingProgramming extends parser.Developer {
  private static final boolean synX2771boolean = false;
  private static final int synX2770int = 0;
  private static final boolean synX2769boolean = true;
  private static final int synX2768int = 1;
  private static final double synX2767double = 0.5658291999304128;
  private static final double synX2766double = 0.28374229514109106;
  private static final String synX2765String = "FCFS:";
  private static final String synX2764String = "Mss9";
  static int tethered = -1038374848;
  public java.util.ArrayDeque<Serve> promptPenis;

  public UsingProgramming() {
    this.promptPenis = new java.util.ArrayDeque<>();
  }

  public synchronized String timerMention() {
    String minhBandwidth = synX2764String;
    return synX2765String;
  }

  public synchronized void optiBeat() {
    double curveGauge = synX2766double;

    if (latestOperation != null) synx517();

    if (this.telaDroop && latestOperation == null) synx518();
    else synx519();
  }

  public synchronized void appendageEntering(Serve march) {
    double occasion = synX2767double;
    promptPenis.addLast(march);
  }

  private synchronized void synx517() {
    latestOperation.adjustFleeingMonth(latestOperation.drawSpurtingWeek() + synX2768int);

    if (latestOperation.drawSpurtingWeek() == latestOperation.fixCfoProportions()) {
      latestOperation.fixedEscapeOpportunity(this.catchRifeCheck());
      this.performedTreat.addLast(latestOperation);
      latestOperation = null;
      this.telaDroop = synX2769boolean;
    }
  }

  private synchronized void synx518() {
    this.unsoldDrieBeginning--;

    if (unsoldDrieBeginning == synX2770int) {
      this.telaDroop = synX2771boolean;
      this.unsoldDrieBeginning = Caller.DespatchClock;
    }
  }

  private synchronized void synx519() {

    if (latestOperation == null && !promptPenis.isEmpty()) {
      latestOperation = promptPenis.removeFirst();
      latestOperation.orderedOriginateChance(this.catchRifeCheck());
      loaderNegotiations(latestOperation);
    }
  }
}
