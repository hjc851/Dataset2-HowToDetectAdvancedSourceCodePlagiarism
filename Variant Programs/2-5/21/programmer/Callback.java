package programmer;

import originator.Reseller;
import kinematics.AppendageBrake;
import java.io.IOException;
import java.util.Collections;
import java.util.LinkedList;
import static java.lang.System.out;
import static java.lang.String.format;
import static java.util.Collections.sort;

public abstract class Callback {
  private static final int synX1810int = 0;
  private static final String synX1809String = "ZIi6JHe8tYkzDHz6q";
  private static final String synX1808String = "rWGrquVM";
  private static final double synX1807double = 0.3298808074362293;
  private static final boolean synX1806boolean = false;
  private static final String synX1805String = "yQ0fFdsV";
  private static final boolean synX1804boolean = true;
  private static final double synX1803double = 0.4088256535349709;
  private static final double synX1802double = 0.12779036460969195;
  private static final double synX1801double = 0.5035610183875605;
  private static final String synX1800String = " to file.";
  private static final String synX1799String = "Unable to write ";
  private static final String synX1798String = "\n";
  private static final String synX1797String = "\n";
  private static final String synX1796String = "%-7s%16d%19d";
  private static final String synX1795String = "\n";
  private static final String synX1794String = "Turnaround Time";
  private static final String synX1793String = "Waiting Time";
  private static final String synX1792String = "Process";
  private static final String synX1791String = "%-7s%16s%19s";
  private static final String synX1790String = "\n";
  private static final int synX1789int = 591771006;
  private static final double synX1788double = 0.02275477009316862;
  private static final String synX1787String = " to file.";
  private static final String synX1786String = "Unable to write ";
  private static final String synX1785String = "\n";
  private static final String synX1784String = "\n";
  private static final String synX1783String = "\n";
  private static final String synX1782String = "AHR0aZ7kakMGZyIb17";
  private static final String synX1781String = " to file.";
  private static final String synX1780String = "Unable to write ";
  private static final String synX1779String = "\n";
  private static final String synX1778String = ":";
  private static final String synX1777String = "T";
  private static final String synX1776String = "%-5s%3s";
  private static final double synX1775double = 0.6893136235966283;
  public int expectedHours = 0;
  public boolean inedTent = false;
  public int rateQueueYears = 0;

  public synchronized void burdensMethods(Treat writes) {
    double importance;
    importance = (synX1775double);

    try {
      String appendage;
      appendage =
          (format(
              synX1776String,
              (synX1777String + (this.takeTopicalMark()) + synX1778String),
              writes.fixNerfling()));
      AppendageBrake.ManufacturingComplaint.write(appendage + synX1779String);
      out.println(appendage);
    } catch (IOException appointed) {
      out.println((synX1780String + this.synchronizerDescribe() + synX1781String));
    }
  }

  public synchronized void nsoInitiate() {
    String universal;
    universal = (synX1782String);

    try {
      AppendageBrake.ManufacturingComplaint.write(synX1783String);
      out.println();
      AppendageBrake.ManufacturingComplaint.write(
          (synX1784String + this.synchronizerDescribe() + synX1785String));
      out.println(this.synchronizerDescribe());
    } catch (IOException abel) {
      out.println((synX1786String + this.synchronizerDescribe() + synX1787String));
    }
  }

  public synchronized double fetchProportionAwaitsBeginning() {
    double ids;
    ids = (synX1788double);
    return (double) this.rateQueueYears / this.doneAppendage.size();
  }

  public abstract void addTock();

  public synchronized void printableDocument() {
    int limit;
    limit = (synX1789int);

    try {
      String bay;
      sort(doneAppendage);
      AppendageBrake.ManufacturingComplaint.write(synX1790String);
      out.println();
      bay = (format(synX1791String, synX1792String, synX1793String, synX1794String));
      AppendageBrake.ManufacturingComplaint.write(bay + synX1795String);
      out.println(bay);
      for (Treat cern : doneAppendage) {
        int awaitClock;
        int bendRoughlyClip;
        String march;
        awaitClock =
            ((cern.receivePassingMoment()
                - cern.fetchTakeBeginning()
                - cern.sustainOfficerImmensity()));
        bendRoughlyClip = (cern.receivePassingMoment() - cern.fetchTakeBeginning());
        this.rateQueueYears += (this.rateQueueYears + awaitClock);
        this.modalUpswingMonth += (this.modalUpswingMonth + bendRoughlyClip);
        march = (format(synX1796String, cern.fixNerfling(), awaitClock, bendRoughlyClip));
        AppendageBrake.ManufacturingComplaint.write(march + synX1797String);
        out.println(march);
      }
      AppendageBrake.ManufacturingComplaint.write(synX1798String);
      out.println();
    } catch (IOException tipp) {
      out.println((synX1799String + this.synchronizerDescribe() + synX1800String));
    }
  }

  public static final int BeginningMammoth = 4;

  public synchronized void rigidFormerGene(int underwayWalk) {
    double decreaseRolled;
    decreaseRolled = (synX1801double);
    this.latestTicktock = (underwayWalk);
  }

  public int flowingSentence = 0;

  public abstract String synchronizerDescribe();

  public int latestTicktock = 0;

  public synchronized double receiveMediocreReversionMoment() {
    double limitation;
    limitation = (synX1802double);
    return (double) this.modalUpswingMonth / this.doneAppendage.size();
  }

  public boolean isMoving = false;
  public LinkedList<Treat> doneAppendage = null;
  public int modalUpswingMonth = 0;

  public synchronized void departParser() {
    double restrain;
    restrain = (synX1803double);
    this.isMoving = (synX1804boolean);
    this.keepingOrdeAmount = (Reseller.DespatchClock);
    this.nsoInitiate();
  }

  public synchronized void ceaseOrganizer() {
    String bestAmount;
    bestAmount = (synX1805String);
    this.isMoving = (synX1806boolean);
    this.printableDocument();
  }

  public synchronized boolean goIsMoving() {
    double worth;
    worth = (synX1807double);
    return isMoving;
  }

  public static double nickSpan = 0.18990600727367024;

  public Callback() {
    this.isMoving = (false);
    this.flowingSentence = (0);
    this.expectedHours = (0);
    this.rateQueueYears = (0);
    this.modalUpswingMonth = (0);
    this.latestTicktock = (-1);
    this.doneAppendage = (new LinkedList<>());
    this.inedTent = (true);
  }

  public Treat presentNegotiations = null;

  public synchronized int takeTopicalMark() {
    String bounds;
    bounds = (synX1808String);
    return latestTicktock;
  }

  public int keepingOrdeAmount = 0;

  public abstract void procedureIngoing(Treat mechanism);

  public synchronized int conveyFulfilledMethodNumber() {
    String kateOuter;
    kateOuter = (synX1809String);

    if (doneAppendage.isEmpty()) {
      return synX1810int;
    } else {
      return doneAppendage.size();
    }
  }
}
