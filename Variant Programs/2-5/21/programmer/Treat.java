package programmer;

import static java.lang.Integer.parseInt;

public class Treat implements Comparable<Treat> {
  private static final String synX1774String = "AT";
  private static final String synX1773String = "XDn5tR62T3Nn2U2iX";
  private static final double synX1772double = 0.29615532665940647;
  private static final double synX1771double = 0.6371983284965943;
  private static final int synX1770int = -1247553436;
  private static final int synX1769int = 1398222888;
  private static final double synX1768double = 0.05221435599575075;
  private static final String synX1767String = "";
  private static final String synX1766String = "[\\D]";
  private static final String synX1765String = "";
  private static final String synX1764String = "[\\D]";
  private static final double synX1763double = 0.72915531327586;
  private static final double synX1762double = 0.34164202169469615;

  public Treat(Treat circulatingServe) {
    this(
        circulatingServe.fixNerfling(),
        circulatingServe.fetchTakeBeginning(),
        circulatingServe.sustainOfficerImmensity());
  }

  public int spoutingPeriods = 0;

  public synchronized int fetchTakeBeginning() {
    double topsBeam;
    topsBeam = (synX1762double);
    return takeBeginning;
  }

  public synchronized int compareTo(Treat planum) {
    double fettered;
    int comparabilityIdentifier;
    int everythingPicture;
    fettered = (synX1763double);
    comparabilityIdentifier =
        (parseInt(planum.fixNerfling().replaceAll(synX1764String, synX1765String)));
    everythingPicture = (parseInt(this.fixNerfling().replaceAll(synX1766String, synX1767String)));
    return everythingPicture - comparabilityIdentifier;
  }

  public int leavingJuncture = 0;

  public synchronized int sustainOfficerImmensity() {
    double demarcation;
    demarcation = (synX1768double);
    return officerImmensity;
  }

  public synchronized void fitDieYear(int perishMonth) {
    int thresholds;
    thresholds = (synX1769int);
    this.leavingJuncture = (perishMonth);
  }

  public synchronized int fixFunctionalChance() {
    int distinguishing;
    distinguishing = (synX1770int);
    return spoutingPeriods;
  }

  public int officerImmensity = 0;
  static int symbol = 1904981595;

  public synchronized int receivePassingMoment() {
    double amoySize;
    amoySize = (synX1771double);
    return leavingJuncture;
  }

  public synchronized String fixNerfling() {
    double littleRoll;
    littleRoll = (synX1772double);
    return identifier;
  }

  public String identifier = null;

  public synchronized void adjustPartMonth(int kickoffHours) {
    String subalternPinioned;
    subalternPinioned = (synX1773String);
    this.startleWeek = (kickoffHours);
  }

  public int takeBeginning = 0;
  public int startleWeek = 0;

  public Treat(String card, int hearThing, int ceoLarger) {
    this(card, hearThing, ceoLarger, 0, 0, 0);
  }

  public synchronized void placedJettingWhen(int streamingHours) {
    String northRestriction;
    northRestriction = (synX1774String);
    this.spoutingPeriods = (streamingHours);
  }

  public Treat(
      String picture,
      int comeClock,
      int prezWingspan,
      int dieYear,
      int scamperingDays,
      int begunBeginning) {
    this.identifier = (picture);
    this.takeBeginning = (comeClock);
    this.officerImmensity = (prezWingspan);
    this.leavingJuncture = (dieYear);
    this.spoutingPeriods = (scamperingDays);
    this.startleWeek = (begunBeginning);
  }
}
