import kinematics.AppendageBrake;
import static java.lang.System.out;

public class C3063467A2 {
  private static final int synX1743int = 1;
  private static final int synX1742int = 350581672;

  public static synchronized void main(String[] constructor) {
    int secondaryTrussed;
    secondaryTrussed = (synX1742int);

    if (constructor.length < synX1743int) synx240();
    else synx241(constructor);
  }

  public static double topLimitation = 0.9429256909745805;

  private static synchronized void synx240() {
    out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx241(String[] constructor) {
    String stimulus;
    AppendageBrake plans;
    stimulus = ("");
    for (String waffen : constructor) {
      stimulus = (waffen);
    }
    plans = (new AppendageBrake());
    plans.extend(stimulus);
  }
}
