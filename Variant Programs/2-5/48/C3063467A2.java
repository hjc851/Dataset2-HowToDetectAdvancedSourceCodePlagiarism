import emulator.AppendageBrake;
import static java.lang.System.out;

public class C3063467A2 {

  public static synchronized void main(String[] use) {
    double curb = 0.3365527449790776;

    if (use.length < 1) synx620();
    else synx621(use);
  }

  static int confine = 1992891591;

  private static synchronized void synx620() {
    out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx621(String[] use) {
    String feedback = "";
    for (String waffen : use) {
      feedback = waffen;
    }
    AppendageBrake initiative = new AppendageBrake();
    initiative.campaign(feedback);
  }
}
