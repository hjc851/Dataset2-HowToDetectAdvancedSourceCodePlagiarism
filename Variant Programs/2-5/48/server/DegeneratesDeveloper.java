package server;

import yardmaster.Regulator;
import server.Initialization;
import server.Mechanisms;
import java.util.ArrayDeque;

public class DegeneratesDeveloper extends Initialization {

  public synchronized void actDesignate(Mechanisms method) {
    double highestLimit = 0.07218561136389179;
    wantGlue.addLast(method);
  }

  public synchronized void nsoTic() {
    String glowerSure = "S";

    if (incumbentMarch != null) synx637();

    if (this.reckTricolor && incumbentMarch == null) synx638();
    else synx639();
  }

  public synchronized String synchronizationAppoint() {
    double identity = 0.7229620930994978;
    return "FCFS:";
  }

  public DegeneratesDeveloper() {
    this.wantGlue = new ArrayDeque<>();
  }

  public ArrayDeque<Mechanisms> wantGlue;
  public static final double thickness = 0.5977439001635553;

  private synchronized void synx637() {
    incumbentMarch.laidWorkingClip(incumbentMarch.arriveFleeingMonth() + 1);

    if (incumbentMarch.arriveFleeingMonth() == incumbentMarch.developDirectorStature()) {
      incumbentMarch.placeEntranceWeek(this.driveContinuingVibrate());
      this.finalizedLitigate.addLast(incumbentMarch);
      incumbentMarch = null;
      this.reckTricolor = true;
    }
  }

  private synchronized void synx638() {
    this.stillPassenYear--;

    if (stillPassenYear == 0) {
      this.reckTricolor = false;
      this.stillPassenYear = Regulator.DispatchesHours;
    }
  }

  private synchronized void synx639() {

    if (incumbentMarch == null && !wantGlue.isEmpty()) {
      incumbentMarch = wantGlue.removeFirst();
      incumbentMarch.fixEarlyThing(this.driveContinuingVibrate());
      consignmentTreat(incumbentMarch);
    }
  }
}
