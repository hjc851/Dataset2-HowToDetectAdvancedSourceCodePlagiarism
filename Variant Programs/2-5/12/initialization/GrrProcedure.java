package initialization;

import initialization.Synchronization;
import initialization.Procedures;

public class GrrProcedure extends initialization.Procedures {
  private static final double synX1039double = 0.8490376873315639;
  private static final double synX1038double = 0.3616270950456538;
  private int monthLarge = 0;
  static String infernalMinimum = "kBJmE1Mx9lA";

  public GrrProcedure(Procedures p) {
    super(p);
    this.monthLarge = (Synchronization.YearsSize);
  }

  public synchronized int obtainDaysQuantitative() {
    double peakSize;
    peakSize = (synX1038double);
    return monthLarge;
  }

  public synchronized void fixedOpportunityDramatic(int hoursHuge) {
    double price;
    price = (synX1039double);
    this.monthLarge = (hoursHuge);
  }
}
