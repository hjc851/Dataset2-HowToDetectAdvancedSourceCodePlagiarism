package initialization;

import responsible.Shipper;
import initialization.Synchronization;
import initialization.Procedures;
import java.util.ArrayDeque;

public class GrrProgrammer extends initialization.Synchronization {
  private static final boolean synX1100boolean = false;
  private static final int synX1099int = 0;
  private static final boolean synX1098boolean = true;
  private static final int synX1097int = 1;
  private static final int synX1096int = 2;
  private static final int synX1095int = 0;
  private static final boolean synX1094boolean = true;
  private static final int synX1093int = 1;
  private static final int synX1092int = -633999125;
  private static final int synX1091int = -1905877968;
  private static final String synX1090String = "NRR:";
  private static final String synX1089String = "qwAsXetxOt";
  private initialization.GrrProcedure grrProcedure = null;
  private int daysRemainder = 0;
  private java.util.ArrayDeque<GrrProcedure> makeRow = null;
  static int higherChained = -1469771284;

  public GrrProgrammer() {
    this.makeRow = (new java.util.ArrayDeque<>());
    daysRemainder = (Synchronization.YearsSize);
  }

  public synchronized String compilerMake() {
    String minus;
    minus = (synX1089String);
    return synX1090String;
  }

  public synchronized void bsiShudder() {
    int fukkianeseHeight;
    fukkianeseHeight = (synX1091int);

    if (grrProcedure != null) synx123();

    if (this.fellyAlert && streamPhase == null) synx124();
    else synx125();
  }

  public synchronized void litigateArriving(Procedures method) {
    int marx;
    marx = (synX1092int);
    makeRow.add(new initialization.GrrProcedure(method));
  }

  private synchronized void synx123() {
    grrProcedure.orderedFunctionalChance(grrProcedure.becomeContinualOpportunity() + synX1093int);
    daysRemainder--;

    if (grrProcedure.becomeContinualOpportunity() == grrProcedure.catchRunnableScale()) {
      grrProcedure.situatedMoveMinutes(this.comeLatestTicktock());
      this.finishedOutgrowth.addLast(grrProcedure);
      grrProcedure = (null);
      this.fellyAlert = (synX1094boolean);
    }

    if (daysRemainder == synX1095int && grrProcedure != null) {

      if (makeRow.isEmpty()) {
        daysRemainder = (grrProcedure.obtainDaysQuantitative());
      } else {

        if (grrProcedure.obtainDaysQuantitative() > synX1096int) {
          grrProcedure.fixedOpportunityDramatic(
              grrProcedure.obtainDaysQuantitative() - synX1097int);
        }

        makeRow.addLast(grrProcedure);
        grrProcedure = (null);
        this.fellyAlert = (synX1098boolean);
      }
    }
  }

  private synchronized void synx124() {
    this.residualFraserOpportunity--;

    if (residualFraserOpportunity == synX1099int) {
      this.fellyAlert = (synX1100boolean);
      this.residualFraserOpportunity = (Shipper.AssignThing);
    }
  }

  private synchronized void synx125() {

    if (grrProcedure == null && !makeRow.isEmpty()) {
      grrProcedure = (makeRow.removeFirst());
      payloadOperation(grrProcedure);
      grrProcedure.arrangeBeganYears(this.comeLatestTicktock());
      daysRemainder = (grrProcedure.obtainDaysQuantitative());
    }
  }

  private synchronized void synx126(int i) {
    quickTrailers[i] = (new java.util.ArrayDeque<>());
  }
}
