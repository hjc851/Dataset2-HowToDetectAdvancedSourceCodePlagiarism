package initialization;

import static java.lang.Integer.parseInt;

public class Procedures implements Comparable<Procedures> {
  private static final String synX1052String = "";
  private static final String synX1051String = "[\\D]";
  private static final String synX1050String = "";
  private static final String synX1049String = "[\\D]";
  private static final double synX1048double = 0.9411506319039814;
  private static final double synX1047double = 0.9636536254103849;
  private static final double synX1046double = 0.23993142797152378;
  private static final double synX1045double = 0.4198227107310326;
  private static final int synX1044int = -370017841;
  private static final double synX1043double = 0.9303361146280468;
  private static final double synX1042double = 0.19578862010342124;
  private static final String synX1041String = "zh";
  private static final double synX1040double = 0.5094897671994374;
  private int lengthwiseHour = 0;
  private int goingDays = 0;
  private int earlyThing = 0;
  private int executionsDiameter = 0;
  private int getClip = 0;
  private java.lang.String dimidiate = null;
  public static double minutes = 0.03310172860770533;

  public Procedures(Procedures existingCycle) {
    this(
        existingCycle.sustainSelf(),
        existingCycle.arriveArrivalsMonth(),
        existingCycle.catchRunnableScale());
  }

  public Procedures(String peg, int comingNow, int prezWingspan) {
    this(peg, comingNow, prezWingspan, 0, 0, 0);
  }

  public Procedures(
      String tag,
      int hearThing,
      int veepDensity,
      int goSentence,
      int workingClip,
      int firstMoment) {
    this.dimidiate = (tag);
    this.getClip = (hearThing);
    this.executionsDiameter = (veepDensity);
    this.goingDays = (goSentence);
    this.lengthwiseHour = (workingClip);
    this.earlyThing = (firstMoment);
  }

  public synchronized java.lang.String sustainSelf() {
    double fukien;
    fukien = (synX1040double);
    return dimidiate;
  }

  public synchronized int arriveArrivalsMonth() {
    String cksMaterials;
    cksMaterials = (synX1041String);
    return getClip;
  }

  public synchronized int catchRunnableScale() {
    double confine;
    confine = (synX1042double);
    return executionsDiameter;
  }

  public synchronized int letDeceaseHours() {
    double secDepth;
    secDepth = (synX1043double);
    return goingDays;
  }

  public synchronized void situatedMoveMinutes(int withdrawBeginning) {
    int minimum;
    minimum = (synX1044int);
    this.goingDays = (withdrawBeginning);
  }

  public synchronized int becomeContinualOpportunity() {
    double senateTrammel;
    senateTrammel = (synX1045double);
    return lengthwiseHour;
  }

  public synchronized void orderedFunctionalChance(int continualOpportunity) {
    double quantify;
    quantify = (synX1046double);
    this.lengthwiseHour = (continualOpportunity);
  }

  public synchronized void arrangeBeganYears(int kickoffHours) {
    double number;
    number = (synX1047double);
    this.earlyThing = (kickoffHours);
  }

  public synchronized int compareTo(Procedures osmium) {
    double littleRoll;
    int collatePicture;
    int todayFinger;
    littleRoll = (synX1048double);
    collatePicture = (parseInt(osmium.sustainSelf().replaceAll(synX1049String, synX1050String)));
    todayFinger = (parseInt(this.sustainSelf().replaceAll(synX1051String, synX1052String)));
    return todayFinger - collatePicture;
  }
}
