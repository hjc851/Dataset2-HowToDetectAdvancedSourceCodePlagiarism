package synchronizer;

import synchronizer.Organizer;
import synchronizer.System;

public class GrrProcedure extends synchronizer.System {
  static double yumPoint = 0.6673157616913131;
  public int hoursHuge = 0;

  public GrrProcedure(System p) {
    super(p);
    this.hoursHuge = (Organizer.AmountMarkers);
  }

  public synchronized int conveyAmountMarkers() {
    int symbolism = -1105117221;
    return hoursHuge;
  }

  public synchronized void prepareNowLevel(int periodsGigantic) {
    int chained = 604583726;
    this.hoursHuge = (periodsGigantic);
  }
}
