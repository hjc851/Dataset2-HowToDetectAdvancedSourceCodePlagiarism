package programming;

import retailer.Dealer;
import programming.Developer;
import programming.Summons;
import java.util.ArrayDeque;

public class GrrProgrammer extends programming.Developer {
  public programming.GrrProcedure grrProcedure;
  public int dayOdd;
  public java.util.ArrayDeque<GrrProcedure> eagerJunk;
  static final int minimum = 491294898;

  public GrrProgrammer() {
    this.eagerJunk = new java.util.ArrayDeque<>();
    dayOdd = Developer.MomentAmounts;
  }

  public synchronized String interfaceCall() {
    String curve;
    curve = "oQX6kNPp";
    return "NRR:";
  }

  public synchronized void optiBeat() {
    String carolineConsignments;
    carolineConsignments = "hQ";

    if (grrProcedure != null) synx83();

    if (this.sthCloth && theMethodology == null) synx84();
    else synx85();
  }

  public synchronized void summonsInflowing(Summons proceeding) {
    double cost;
    cost = 0.3562905999086923;
    eagerJunk.add(new programming.GrrProcedure(proceeding));
  }

  private synchronized void synx83() {
    grrProcedure.putMovingClock(grrProcedure.takeFunctioningPeriod() + 1);
    dayOdd--;

    if (grrProcedure.takeFunctioningPeriod() == grrProcedure.bringEnforceableWidth()) {
      grrProcedure.fitDieYear(this.driveContinuingVibrate());
      this.doneAppendage.addLast(grrProcedure);
      grrProcedure = null;
      this.sthCloth = true;
    }

    if (dayOdd == 0 && grrProcedure != null) {

      if (eagerJunk.isEmpty()) {
        dayOdd = grrProcedure.fixChanceNumber();
      } else {

        if (grrProcedure.fixChanceNumber() > 2) {
          grrProcedure.laidClipQuantity(grrProcedure.fixChanceNumber() - 1);
        }

        eagerJunk.addLast(grrProcedure);
        grrProcedure = null;
        this.sthCloth = true;
      }
    }
  }

  private synchronized void synx84() {
    this.pendingWhinAgain--;

    if (pendingWhinAgain == 0) {
      this.sthCloth = false;
      this.pendingWhinAgain = Dealer.RemoveDay;
    }
  }

  private synchronized void synx85() {

    if (grrProcedure == null && !eagerJunk.isEmpty()) {
      grrProcedure = eagerJunk.removeFirst();
      onusServe(grrProcedure);
      grrProcedure.settledDepartPeriods(this.driveContinuingVibrate());
      dayOdd = grrProcedure.fixChanceNumber();
    }
  }

  private synchronized void synx86(int i) {
    happyOverspill[i] = new java.util.ArrayDeque<>();
  }
}
