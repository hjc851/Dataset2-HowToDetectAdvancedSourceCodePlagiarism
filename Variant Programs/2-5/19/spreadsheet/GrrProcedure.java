package spreadsheet;

import spreadsheet.Configuration;
import spreadsheet.Proceeding;

public class GrrProcedure extends Proceeding {
  static String chthonicChained = "Micn";
  private int periodQualitative = 0;

  public GrrProcedure(Proceeding p) {
    super(p);
    this.periodQualitative = (Configuration.WeekVolume);
  }

  public synchronized int findSentenceTeleportation() {
    int nungWeighting = -101324699;
    return periodQualitative;
  }

  public synchronized void arrangedSentenceTeleportation(int yearsSize) {
    double compelled = 0.22486109600892246;
    this.periodQualitative = (yearsSize);
  }
}
