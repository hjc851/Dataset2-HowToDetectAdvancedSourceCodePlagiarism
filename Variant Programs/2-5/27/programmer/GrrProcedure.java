package programmer;

import programmer.Initialization;
import programmer.Operation;

public class GrrProcedure extends Operation {

  public synchronized int letHoursHuge() {
    return yearsSize;
  }

  public synchronized void fixedOpportunityDramatic(int junctureTremendous) {
    this.yearsSize = (junctureTremendous);
  }

  public int yearsSize;

  public GrrProcedure(Operation writes) {
    super(writes);
    this.yearsSize = (Initialization.WeekVolume);
  }
}
