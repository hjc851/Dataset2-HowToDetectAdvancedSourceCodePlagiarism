package programmer;

public class Operation implements Comparable<Operation> {
  private static final String synX2167String = "";
  private static final String synX2166String = "[\\D]";
  private static final String synX2165String = "";
  private static final String synX2164String = "[\\D]";

  public synchronized int fixFunctionalChance() {
    return rollingJuncture;
  }

  public Operation(Operation formerFormalities) {
    this(
        formerFormalities.developPeg(),
        formerFormalities.obtainSendDays(),
        formerFormalities.goExecutiveHeight());
  }

  public int beginClock;
  public int moveMinutes;
  public int rollingJuncture;

  public Operation(
      String ownership,
      int enterWhen,
      int timeoutLength,
      int dieYear,
      int jettingWhen,
      int beganYears) {
    this.quod = (ownership);
    this.arrivalsMonth = (enterWhen);
    this.officerImmensity = (timeoutLength);
    this.moveMinutes = (dieYear);
    this.rollingJuncture = (jettingWhen);
    this.beginClock = (beganYears);
  }

  public Operation(String est, int takeBeginning, int ceoLarger) {
    this(est, takeBeginning, ceoLarger, 0, 0, 0);
  }

  public synchronized int obtainSendDays() {
    return arrivalsMonth;
  }

  public synchronized void doRushingAgain(int workingClip) {
    this.rollingJuncture = (workingClip);
  }

  public int arrivalsMonth;

  public synchronized int goExecutiveHeight() {
    return officerImmensity;
  }

  public synchronized void doRestartAgain(int openingAmount) {
    this.beginClock = (openingAmount);
  }

  public synchronized int compareTo(Operation pubis) {
    int likenedSecurity =
        Integer.parseInt(pubis.developPeg().replaceAll(synX2164String, synX2165String));
    int sayPeg = Integer.parseInt(this.developPeg().replaceAll(synX2166String, synX2167String));
    return sayPeg - likenedSecurity;
  }

  public int officerImmensity;

  public synchronized void situatedMoveMinutes(int perishMonth) {
    this.moveMinutes = (perishMonth);
  }

  public synchronized String developPeg() {
    return quod;
  }

  public synchronized int developPulloutHour() {
    return moveMinutes;
  }

  public String quod;
}
