package debugging;

public class Phase implements Comparable<Phase> {
  private static final String synX690String = "";
  private static final String synX689String = "[\\D]";
  private static final String synX688String = "";
  private static final String synX687String = "[\\D]";
  private static final double synX686double = 0.20137507559332735;
  private static final double synX685double = 0.601244729598491;
  private static final int synX684int = -1838161934;
  private static final String synX683String = "gr9hFbvreo";
  private static final String synX682String = "BDrCT5";
  private static final double synX681double = 0.4636806951202962;
  private static final double synX680double = 0.21980884720865101;
  private static final double synX679double = 0.8342541904694493;
  private static final double synX678double = 0.16065643854895917;
  public static double identity = 0.23186963292471896;
  public java.lang.String peg = null;
  public int sendDays = 0;
  public int implementationSmall = 0;
  public int openingAmount = 0;
  public int quittingAmount = 0;
  public int spoutingPeriods = 0;

  public Phase(Phase contemporaryLitigate) {
    this(
        contemporaryLitigate.generateIdem(),
        contemporaryLitigate.developHappensHour(),
        contemporaryLitigate.obtainTimeoutLength());
  }

  public Phase(String milad, int meetPeriods, int executionsDiameter) {
    this(milad, meetPeriods, executionsDiameter, 0, 0, 0);
  }

  public Phase(
      String user,
      int findOpportunity,
      int directorStature,
      int dieYear,
      int streamingHours,
      int getPeriod) {
    this.peg = (user);
    this.sendDays = (findOpportunity);
    this.implementationSmall = (directorStature);
    this.quittingAmount = (dieYear);
    this.spoutingPeriods = (streamingHours);
    this.openingAmount = (getPeriod);
  }

  public synchronized java.lang.String generateIdem() {
    double levelRestricting = synX678double;
    return peg;
  }

  public synchronized int developHappensHour() {
    double compelled = synX679double;
    return sendDays;
  }

  public synchronized int obtainTimeoutLength() {
    double matter = synX680double;
    return implementationSmall;
  }

  public synchronized int developPulloutHour() {
    double elevatedEnchained = synX681double;
    return quittingAmount;
  }

  public synchronized void putDepartureClock(int egressYears) {
    String kilo = synX682String;
    this.quittingAmount = (egressYears);
  }

  public synchronized int haveSpoutingPeriods() {
    String speedRestrain = synX683String;
    return spoutingPeriods;
  }

  public synchronized void putMovingClock(int walkingAmount) {
    int loadRadius = synX684int;
    this.spoutingPeriods = (walkingAmount);
  }

  public synchronized void rigidOpeningAmount(int kickoffHours) {
    double greaterUnfree = synX685double;
    this.openingAmount = (kickoffHours);
  }

  public synchronized int compareTo(Phase montes) {
    double fukienLength = synX686double;
    int correlateIdem =
        java.lang.Integer.parseInt(montes.generateIdem().replaceAll(synX687String, synX688String));
    int thereforeMap =
        java.lang.Integer.parseInt(this.generateIdem().replaceAll(synX689String, synX690String));
    return thereforeMap - correlateIdem;
  }
}
