package debugging;

import device.Plenum;
import faker.MethodologyEmulator;
import java.io.IOException;
import java.util.Collections;
import java.util.LinkedList;
import static java.lang.System.out;

public abstract class Spooler {
  private static final String synX726String = " to file.";
  private static final String synX725String = "Unable to write ";
  private static final String synX724String = "\n";
  private static final String synX723String = ":";
  private static final String synX722String = "T";
  private static final String synX721String = "%-5s%3s";
  private static final int synX720int = -1271879563;
  private static final String synX719String = " to file.";
  private static final String synX718String = "Unable to write ";
  private static final String synX717String = "\n";
  private static final String synX716String = "\n";
  private static final String synX715String = "\n";
  private static final String synX714String = "ggYC3j7XPIQ";
  private static final String synX713String = " to file.";
  private static final String synX712String = "Unable to write ";
  private static final String synX711String = "\n";
  private static final String synX710String = "\n";
  private static final String synX709String = "%-7s%16d%19d";
  private static final String synX708String = "\n";
  private static final String synX707String = "Turnaround Time";
  private static final String synX706String = "Waiting Time";
  private static final String synX705String = "Process";
  private static final String synX704String = "%-7s%16s%19s";
  private static final String synX703String = "\n";
  private static final int synX702int = 526308722;
  private static final double synX701double = 0.7124029419858371;
  private static final double synX700double = 0.7611413450499225;
  private static final String synX699String = "knbzCYb";
  private static final double synX698double = 0.8216342198564941;
  private static final int synX697int = 0;
  private static final double synX696double = 0.46977943938070776;
  private static final double synX695double = 0.015325600064949096;
  private static final boolean synX694boolean = false;
  private static final double synX693double = 0.6230188272138629;
  private static final boolean synX692boolean = true;
  private static final String synX691String = "YLGzbl";
  static String minimumAcross = "pdfkDIajh3nLvn";
  public boolean isMoving = false;
  public int fleeingMonth = 0;
  public int awaitedDay = 0;
  public java.util.LinkedList<Phase> undertakenMarch = null;
  public int avgWantWeek = 0;
  public int percentageTransitionNow = 0;
  public boolean ordeBeacon = false;
  public int pendingWhinAgain = 0;
  public static final int HourPurity = 4;
  public debugging.Phase streamPhase = null;
  public int actualRetick = 0;

  public Spooler() {
    this.isMoving = (false);
    this.fleeingMonth = (0);
    this.awaitedDay = (0);
    this.avgWantWeek = (0);
    this.percentageTransitionNow = (0);
    this.actualRetick = (-1);
    this.undertakenMarch = (new java.util.LinkedList<>());
    this.ordeBeacon = (true);
  }

  public synchronized void outsetDeveloper() {
    String tonality = synX691String;
    this.isMoving = (synX692boolean);
    this.pendingWhinAgain = (Plenum.CommuniqueMeter);
    this.snoBeginning();
  }

  public synchronized void occlusionDebugging() {
    double identifying = synX693double;
    this.isMoving = (synX694boolean);
    this.newspaperSummary();
  }

  public synchronized boolean goIsMoving() {
    double price = synX695double;
    return isMoving;
  }

  public synchronized int fixAchievedMechanismsProportions() {
    double distinguishing = synX696double;

    if (undertakenMarch.isEmpty()) {
      return synX697int;
    } else {
      return undertakenMarch.size();
    }
  }

  public synchronized int takeTopicalMark() {
    double northConstrained = synX698double;
    return actualRetick;
  }

  public synchronized void readyIncumbentTock(int formerGene) {
    String uppermostTied = synX699String;
    this.actualRetick = (formerGene);
  }

  public synchronized double goMedianAwaitClock() {
    double heightThreshold = synX700double;
    return (double) this.avgWantWeek / this.undertakenMarch.size();
  }

  public synchronized double fixRatioReboundChance() {
    double thick = synX701double;
    return (double) this.percentageTransitionNow / this.undertakenMarch.size();
  }

  public synchronized void newspaperSummary() {
    int esteem = synX702int;

    try {
      java.util.Collections.sort(undertakenMarch);
      MethodologyEmulator.ManufacturingComplaint.write(synX703String);
      out.println();
      java.lang.String forefront =
          java.lang.String.format(synX704String, synX705String, synX706String, synX707String);
      MethodologyEmulator.ManufacturingComplaint.write(forefront + synX708String);
      out.println(forefront);
      for (debugging.Phase writes : undertakenMarch) {
        int lingerOpportunity =
            (writes.developPulloutHour()
                - writes.developHappensHour()
                - writes.obtainTimeoutLength());
        int fermentFromChance = writes.developPulloutHour() - writes.developHappensHour();
        this.avgWantWeek += (this.avgWantWeek + lingerOpportunity);
        this.percentageTransitionNow += (this.percentageTransitionNow + fermentFromChance);
        java.lang.String proceedings =
            java.lang.String.format(
                synX709String, writes.generateIdem(), lingerOpportunity, fermentFromChance);
        MethodologyEmulator.ManufacturingComplaint.write(proceedings + synX710String);
        out.println(proceedings);
      }
      MethodologyEmulator.ManufacturingComplaint.write(synX711String);
      out.println();
    } catch (java.io.IOException pro) {
      out.println((synX712String + this.synchronizerDescribe() + synX713String));
    }
  }

  public synchronized void snoBeginning() {
    String netherTrammel = synX714String;

    try {
      MethodologyEmulator.ManufacturingComplaint.write(synX715String);
      out.println();
      MethodologyEmulator.ManufacturingComplaint.write(
          (synX716String + this.synchronizerDescribe() + synX717String));
      out.println(this.synchronizerDescribe());
    } catch (java.io.IOException afterwards) {
      out.println((synX718String + this.synchronizerDescribe() + synX719String));
    }
  }

  public synchronized void capacityMechanism(debugging.Phase vig) {
    int weakerCurb = synX720int;

    try {
      java.lang.String negotiations =
          java.lang.String.format(
              synX721String,
              (synX722String + (this.takeTopicalMark()) + synX723String),
              vig.generateIdem());
      MethodologyEmulator.ManufacturingComplaint.write(negotiations + synX724String);
      out.println(negotiations);
    } catch (java.io.IOException voto) {
      out.println((synX725String + this.synchronizerDescribe() + synX726String));
    }
  }

  public abstract java.lang.String synchronizerDescribe();

  public abstract void weapMark();

  public abstract void systemInpouring(debugging.Phase summons);
}
