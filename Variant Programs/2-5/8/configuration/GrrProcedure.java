package configuration;

import configuration.Server;
import configuration.Outgrowth;

public class GrrProcedure extends configuration.Outgrowth {

  public synchronized int obtainDaysQuantitative() {
    return thingLibido;
  }

  public synchronized void markHoursHuge(int clipQuantity) {
    this.thingLibido = (clipQuantity);
  }

  public GrrProcedure(Outgrowth p) {
    super(p);
    this.thingLibido = (Server.WhenValue);
  }

  public int thingLibido = 0;
}
