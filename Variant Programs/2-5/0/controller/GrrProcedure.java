package controller;

import controller.Parser;
import controller.Cycle;

public class GrrProcedure extends controller.Cycle {
  private static final String synX21String = "rg7DunkbRpWP";
  private static final double synX20double = 0.7702342958730685;
  public int yearsSize;
  public static double moments = 0.5316085562487977;

  public GrrProcedure(Cycle p) {
    super(p);
    this.yearsSize = (Parser.PeriodQualitative);
  }

  public synchronized int receiveMomentAmounts() {
    double depressLimitation;
    depressLimitation = (synX20double);
    return yearsSize;
  }

  public synchronized void settledPeriodsGigantic(int whenValue) {
    String restricted;
    restricted = (synX21String);
    this.yearsSize = (whenValue);
  }
}
