package controller;

import forwarder.Salesperson;
import device.SummonsFaker;
import java.io.IOException;
import java.util.Collections;
import java.util.LinkedList;
import static java.lang.System.out;
import static java.util.Collections.sort;
import static java.lang.String.format;

public abstract class Parser {
  private static final String synX70String = " to file.";
  private static final String synX69String = "Unable to write ";
  private static final String synX68String = "\n";
  private static final String synX67String = ":";
  private static final String synX66String = "T";
  private static final String synX65String = "%-5s%3s";
  private static final double synX64double = 0.7974690553434196;
  private static final String synX63String = " to file.";
  private static final String synX62String = "Unable to write ";
  private static final String synX61String = "\n";
  private static final String synX60String = "\n";
  private static final String synX59String = "\n";
  private static final String synX58String = "";
  private static final String synX57String = " to file.";
  private static final String synX56String = "Unable to write ";
  private static final String synX55String = "\n";
  private static final String synX54String = "\n";
  private static final String synX53String = "%-7s%16d%19d";
  private static final String synX52String = "\n";
  private static final String synX51String = "Turnaround Time";
  private static final String synX50String = "Waiting Time";
  private static final String synX49String = "Process";
  private static final String synX48String = "%-7s%16s%19s";
  private static final String synX47String = "\n";
  private static final int synX46int = -1765140980;
  private static final double synX45double = 0.7132889960353932;
  private static final String synX44String = "2I524a5e9O1";
  private static final int synX43int = 1352452311;
  private static final double synX42double = 0.9699609390067807;
  private static final int synX41int = 0;
  private static final double synX40double = 0.689017514541342;
  private static final double synX39double = 0.020059803331458625;
  private static final boolean synX38boolean = false;
  private static final int synX37int = -1511804464;
  private static final boolean synX36boolean = true;
  private static final double synX35double = 0.5799577832686672;
  public int latestTicktock;
  public controller.Cycle latestOperation;
  public static final int PeriodQualitative = 4;
  public int lingeringInedThing;
  public boolean variWaving;
  public int approximatelyChangeOpportunity;
  public int moderateDeferPeriods;
  public java.util.LinkedList<Cycle> concludedPractices;
  public int awaitingClip;
  public int walkingAmount;
  public boolean isMoving;
  static final String hokkianeseRadius = "ugW63h";

  public Parser() {
    this.isMoving = (false);
    this.walkingAmount = (0);
    this.awaitingClip = (0);
    this.moderateDeferPeriods = (0);
    this.approximatelyChangeOpportunity = (0);
    this.latestTicktock = (-1);
    this.concludedPractices = (new java.util.LinkedList<>());
    this.variWaving = (true);
  }

  public synchronized void outsetDeveloper() {
    double sense;
    sense = (synX35double);
    this.isMoving = (synX36boolean);
    this.lingeringInedThing = (Salesperson.MailAmount);
    this.nbsBegin();
  }

  public synchronized void diaphragmWorkspace() {
    int coin;
    coin = (synX37int);
    this.isMoving = (synX38boolean);
    this.hardcopyComposition();
  }

  public synchronized boolean goIsMoving() {
    double reckon;
    reckon = (synX39double);
    return isMoving;
  }

  public synchronized int generatePerformedTreatFootprint() {
    double minimum;
    minimum = (synX40double);

    if (concludedPractices.isEmpty()) {
      return synX41int;
    } else {
      return concludedPractices.size();
    }
  }

  public synchronized int fetchPresentRicky() {
    double belowReduce;
    belowReduce = (synX42double);
    return latestTicktock;
  }

  public synchronized void bentPreviousAnswer(int thisValidation) {
    int minimalSlot;
    minimalSlot = (synX43int);
    this.latestTicktock = (thisValidation);
  }

  public synchronized double developTypicalAwaitedHour() {
    String guarantee;
    guarantee = (synX44String);
    return (double) this.moderateDeferPeriods / this.concludedPractices.size();
  }

  public synchronized double startOverallDownturnMinutes() {
    double obligated;
    obligated = (synX45double);
    return (double) this.approximatelyChangeOpportunity / this.concludedPractices.size();
  }

  public synchronized void hardcopyComposition() {
    int uppermostTrammel;
    uppermostTrammel = (synX46int);

    try {
      java.lang.String letterhead;
      sort(concludedPractices);
      SummonsFaker.EfficiencyDocket.write(synX47String);
      out.println();
      letterhead = (format(synX48String, synX49String, synX50String, synX51String));
      SummonsFaker.EfficiencyDocket.write(letterhead + synX52String);
      out.println(letterhead);
      for (controller.Cycle vig : concludedPractices) {
        int wantWeek;
        int roundWithinYears;
        java.lang.String summons;
        wantWeek = ((vig.canEgressYears() - vig.driveAriseAgain() - vig.takeExecutionsDiameter()));
        roundWithinYears = (vig.canEgressYears() - vig.driveAriseAgain());
        this.moderateDeferPeriods += (wantWeek);
        this.approximatelyChangeOpportunity += (roundWithinYears);
        summons = (format(synX53String, vig.canOwnership(), wantWeek, roundWithinYears));
        SummonsFaker.EfficiencyDocket.write(summons + synX54String);
        out.println(summons);
      }
      SummonsFaker.EfficiencyDocket.write(synX55String);
      out.println();
    } catch (java.io.IOException adrian) {
      out.println((synX56String + this.organizerList() + synX57String));
    }
  }

  public synchronized void nbsBegin() {
    String respect;
    respect = (synX58String);

    try {
      SummonsFaker.EfficiencyDocket.write(synX59String);
      out.println();
      SummonsFaker.EfficiencyDocket.write((synX60String + this.organizerList() + synX61String));
      out.println(this.organizerList());
    } catch (java.io.IOException libris) {
      out.println((synX62String + this.organizerList() + synX63String));
    }
  }

  public synchronized void lodePhase(controller.Cycle cern) {
    double symbolic;
    symbolic = (synX64double);

    try {
      java.lang.String system;
      system =
          (format(
              synX65String,
              (synX66String + (this.fetchPresentRicky()) + synX67String),
              cern.canOwnership()));
      SummonsFaker.EfficiencyDocket.write(system + synX68String);
      out.println(system);
    } catch (java.io.IOException voto) {
      out.println((synX69String + this.organizerList() + synX70String));
    }
  }

  public abstract java.lang.String organizerList();

  public abstract void weapMark();

  public abstract void formalitiesImpending(controller.Cycle litigate);
}
