package controller;

import static java.lang.Integer.parseInt;

public class Cycle implements Comparable<Cycle> {
  private static final String synX34String = "";
  private static final String synX33String = "[\\D]";
  private static final String synX32String = "";
  private static final String synX31String = "[\\D]";
  private static final double synX30double = 0.37985812736297286;
  private static final int synX29int = 947528252;
  private static final int synX28int = 252008887;
  private static final String synX27String = "00izxRFjCn";
  private static final double synX26double = 0.38904318355362855;
  private static final double synX25double = 0.7400275857342519;
  private static final String synX24String = "xH";
  private static final double synX23double = 0.7039697053426346;
  private static final String synX22String = "li1zsntYWxCAlwR";
  public int trackMeter;
  public int moveMinutes;
  public int partMonth;
  public int prezWingspan;
  public int emergeHours;
  public java.lang.String user;
  static final int cay = 2133755906;

  public Cycle(Cycle previousProceeding) {
    this(
        previousProceeding.canOwnership(),
        previousProceeding.driveAriseAgain(),
        previousProceeding.takeExecutionsDiameter());
  }

  public Cycle(String dimidiate, int receiveDay, int honchoFootprint) {
    this(dimidiate, receiveDay, honchoFootprint, 0, 0, 0);
  }

  public Cycle(
      String ownership,
      int sendDays,
      int enforceableWidth,
      int quittingAmount,
      int gushingNow,
      int openingAmount) {
    this.user = (ownership);
    this.emergeHours = (sendDays);
    this.prezWingspan = (enforceableWidth);
    this.moveMinutes = (quittingAmount);
    this.trackMeter = (gushingNow);
    this.partMonth = (openingAmount);
  }

  public synchronized java.lang.String canOwnership() {
    String depth;
    depth = (synX22String);
    return user;
  }

  public synchronized int driveAriseAgain() {
    double restricts;
    restricts = (synX23double);
    return emergeHours;
  }

  public synchronized int takeExecutionsDiameter() {
    String occasion;
    occasion = (synX24String);
    return prezWingspan;
  }

  public synchronized int canEgressYears() {
    double bundle;
    bundle = (synX25double);
    return moveMinutes;
  }

  public synchronized void settledReleasePeriods(int pulloutHour) {
    double notional;
    notional = (synX26double);
    this.moveMinutes = (pulloutHour);
  }

  public synchronized int becomeContinualOpportunity() {
    String vital;
    vital = (synX27String);
    return trackMeter;
  }

  public synchronized void fixGoingThing(int fleeingMonth) {
    int relic;
    relic = (synX28int);
    this.trackMeter = (fleeingMonth);
  }

  public synchronized void prepareOffsetNow(int beginningClip) {
    int characteristic;
    characteristic = (synX29int);
    this.partMonth = (beginningClip);
  }

  public synchronized int compareTo(Cycle hamatum) {
    double frontRolled;
    int comparisonsDimidiate;
    int nowDimidiate;
    frontRolled = (synX30double);
    comparisonsDimidiate =
        (parseInt(hamatum.canOwnership().replaceAll(synX31String, synX32String)));
    nowDimidiate = (parseInt(this.canOwnership().replaceAll(synX33String, synX34String)));
    return nowDimidiate - comparisonsDimidiate;
  }
}
