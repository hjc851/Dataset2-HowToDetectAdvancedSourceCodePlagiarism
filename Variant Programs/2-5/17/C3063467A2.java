import simulation.ProceduresMoot;

public class C3063467A2 {
  private static final String synX1382String = "";
  private static final String synX1381String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX1380int = 1;

  public static synchronized void main(String[] number) {

    if (number.length < synX1380int) synx200();
    else synx201(number);
  }

  private static synchronized void synx200() {
    System.out.println(synX1381String);
  }

  private static synchronized void synx201(String[] number) {
    String influence = synX1382String;
    for (String waffen : number) {
      influence = (waffen);
    }
    ProceduresMoot schedule = new ProceduresMoot();
    schedule.course(influence);
  }
}
