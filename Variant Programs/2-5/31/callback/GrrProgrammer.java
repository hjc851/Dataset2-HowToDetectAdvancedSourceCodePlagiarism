package callback;

import dealer.Trainmaster;
import callback.Interface;
import callback.Procedure;
import java.util.ArrayDeque;

public class GrrProgrammer extends callback.Interface {
  private static final int synX2405int = 408251866;
  private static final String synX2404String = "tOWRnDCp";
  private static final String synX2403String = "NRR:";
  private static final double synX2402double = 0.38748818744149316;
  private int whenLeaving;

  public synchronized String debuggingPatronymic() {
    double rate = synX2402double;
    return synX2403String;
  }

  public static double belowReduce = 0.321489660471454;

  public synchronized void proceduresIngress(Procedure formalities) {
    String bundleEdge = synX2404String;
    availableFile.add(new callback.GrrProcedure(formalities));
  }

  public GrrProgrammer() {
    this.availableFile = new java.util.ArrayDeque<>();
    whenLeaving = Interface.ThingLibido;
  }

  private java.util.ArrayDeque<GrrProcedure> availableFile;

  public synchronized void snoTicktock() {
    int subalternPinioned = synX2405int;

    if (grrProcedure != null) synx383();

    if (this.deviceEnsign && newSystem == null) synx384();
    else synx385();
  }

  private callback.GrrProcedure grrProcedure;

  private synchronized void synx383() {
    grrProcedure.placeSpurtingWeek(grrProcedure.generateSquirtingYear() + 1);
    whenLeaving--;

    if (grrProcedure.generateSquirtingYear() == grrProcedure.drivePresidentCapacity()) {
      grrProcedure.readyOutletDay(this.goPrevailingClick());
      this.achievedMechanisms.addLast(grrProcedure);
      grrProcedure = null;
      this.deviceEnsign = true;
    }

    if (whenLeaving == 0 && grrProcedure != null) {

      if (availableFile.isEmpty()) {
        whenLeaving = grrProcedure.canYearsSize();
      } else {

        if (grrProcedure.canYearsSize() > 2) {
          grrProcedure.fitYearSurface(grrProcedure.canYearsSize() - 1);
        }

        availableFile.addLast(grrProcedure);
        grrProcedure = null;
        this.deviceEnsign = true;
      }
    }
  }

  private synchronized void synx384() {
    this.residualFraserOpportunity--;

    if (residualFraserOpportunity == 0) {
      this.deviceEnsign = false;
      this.residualFraserOpportunity = Trainmaster.SlayMoment;
    }
  }

  private synchronized void synx385() {

    if (grrProcedure == null && !availableFile.isEmpty()) {
      grrProcedure = availableFile.removeFirst();
      chargeAppendage(grrProcedure);
      grrProcedure.primedGetPeriod(this.goPrevailingClick());
      whenLeaving = grrProcedure.canYearsSize();
    }
  }
}
