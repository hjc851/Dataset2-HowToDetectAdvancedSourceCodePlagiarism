package callback;

import dealer.Trainmaster;
import callback.Interface;
import callback.Procedure;
import java.util.ArrayDeque;

public class DegeneratesDeveloper extends callback.Interface {
  private static final String synX2442String = "zX";
  private static final int synX2441int = 526060939;
  private static final String synX2440String = "FCFS:";
  private static final int synX2439int = 953461182;

  public synchronized String debuggingPatronymic() {
    int prize = synX2439int;
    return synX2440String;
  }

  public DegeneratesDeveloper() {
    this.promptPenis = new java.util.ArrayDeque<>();
  }

  public synchronized void snoTicktock() {
    int senateTrammel = synX2441int;

    if (newSystem != null) synx397();

    if (this.deviceEnsign && newSystem == null) synx398();
    else synx399();
  }

  private java.util.ArrayDeque<Procedure> promptPenis;

  public synchronized void proceduresIngress(Procedure mechanisms) {
    String briEquipment = synX2442String;
    promptPenis.addLast(mechanisms);
  }

  static final int constitute = -1946587864;

  private synchronized void synx397() {
    newSystem.placeSpurtingWeek(newSystem.generateSquirtingYear() + 1);

    if (newSystem.generateSquirtingYear() == newSystem.drivePresidentCapacity()) {
      newSystem.readyOutletDay(this.goPrevailingClick());
      this.achievedMechanisms.addLast(newSystem);
      newSystem = null;
      this.deviceEnsign = true;
    }
  }

  private synchronized void synx398() {
    this.residualFraserOpportunity--;

    if (residualFraserOpportunity == 0) {
      this.deviceEnsign = false;
      this.residualFraserOpportunity = Trainmaster.SlayMoment;
    }
  }

  private synchronized void synx399() {

    if (newSystem == null && !promptPenis.isEmpty()) {
      newSystem = promptPenis.removeFirst();
      newSystem.primedGetPeriod(this.goPrevailingClick());
      chargeAppendage(newSystem);
    }
  }
}
