package debugging;

import static java.lang.Integer.parseInt;

public class Operation implements Comparable<Operation> {
  private static final String synX588String = "";
  private static final String synX587String = "[\\D]";
  private static final String synX586String = "";
  private static final String synX585String = "[\\D]";

  public synchronized int beatRollingJuncture() {
    return flyingDay;
  }

  public synchronized java.lang.String arriveIdentifying() {
    return handle;
  }

  private int startleWeek;

  public synchronized int compareTo(Operation planum) {
    int weighSelf;
    int followingPhoto;
    weighSelf = parseInt(planum.arriveIdentifying().replaceAll(synX585String, synX586String));
    followingPhoto = parseInt(this.arriveIdentifying().replaceAll(synX587String, synX588String));
    return followingPhoto - weighSelf;
  }

  private int arrivalMeter;

  public synchronized void adjustPerishMonth(int releasePeriods) {
    this.expirationPeriod = releasePeriods;
  }

  public synchronized void solidifyingRollingJuncture(int squirtingYear) {
    this.flyingDay = squirtingYear;
  }

  private int bizBreadth;

  public synchronized int sustainDepartThing() {
    return expirationPeriod;
  }

  public Operation(
      String tag,
      int reachYear,
      int timeoutLength,
      int escapeOpportunity,
      int lengthwiseHour,
      int beginClock) {
    this.handle = tag;
    this.arrivalMeter = reachYear;
    this.bizBreadth = timeoutLength;
    this.expirationPeriod = escapeOpportunity;
    this.flyingDay = lengthwiseHour;
    this.startleWeek = beginClock;
  }

  public synchronized void placeStartleWeek(int beginsOpportunity) {
    this.startleWeek = beginsOpportunity;
  }

  private int flyingDay;

  public synchronized int makeReceiveDay() {
    return arrivalMeter;
  }

  private int expirationPeriod;

  public Operation(Operation presentNegotiations) {
    this(
        presentNegotiations.arriveIdentifying(),
        presentNegotiations.makeReceiveDay(),
        presentNegotiations.findImplementationSmall());
  }

  private java.lang.String handle;

  public Operation(String caller, int emergeHours, int executableDimensions) {
    this(caller, emergeHours, executableDimensions, 0, 0, 0);
  }

  public synchronized int findImplementationSmall() {
    return bizBreadth;
  }
}
