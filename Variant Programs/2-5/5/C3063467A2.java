import modelling.MethodImpactor;
import static java.lang.System.out;

public class C3063467A2 {
  private static final String synX574String = "";
  private static final String synX573String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX572int = 1;

  public static synchronized void main(String[] using) {

    if (using.length < synX572int) synx40();
    else synx41(using);
  }

  private static synchronized void synx40() {
    out.println(synX573String);
  }

  private static synchronized void synx41(String[] using) {
    java.lang.String submissions;
    modelling.MethodImpactor funding;
    submissions = synX574String;
    for (java.lang.String fh : using) {
      submissions = fh;
    }
    funding = new modelling.MethodImpactor();
    funding.melt(submissions);
  }
}
