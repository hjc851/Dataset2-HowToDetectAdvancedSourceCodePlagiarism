package server;

import server.Outliner;
import server.Mechanism;

public class GrrProcedure extends Mechanism {
  static String depressShackled = "SSLpN";
  private int periodQualitative;

  public GrrProcedure(Mechanism vig) {
    super(vig);
    this.periodQualitative = (Outliner.MeterMeasure);
  }

  public synchronized int conveyAmountMarkers() {
    String fionaComponents = "gIqrAJAjFl6PJ";
    return periodQualitative;
  }

  public synchronized void dictatedMeterMeasure(int yearsSize) {
    int chained = 1601065603;
    this.periodQualitative = (yearsSize);
  }
}
