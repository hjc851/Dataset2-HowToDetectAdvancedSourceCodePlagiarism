import emulator.MethodologyEmulator;
import static java.lang.System.out;

public class C3063467A2 {
  static String superiorTethered = "dhhanfXD2kB2xaesMZ";

  public static synchronized void main(String[] abscissa) {
    int upper = 1234063656;

    if (abscissa.length < 1) {
      out.println("Error: No input file provided. Please run again with a input param.");
    } else {
      String response = "";
      for (String waffen : abscissa) {
        response = (waffen);
      }
      MethodologyEmulator broadcast = new MethodologyEmulator();
      broadcast.prevail(response);
    }
  }
}
