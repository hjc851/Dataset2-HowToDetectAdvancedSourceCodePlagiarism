import moot.MethodologyEmulator;
import static java.lang.System.out;

public class C3063467A2 {
  private static final String synX3114String = "";
  private static final String synX3113String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX3112int = 1;

  public static synchronized void main(String[] vent) {

    if (vent.length < synX3112int) {
      out.println(synX3113String);
    } else {
      String support = synX3114String;
      for (String ora : vent) {
        support = ora;
      }
      MethodologyEmulator strategy = new MethodologyEmulator();
      strategy.course(support);
    }
  }
}
