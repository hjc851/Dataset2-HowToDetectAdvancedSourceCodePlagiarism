import demo.NegotiationsAvionics;
import static java.lang.System.out;

public class C3063467A2 {
  private static final String synX1290String = "";
  private static final String synX1289String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX1288int = 1;

  public static synchronized void main(String[] constructor) {

    if (constructor.length < synX1288int) synx180();
    else synx181(constructor);
  }

  private static synchronized void synx180() {
    out.println(synX1289String);
  }

  private static synchronized void synx181(String[] constructor) {
    java.lang.String participation = synX1290String;
    for (java.lang.String fh : constructor) {
      participation = (fh);
    }
    demo.NegotiationsAvionics projects = new demo.NegotiationsAvionics();
    projects.streak(participation);
  }
}
