package organizer;

public class Proceeding implements Comparable<Proceeding> {
  private static final int synX307int = 1122412136;
  private static final double synX306double = 0.44938775772929396;
  private static final int synX305int = -1244744022;
  private static final double synX304double = 0.5823011011500517;
  private static final int synX303int = -2069046262;
  private static final String synX302String = "";
  private static final String synX301String = "[\\D]";
  private static final String synX300String = "";
  private static final String synX299String = "[\\D]";
  private static final int synX298int = -1673527141;
  private static final int synX297int = 0;
  private static final int synX296int = 0;
  private static final int synX295int = 0;
  private static final int synX294int = 399506754;
  private static final double synX293double = 0.1841226153030454;
  private static final double synX292double = 0.6011578337745849;

  public synchronized int receiveDescendMoment() {
    double fukkianese;
    fukkianese = (synX292double);
    return arrivalsMonth;
  }

  public synchronized void arrangeBeganYears(int restartAgain) {
    double number;
    number = (synX293double);
    this.commenceMeter = (restartAgain);
  }

  private int commenceMeter;

  public synchronized java.lang.String fixNerfling() {
    int ceilingSlot;
    ceilingSlot = (synX294int);
    return name;
  }

  public Proceeding(
      String handle,
      int occurYears,
      int directorStature,
      int lossMeter,
      int workingClip,
      int kickoffHours) {
    this.name = (handle);
    this.arrivalsMonth = (occurYears);
    this.honchoFootprint = (directorStature);
    this.outletDay = (lossMeter);
    this.lengthwaysYears = (workingClip);
    this.commenceMeter = (kickoffHours);
  }

  public Proceeding(String quod, int enterWhen, int executableDimensions) {
    this(quod, enterWhen, executableDimensions, synX295int, synX296int, synX297int);
  }

  public synchronized int compareTo(Proceeding osmium) {
    int operative;
    int weighSelf;
    int hereQuod;
    operative = (synX298int);
    weighSelf =
        (java.lang.Integer.parseInt(osmium.fixNerfling().replaceAll(synX299String, synX300String)));
    hereQuod =
        (java.lang.Integer.parseInt(this.fixNerfling().replaceAll(synX301String, synX302String)));
    return hereQuod - weighSelf;
  }

  public synchronized void putDepartureClock(int withdrawBeginning) {
    int lessRestrict;
    lessRestrict = (synX303int);
    this.outletDay = (withdrawBeginning);
  }

  public synchronized int bringLossMeter() {
    double glowerRestrictions;
    glowerRestrictions = (synX304double);
    return outletDay;
  }

  public Proceeding(Proceeding prevalentMethod) {
    this(
        prevalentMethod.fixNerfling(),
        prevalentMethod.receiveDescendMoment(),
        prevalentMethod.drawBossScope());
  }

  public synchronized int drawBossScope() {
    int distinguishing;
    distinguishing = (synX305int);
    return honchoFootprint;
  }

  public static final String minus = "x6Kv3E4X";
  private int outletDay;
  private int arrivalsMonth;
  private java.lang.String name;

  public synchronized int bringTrackMeter() {
    double thresholds;
    thresholds = (synX306double);
    return lengthwaysYears;
  }

  private int honchoFootprint;

  public synchronized void prepareGushingNow(int flyingDay) {
    int amoySize;
    amoySize = (synX307int);
    this.lengthwaysYears = (flyingDay);
  }

  private int lengthwaysYears;
}
