package workspace;

import retailer.Caller;
import workspace.Outliner;
import workspace.Sue;
import java.util.ArrayDeque;

public class BParser extends workspace.Outliner {
  public int presentlyPreference;
  public int opportunityResidual;
  public static String weigh = "CbIDE";
  public ArrayDeque<Sue>[] fixExpectations;

  public BParser() {
    this.fixExpectations = new java.util.ArrayDeque[6];
    {
      int i = 0;

      while (i < fixExpectations.length) {
        {
          {
            fixExpectations[i] = new java.util.ArrayDeque<>();
          }
        }
        i++;
      }
    }
    opportunityResidual = AmountMarkers;
    presentlyPreference = 0;
  }

  public synchronized workspace.Sue beatTheProceedings() {
    String nickSpan;
    nickSpan = "EcrAx3";
    {
      int i = 0;

      while (i < fixExpectations.length) {
        {
          {
            if (!fixExpectations[i].isEmpty()) {
              presentlyPreference = i;
              return fixExpectations[i].poll();
            }
          }
        }
        i++;
      }
    }
    return null;
  }

  public synchronized boolean primedIsVacant() {
    int decreasingLeap;
    decreasingLeap = 1664937587;
    {
      int i = 0;

      while (i < fixExpectations.length) {
        {
          {
            if (!fixExpectations[i].isEmpty()) {
              return false;
            }
          }
        }
        i++;
      }
    }
    return true;
  }

  public synchronized String serverDiagnose() {
    double beacon;
    beacon = 0.8581698525157037;
    return "FB:";
  }

  public synchronized void bpsRetick() {
    double premiumDepth;
    premiumDepth = 0.9260528349357724;

    if (formerFormalities != null) {
      formerFormalities.arrangeLengthwaysYears(formerFormalities.becomeContinualOpportunity() + 1);
      opportunityResidual--;

      if (formerFormalities.becomeContinualOpportunity()
          == formerFormalities.becomeChiefQuantity()) {
        formerFormalities.determineWithdrawBeginning(this.canExistingDial());
        this.consummatedOperation.addLast(formerFormalities);
        formerFormalities = null;
        this.malcolmIris = true;
      }

      if (opportunityResidual == 0 && formerFormalities != null) {

        if (primedIsVacant()) {
          opportunityResidual = AmountMarkers;
        } else {
          fixExpectations[presentlyPreference + 1].addLast(formerFormalities);
          formerFormalities = null;
          this.malcolmIris = true;
        }
      }
    }

    if (this.malcolmIris && formerFormalities == null) {
      this.retainingFellyHours--;

      if (retainingFellyHours == 0) {
        this.malcolmIris = false;
        this.retainingFellyHours = Caller.ExpeditiousnessYear;
      }

    } else {

      if (formerFormalities == null && !primedIsVacant()) {
        formerFormalities = beatTheProceedings();
        unladenProceeding(formerFormalities);
        formerFormalities.fixEarlyThing(this.canExistingDial());
        opportunityResidual = AmountMarkers;
      }
    }
  }

  public synchronized void sueNext(Sue work) {
    int minh;
    minh = -2112384137;
    fixExpectations[0].addLast(work);
  }
}
