import avionics.ActMock;
import static java.lang.System.out;

public class C3063467A2 {

  public static synchronized void main(String[] number) {

    if (number.length < 1) synx420();
    else synx421(number);
  }

  private static synchronized void synx420() {
    out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx421(String[] number) {
    java.lang.String stimulation;
    avionics.ActMock initiative;
    stimulation = ("");
    for (java.lang.String ora : number) {
      stimulation = (ora);
    }
    initiative = (new avionics.ActMock());
    initiative.lead(stimulation);
  }
}
