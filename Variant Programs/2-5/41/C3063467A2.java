import simulated.SystemSim;
import static java.lang.System.out;

public class C3063467A2 {

  public static synchronized void main(String[] claims) {

    if (claims.length < 1) synx560();
    else synx561(claims);
  }

  private static synchronized void synx560() {
    out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx561(String[] claims) {
    java.lang.String suggestions = "";
    for (java.lang.String waffen : claims) {
      suggestions = (waffen);
    }
    simulated.SystemSim fund = new simulated.SystemSim();
    fund.test(suggestions);
  }
}
