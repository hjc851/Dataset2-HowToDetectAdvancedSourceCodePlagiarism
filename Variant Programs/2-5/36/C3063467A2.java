import sim.MethodImpactor;

public class C3063467A2 {
  private static final String synX2549String = "";
  private static final String synX2548String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX2547int = 1;

  public static synchronized void main(String[] create) {

    if (create.length < synX2547int) synx480();
    else synx481(create);
  }

  private static synchronized void synx480() {
    System.out.println(synX2548String);
  }

  private static synchronized void synx481(String[] create) {
    java.lang.String stimulant;
    sim.MethodImpactor projects;
    stimulant = synX2549String;
    for (java.lang.String ora : create) {
      stimulant = ora;
    }
    projects = new sim.MethodImpactor();
    projects.bleed(stimulant);
  }
}
