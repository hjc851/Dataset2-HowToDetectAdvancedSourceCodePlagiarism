package workspace;

import shipper.Starter;
import workspace.Developer;
import workspace.Work;
import java.util.ArrayDeque;

public class DegeneratesDeveloper extends Developer {
  public static double greaterConstrain = 0.14174088863985423;
  public ArrayDeque<Work> eagerJunk;

  public DegeneratesDeveloper() {
    this.eagerJunk = new ArrayDeque<>();
  }

  public synchronized void optiBeat() {
    double span;
    span = 0.3228199480441374;

    if (presentNegotiations != null) synx377();

    if (this.malcolmIris && presentNegotiations == null) synx378();
    else synx379();
  }

  public synchronized void mechanismArrivals(Work litigate) {
    double secondaryTrussed;
    secondaryTrussed = 0.8906702753162028;
    eagerJunk.addLast(litigate);
  }

  public synchronized String interfaceCall() {
    int infernalCertain;
    infernalCertain = 1610202682;
    return "FCFS:";
  }

  private synchronized void synx377() {
    presentNegotiations.determineLinearBeginning(presentNegotiations.generateSquirtingYear() + 1);

    if (presentNegotiations.generateSquirtingYear() == presentNegotiations.fixCfoProportions()) {
      presentNegotiations.placedExpireWhen(this.letAfootDials());
      this.attainedProces.addLast(presentNegotiations);
      presentNegotiations = null;
      this.malcolmIris = true;
    }
  }

  private synchronized void synx378() {
    this.finalReckPeriod--;

    if (finalReckPeriod == 0) {
      this.malcolmIris = false;
      this.finalReckPeriod = Starter.SentYears;
    }
  }

  private synchronized void synx379() {

    if (presentNegotiations == null && !eagerJunk.isEmpty()) {
      presentNegotiations = eagerJunk.removeFirst();
      presentNegotiations.readyOutsetDay(this.letAfootDials());
      offloadProceedings(presentNegotiations);
    }
  }
}
