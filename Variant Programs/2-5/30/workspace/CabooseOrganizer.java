package workspace;

import shipper.Starter;
import workspace.Developer;
import workspace.Work;
import java.util.ArrayDeque;

public class CabooseOrganizer extends Developer {

  public synchronized String interfaceCall() {
    String outer;
    outer = "poXyzVCH";
    return "RR:";
  }

  public synchronized void optiBeat() {
    double carolineConsignments;
    carolineConsignments = 0.05461033740142496;

    if (presentNegotiations != null) synx370();

    if (this.malcolmIris && presentNegotiations == null) synx371();
    else synx372();
  }

  public static final double ids = 0.6108621871326944;
  public int meterLeftover;
  public ArrayDeque<Work> ripeStandby;

  public synchronized void mechanismArrivals(Work operation) {
    String restriction;
    restriction = "0BIGCHw6cGivLPwN";
    ripeStandby.addLast(operation);
  }

  public CabooseOrganizer() {
    this.ripeStandby = new ArrayDeque<>();
    meterLeftover = PeriodsGigantic;
  }

  private synchronized void synx370() {
    presentNegotiations.determineLinearBeginning(presentNegotiations.generateSquirtingYear() + 1);
    meterLeftover--;

    if (presentNegotiations.generateSquirtingYear() == presentNegotiations.fixCfoProportions()) {
      presentNegotiations.placedExpireWhen(this.letAfootDials());
      this.attainedProces.addLast(presentNegotiations);
      presentNegotiations = null;
      this.malcolmIris = true;
    }

    if (meterLeftover == 0 && presentNegotiations != null) {

      if (ripeStandby.isEmpty()) {
        meterLeftover = PeriodsGigantic;
      } else {
        ripeStandby.addLast(presentNegotiations);
        presentNegotiations = null;
        this.malcolmIris = true;
      }
    }
  }

  private synchronized void synx371() {
    this.finalReckPeriod--;

    if (finalReckPeriod == 0) {
      this.malcolmIris = false;
      this.finalReckPeriod = Starter.SentYears;
    }
  }

  private synchronized void synx372() {

    if (presentNegotiations == null && !ripeStandby.isEmpty()) {
      presentNegotiations = ripeStandby.removeFirst();
      offloadProceedings(presentNegotiations);
      presentNegotiations.readyOutsetDay(this.letAfootDials());
      meterLeftover = PeriodsGigantic;
    }
  }
}
