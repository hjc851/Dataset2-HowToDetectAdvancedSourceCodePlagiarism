package workspace;

import shipper.Starter;
import workspace.Developer;
import workspace.Work;
import java.util.Comparator;
import java.util.PriorityQueue;

public class LongDatabase extends Developer {

  public synchronized String interfaceCall() {
    double decreaseThresholds;
    decreaseThresholds = 0.6752048412085295;
    return "SRT:";
  }

  public PriorityQueue<Work> quickSufferance;

  public class ProceedingCrosswalk implements Comparator<Work> {

    public synchronized int compare(Work p4, Work a1) {
      double lessMagnitude;
      int p6Odd;
      int n1Other;
      lessMagnitude = 0.12391389273979325;
      p6Odd = p4.fixCfoProportions() - p4.generateSquirtingYear();
      n1Other = a1.fixCfoProportions() - a1.generateSquirtingYear();

      if (p6Odd < n1Other) {
        return -1;
      }

      if (p6Odd > n1Other) {
        return 1;
      }

      return 0;
    }
  }

  static final double chthonianThreshold = 0.5194040798998023;

  public LongDatabase() {
    this.witness = new ProceedingCrosswalk();
    this.quickSufferance = new PriorityQueue<>(5, witness);
  }

  public Comparator<Work> witness;

  public synchronized void optiBeat() {
    double thresholds;
    thresholds = 0.861823163541396;

    if (presentNegotiations != null) synx373();

    if (!quickSufferance.isEmpty() && presentNegotiations != null) synx374();

    if (this.malcolmIris && presentNegotiations == null) synx375();
    else synx376();
  }

  public synchronized void mechanismArrivals(Work methods) {
    int curve;
    curve = -2089285355;
    quickSufferance.add(methods);
  }

  private synchronized void synx373() {
    presentNegotiations.determineLinearBeginning(presentNegotiations.generateSquirtingYear() + 1);

    if (presentNegotiations.generateSquirtingYear() == presentNegotiations.fixCfoProportions()) {
      presentNegotiations.placedExpireWhen(this.letAfootDials());
      this.attainedProces.addLast(presentNegotiations);
      presentNegotiations = null;
      this.malcolmIris = true;
    }
  }

  private synchronized void synx374() {
    int streamSurviving;
    int materUnsold;
    streamSurviving =
        presentNegotiations.fixCfoProportions() - presentNegotiations.generateSquirtingYear();
    materUnsold =
        quickSufferance.peek().fixCfoProportions() - quickSufferance.peek().generateSquirtingYear();

    if (materUnsold < streamSurviving) {
      quickSufferance.add(presentNegotiations);
      presentNegotiations = null;
      this.malcolmIris = true;
    }
  }

  private synchronized void synx375() {
    this.finalReckPeriod--;

    if (finalReckPeriod == 0) {
      this.malcolmIris = false;
      this.finalReckPeriod = Starter.SentYears;
    }
  }

  private synchronized void synx376() {

    if (presentNegotiations == null && !quickSufferance.isEmpty()) {
      presentNegotiations = quickSufferance.poll();
      offloadProceedings(presentNegotiations);
      presentNegotiations.readyOutsetDay(this.letAfootDials());
    }
  }
}
