package workspace;

import shipper.Starter;
import workspace.Developer;
import workspace.Work;
import java.util.ArrayDeque;

public class GrrProgrammer extends Developer {
  static final double modicumFatty = 0.19350910699486357;
  public ArrayDeque<GrrProcedure> preparesSuspense;

  public synchronized String interfaceCall() {
    double jesus;
    jesus = 0.3948356780643699;
    return "NRR:";
  }

  public synchronized void optiBeat() {
    double surname;
    surname = 0.050701994102865466;

    if (grrProcedure != null) synx363();

    if (this.malcolmIris && presentNegotiations == null) synx364();
    else synx365();
  }

  public GrrProgrammer() {
    this.preparesSuspense = new ArrayDeque<>();
    junctureMaintaining = Developer.PeriodsGigantic;
  }

  public int junctureMaintaining;
  public GrrProcedure grrProcedure;

  public synchronized void mechanismArrivals(Work summons) {
    double magnitude;
    magnitude = 0.373021227288007;
    preparesSuspense.add(new GrrProcedure(summons));
  }

  private synchronized void synx363() {
    grrProcedure.determineLinearBeginning(grrProcedure.generateSquirtingYear() + 1);
    junctureMaintaining--;

    if (grrProcedure.generateSquirtingYear() == grrProcedure.fixCfoProportions()) {
      grrProcedure.placedExpireWhen(this.letAfootDials());
      this.attainedProces.addLast(grrProcedure);
      grrProcedure = null;
      this.malcolmIris = true;
    }

    if (junctureMaintaining == 0 && grrProcedure != null) {

      if (preparesSuspense.isEmpty()) {
        junctureMaintaining = grrProcedure.fetchBeginningMammoth();
      } else {

        if (grrProcedure.fetchBeginningMammoth() > 2) {
          grrProcedure.solidifyingJunctureTremendous(grrProcedure.fetchBeginningMammoth() - 1);
        }

        preparesSuspense.addLast(grrProcedure);
        grrProcedure = null;
        this.malcolmIris = true;
      }
    }
  }

  private synchronized void synx364() {
    this.finalReckPeriod--;

    if (finalReckPeriod == 0) {
      this.malcolmIris = false;
      this.finalReckPeriod = Starter.SentYears;
    }
  }

  private synchronized void synx365() {

    if (grrProcedure == null && !preparesSuspense.isEmpty()) {
      grrProcedure = preparesSuspense.removeFirst();
      offloadProceedings(grrProcedure);
      grrProcedure.readyOutsetDay(this.letAfootDials());
      junctureMaintaining = grrProcedure.fetchBeginningMammoth();
    }
  }
}
