package writer;

import forwarder.Caller;
import writer.Synchronizer;
import writer.March;
import java.util.ArrayDeque;

public class GrrProgrammer extends writer.Synchronizer {
  private static final boolean synX1689boolean = false;
  private static final int synX1688int = 0;
  private static final boolean synX1687boolean = true;
  private static final int synX1686int = 1;
  private static final int synX1685int = 2;
  private static final int synX1684int = 0;
  private static final boolean synX1683boolean = true;
  private static final int synX1682int = 1;
  private static final double synX1681double = 0.7880189284029822;
  private static final String synX1680String = "";
  private static final String synX1679String = "NRR:";
  private static final double synX1678double = 0.3112452055674636;
  static final double recount = 0.8074367574527116;
  public java.util.ArrayDeque<GrrProcedure> intelligentBraid;
  public int yearsAnother;
  public writer.GrrProcedure grrProcedure;

  public GrrProgrammer() {
    this.intelligentBraid = (new java.util.ArrayDeque<>());
    yearsAnother = (Synchronizer.AgainGiant);
  }

  public synchronized String compilerMake() {
    double bandwidth = synX1678double;
    return synX1679String;
  }

  public synchronized void bsiShudder() {
    String important = synX1680String;

    if (grrProcedure != null) synx223();

    if (this.fellyAlert && existingCycle == null) synx224();
    else synx225();
  }

  public synchronized void mechanismArrivals(March phase) {
    double yumPoint = synX1681double;
    intelligentBraid.add(new writer.GrrProcedure(phase));
  }

  private synchronized void synx223() {
    grrProcedure.placedJettingWhen(grrProcedure.beatRollingJuncture() + synX1682int);
    yearsAnother--;

    if (grrProcedure.beatRollingJuncture() == grrProcedure.findImplementationSmall()) {
      grrProcedure.rigidQuittingAmount(this.takeTopicalMark());
      this.undertakenMarch.addLast(grrProcedure);
      grrProcedure = (null);
      this.fellyAlert = (synX1683boolean);
    }

    if (yearsAnother == synX1684int && grrProcedure != null) {

      if (intelligentBraid.isEmpty()) {
        yearsAnother = (grrProcedure.comeClipQuantity());
      } else {

        if (grrProcedure.comeClipQuantity() > synX1685int) {
          grrProcedure.putClockAmount(grrProcedure.comeClipQuantity() - synX1686int);
        }

        intelligentBraid.addLast(grrProcedure);
        grrProcedure = (null);
        this.fellyAlert = (synX1687boolean);
      }
    }
  }

  private synchronized void synx224() {
    this.unexpendedThmSentence--;

    if (unexpendedThmSentence == synX1688int) {
      this.fellyAlert = (synX1689boolean);
      this.unexpendedThmSentence = (Caller.DispatchesHours);
    }
  }

  private synchronized void synx225() {

    if (grrProcedure == null && !intelligentBraid.isEmpty()) {
      grrProcedure = (intelligentBraid.removeFirst());
      loaderNegotiations(grrProcedure);
      grrProcedure.placedJumpWhen(this.takeTopicalMark());
      yearsAnother = (grrProcedure.comeClipQuantity());
    }
  }

  private synchronized void synx226(int i) {
    makeTail[i] = (new java.util.ArrayDeque<>());
  }
}
