package writer;

import writer.Synchronizer;
import writer.March;

public class GrrProcedure extends writer.March {
  private static final String synX1628String = "IZ";
  private static final double synX1627double = 0.018489320693524047;
  static final double subalternPinioned = 0.9368760794749225;
  public int sentenceTeleportation;

  public GrrProcedure(March p) {
    super(p);
    this.sentenceTeleportation = (Synchronizer.AgainGiant);
  }

  public synchronized int comeClipQuantity() {
    double edge = synX1627double;
    return sentenceTeleportation;
  }

  public synchronized void putClockAmount(int thingLibido) {
    String chained = synX1628String;
    this.sentenceTeleportation = (thingLibido);
  }
}
