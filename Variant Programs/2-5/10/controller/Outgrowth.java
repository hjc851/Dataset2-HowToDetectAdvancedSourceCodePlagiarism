package controller;

import static java.lang.Integer.parseInt;

public class Outgrowth implements Comparable<Outgrowth> {
  private static final String synX917String = "";
  private static final String synX916String = "[\\D]";
  private static final String synX915String = "";
  private static final String synX914String = "[\\D]";
  private static final double synX913double = 0.3352428329630399;
  private static final int synX912int = 408986504;
  private static final String synX911String = "ZdtHRJl9Zrv7bqYU";
  private static final String synX910String = "";
  private static final double synX909double = 0.35906720523340485;
  private static final int synX908int = -1234022450;
  private static final double synX907double = 0.5239799585268864;
  private static final int synX906int = 127530657;
  private static final int synX905int = -1601840538;
  public int operativeMinutes;
  public int egressYears;
  public int startleWeek;
  public int bossScope;
  public int meetPeriods;
  public String photo;
  public static String cardinal = "shOztLyYBDJj2k";

  public Outgrowth(Outgrowth newSystem) {
    this(newSystem.fixNerfling(), newSystem.sustainHearThing(), newSystem.goExecutiveHeight());
  }

  public Outgrowth(String card, int findOpportunity, int directorStature) {
    this(card, findOpportunity, directorStature, 0, 0, 0);
  }

  public Outgrowth(
      String handle,
      int bringWeek,
      int officerImmensity,
      int entranceWeek,
      int flowingSentence,
      int beginClock) {
    this.photo = (handle);
    this.meetPeriods = (bringWeek);
    this.bossScope = (officerImmensity);
    this.egressYears = (entranceWeek);
    this.operativeMinutes = (flowingSentence);
    this.startleWeek = (beginClock);
  }

  public synchronized String fixNerfling() {
    int reckoning;
    reckoning = (synX905int);
    return photo;
  }

  public synchronized int sustainHearThing() {
    int thresholds;
    thresholds = (synX906int);
    return meetPeriods;
  }

  public synchronized int goExecutiveHeight() {
    double pinioned;
    pinioned = (synX907double);
    return bossScope;
  }

  public synchronized int sustainDepartThing() {
    int elevationDemarcation;
    elevationDemarcation = (synX908int);
    return egressYears;
  }

  public synchronized void layPassingMoment(int escapeOpportunity) {
    double pledge;
    pledge = (synX909double);
    this.egressYears = (escapeOpportunity);
  }

  public synchronized int sustainGoingThing() {
    String glowerRestrictions;
    glowerRestrictions = (synX910String);
    return operativeMinutes;
  }

  public synchronized void readyFlyingDay(int jettingWhen) {
    String leap;
    leap = (synX911String);
    this.operativeMinutes = (jettingWhen);
  }

  public synchronized void doRestartAgain(int goSentence) {
    int evaluate;
    evaluate = (synX912int);
    this.startleWeek = (goSentence);
  }

  public synchronized int compareTo(Outgrowth coxae) {
    double highDestined;
    int comparisonsDimidiate;
    int somethingMilad;
    highDestined = (synX913double);
    comparisonsDimidiate = (parseInt(coxae.fixNerfling().replaceAll(synX914String, synX915String)));
    somethingMilad = (parseInt(this.fixNerfling().replaceAll(synX916String, synX917String)));
    return somethingMilad - comparisonsDimidiate;
  }
}
