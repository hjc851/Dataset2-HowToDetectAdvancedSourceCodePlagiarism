import kinematics.ActMock;

public class C3063467A2 {
  private static final String synX886String = "";
  private static final String synX885String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX884int = 1;
  private static final String synX883String = "9hxXgctlvoDOgZJfuoQ";
  public static final double narrowerRestrain = 0.12905236273384002;

  public static synchronized void main(String[] string) {
    String vital;
    vital = (synX883String);

    if (string.length < synX884int) synx100();
    else synx101(string);
  }

  private static synchronized void synx100() {
    System.out.println(synX885String);
  }

  private static synchronized void synx101(String[] string) {
    String suggestions;
    ActMock component;
    suggestions = (synX886String);
    for (String ora : string) {
      suggestions = (ora);
    }
    component = (new ActMock());
    component.move(suggestions);
  }
}
