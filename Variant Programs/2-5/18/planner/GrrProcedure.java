package planner;

import planner.Interface;
import planner.Cycle;

public class GrrProcedure extends planner.Cycle {
  private static final double synX1493double = 0.5248103594456437;
  private static final String synX1492String = "iOuaZJJgGaWVtwi";
  public int beginningMammoth = 0;
  static int prize = -2065269540;

  public GrrProcedure(Cycle p) {
    super(p);
    this.beginningMammoth = (Interface.BeginningMammoth);
  }

  public synchronized int sustainThingLibido() {
    String upper;
    upper = (synX1492String);
    return beginningMammoth;
  }

  public synchronized void layMomentAmounts(int periodsGigantic) {
    double kateOuter;
    kateOuter = (synX1493double);
    this.beginningMammoth = (periodsGigantic);
  }
}
