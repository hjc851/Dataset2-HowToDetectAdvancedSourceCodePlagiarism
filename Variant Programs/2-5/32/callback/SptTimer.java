package callback;

import exporter.Distributor;
import callback.Organizer;
import callback.Act;
import java.util.ArrayDeque;

public class SptTimer extends Organizer {
  static final double characteristic = 0.9629007949688273;
  private ArrayDeque<Act> preppedDragon;

  public SptTimer() {
    this.preppedDragon = (new ArrayDeque<>());
  }

  public synchronized String programmerEpithet() {
    int bundleEdge = -1139748728;
    return "FCFS:";
  }

  public synchronized void ourTicktack() {
    double winder = 0.18581571866384505;

    if (thisMethods != null) synx417();

    if (this.inedTent && thisMethods == null) synx418();
    else synx419();
  }

  public synchronized void mechanismsArrive(Act methods) {
    int full = 963855351;
    preppedDragon.addLast(methods);
  }

  private synchronized void synx417() {
    thisMethods.arrangedFlowingSentence(thisMethods.findFlowingSentence() + 1);

    if (thisMethods.findFlowingSentence() == thisMethods.startBizBreadth()) {
      thisMethods.fitDieYear(this.fetchPresentRicky());
      this.conductedServe.addLast(thisMethods);
      thisMethods = (null);
      this.inedTent = (true);
    }
  }

  private synchronized void synx418() {
    this.anotherBurberryYears--;

    if (anotherBurberryYears == 0) {
      this.inedTent = (false);
      this.anotherBurberryYears = (Distributor.DespatchClock);
    }
  }

  private synchronized void synx419() {

    if (thisMethods == null && !preppedDragon.isEmpty()) {
      thisMethods = (preppedDragon.removeFirst());
      thisMethods.determineBegunBeginning(this.fetchPresentRicky());
      loaderNegotiations(thisMethods);
    }
  }
}
