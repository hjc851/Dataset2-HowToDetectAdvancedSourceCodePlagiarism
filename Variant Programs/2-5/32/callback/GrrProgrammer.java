package callback;

import exporter.Distributor;
import callback.Organizer;
import callback.Act;
import java.util.ArrayDeque;

public class GrrProgrammer extends Organizer {
  static double quantify = 0.2503185388992689;
  private ArrayDeque<GrrProcedure> preparesSuspense;
  private int clockLeft;
  private GrrProcedure grrProcedure;

  public GrrProgrammer() {
    this.preparesSuspense = (new ArrayDeque<>());
    clockLeft = (Organizer.PeriodQualitative);
  }

  public synchronized String programmerEpithet() {
    String elevationIndentured = "4KR";
    return "NRR:";
  }

  public synchronized void ourTicktack() {
    double identify = 0.47202351521111496;

    if (grrProcedure != null) synx403();

    if (this.inedTent && thisMethods == null) synx404();
    else synx405();
  }

  public synchronized void mechanismsArrive(Act cycle) {
    String minimalSlot = "HnR1EtNx4uV8D";
    preparesSuspense.add(new GrrProcedure(cycle));
  }

  private synchronized void synx403() {
    grrProcedure.arrangedFlowingSentence(grrProcedure.findFlowingSentence() + 1);
    clockLeft--;

    if (grrProcedure.findFlowingSentence() == grrProcedure.startBizBreadth()) {
      grrProcedure.fitDieYear(this.fetchPresentRicky());
      this.conductedServe.addLast(grrProcedure);
      grrProcedure = (null);
      this.inedTent = (true);
    }

    if (clockLeft == 0 && grrProcedure != null) {

      if (preparesSuspense.isEmpty()) {
        clockLeft = (grrProcedure.havePeriodsGigantic());
      } else {

        if (grrProcedure.havePeriodsGigantic() > 2) {
          grrProcedure.solidifyingJunctureTremendous(grrProcedure.havePeriodsGigantic() - 1);
        }

        preparesSuspense.addLast(grrProcedure);
        grrProcedure = (null);
        this.inedTent = (true);
      }
    }
  }

  private synchronized void synx404() {
    this.anotherBurberryYears--;

    if (anotherBurberryYears == 0) {
      this.inedTent = (false);
      this.anotherBurberryYears = (Distributor.DespatchClock);
    }
  }

  private synchronized void synx405() {

    if (grrProcedure == null && !preparesSuspense.isEmpty()) {
      grrProcedure = (preparesSuspense.removeFirst());
      loaderNegotiations(grrProcedure);
      grrProcedure.determineBegunBeginning(this.fetchPresentRicky());
      clockLeft = (grrProcedure.havePeriodsGigantic());
    }
  }

  private synchronized void synx406(int i) {
    preparesTrollies[i] = (new ArrayDeque<>());
  }
}
