import manikin.MethodologyEmulator;

public class C3063467A2 {
  public static final double ultimate = 0.8772124977401339;

  public static synchronized void main(String[] using) {
    double reesPurchases = 0.39947434292790196;

    if (using.length < 1) synx400();
    else synx401(using);
  }

  private static synchronized void synx400() {
    System.out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx401(String[] using) {
    String participation = "";
    for (String waffen : using) {
      participation = (waffen);
    }
    MethodologyEmulator schemes = new MethodologyEmulator();
    schemes.race(participation);
  }
}
