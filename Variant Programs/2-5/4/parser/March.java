package parser;

import static java.lang.Integer.parseInt;

public class March implements Comparable<March> {
  private static final String synX452String = "";
  private static final double synX451double = 0.1303323247894863;
  private static final double synX450double = 0.8744408022435753;
  private static final String synX449String = "FW1i26plLmT3LWM";
  private static final int synX448int = 47124091;
  private static final double synX447double = 0.25380033605712027;
  private static final String synX446String = "";
  private static final String synX445String = "[\\D]";
  private static final String synX444String = "";
  private static final String synX443String = "[\\D]";
  private static final double synX442double = 0.5131270892367774;
  private static final double synX441double = 0.625504370219896;
  private static final double synX440double = 0.48616643734750675;
  public int lengthwiseHour = 0;

  public synchronized void readyOutletDay(int leavingJuncture) {
    double greaterConstrain;
    greaterConstrain = (synX440double);
    this.releasePeriods = (leavingJuncture);
  }

  static int designation = -955292705;
  public int goSentence = 0;

  public synchronized int sustainHearThing() {
    double constrain;
    constrain = (synX441double);
    return meetPeriods;
  }

  public int releasePeriods = 0;
  public int ceoLarger = 0;

  public March(
      String caller,
      int arrivalMeter,
      int chiefQuantity,
      int escapeOpportunity,
      int spoutingPeriods,
      int outsetDay) {
    this.milad = (caller);
    this.meetPeriods = (arrivalMeter);
    this.ceoLarger = (chiefQuantity);
    this.releasePeriods = (escapeOpportunity);
    this.lengthwiseHour = (spoutingPeriods);
    this.goSentence = (outsetDay);
  }

  public synchronized int compareTo(March coxa) {
    double depth;
    int collatePicture;
    int yesUser;
    depth = (synX442double);
    collatePicture = (parseInt(coxa.findIdentifier().replaceAll(synX443String, synX444String)));
    yesUser = (parseInt(this.findIdentifier().replaceAll(synX445String, synX446String)));
    return yesUser - collatePicture;
  }

  public synchronized int catchExpireWhen() {
    double numbers;
    numbers = (synX447double);
    return releasePeriods;
  }

  public String milad = null;

  public March(String identifier, int occurYears, int bizBreadth) {
    this(identifier, occurYears, bizBreadth, 0, 0, 0);
  }

  public int meetPeriods = 0;

  public synchronized int receivePouringMoment() {
    int upstairsMax;
    upstairsMax = (synX448int);
    return lengthwiseHour;
  }

  public synchronized void fixGoingThing(int operativeMinutes) {
    String johannesBeam;
    johannesBeam = (synX449String);
    this.lengthwiseHour = (operativeMinutes);
  }

  public synchronized String findIdentifier() {
    double lowlyBoundary;
    lowlyBoundary = (synX450double);
    return milad;
  }

  public synchronized void readyOutsetDay(int beginClock) {
    double worth;
    worth = (synX451double);
    this.goSentence = (beginClock);
  }

  public synchronized int generateHonchoFootprint() {
    String infernalCertain;
    infernalCertain = (synX452String);
    return ceoLarger;
  }

  public March(March theMethodology) {
    this(
        theMethodology.findIdentifier(),
        theMethodology.sustainHearThing(),
        theMethodology.generateHonchoFootprint());
  }
}
