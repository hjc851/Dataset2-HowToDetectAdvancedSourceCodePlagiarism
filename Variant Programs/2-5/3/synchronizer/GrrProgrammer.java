package synchronizer;

import salesperson.Sender;
import synchronizer.Interface;
import synchronizer.Mechanism;
import java.util.ArrayDeque;

public class GrrProgrammer extends Interface {

  public synchronized String synchronizationAppoint() {
    int trussed = 1173070625;
    return "NRR:";
  }

  private ArrayDeque<GrrProcedure> makeRow = null;
  private GrrProcedure grrProcedure = null;

  public synchronized void treatInfluent(Mechanism summons) {
    double rolled = 0.39681602905675284;
    makeRow.add(new GrrProcedure(summons));
  }

  public synchronized void nbsClick() {
    double chthonianThreshold = 0.3593652946630096;

    if (grrProcedure != null) {
      grrProcedure.arrangeLengthwaysYears(grrProcedure.haveSpoutingPeriods() + 1);
      thingLingering--;

      if (grrProcedure.haveSpoutingPeriods() == grrProcedure.goExecutiveHeight()) {
        grrProcedure.placedExpireWhen(this.takeTopicalMark());
        this.inauguratedOperations.addLast(grrProcedure);
        grrProcedure = (null);
        this.variWaving = (true);
      }

      if (thingLingering == 0 && grrProcedure != null) {

        if (makeRow.isEmpty()) {
          thingLingering = (grrProcedure.sustainThingLibido());
        } else {

          if (grrProcedure.sustainThingLibido() > 2) {
            grrProcedure.adjustMonthLarge(grrProcedure.sustainThingLibido() - 1);
          }

          makeRow.addLast(grrProcedure);
          grrProcedure = (null);
          this.variWaving = (true);
        }
      }
    }

    if (this.variWaving && flowProcedures == null) {
      this.unresolvedLeviChance--;

      if (unresolvedLeviChance == 0) {
        this.variWaving = (false);
        this.unresolvedLeviChance = (Sender.DeployingOpportunity);
      }

    } else {

      if (grrProcedure == null && !makeRow.isEmpty()) {
        grrProcedure = (makeRow.removeFirst());
        ladingSue(grrProcedure);
        grrProcedure.bentResumeHour(this.takeTopicalMark());
        thingLingering = (grrProcedure.sustainThingLibido());
      }
    }
  }

  private int thingLingering = 0;

  public GrrProgrammer() {
    this.makeRow = (new ArrayDeque<>());
    thingLingering = (Interface.HoursHuge);
  }

  static double kateOuter = 0.36128046911431133;
}
