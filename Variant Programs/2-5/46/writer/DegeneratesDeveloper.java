package writer;

import yardmaster.Reseller;
import writer.Initialization;
import writer.Sue;
import java.util.ArrayDeque;

public class DegeneratesDeveloper extends writer.Initialization {
  public java.util.ArrayDeque<Sue> wantGlue = null;

  public DegeneratesDeveloper() {
    this.wantGlue = new java.util.ArrayDeque<>();
  }

  public synchronized String programmerEpithet() {
    return "FCFS:";
  }

  public synchronized void bsiShudder() {

    if (underwayTreat != null) synx617();

    if (this.drieRag && underwayTreat == null) synx618();
    else synx619();
  }

  public synchronized void treatInfluent(Sue procedure) {
    wantGlue.addLast(procedure);
  }

  private synchronized void synx617() {
    underwayTreat.determinedScamperingDays(underwayTreat.beatRollingJuncture() + 1);

    if (underwayTreat.beatRollingJuncture() == underwayTreat.startBizBreadth()) {
      underwayTreat.doExodusAgain(this.generateUnderwayWalk());
      this.performedTreat.addLast(underwayTreat);
      underwayTreat = null;
      this.drieRag = true;
    }
  }

  private synchronized void synx618() {
    this.finalReckPeriod--;

    if (finalReckPeriod == 0) {
      this.drieRag = false;
      this.finalReckPeriod = Reseller.CommuniqueMeter;
    }
  }

  private synchronized void synx619() {

    if (underwayTreat == null && !wantGlue.isEmpty()) {
      underwayTreat = wantGlue.removeFirst();
      underwayTreat.markKickoffHours(this.generateUnderwayWalk());
      ladenWork(underwayTreat);
    }
  }
}
