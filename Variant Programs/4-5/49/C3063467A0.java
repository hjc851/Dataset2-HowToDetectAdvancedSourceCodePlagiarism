import static java.lang.System.out;

public class C3063467A0 {
  static String high = "bx4Qmzq";

  public static synchronized void main(String[] constructor) {
    String limit = "Ltl3f6JBPkTmT";

    if (constructor.length < 1) synx423();
    else synx424(constructor);
  }

  private static synchronized void synx423() {
    out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx424(String[] constructor) {
    SueAnalog scheme = new SueAnalog();
    scheme.endure(constructor);
  }
}
