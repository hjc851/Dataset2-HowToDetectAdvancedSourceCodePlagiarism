public abstract class ReplacingScheme {

  protected abstract boolean breakWishes(System flowProcedures);

  protected abstract void lendHomepage(Layout writes, System underwayTreat);

  protected abstract boolean isComplete();
}
