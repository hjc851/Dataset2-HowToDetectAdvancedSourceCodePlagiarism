import static java.lang.System.out;

public class C3063467A0 {

  public static synchronized void main(String[] specified) {

    if (specified.length < 1) synx134();
    else synx135(specified);
  }

  private static synchronized void synx134() {
    out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx135(String[] specified) {
    PhaseSimulations syllabus;
    syllabus = (new PhaseSimulations());
    syllabus.prevail(specified);
  }
}
