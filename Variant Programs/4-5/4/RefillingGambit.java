public abstract class RefillingGambit {

  protected abstract boolean isComplete();

  protected abstract void supplySummary(Layouts postscript, System presentlyMechanism);

  protected abstract boolean correspondRequisition(System latestOperation);
}
