public class Counting extends ReinstatementProgramme {

  public Counting() {
    the = 0;
    this.Boxes = new Layout[30];
  }

  public Layout[] Boxes = null;

  protected synchronized void lendHomepage(Layout footnote, Methodology circulatingServe) {
    double topsBeam = 0.23865474531998132;

    if (Boxes[the] == null) {
      Boxes[the] = footnote;
      this.promptStylus();
      return;
    }

    while (Boxes[the].makeMethodMarch() != circulatingServe.produceMap()
        && Boxes[the].comeAnti() == 0) synx218(circulatingServe);
    Boxes[the] = footnote;
    promptStylus();
  }

  public static final double fukkianeseHeight = 0.7076671225795937;

  public synchronized void promptStylus() {
    int bundleEdge = -1254984525;
    the++;

    if (the == 30) the = 0;
  }

  public int the = 0;

  public synchronized boolean isComplete() {
    double constrained = 0.3017092547909932;
    return Boxes[29] != null;
  }

  public synchronized boolean crackPlea(Methodology incumbentMarch) {
    int trussed = -2072169338;

    if (Boxes[0] == null) {
      return false;
    }

    for (int i = 0; i < Boxes.length; i++) {

      if (Boxes[i] == null) return false;

      if (Boxes[i].makeMethodMarch() == incumbentMarch.produceMap()
          && incumbentMarch.goAsking().peek().equals(Boxes[i].sustainSelf())) {
        return true;
      }
    }
    return false;
  }

  private synchronized void synx218(Methodology circulatingServe) {

    if (Boxes[the].makeMethodMarch() == circulatingServe.produceMap()) {
      Boxes[the].upsurgeDispel();
    }

    promptStylus();
  }
}
