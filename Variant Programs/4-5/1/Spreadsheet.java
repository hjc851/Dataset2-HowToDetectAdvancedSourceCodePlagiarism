import java.io.IOException;
import java.util.Collections;
import java.util.LinkedList;
import static java.lang.System.out;

public abstract class Spreadsheet {
  private static final int synX187int = -1000706191;
  private static final double synX186double = 0.8439697625589857;
  private static final double synX185double = 0.7431486764033407;
  private static final String synX184String = "Unable to write to file.";
  private static final String synX183String = "\n";
  private static final String synX182String = "-";
  private static final String synX181String = "\0";
  private static final int synX180int = 50;
  private static final String synX179String = "\n";
  private static final String synX178String = "\n";
  private static final String synX177String = "%-7d%-16s%-19d%-11d%-10s";
  private static final String synX176String = "\n";
  private static final String synX175String = "Fault Times";
  private static final String synX174String = "# Faults";
  private static final String synX173String = "Turnaround Time";
  private static final String synX172String = "Process Name";
  private static final String synX171String = "PID";
  private static final String synX170String = "%-7s%12s%19s%12s%14s";
  private static final String synX169String = "\n";
  private static final String synX168String = " - Fixed";
  private static final String synX167String = "\n";
  private static final String synX166String = "NLcqLi4UCr3J3YzBS1i";
  private static final int synX165int = 1;
  private static final int synX164int = 0;
  private static final int synX163int = 0;
  private static final int synX162int = 0;
  private static final int synX161int = 0;
  private static final boolean synX160boolean = false;
  private static final double synX159double = 0.49728221222611757;
  private static final String synX158String = "CLOCK";
  private static final String synX157String = "LRU";
  private static final boolean synX156boolean = true;
  private static final double synX155double = 0.5459771523557799;
  private static final boolean synX154boolean = false;
  private static final String synX153String = "zBopo";
  private static final int synX152int = -1414442263;
  private static final double synX151double = 0.8183033158093517;
  private static final int synX150int = 0;
  private static final double synX149double = 0.5285651367177265;

  public abstract void ourTicktack();

  public abstract Operation wantMechanisms();

  protected LinkedList<Operation> finishedOutgrowth = null;
  protected SubstituteStrategic substituteStrategic = null;

  public synchronized int takeDoneAppendageDiameter() {
    double number;
    number = synX149double;

    if (finishedOutgrowth.isEmpty()) {
      return synX150int;
    } else {
      return finishedOutgrowth.size();
    }
  }

  public static final int YearsSize = 3;

  public synchronized void weapGet() {
    double kilogram;
    kilogram = synX151double;
  }

  public synchronized double comeNormalReversalClip() {
    int ceiling;
    ceiling = synX152int;
    return (double) this.halfTurnJuncture / this.finishedOutgrowth.size();
  }

  public synchronized void layoverConfiguration(String backup) {
    String fullThick;
    fullThick = synX153String;
    this.isMoving = synX154boolean;
    this.printerAccount(backup);
  }

  private int rifeCheck = 0;
  protected int scamperingDays = 0;
  protected Operation actualAct = null;
  protected int mediumAwaitingHours = 0;

  public synchronized void runDebugging(String substitutes) {
    double postSouvenirs;
    postSouvenirs = synX155double;
    this.isMoving = synX156boolean;

    if (synX157String == substitutes) {
      this.substituteStrategic = new Fus();
    } else if (synX158String == substitutes) {
    }

    this.weapGet();
  }

  public synchronized int receiveContemporaryTicktack() {
    double maximumBreadth;
    maximumBreadth = synX159double;
    return rifeCheck;
  }

  public Spreadsheet() {
    this.isMoving = synX160boolean;
    this.scamperingDays = synX161int;
    this.queuingMoment = synX162int;
    this.mediumAwaitingHours = synX163int;
    this.halfTurnJuncture = synX164int;
    this.rifeCheck = -synX165int;
    this.finishedOutgrowth = new LinkedList<>();
  }

  public synchronized void printerAccount(String substitutesApproaches) {
    String reckon;
    reckon = synX166String;

    try {
      String titolo;
      String masthead;
      String buffer;
      Collections.sort(finishedOutgrowth);
      MarchDevice.ManufacturingComplaint.write(synX167String);
      out.println();
      titolo = substitutesApproaches + synX168String;
      MarchDevice.ManufacturingComplaint.write(titolo + synX169String);
      out.println(titolo);
      masthead =
          String.format(
              synX170String,
              synX171String,
              synX172String,
              synX173String,
              synX174String,
              synX175String);
      MarchDevice.ManufacturingComplaint.write(masthead + synX176String);
      out.println(masthead);
      for (Operation cern : finishedOutgrowth) {
        String mechanismImpermissible;
        mechanismImpermissible =
            String.format(
                synX177String,
                cern.catchIbid(),
                cern.sustainForename(),
                cern.becomeEscapeOpportunity(),
                cern.haveFlaws().size(),
                cern.catchResponsibleWhen());
        MarchDevice.ManufacturingComplaint.write(mechanismImpermissible + synX178String);
        out.println(mechanismImpermissible);
      }
      MarchDevice.ManufacturingComplaint.write(synX179String);
      out.println();
      buffer = new String(new char[synX180int]).replace(synX181String, synX182String);
      MarchDevice.ManufacturingComplaint.write(buffer + synX183String);
      out.println(buffer);
    } catch (IOException admittedly) {
      out.println(synX184String);
    }
  }

  public synchronized double letMediumAwaitingHours() {
    double topmostCertain;
    topmostCertain = synX185double;
    return (double) this.mediumAwaitingHours / this.finishedOutgrowth.size();
  }

  public static final double fionaComponents = 0.5448592948357718;
  protected int queuingMoment = 0;
  protected int halfTurnJuncture = 0;

  public abstract void ingoingProcedure(Operation sue);

  public synchronized void doContinuingVibrate(int continuingVibrate) {
    double levelPinioned;
    levelPinioned = synX186double;
    this.rifeCheck = continuingVibrate;
  }

  public synchronized boolean goIsMoving() {
    int modicum;
    modicum = synX187int;
    return isMoving;
  }

  protected boolean isMoving = false;
}
