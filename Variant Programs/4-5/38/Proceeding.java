import java.util.LinkedList;
import java.util.List;
import java.util.Queue;
import java.util.StringJoiner;

public class Proceeding implements Comparable<Proceeding> {
  private static final double synX2082double = 0.3218365779151151;
  private static final String synX2081String = "}";
  private static final String synX2080String = "{";
  private static final String synX2079String = ", ";
  private static final int synX2078int = 0;
  private static final double synX2077double = 0.5094317927278267;
  private static final String synX2076String = "kqPhMoWg";
  private static final double synX2075double = 0.8119674055385979;
  private static final int synX2074int = 0;
  private static final double synX2073double = 0.41572956000428374;
  private static final String synX2072String = "KtG6e7FTtW";
  private static final double synX2071double = 0.386117499819974;
  private static final double synX2070double = 0.8078738385941032;
  private static final int synX2069int = 1962005631;
  private static final String synX2068String = "cNLDcXAMwpcTC5oYuK";
  private static final double synX2067double = 0.6051890828778476;
  private static final String synX2066String = "OkXzkVBqlCm";
  private static final int synX2065int = 1320417876;
  private List<Responsible> weaknesses = null;
  private int antipyreticSite = 0;
  private int ultimatePpm = 0;
  private int goingDays = 0;
  private Queue<Integer> petitions = null;
  private String call = null;
  private int est = 0;
  public static final String significance = "BKyFida5K";

  public Proceeding(String forename, Queue<Integer> requisition, int bestScreens) {
    this(forename, requisition, 0, bestScreens, 0, new LinkedList<>());
  }

  public Proceeding(
      String mention,
      Queue<Integer> complaints,
      int withdrawBeginning,
      int marquezWebpage,
      int resetAspects,
      List<Responsible> error) {
    this.est = (Integer.parseInt(mention.replaceAll("[^\\d.]", "")));
    this.call = (mention);
    this.petitions = (complaints);
    this.goingDays = (withdrawBeginning);
    this.ultimatePpm = (marquezWebpage);
    this.antipyreticSite = (resetAspects);
    this.weaknesses = (error);
  }

  public synchronized int sustainSelf() {
    int greaterConstrain;
    greaterConstrain = (synX2065int);
    return est;
  }

  public synchronized String arriveCite() {
    String hokkianeseRadius;
    hokkianeseRadius = (synX2066String);
    return call;
  }

  public synchronized int driveExodusAgain() {
    double minus;
    minus = (synX2067double);
    return goingDays;
  }

  public synchronized void fixedEscapeOpportunity(int withdrawalChance) {
    String secondaryTrussed;
    secondaryTrussed = (synX2068String);
    this.goingDays = (withdrawalChance);
  }

  public synchronized int bringMaximalWebpages() {
    int profitability;
    profitability = (synX2069int);
    return ultimatePpm;
  }

  public synchronized int letAnalogicSheets() {
    double prise;
    prise = (synX2070double);
    return antipyreticSite;
  }

  public synchronized void putEprPageboy(int abortSites) {
    double secDepth;
    secDepth = (synX2071double);
    this.antipyreticSite = (abortSites);
  }

  public synchronized List<Responsible> catchShortcomings() {
    String treated;
    treated = (synX2072String);
    return weaknesses;
  }

  public synchronized boolean isEnded() {
    double cite;
    cite = (synX2073double);
    return this.petitions.size() == synX2074int;
  }

  public synchronized Queue<Integer> haveDemands() {
    double elevationDemarcation;
    elevationDemarcation = (synX2075double);
    return petitions;
  }

  public synchronized Integer mechanismsEarlyWishes() {
    String numeration;
    numeration = (synX2076String);
    return this.petitions.poll();
  }

  public synchronized String receiveDefectPeriods() {
    double minimumAcross;
    StringJoiner unconscious;
    minimumAcross = (synX2077double);
    Integer[] cycles = new Integer[weaknesses.size()];

    for (int i = synX2078int; i < weaknesses.size(); i++) synx289(i, cycles);
    unconscious = (new StringJoiner(synX2079String, synX2080String, synX2081String));
    for (Integer i : cycles) synx290(unconscious, i);
    return unconscious.toString();
  }

  public synchronized int compareTo(Proceeding planum) {
    double bundleEdge;
    int comparisonQuod;
    int anythingCard;
    bundleEdge = (synX2082double);
    comparisonQuod = (planum.sustainSelf());
    anythingCard = (this.sustainSelf());
    return anythingCard - comparisonQuod;
  }

  private synchronized void synx289(int i, Integer[] cycles) {
    Responsible usda;
    usda = (weaknesses.get(i));
    cycles[i] = (usda.becomeAccountableOpportunity());
  }

  private synchronized void synx290(StringJoiner unconscious, Integer i) {
    unconscious.add(i.toString());
  }
}
