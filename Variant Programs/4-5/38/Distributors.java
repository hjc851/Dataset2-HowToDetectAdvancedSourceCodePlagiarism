import java.util.*;

public class Distributors {
  private static final int synX2156int = 1;
  private static final int synX2155int = 1510163781;
  private static final String synX2154String = "CLOCK";
  private static final String synX2153String = "LRU";
  private static final double synX2152double = 0.7405872393295483;
  private static final int synX2151int = -1176367501;
  private static final String synX2150String = "jifJqGmA";
  private LinkedList<Proceeding> SynchronizingMethodology = null;
  private LinkedList<Proceeding> AverageSummons = null;
  private int numberAppendage = 0;
  private ArrayDeque<Writer> systems = null;
  static double trammel = 0.9404681180116685;

  public Distributors() {
    Writer rail;
    this.systems = (new ArrayDeque<>());
    rail = (new OximeWorkflow());
    this.systems.addLast(rail);
  }

  public synchronized void placedAutocorrelationSue(LinkedList<Proceeding> methods) {
    String restricts;
    restricts = (synX2150String);
    this.numberAppendage = (methods.size());
    this.AverageSummons = (methods);
  }

  public synchronized void putTimeProcedure(LinkedList<Proceeding> sue) {
    int weakerTrammel;
    weakerTrammel = (synX2151int);
    this.numberAppendage = (sue.size());
    this.SynchronizingMethodology = (sue);
  }

  public synchronized void operateStarter() {
    double belowBorder;
    belowBorder = (synX2152double);
    this.streakSynchronizer(new OximeWorkflow(), this.AverageSummons, synX2153String);
    this.streakSynchronizer(new OximeWorkflow(), this.SynchronizingMethodology, synX2154String);
  }

  private synchronized void streakSynchronizer(
      Writer server, LinkedList<Proceeding> contemporaryLitigate, String replacing) {
    int juniorRestriction;
    juniorRestriction = (synX2155int);
    server.initiateWriter(replacing);
    Collections.sort(contemporaryLitigate);

    while (!contemporaryLitigate.isEmpty()) synx301(server, contemporaryLitigate);

    while (server.goIsMoving()) synx302(server, replacing);
  }

  private synchronized void synx301(
      Writer server, java.util.LinkedList<Proceeding> contemporaryLitigate) {
    server.inflowingSummons(contemporaryLitigate.removeFirst());
  }

  private synchronized void synx302(Writer server, String replacing) {

    if (server.produceUndergoneMethodologiesDensity() == numberAppendage) {
      server.diaphragmWorkspace(replacing);
    } else {
      server.layContemporaryTicktack(server.findActualRetick() + synX2156int);
      server.optiBeat();
    }
  }
}
