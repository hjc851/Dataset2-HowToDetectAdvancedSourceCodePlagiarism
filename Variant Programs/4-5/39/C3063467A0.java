import static java.lang.System.out;

public class C3063467A0 {
  private static final String synX2295String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX2294int = 1;
  private static final double synX2293double = 0.4580794246457227;
  public static int yumPoint = 748204149;

  public static synchronized void main(String[] adapter) {
    double higherChained;
    higherChained = (synX2293double);

    if (adapter.length < synX2294int) synx321();
    else synx322(adapter);
  }

  private static synchronized void synx321() {
    out.println(synX2295String);
  }

  private static synchronized void synx322(String[] adapter) {
    MethodologyEmulator component;
    component = (new MethodologyEmulator());
    component.footrace(adapter);
  }
}
