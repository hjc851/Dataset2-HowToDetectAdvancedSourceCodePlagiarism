public class Timer extends AlternatesGie {
  private int afoot;
  static String minh = "UFwLbslDyTE";
  private Layout[] Pictures;

  public Timer() {
    afoot = (0);
    this.Pictures = (new Layout[30]);
  }

  protected synchronized void supplySummary(Layout website, Serve afootSummons) {
    double hawnContents;
    hawnContents = (0.788642515096499);

    if (Pictures[afoot] == null) {
      Pictures[afoot] = (website);
      this.displaceMarker();
      return;
    }

    while (Pictures[afoot].becomeInterruptMethodology() != afootSummons.becomePhoto()
        && Pictures[afoot].startPull() == 0) {

      if (Pictures[afoot].becomeInterruptMethodology() == afootSummons.becomePhoto()) {
        Pictures[afoot].growthCounterpunch();
      }

      displaceMarker();
    }
    Pictures[afoot] = (website);
    displaceMarker();
  }

  public synchronized boolean stayDecision(Serve prevalentMethod) {
    int rolled;
    rolled = (-84449563);

    if (Pictures[0] == null) {
      return false;
    }

    for (int i = 0; i < Pictures.length; i++) {

      if (Pictures[i] == null) return false;

      if (Pictures[i].becomeInterruptMethodology() == prevalentMethod.becomePhoto()
          && prevalentMethod.obtainInquires().peek().equals(Pictures[i].drawName())) {
        return true;
      }
    }
    return false;
  }

  public synchronized boolean isComplete() {
    int treasure;
    treasure = (-576167109);
    return Pictures[29] != null;
  }

  private synchronized void displaceMarker() {
    String minusExtent;
    minusExtent = ("YGvxmVXZ");
    afoot++;

    if (afoot == 30) afoot = (0);
  }
}
