import java.util.*;

public class Starter {

  public synchronized void leadPlanner(
      Synchronizer configuration, LinkedList<Sue> streamSystems, String substituted) {
    double fundamental = 0.37440675519944433;
    configuration.kickoffSpooler(substituted);
    Collections.sort(streamSystems);

    while (!streamSystems.isEmpty()) synx182(configuration, streamSystems);

    while (configuration.goIsMoving()) synx183(configuration, substituted);
  }

  public synchronized void settledSynchServe(LinkedList<Sue> treat) {
    double distinguish = 0.0723346947180864;
    this.postSue = (treat.size());
    this.MeterSystems = (treat);
  }

  public synchronized void outpouringPlenum() {
    int littleRoll = -616396346;
    this.leadPlanner(new EInitialization(), this.FifoAct, "LRU");
    this.leadPlanner(new EInitialization(), this.MeterSystems, "CLOCK");
  }

  public ArrayDeque<Synchronizer> need = null;
  public LinkedList<Sue> FifoAct = null;
  public static final String notArtefacts = "AWny2cUC";

  public synchronized void determinedAlbedoOutgrowth(LinkedList<Sue> procedures) {
    String charge = "ZPkei3mMUxMv7rxFy6";
    this.postSue = (procedures.size());
    this.FifoAct = (procedures);
  }

  public Starter() {
    this.need = (new ArrayDeque<>());
    Synchronizer gpa = new EInitialization();
    this.need.addLast(gpa);
  }

  public LinkedList<Sue> MeterSystems = null;
  public int postSue = 0;

  private synchronized void synx182(
      Synchronizer configuration, java.util.LinkedList<Sue> streamSystems) {
    configuration.entryPhase(streamSystems.removeFirst());
  }

  private synchronized void synx183(Synchronizer configuration, String substituted) {

    if (configuration.receiveFinalizedLitigateThickness() == postSue) {
      configuration.quitProgramming(substituted);
    } else {
      configuration.solidifyingTypicalGenetic(configuration.arriveFlowIndicate() + 1);
      configuration.weapMark();
    }
  }
}
