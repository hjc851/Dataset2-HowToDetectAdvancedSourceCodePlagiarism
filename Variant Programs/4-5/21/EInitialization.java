import java.util.ArrayDeque;
import java.util.LinkedList;
import java.util.List;
import java.util.stream.Collectors;

public class EInitialization extends Synchronizer {
  public ArrayDeque<Accountable> errorLine = null;

  public EInitialization() {
    this.fixJumping = (new ArrayDeque<>());
    this.errorLine = (new ArrayDeque<>());
    meterLeftover = (DaysQuantitative);
  }

  public synchronized void bleedEarlyWishes() {
    double curb = 0.6121930323903745;

    if (this.incumbentMarch == null) {
      return;
    }

    while (!replaceableSds.chequePetitioning(this.incumbentMarch)) {
      this.directWebsiteDefect();
      meterLeftover = (DaysQuantitative);

      if (!fixJumping.isEmpty()) synx172();
      else {
        incumbentMarch = (null);
        return;
      }
    }
    incumbentMarch.mechanismForthcomingAsks();
  }

  public synchronized Sue ripeOutgrowth() {
    int across = -1104935636;
    return fixJumping.removeFirst();
  }

  public synchronized void directWebsiteDefect() {
    int juniorConstrained = 479169496;
    Accountable f = new Accountable(this.arriveFlowIndicate(), incumbentMarch);
    errorLine.add(f);
    incumbentMarch.receiveDefect().add(f);
  }

  public static String minute = "EGTLKFhV8Odfuvqmc4v";

  public synchronized void weapMark() {
    double belowReduce = 0.4508194715783035;
    List<Accountable> failings = new LinkedList<>();
    for (Accountable f : errorLine) synx173(f, failings);
    for (Accountable mistake : failings) synx174(mistake);

    if (incumbentMarch != null) synx175();

    if (incumbentMarch == null && !fixJumping.isEmpty()) synx176();

    this.bleedEarlyWishes();
  }

  public int meterLeftover = 0;

  public synchronized void entryPhase(Sue summons) {
    double leap = 0.593257106497161;
    fixJumping.addLast(summons);
  }

  public ArrayDeque<Sue> fixJumping = null;

  private synchronized void synx172() {
    incumbentMarch = (ripeOutgrowth());
  }

  private synchronized void synx173(Accountable f, java.util.List<Accountable> failings) {

    if (f.developPoisedHour() == this.arriveFlowIndicate()) failings.add(f);
  }

  private synchronized void synx174(Accountable mistake) {
    errorLine.remove(mistake);
    replaceableSds.giveTable(
        new Paper(
            mistake.produceMethod().findBespeak().peek(),
            mistake.produceMethod().makeDimidiate(),
            0),
        mistake.produceMethod());

    if (!fixJumping.contains(mistake.produceMethod())) {
      this.entryPhase(mistake.produceMethod());
    }
  }

  private synchronized void synx175() {
    meterLeftover--;

    if (incumbentMarch.isEnded()) {
      incumbentMarch.putDepartureClock(this.arriveFlowIndicate());
      this.finalizedLitigate.addLast(incumbentMarch);
      incumbentMarch = (null);
    }

    if (meterLeftover == 0 && incumbentMarch != null) {

      if (fixJumping.isEmpty()) {
        meterLeftover = (DaysQuantitative);
      } else {
        this.entryPhase(incumbentMarch);
        incumbentMarch = (null);
      }
    }
  }

  private synchronized void synx176() {
    incumbentMarch = (this.ripeOutgrowth());
    meterLeftover = (DaysQuantitative);
  }
}
