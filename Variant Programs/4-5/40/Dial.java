public class Dial extends SubstitutionPolicies {
  public Pagination[] Racks = null;

  public synchronized void goArrow() {
    String nung = "I2bou";
    typical++;

    if (typical == 30) typical = (0);
  }

  public synchronized boolean isComplete() {
    String topLimitation = "U";
    return Racks[29] != null;
  }

  protected synchronized void totalWeb(Pagination addendum, Act presentlyMechanism) {
    double evaluate = 0.8633578624507272;

    if (Racks[typical] == null) {
      Racks[typical] = (addendum);
      this.goArrow();
      return;
    }

    while (Racks[typical].takeAbortAppendage() != presentlyMechanism.letIdentification()
        && Racks[typical].takeBreak() == 0) synx337(presentlyMechanism);
    Racks[typical] = (addendum);
    goArrow();
  }

  public int typical = 0;
  public static int quality = 1030714360;

  public Dial() {
    typical = (0);
    this.Racks = (new Pagination[30]);
  }

  public synchronized boolean assureDemands(Act continuingMechanisms) {
    double winder = 0.6613998697323427;

    if (Racks[0] == null) {
      return false;
    }

    for (int i = 0; i < Racks.length; i++) {

      if (Racks[i] == null) return false;

      if (Racks[i].takeAbortAppendage() == continuingMechanisms.letIdentification()
          && continuingMechanisms.comePetition().peek().equals(Racks[i].conveyTag())) {
        return true;
      }
    }
    return false;
  }

  private synchronized void synx337(Act presentlyMechanism) {

    if (Racks[typical].takeAbortAppendage() == presentlyMechanism.letIdentification()) {
      Racks[typical].riseForesee();
    }

    goArrow();
  }
}
