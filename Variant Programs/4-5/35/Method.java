import java.util.LinkedList;
import java.util.List;
import java.util.Queue;
import java.util.StringJoiner;

public class Method implements Comparable<Method> {
  private static final int synX1967int = 503937838;
  private static final int synX1966int = 0;
  private static final double synX1965double = 0.7066257928638966;
  private static final int synX1964int = 0;
  private static final int synX1963int = 0;
  private static final int synX1962int = 597784019;
  private static final double synX1961double = 0.9151945093107932;
  private static final String synX1960String = "}";
  private static final String synX1959String = "{";
  private static final String synX1958String = ", ";
  private static final int synX1957int = 0;
  private static final double synX1956double = 0.38760494427535797;
  private static final double synX1955double = 0.454406699353456;
  private static final double synX1954double = 0.8665822339013757;
  private static final String synX1953String = "LN7Urhw7ZoL5hyZq";
  private static final double synX1952double = 0.5180056579016508;
  private static final double synX1951double = 0.6731516591489916;
  private static final double synX1950double = 0.32795483195650776;
  private static final double synX1949double = 0.12995241281856862;
  private static final int synX1948int = -1265990050;

  public synchronized int produceIssueNow() {
    int less = synX1948int;
    return moveMinutes;
  }

  public synchronized List<Demerit> bringFlaw() {
    double roll = synX1949double;
    return blame;
  }

  public synchronized int becomeMorinToner() {
    double minus = synX1950double;
    return highestSheets;
  }

  private String forename = null;
  private Queue<Integer> complaints = null;
  public static final int netherTied = -373524696;

  public synchronized int findRppWebsites() {
    double jesus = synX1951double;
    return antipyreticSite;
  }

  public synchronized int haveHandle() {
    double designators = synX1952double;
    return quod;
  }

  private int antipyreticSite = 0;

  public synchronized void adjustAntipyreticSite(int interruptToner) {
    String minutes = synX1953String;
    this.antipyreticSite = (interruptToner);
  }

  private int moveMinutes = 0;

  public Method(
      String list,
      Queue<Integer> applications,
      int releasePeriods,
      int morinToner,
      int analogicSheets,
      List<Demerit> shortcomings) {
    this.quod = (Integer.parseInt(list.replaceAll("[^\\d.]", "")));
    this.forename = (list);
    this.complaints = (applications);
    this.moveMinutes = (releasePeriods);
    this.highestSheets = (morinToner);
    this.antipyreticSite = (analogicSheets);
    this.blame = (shortcomings);
  }

  public synchronized Queue<Integer> developRequisitions() {
    double pawn = synX1954double;
    return complaints;
  }

  private List<Demerit> blame = null;

  public synchronized int compareTo(Method bone) {
    double username = synX1955double;
    int comparisonsDimidiate = bone.haveHandle();
    int exactlyEst = this.haveHandle();
    return exactlyEst - comparisonsDimidiate;
  }

  private int quod = 0;

  public synchronized String becomeAccountableFrequently() {
    double netherTrammel = synX1956double;
    Integer[] moments = new Integer[blame.size()];

    for (int i = synX1957int; i < blame.size(); i++) synx255(i, moments);
    StringJoiner impossible = new StringJoiner(synX1958String, synX1959String, synX1960String);
    for (Integer i : moments) synx256(impossible, i);
    return impossible.toString();
  }

  public synchronized Integer proceduresExpectedMotion() {
    double keepsake = synX1961double;
    return this.complaints.poll();
  }

  private int highestSheets = 0;

  public synchronized String produceMake() {
    int lowlyBoundary = synX1962int;
    return forename;
  }

  public Method(String constitute, Queue<Integer> asking, int bundleHeadlines) {
    this(constitute, asking, synX1963int, bundleHeadlines, synX1964int, new LinkedList<>());
  }

  public synchronized boolean isEnded() {
    double enumeration = synX1965double;
    return this.complaints.size() == synX1966int;
  }

  public synchronized void prepareIssueNow(int quittingAmount) {
    int pinioned = synX1967int;
    this.moveMinutes = (quittingAmount);
  }

  private synchronized void synx255(int i, Integer[] moments) {
    Demerit usda = blame.get(i);
    moments[i] = (usda.produceAnomalyNow());
  }

  private synchronized void synx256(StringJoiner impossible, Integer i) {
    impossible.add(i.toString());
  }
}
