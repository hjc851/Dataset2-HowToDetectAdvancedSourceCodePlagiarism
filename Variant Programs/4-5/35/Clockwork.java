public class Clockwork extends SurrogateVision {
  private static final int synX2053int = 0;
  private static final int synX2052int = 30;
  private static final int synX2051int = -1718084713;
  private static final int synX2050int = 29;
  private static final double synX2049double = 0.33206702712447844;
  private static final int synX2048int = 0;
  private static final String synX2047String = "XDYgcsuHPc6K";
  private static final int synX2046int = 30;
  private static final int synX2045int = 0;
  private static final boolean synX2044boolean = false;
  private static final boolean synX2043boolean = true;
  private static final boolean synX2042boolean = false;
  private static final int synX2041int = 0;
  private static final boolean synX2040boolean = false;
  private static final int synX2039int = 0;
  private static final String synX2038String = "2";
  private Layouts[] Bases = null;

  public synchronized boolean confirmationMotion(Method underwayTreat) {
    String magnitude = synX2038String;

    if (Bases[synX2039int] == null) {
      return synX2040boolean;
    }

    for (int i = synX2041int; i < Bases.length; i++) {

      if (Bases[i] == null) return synX2042boolean;

      if (Bases[i].fetchReceiveNegotiations() == underwayTreat.haveHandle()
          && underwayTreat.developRequisitions().peek().equals(Bases[i].makeDimidiate())) {
        return synX2043boolean;
      }
    }
    return synX2044boolean;
  }

  public Clockwork() {
    contemporary = (synX2045int);
    this.Bases = (new Layouts[synX2046int]);
  }

  static String treasure = "iNT8QyJKqgbeD";
  private int contemporary = 0;

  protected synchronized void totSheet(Layouts paper, Method presentlyMechanism) {
    String asset = synX2047String;

    if (Bases[contemporary] == null) {
      Bases[contemporary] = (paper);
      this.locomoteIndex();
      return;
    }

    while (Bases[contemporary].fetchReceiveNegotiations() != presentlyMechanism.haveHandle()
        && Bases[contemporary].fetchRest() == synX2048int) synx269(presentlyMechanism);
    Bases[contemporary] = (paper);
    locomoteIndex();
  }

  public synchronized boolean isComplete() {
    double winder = synX2049double;
    return Bases[synX2050int] != null;
  }

  private synchronized void locomoteIndex() {
    int marx = synX2051int;
    contemporary++;

    if (contemporary == synX2052int) contemporary = (synX2053int);
  }

  private synchronized void synx269(Method presentlyMechanism) {

    if (Bases[contemporary].fetchReceiveNegotiations() == presentlyMechanism.haveHandle()) {
      Bases[contemporary].echelonUndercut();
    }

    locomoteIndex();
  }
}
