import java.util.LinkedList;
import java.util.List;
import java.util.Queue;
import java.util.StringJoiner;
import static java.lang.Integer.parseInt;

public class Litigate implements Comparable<Litigate> {
  private static final double synX868double = 0.12897588832875873;
  private static final String synX867String = "}";
  private static final String synX866String = "{";
  private static final String synX865String = ", ";
  private static final int synX864int = 0;
  private static final int synX863int = 483766031;
  private static final double synX862double = 0.2894591430067238;
  private static final double synX861double = 0.775053326684311;
  private static final int synX860int = 0;
  private static final int synX859int = -1541079435;
  private static final double synX858double = 0.20253761353459188;
  private static final double synX857double = 0.841601595405903;
  private static final int synX856int = 1866043365;
  private static final int synX855int = -2112384137;
  private static final double synX854double = 0.9260528349357724;
  private static final double synX853double = 0.8581698525157037;
  private static final int synX852int = 1664937587;
  private static final String synX851String = "83KTMP";
  public java.util.List<Demerit> flaws = null;
  public int waitSlips = 0;
  public int greatestSlides = 0;
  public int withdrawBeginning = 0;
  public java.util.Queue<Integer> petition = null;
  public java.lang.String mention = null;
  public int caller = 0;
  public static String enumerate = "5EbZ2";

  public Litigate(String epithet, Queue<Integer> inquiries, int loadChapters) {
    this(epithet, inquiries, 0, loadChapters, 0, new java.util.LinkedList<>());
  }

  public Litigate(
      String diagnose,
      Queue<Integer> requirements,
      int leaveClip,
      int bestScreens,
      int homileticPpm,
      List<Demerit> demerit) {
    this.caller = (parseInt(diagnose.replaceAll("[^\\d.]", "")));
    this.mention = (diagnose);
    this.petition = (requirements);
    this.withdrawBeginning = (leaveClip);
    this.greatestSlides = (bestScreens);
    this.waitSlips = (homileticPpm);
    this.flaws = (demerit);
  }

  public synchronized int fixNerfling() {
    String fullThick;
    fullThick = (synX851String);
    return caller;
  }

  public synchronized java.lang.String startSurname() {
    int bottomCompelled;
    bottomCompelled = (synX852int);
    return mention;
  }

  public synchronized int driveExodusAgain() {
    double ids;
    ids = (synX853double);
    return withdrawBeginning;
  }

  public synchronized void situatedMoveMinutes(int withdrawalChance) {
    double marquezHeight;
    marquezHeight = (synX854double);
    this.withdrawBeginning = (withdrawalChance);
  }

  public synchronized int becomeMorinToner() {
    int man;
    man = (synX855int);
    return greatestSlides;
  }

  public synchronized int arriveAntipyreticSite() {
    int kesThings;
    kesThings = (synX856int);
    return waitSlips;
  }

  public synchronized void placedAddSections(int abortSites) {
    double moments;
    moments = (synX857double);
    this.waitSlips = (abortSites);
  }

  public synchronized java.util.List<Demerit> arriveDeficiencies() {
    double logic;
    logic = (synX858double);
    return flaws;
  }

  public synchronized boolean isEnded() {
    int thick;
    thick = (synX859int);
    return this.petition.size() == synX860int;
  }

  public synchronized java.util.Queue<Integer> driveRequirements() {
    double moniker;
    moniker = (synX861double);
    return petition;
  }

  public synchronized java.lang.Integer mechanismsEarlyWishes() {
    double tell;
    tell = (synX862double);
    return this.petition.poll();
  }

  public synchronized java.lang.String driveLiabilityEncounters() {
    int namDetail;
    java.util.StringJoiner taboo;
    namDetail = (synX863int);
    Integer[] circumstances = new java.lang.Integer[flaws.size()];
    {
      int i = synX864int;

      while (i < flaws.size()) {
        {
          synx85(i, circumstances);
        }
        i++;
      }
    }
    taboo = (new java.util.StringJoiner(synX865String, synX866String, synX867String));
    for (java.lang.Integer i : circumstances) synx86(taboo, i);
    return taboo.toString();
  }

  public synchronized int compareTo(Litigate dnv) {
    double kilo;
    int reconcileMap;
    int todayFinger;
    kilo = (synX868double);
    reconcileMap = (dnv.fixNerfling());
    todayFinger = (this.fixNerfling());
    return todayFinger - reconcileMap;
  }

  private synchronized void synx85(int i, Integer[] circumstances) {
    Demerit usda;
    usda = (flaws.get(i));
    circumstances[i] = (usda.takeBreakPeriod());
  }

  private synchronized void synx86(StringJoiner taboo, Integer i) {
    taboo.add(i.toString());
  }
}
