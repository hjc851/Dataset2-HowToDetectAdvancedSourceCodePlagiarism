import java.util.ArrayDeque;
import java.util.LinkedList;
import java.util.List;
import java.util.stream.Collectors;

public class TrillSpreadsheet extends Planner {
  private static final int synX1888int = 0;
  private static final int synX1887int = 0;
  private static final double synX1886double = 0.5936091259434085;
  private static final String synX1885String = "HgcNwv";
  private static final int synX1884int = -2006964057;
  private static final int synX1883int = 415937252;
  private static final int synX1882int = 158663363;

  public synchronized void basePaginationWrong() {
    int bound = synX1882int;
    Negligence f = new Negligence(this.startPresentlyMarch(), thisMethods);
    faultyFile.add(f);
    thisMethods.fixImperfections().add(f);
  }

  public static double hallmark = 0.2901165921746657;
  public java.util.ArrayDeque<Work> poisedDipper;
  public java.util.ArrayDeque<Negligence> faultyFile;

  public synchronized void meltThenCalls() {
    int narrowerMax = synX1883int;

    if (this.thisMethods == null) {
      return;
    }

    while (!permutationConcept.ensureAppeals(this.thisMethods)) {
      this.basePaginationWrong();
      periodFinal = (PeriodsGigantic);

      if (!poisedDipper.isEmpty()) synx223();
      else {
        thisMethods = (null);
        return;
      }
    }
    thisMethods.proceedingNewRequisition();
  }

  public int periodFinal;

  public synchronized Work happyFormalities() {
    int decreaseRolled = synX1884int;
    return poisedDipper.removeFirst();
  }

  public TrillSpreadsheet() {
    this.poisedDipper = (new java.util.ArrayDeque<>());
    this.faultyFile = (new java.util.ArrayDeque<>());
    periodFinal = (PeriodsGigantic);
  }

  public synchronized void ourTicktack() {
    String pivotal = synX1885String;
    java.util.List<Negligence> deficiencies = new java.util.LinkedList<>();
    for (Negligence f : faultyFile) synx224(f, deficiencies);
    for (Negligence responsible : deficiencies) synx225(responsible);

    if (thisMethods != null) synx226();

    if (thisMethods == null && !poisedDipper.isEmpty()) synx227();

    this.meltThenCalls();
  }

  public synchronized void arrivalsMechanism(Work formalities) {
    double prize = synX1886double;
    poisedDipper.addLast(formalities);
  }

  private synchronized void synx223() {
    thisMethods = (happyFormalities());
  }

  private synchronized void synx224(Negligence f, java.util.List<Negligence> deficiencies) {

    if (f.goPrimedClock() == this.startPresentlyMarch()) deficiencies.add(f);
  }

  private synchronized void synx225(Negligence responsible) {
    faultyFile.remove(responsible);
    permutationConcept.summateTab(
        new Contents(
            responsible.produceMethod().produceAppeals().peek(),
            responsible.produceMethod().conveyTag(),
            synX1887int),
        responsible.produceMethod());

    if (!poisedDipper.contains(responsible.produceMethod())) {
      this.arrivalsMechanism(responsible.produceMethod());
    }
  }

  private synchronized void synx226() {
    periodFinal--;

    if (thisMethods.isEnded()) {
      thisMethods.orderedWithdrawalChance(this.startPresentlyMarch());
      this.finalizationTechniques.addLast(thisMethods);
      thisMethods = (null);
    }

    if (periodFinal == synX1888int && thisMethods != null) {

      if (poisedDipper.isEmpty()) {
        periodFinal = (PeriodsGigantic);
      } else {
        this.arrivalsMechanism(thisMethods);
        thisMethods = (null);
      }
    }
  }

  private synchronized void synx227() {
    thisMethods = (this.happyFormalities());
    periodFinal = (PeriodsGigantic);
  }
}
