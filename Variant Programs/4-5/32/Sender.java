import java.util.*;
import static java.util.Collections.sort;

public class Sender {
  private static final String synX1921String = "CLOCK";
  private static final String synX1920String = "LRU";
  private static final double synX1919double = 0.38680344021620106;
  private static final int synX1918int = -507229968;
  private static final double synX1917double = 0.6510748368048526;
  private static final String synX1916String = "fZveS";

  public synchronized void extendSynchronization(
      Planner synchronizer,
      java.util.LinkedList<Work> continuingTechniques,
      java.lang.String substitutes) {
    String restricts = synX1916String;
    synchronizer.runDebugging(substitutes);
    sort(continuingTechniques);

    while (!continuingTechniques.isEmpty()) synx233(synchronizer, continuingTechniques);

    while (synchronizer.goIsMoving()) synx234(synchronizer, substitutes);
  }

  public java.util.ArrayDeque<Planner> algorithms;

  public synchronized void prepareAzimuthMethodologies(java.util.LinkedList<Work> methodology) {
    double evaluate = synX1917double;
    this.yumAct = (methodology.size());
    this.AzimuthMethodologies = (methodology);
  }

  public java.util.LinkedList<Work> AzimuthMethodologies;
  public int yumAct;

  public synchronized void arrangedTimerAct(java.util.LinkedList<Work> summons) {
    int numbersObjects = synX1918int;
    this.yumAct = (summons.size());
    this.SyncAppendage = (summons);
  }

  public Sender() {
    this.algorithms = (new java.util.ArrayDeque<>());
    Planner diddle = new TrillSpreadsheet();
    this.algorithms.addLast(diddle);
  }

  public synchronized void goTrainmaster() {
    double ids = synX1919double;
    this.extendSynchronization(new TrillSpreadsheet(), this.AzimuthMethodologies, synX1920String);
    this.extendSynchronization(new TrillSpreadsheet(), this.SyncAppendage, synX1921String);
  }

  public java.util.LinkedList<Work> SyncAppendage;
  public static final String ultimate = "zIjCQ0ZB48pzx";

  private synchronized void synx233(
      Planner synchronizer, java.util.LinkedList<Work> continuingTechniques) {
    synchronizer.arrivalsMechanism(continuingTechniques.removeFirst());
  }

  private synchronized void synx234(Planner synchronizer, String substitutes) {

    if (synchronizer.startConstructedMethodologyBreadth() == yumAct) {
      synchronizer.discontinueTimer(substitutes);
    } else {
      synchronizer.putPrevailingClick(synchronizer.startPresentlyMarch() + 1);
      synchronizer.ourTicktack();
    }
  }
}
