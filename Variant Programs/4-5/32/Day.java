public class Day extends SubstituteStrategic {
  private static final boolean synX1935boolean = false;
  private static final boolean synX1934boolean = true;
  private static final boolean synX1933boolean = false;
  private static final int synX1932int = 0;
  private static final boolean synX1931boolean = false;
  private static final int synX1930int = 0;
  private static final double synX1929double = 0.6448646208274043;
  private static final int synX1928int = 29;
  private static final int synX1927int = 1191728937;
  private static final int synX1926int = 0;
  private static final int synX1925int = 30;
  private static final int synX1924int = 238487416;
  private static final int synX1923int = 0;
  private static final double synX1922double = 0.28744387245466396;

  protected synchronized void summateTab(Contents tab, Work actualAct) {
    double appraise = synX1922double;

    if (Tires[existing] == null) {
      Tires[existing] = (tab);
      this.relocateIndication();
      return;
    }

    while (Tires[existing].arriveAntipyreticProcedures() != actualAct.conveyTag()
        && Tires[existing].beatTile() == synX1923int) synx235(actualAct);
    Tires[existing] = (tab);
    relocateIndication();
  }

  public Contents[] Tires;

  public synchronized void relocateIndication() {
    int littleRoll = synX1924int;
    existing++;

    if (existing == synX1925int) existing = (synX1926int);
  }

  public static int upstreamReduce = -1054445987;

  public Day() {
    existing = (0);
    this.Tires = (new Contents[30]);
  }

  public synchronized boolean isComplete() {
    int modicumFatty = synX1927int;
    return Tires[synX1928int] != null;
  }

  public int existing;

  public synchronized boolean ensureAppeals(Work typicalProceedings) {
    double nominal = synX1929double;

    if (Tires[synX1930int] == null) {
      return synX1931boolean;
    }

    for (int i = synX1932int; i < Tires.length; i++) {

      if (Tires[i] == null) return synX1933boolean;

      if (Tires[i].arriveAntipyreticProcedures() == typicalProceedings.conveyTag()
          && typicalProceedings.produceAppeals().peek().equals(Tires[i].generateIdem())) {
        return synX1934boolean;
      }
    }
    return synX1935boolean;
  }

  private synchronized void synx235(Work actualAct) {

    if (Tires[existing].arriveAntipyreticProcedures() == actualAct.conveyTag()) {
      Tires[existing].gainReverse();
    }

    relocateIndication();
  }
}
