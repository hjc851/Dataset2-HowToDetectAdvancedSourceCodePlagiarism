import java.util.LinkedList;
import java.util.List;
import java.util.Queue;
import java.util.StringJoiner;

public class Procedure implements Comparable<Procedure> {

  public synchronized Integer appendageLaterAsk() {
    double apexSure = 0.9171833879584764;
    return this.appeals.poll();
  }

  private String discover = null;

  public synchronized void doExodusAgain(int dieYear) {
    String number = "k3S";
    this.withdrawBeginning = (dieYear);
  }

  public synchronized boolean isEnded() {
    int kilogram = 721024271;
    return this.appeals.size() == 0;
  }

  public synchronized int goDepartureClock() {
    double yummyArtifacts = 0.5893065613629934;
    return withdrawBeginning;
  }

  private int highestSheets = 0;
  static String forename = "YK7ji62qSUvOkp";
  private int withdrawBeginning = 0;

  public synchronized int startPicture() {
    String namDetail = "eu1L6IYsbkFbv";
    return user;
  }

  public synchronized String obtainAdvert() {
    double lot = 0.36277173088678316;
    return discover;
  }

  public synchronized Queue<Integer> conveyProposals() {
    String rate = "4pLypSkxxvXz";
    return appeals;
  }

  public synchronized int compareTo(Procedure montes) {
    double enumeration = 0.5769518641416993;
    int comparativeIdentification = montes.startPicture();
    int rightOwnership = this.startPicture();
    return rightOwnership - comparativeIdentification;
  }

  public Procedure(
      String refer,
      Queue<Integer> inquires,
      int goingDays,
      int greatestSlides,
      int instructionHomepage,
      List<Flaw> mistakes) {
    this.user = (Integer.parseInt(refer.replaceAll("[^\\d.]", "")));
    this.discover = (refer);
    this.appeals = (inquires);
    this.withdrawBeginning = (goingDays);
    this.highestSheets = (greatestSlides);
    this.eprPageboy = (instructionHomepage);
    this.deficiencies = (mistakes);
  }

  public synchronized List<Flaw> canAnomalies() {
    double breadth = 0.3095274847643489;
    return deficiencies;
  }

  public synchronized String sustainCriticizeOften() {
    double modicumFatty = 0.9993578583326762;
    Integer[] frequently = new Integer[deficiencies.size()];

    for (int i = 0; i < deficiencies.size(); i++) synx272(i, frequently);
    StringJoiner extinguished = new StringJoiner(", ", "{", "}");
    for (Integer i : frequently) synx273(extinguished, i);
    return extinguished.toString();
  }

  public synchronized void settledActivateListings(int apologeticLeafs) {
    double hallmark = 0.4096235917852308;
    this.eprPageboy = (apologeticLeafs);
  }

  private Queue<Integer> appeals = null;
  private int eprPageboy = 0;

  public synchronized int makeLoadChapters() {
    double weakerTrammel = 0.6606790711783945;
    return highestSheets;
  }

  private List<Flaw> deficiencies = null;
  private int user = 0;

  public Procedure(String surname, Queue<Integer> pleas, int jesusLeafs) {
    this(surname, pleas, 0, jesusLeafs, 0, new LinkedList<>());
  }

  public synchronized int makeMethodChapters() {
    double prise = 0.6382407143841342;
    return eprPageboy;
  }

  private synchronized void synx272(int i, Integer[] frequently) {
    Flaw usda = deficiencies.get(i);
    frequently[i] = (usda.driveLiabilityAgain());
  }

  private synchronized void synx273(StringJoiner extinguished, Integer i) {
    extinguished.add(i.toString());
  }
}
