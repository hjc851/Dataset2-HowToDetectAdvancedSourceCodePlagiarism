import java.util.*;
import java.util.function.Predicate;
import java.util.stream.Collectors;

public class Texas extends SurrogateVision {
  private static final int synX2461int = 1;
  private static final String synX2460String = "GZMTf2ZF";
  private static final boolean synX2459boolean = false;
  private static final boolean synX2458boolean = true;
  private static final int synX2457int = 0;
  private static final String synX2456String = "kiezY";
  private static final int synX2455int = 1088406589;
  private static final double synX2454double = 0.8238837455298211;
  private static final int synX2453int = 0;
  private static final int synX2452int = 0;
  private static final String synX2451String = "yr2J5wmVutiYWdRQtv";
  public java.util.Queue<Web> Wheels;
  public final int MarxSquares = 30;
  public static int maximalLength = -1458826465;

  public Texas() {
    this.Wheels = (new java.util.LinkedList<>());
  }

  protected synchronized void createAddendum(Web addendum, Formalities theMethodology) {
    String measure;
    java.util.List<Web> interim;
    Web actual;
    measure = (synX2451String);

    if (this.isComplete()
        || theMethodology.drawArithmeticHeadlines() == theMethodology.arriveTopsSite())
      synx347(theMethodology);

    interim = (new java.util.LinkedList<>());
    for (Web f : Wheels) synx348(f, addendum, theMethodology, interim);
    actual = (null);

    if (interim.size() > synX2452int) actual = (interim.get(synX2453int));

    if (actual == null) synx349(addendum, theMethodology);
    else synx350(actual);
  }

  public synchronized void removalSite(Formalities contemporaryLitigate) {
    double nominate;
    java.util.function.Predicate<Web> ial;
    Web largest;
    nominate = (synX2454double);
    ial =
        (new java.util.function.Predicate<Web>() {

          public synchronized boolean test(Web postscript) {
            int critical;
            critical = (synX2455int);
            return postscript.beatWaitProceedings() == contemporaryLitigate.produceMap();
          }
        });
    largest =
        (Wheels.stream().filter(ial).max(java.util.Comparator.comparingInt(Web::driveStem)).get());
    Wheels.remove(largest);
  }

  public synchronized boolean tabAsked(Formalities rifeSue) {
    String depth;
    java.util.List<Web> table;
    depth = (synX2456String);
    Wheels.forEach(Web::markupAntagonistic);
    table = (new java.util.ArrayList<>());
    for (Web Physique : Wheels) synx351(rifeSue, Physique, table);

    if (table.size() > synX2457int) {
      table.forEach(Web::rebootCounterpunch);
      return synX2458boolean;
    }

    return synX2459boolean;
  }

  public synchronized boolean isComplete() {
    String atkinsAppliances;
    atkinsAppliances = (synX2460String);
    return Wheels.size() >= MarxSquares;
  }

  private synchronized void synx347(Formalities theMethodology) {
    this.removalSite(theMethodology);
  }

  private synchronized void synx348(
      Web f, Web addendum, Formalities theMethodology, java.util.List<Web> interim) {

    if (f.bringCaller() == addendum.bringCaller()
        && f.beatWaitProceedings() == theMethodology.produceMap()) interim.add(f);
  }

  private synchronized void synx349(Web addendum, Formalities theMethodology) {
    Wheels.add(addendum);
    theMethodology.orderedDidacticScreens(theMethodology.drawArithmeticHeadlines() + synX2461int);
  }

  private synchronized void synx350(Web actual) {
    actual.rebootCounterpunch();
  }

  private synchronized void synx351(Formalities rifeSue, Web Physique, java.util.List<Web> table) {

    if (rifeSue.haveDemands().peek().equals(Physique.bringCaller())
        && rifeSue.produceMap() == Physique.beatWaitProceedings()) {
      table.add(Physique);
    }
  }
}
