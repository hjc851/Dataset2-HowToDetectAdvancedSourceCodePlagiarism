import static java.lang.System.out;

public class C3063467A0 {
  private static final String synX2485String =
      "Error: No input file provided. Please run again with a input param.";
  private static final int synX2484int = 1;
  private static final double synX2483double = 0.4014732275830296;
  public static final double shackled = 0.6790875422656221;

  public static synchronized void main(String[] arguments) {
    double outer;
    outer = (synX2483double);

    if (arguments.length < synX2484int) synx355();
    else synx356(arguments);
  }

  private static synchronized void synx355() {
    out.println(synX2485String);
  }

  private static synchronized void synx356(String[] arguments) {
    WorkSimulating plans;
    plans = (new WorkSimulating());
    plans.lean(arguments);
  }
}
