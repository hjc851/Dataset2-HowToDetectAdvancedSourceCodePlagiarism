public abstract class FallbackStrategist {

  protected abstract void summateTab(Footnote cern, Proceedings thisMethods);

  protected abstract boolean seePetition(Proceedings actualAct);

  protected abstract boolean isComplete();
}
