import java.util.*;

public class Exporter {

  public synchronized void scarperCaller() {
    this.leanController(new EInitialization(), this.BlockbusterTechnologies, "LRU");
    this.leanController(new EInitialization(), this.HourSummons, "CLOCK");
  }

  public synchronized void leanController(
      Workflow timer, LinkedList<Sue> formerMethod, String substitution) {
    timer.firstPlanner(substitution);
    Collections.sort(formerMethod);

    while (!formerMethod.isEmpty()) {
      timer.outboundProceeding(formerMethod.removeFirst());
    }

    while (timer.goIsMoving()) {

      if (timer.arriveUndertookProceduresWingspan() == tedSystems) {
        timer.kiboshSynchronization(substitution);
      } else {
        timer.placedRifeCheck(timer.conveyFormerGene() + 1);
        timer.nbsClick();
      }
    }
  }

  public int tedSystems = 0;
  public LinkedList<Sue> HourSummons = null;

  public synchronized void arrangeSynchronizationMethods(LinkedList<Sue> act) {
    this.tedSystems = (act.size());
    this.HourSummons = (act);
  }

  public ArrayDeque<Workflow> makers = null;
  public LinkedList<Sue> BlockbusterTechnologies = null;

  public Exporter() {
    this.makers = (new ArrayDeque<>());
    Workflow gpa = new EInitialization();
    this.makers.addLast(gpa);
  }

  public synchronized void markAverageSummons(LinkedList<Sue> methodologies) {
    this.tedSystems = (methodologies.size());
    this.BlockbusterTechnologies = (methodologies);
  }
}
