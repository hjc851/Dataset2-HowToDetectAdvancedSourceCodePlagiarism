public abstract class SubstitutionPolicies {

  protected abstract boolean isComplete();

  protected abstract boolean ensureAppeals(Sue typicalProceedings);

  protected abstract void enhanceFront(Addendum vig, Sue newSystem);
}
