public class Carelessness {
  public Sue summons = null;
  public int willingMeter = 0;

  public Carelessness(int errorSentence, Sue formalities) {
    this.mistakeClip = (errorSentence);
    this.willingMeter = (errorSentence + SubstitutionDays);
    this.summons = (formalities);
  }

  public synchronized Sue comeOperation() {
    return summons;
  }

  public synchronized int goPrimedClock() {
    return willingMeter;
  }

  public synchronized int goBlameClock() {
    return mistakeClip;
  }

  public static final int SubstitutionDays = 6;
  public int mistakeClip = 0;
}
