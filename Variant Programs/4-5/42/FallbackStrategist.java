public abstract class FallbackStrategist {

  protected abstract void appendWebpage(Summary postscript, Treat underwayTreat);

  protected abstract boolean isComplete();

  protected abstract boolean substantiationInsistence(Treat prevailingProcedure);
}
