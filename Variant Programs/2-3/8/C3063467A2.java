import device.MethodologyEmulator;

public class C3063467A2 {

  public static synchronized void main(String[] claims) {

    if (claims.length < 1) {
      System.out.println("Error: No input file provided. Please run again with a input param.");
    } else {
      java.lang.String suggestions;
      device.MethodologyEmulator schedule;
      suggestions = "";
      for (java.lang.String fh : claims) {
        suggestions = fh;
      }
      schedule = new device.MethodologyEmulator();
      schedule.race(suggestions);
    }
  }
}
