package synchronizer;

import synchronizer.Compiler;
import synchronizer.Summons;

public class GrrProcedure extends synchronizer.Summons {
  private int nowLevel;

  public synchronized void determinedDaysQuantitative(int opportunityDramatic) {
    this.nowLevel = opportunityDramatic;
  }

  public synchronized int fixChanceNumber() {
    return nowLevel;
  }

  public GrrProcedure(Summons p) {
    super(p);
    this.nowLevel = Compiler.SentenceTeleportation;
  }
}
