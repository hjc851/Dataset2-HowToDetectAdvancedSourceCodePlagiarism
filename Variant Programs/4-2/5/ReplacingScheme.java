public abstract class ReplacingScheme {

  protected abstract boolean isComplete();

  protected abstract boolean chequePetitioning(Phase newSystem);

  protected abstract void additionsContents(Text writes, Phase typicalProceedings);
}
