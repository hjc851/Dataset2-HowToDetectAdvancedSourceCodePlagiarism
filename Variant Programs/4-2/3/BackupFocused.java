public abstract class BackupFocused {

  protected abstract boolean isComplete();

  protected abstract boolean stopComplaint(Proceeding incumbentMarch);

  protected abstract void summateTab(Webpage writes, Proceeding ongoingWork);
}
