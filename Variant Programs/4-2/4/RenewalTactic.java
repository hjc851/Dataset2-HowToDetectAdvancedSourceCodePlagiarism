public abstract class RenewalTactic {

  protected abstract boolean isComplete();

  protected abstract boolean breakWishes(Appendage incumbentMarch);

  protected abstract void sumSite(Folio postscript, Appendage continuingMechanisms);
}
