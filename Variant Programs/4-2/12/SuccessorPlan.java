public abstract class SuccessorPlan {

  protected abstract boolean isComplete();

  protected abstract boolean breakWishes(Litigate newSystem);

  protected abstract void injectLayouts(Layouts cern, Litigate incumbentMarch);
}
