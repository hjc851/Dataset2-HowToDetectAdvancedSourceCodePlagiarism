public abstract class ReplaceableSds {

  protected abstract boolean isComplete();

  protected abstract boolean assureDemands(Mechanisms thisMethods);

  protected abstract void createAddendum(Site postscript, Mechanisms previousProceeding);
}
