public abstract class ReplacesFramework {

  protected abstract boolean isComplete();

  protected abstract boolean checkoutInvitation(March latestOperation);

  protected abstract void totalWeb(Paper cern, March topicalAppendage);
}
