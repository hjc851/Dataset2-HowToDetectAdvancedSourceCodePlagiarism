public abstract class SwitchApproach {

  protected abstract boolean isComplete();

  protected abstract boolean watchQuest(Procedures existingCycle);

  protected abstract void augmentPagination(Tab writes, Procedures typicalProceedings);
}
