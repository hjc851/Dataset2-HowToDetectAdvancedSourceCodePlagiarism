public abstract class ReplacingScheme {

  protected abstract boolean isComplete();

  protected abstract boolean confirmationMotion(March streamPhase);

  protected abstract void introduceChapter(Summary writes, March liveOutgrowth);
}
