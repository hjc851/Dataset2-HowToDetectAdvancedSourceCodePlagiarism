public abstract class SuccessorPlan {

  protected abstract boolean isComplete();

  protected abstract boolean chequePetitioning(Phase existingCycle);

  protected abstract void provideLayout(Footnote cern, Phase presentlyMechanism);
}
