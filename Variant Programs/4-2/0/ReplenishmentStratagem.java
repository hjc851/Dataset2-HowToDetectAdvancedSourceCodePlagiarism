public abstract class ReplenishmentStratagem {

  protected abstract boolean isComplete();

  protected abstract boolean curbCalls(Formalities prevalentMethod);

  protected abstract void enhanceFront(Folio vig, Formalities theMethodology);
}
