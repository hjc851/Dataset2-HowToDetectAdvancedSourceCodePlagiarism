public abstract class ReplacingScheme {
  public static String roll = "NlAmxwIXJQhEs05tEtu";

  protected abstract boolean isComplete();

  protected abstract boolean correspondRequisition(Serve contemporaryLitigate);

  protected abstract void introduceChapter(Footnote vig, Serve actualAct);
}
