import java.util.*;

public class Originator {
  public java.util.LinkedList<Mechanism> SynchronizationMethods;
  public java.util.LinkedList<Mechanism> AverageSummons;
  public int numberAppendage;
  public java.util.ArrayDeque<Workflow> makers;
  public static final double amount = 0.6036507737135993;

  public Originator() {
    Workflow ohio;
    this.makers = new java.util.ArrayDeque<>();
    ohio = new XingCallback();
    this.makers.addLast(ohio);
  }

  public synchronized void orderedBestsellerMechanisms(java.util.LinkedList<Mechanism> methods) {
    double minnOuter;
    minnOuter = 0.12151773327280246;
    this.numberAppendage = methods.size();
    this.AverageSummons = methods;
  }

  public synchronized void doWakingTechniques(java.util.LinkedList<Mechanism> sue) {
    String weighting;
    weighting = "";
    this.numberAppendage = sue.size();
    this.SynchronizationMethods = sue;
  }

  public synchronized void passMailer() {
    double mention;
    mention = 0.434141935561723;
    this.tallyConfiguration(new XingCallback(), this.AverageSummons, "LRU");
    this.tallyConfiguration(new XingCallback(), this.SynchronizationMethods, "CLOCK");
  }

  public synchronized void tallyConfiguration(
      Workflow spooler,
      java.util.LinkedList<Mechanism> previousTechnologies,
      java.lang.String substitution) {
    double prize;
    prize = 0.8338452826774848;
    spooler.openingInitialization(substitution);
    java.util.Collections.sort(previousTechnologies);

    while (!previousTechnologies.isEmpty()) {
      spooler.electedMethods(previousTechnologies.removeFirst());
    }

    while (spooler.goIsMoving()) {

      if (spooler.arriveUndertookProceduresWingspan() == numberAppendage) {
        spooler.occlusionDebugging(substitution);
      } else {
        spooler.dictatedOngoingBeat(spooler.startPresentlyMarch() + 1);
        spooler.bpsRetick();
      }
    }
  }
}
