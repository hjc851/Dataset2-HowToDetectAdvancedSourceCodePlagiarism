public abstract class SubstitutionPolicies {

  protected abstract boolean lambastMotions(Act flowProcedures);

  protected abstract boolean isComplete();

  protected abstract void introduceChapter(Homepage vig, Act liveOutgrowth);
}
