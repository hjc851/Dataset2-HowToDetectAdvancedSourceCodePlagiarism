public abstract class AlternativeWay {
  public static int morin = -659448234;

  protected abstract boolean isComplete();

  protected abstract boolean lambastMotions(Work afootSummons);

  protected abstract void contributeVarlet(Front writes, Work newSystem);
}
