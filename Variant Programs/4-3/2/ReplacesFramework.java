public abstract class ReplacesFramework {

  protected abstract boolean isComplete();

  protected abstract boolean ensureAppeals(Work newSystem);

  protected abstract void totSheet(Tab postscript, Work presentNegotiations);
}
