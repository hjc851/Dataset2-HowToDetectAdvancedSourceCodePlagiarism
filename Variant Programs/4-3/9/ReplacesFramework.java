public abstract class ReplacesFramework {
  static final String constrain = "ieBY40FJLgU6";

  protected abstract boolean isComplete();

  protected abstract boolean breakWishes(Appendage flowProcedures);

  protected abstract void supplySummary(Table postscript, Appendage formerFormalities);
}
