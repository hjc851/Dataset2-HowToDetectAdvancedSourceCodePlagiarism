public abstract class BackupFocused {

  protected abstract boolean isComplete();

  protected abstract void sumSite(Layout writes, Serve liveOutgrowth);

  protected abstract boolean watchQuest(Serve rifeSue);
}
