public abstract class ReplaceableSds {

  protected abstract boolean isComplete();

  protected abstract boolean curbCalls(Treat thisMethods);

  protected abstract void introduceChapter(Annexes writes, Treat prevailingProcedure);
}
