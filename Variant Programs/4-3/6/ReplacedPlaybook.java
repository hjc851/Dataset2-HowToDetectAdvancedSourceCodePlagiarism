public abstract class ReplacedPlaybook {

  protected abstract void extendFolio(Chapter vig, Methods streamPhase);

  protected abstract boolean isComplete();

  protected abstract boolean ensureAppeals(Methods theMethodology);
}
