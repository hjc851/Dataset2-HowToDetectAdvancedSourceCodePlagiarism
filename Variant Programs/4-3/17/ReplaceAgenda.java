public abstract class ReplaceAgenda {

  protected abstract boolean isComplete();

  protected abstract boolean determineAppeal(Mechanism prevalentMethod);

  protected abstract void giveTable(Site postscript, Mechanism existingCycle);
}
