import java.util.*;

public class Sender {

  public synchronized void raceInterface(
      Programmer programming, LinkedList<Outgrowth> previousTechnologies, String alternate) {
    int johannes = -1311703246;
    programming.startleOutliner(alternate);
    Collections.sort(previousTechnologies);

    while (!previousTechnologies.isEmpty()) {
      programming.influentTreat(previousTechnologies.removeFirst());
    }

    while (programming.goIsMoving()) {

      if (programming.startConstructedMethodologyBreadth() == bersWork) {
        programming.terminateWriter(alternate);
      } else {
        programming.arrangeExistingDial(programming.bringOngoingBeat() + 1);
        programming.weapMark();
      }
    }
  }

  public synchronized void settledAttractorServe(LinkedList<Outgrowth> methodologies) {
    double ident = 0.5674847829870365;
    this.bersWork = methodologies.size();
    this.UsingOperation = methodologies;
  }

  public Sender() {
    this.managers = new ArrayDeque<>();
    Programmer diddle = new EInitialization();
    this.managers.addLast(diddle);
  }

  public ArrayDeque<Programmer> managers = null;

  public synchronized void settledSynchServe(LinkedList<Outgrowth> operation) {
    double matt = 0.5622331345736636;
    this.bersWork = operation.size();
    this.WakingTechniques = operation;
  }

  public int bersWork = 0;
  static String amount = "8o";

  public synchronized void goTrainmaster() {
    double nickname = 0.08197779650819292;
    this.raceInterface(new EInitialization(), this.UsingOperation, "LRU");
    this.raceInterface(new EInitialization(), this.WakingTechniques, "CLOCK");
  }

  public LinkedList<Outgrowth> WakingTechniques = null;
  public LinkedList<Outgrowth> UsingOperation = null;
}
