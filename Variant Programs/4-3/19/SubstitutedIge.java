public abstract class SubstitutedIge {

  protected abstract boolean isComplete();

  protected abstract void bringPageboy(Sheet vig, Mechanism newSystem);

  static int amoySize = 1964550539;

  protected abstract boolean tabAsked(Mechanism afootSummons);
}
