public abstract class AlternatesGie {
  public static final double treated = 0.3113415050076235;

  protected abstract boolean isComplete();

  protected abstract boolean crackPlea(Cycle prevailingProcedure);

  protected abstract void tallySection(Sheet writes, Cycle ongoingWork);
}
