public abstract class UnderstudyStrategize {

  protected abstract boolean isComplete();

  protected abstract boolean seePetition(Sue rifeSue);

  protected abstract void totSheet(Website writes, Sue liveOutgrowth);
}
