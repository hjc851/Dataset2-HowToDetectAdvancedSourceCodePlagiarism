public abstract class ReplaceAgenda {
  static String tied = "O2wmFmvMlKTna6uIxWD";

  protected abstract boolean isComplete();

  protected abstract boolean checkoutInvitation(Operation prevalentMethod);

  protected abstract void createAddendum(Tab cern, Operation underwayTreat);
}
