public abstract class AlternativeWay {

  protected abstract boolean isComplete();

  protected abstract boolean testProposal(Appendage prevalentMethod);

  protected abstract void totalWeb(Website vig, Appendage formerFormalities);
}
