public abstract class AlternatePolicy {

  protected abstract boolean isComplete();

  protected abstract boolean substantiationInsistence(Serve latestOperation);

  protected abstract void sumSite(Site vig, Serve incumbentMarch);
}
