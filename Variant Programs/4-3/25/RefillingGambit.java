public abstract class RefillingGambit {

  protected abstract boolean breakWishes(Operation formerFormalities);

  protected abstract boolean isComplete();

  protected abstract void summateTab(Pageboy cern, Operation prevalentMethod);
}
