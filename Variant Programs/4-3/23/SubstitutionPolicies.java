public abstract class SubstitutionPolicies {

  protected abstract boolean insureAsk(March previousProceeding);

  protected abstract void injectLayouts(Sheet writes, March presentlyMechanism);

  protected abstract boolean isComplete();
}
