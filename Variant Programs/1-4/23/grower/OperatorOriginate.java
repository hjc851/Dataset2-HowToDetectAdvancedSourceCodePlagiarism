package grower;

import producesJewels.DepositionalObjet;
import safekeeping.*;

public class OperatorOriginate extends grower.Output {

  public OperatorOriginate(double normal, double wander, Store now) {
    randomizing(normal, wander, now, null);
    this.sovereign = ProviderForeign.collaborating;
  }

  protected synchronized void welcomeSucceedingArtifact() throws SafekeepingCleanDerogate {
    this.thisArgue = new producesJewels.DepositionalObjet();
  }

  protected synchronized void motionTopicalResistEapsDisk() throws WarehousesOverfullExempted {

    try {
      this.lastStored.bringParticular(this.thisArgue);
      this.thisArgue = null;
    } catch (safekeeping.WarehousesOverfullExempted ej) {
      throw ej;
    }
  }
}
