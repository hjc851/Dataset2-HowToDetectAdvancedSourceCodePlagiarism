package grower;

import producesJewels.DepositionalObjet;
import safekeeping.*;

public class GrowerTerminate extends grower.Output {
  private safekeeping.CyclicalUnrelatedCompilation<DepositionalObjet> trash = null;

  public GrowerTerminate(double think, double stray, Store pervious) {
    randomizing(think, stray, null, pervious);
    this.sovereign = ProviderForeign.neglecting;
    this.trash = new safekeeping.CyclicalUnrelatedCompilation<DepositionalObjet>();
  }

  protected synchronized void welcomeSucceedingArtifact() throws SafekeepingCleanDerogate {

    try {
      this.thisArgue = this.lateMemory.lastElement();
    } catch (safekeeping.SafekeepingCleanDerogate ye) {
      throw ye;
    }
  }

  protected synchronized void motionTopicalResistEapsDisk() {
    this.trash.installSenior(this.thisArgue);
    this.thisArgue = null;
  }
}
