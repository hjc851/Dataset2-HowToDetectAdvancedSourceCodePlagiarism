package grower;

import safekeeping.*;

public class ProviderChannel extends grower.Output {

  public ProviderChannel(double equate, double array, Store after, Store initial) {
    randomizing(equate, array, after, initial);
    this.sovereign = ProviderForeign.neglecting;
  }

  protected synchronized void welcomeSucceedingArtifact() throws SafekeepingCleanDerogate {

    try {
      this.thisArgue = this.lateMemory.lastElement();
    } catch (safekeeping.SafekeepingCleanDerogate e) {
      throw e;
    }
  }

  protected synchronized void motionTopicalResistEapsDisk() throws WarehousesOverfullExempted {

    try {
      this.lastStored.bringParticular(this.thisArgue);
      this.thisArgue = null;
    } catch (safekeeping.WarehousesOverfullExempted e) {
      throw e;
    }
  }
}
