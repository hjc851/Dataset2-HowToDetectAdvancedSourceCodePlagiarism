package maker;

import producingAim.PerformableObjection;
import garage.*;

public class OperatorOriginate extends Farm {

  public OperatorOriginate(double spiteful, double grasp, Shelving future) {
    encode(spiteful, grasp, future, null);
    this.land = CommoditySovereign.struggling;
  }

  protected synchronized void incurCloseAim() throws WarehouseOpenDeviation {
    this.prevailingObjective = new PerformableObjection();
  }

  protected synchronized void goPrevailingObjectiveMousStorehouse() throws SafekeepingGoodDerogate {

    try {
      this.forthcomingSafekeeping.bringParticular(this.prevailingObjective);
      this.prevailingObjective = null;
    } catch (SafekeepingGoodDerogate ej) {
      throw ej;
    }
  }
}
