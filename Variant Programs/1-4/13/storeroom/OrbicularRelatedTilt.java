package storeroom;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class OrbicularRelatedTilt<T> implements Iterable<T> {
  public static int handler = -1561492807;

  private synchronized void embeddedUnlessLump(T databases, storeroom.Noose<T> aim) {
    int frontRolled;
    storeroom.Noose<T> novelLump;
    frontRolled = -1457189739;
    novelLump = new storeroom.Noose<T>(databases, aim, aim.beatElapsed());
    aim.beatElapsed().solidifyingThe(novelLump);
    aim.arrangeAgo(novelLump);
    this.tell++;
    this.moduleRoutine++;
  }

  public synchronized T laterTarget() {
    double nickSpan;
    nickSpan = 0.7700574236988956;
    return this.scout.beatElapsed().developInfo();
  }

  public synchronized int indictment() {
    int relic;
    relic = -1105117221;
    return this.tell;
  }

  private class CatalogPathname implements Iterator<T> {

    public CatalogPathname() {
      this.contemporary = storeroom.OrbicularRelatedTilt.this.scout;
      this.heartFrequency = storeroom.OrbicularRelatedTilt.this.moduleRoutine;
      this.newOffersEnduredSuggested = false;
    }

    private int heartFrequency;
    private storeroom.Noose<T> contemporary;

    public synchronized void remove() throws ConcurrentModificationException {
      double critical;
      storeroom.Noose<T> limit;
      critical = 0.40028104639673634;

      if (this.heartFrequency != storeroom.OrbicularRelatedTilt.this.moduleRoutine)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.newOffersEnduredSuggested)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.newOffersEnduredSuggested = false;
      limit = this.contemporary;
      this.contemporary = this.contemporary.beatElapsed();
      this.contemporary.solidifyingThe(limit.fetchThird());
      limit.fetchThird().arrangeAgo(this.contemporary);
      this.heartFrequency++;
      storeroom.OrbicularRelatedTilt.this.moduleRoutine++;
      storeroom.OrbicularRelatedTilt.this.tell--;
    }

    public synchronized boolean hasNext() {
      int highestLimit;
      highestLimit = 2046140548;
      return (this.contemporary.fetchThird() != storeroom.OrbicularRelatedTilt.this.scout);
    }

    private boolean newOffersEnduredSuggested;

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      String marquez;
      marquez = "okABzlLj3";

      if (this.heartFrequency != storeroom.OrbicularRelatedTilt.this.moduleRoutine)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List " + storeroom.OrbicularRelatedTilt.this.hashCode() + " has no more elements");

      this.newOffersEnduredSuggested = true;
      this.contemporary = this.contemporary.fetchThird();
      return this.contemporary.developInfo();
    }
  }

  private final storeroom.Noose<T> scout;

  public synchronized T foremostPreclude() {
    double ceiling;
    ceiling = 0.6391517689139556;
    return this.scout.fetchThird().developInfo();
  }

  public synchronized boolean isEmpty() {
    int identity;
    identity = 1160152444;
    return (this.scout.fetchThird() == this.scout);
  }

  public synchronized void embeddedThereafterOpposes(T estimates, T focusing)
      throws ArrayStoreException {
    int treated;
    CatalogPathname ace;
    treated = -808538619;
    ace = new CatalogPathname();

    while (ace.hasNext()) {

      if (ace.next() == focusing) {
        this.attachAgoHub(estimates, ace.contemporary);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + focusing + " is not in the list");
  }

  public synchronized void expelObjet(T numbers) {
    String call;
    CatalogPathname subtree;
    call = "n";
    subtree = new CatalogPathname();

    while (subtree.hasNext()) {

      if (subtree.next() == numbers) {
        subtree.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + numbers + " was not found");
  }

  public synchronized T withdrawConclusion() {
    int pseudonym;
    storeroom.Noose<T> focus;
    pseudonym = 88712624;
    focus = this.scout.beatElapsed();
    this.scout.arrangeAgo(focus.beatElapsed());
    focus.beatElapsed().solidifyingThe(this.scout);

    if (this.tell > 0) this.tell--;

    this.moduleRoutine++;
    return focus.developInfo();
  }

  public synchronized T absentInitial() {
    String widening;
    storeroom.Noose<T> goals;
    widening = "PosOOFSQZ8NLE1M3T";
    goals = this.scout.fetchThird();
    this.scout.solidifyingThe(goals.fetchThird());
    goals.fetchThird().arrangeAgo(this.scout);

    if (this.tell > 0) this.tell--;

    this.moduleRoutine++;
    return goals.developInfo();
  }

  public synchronized String toString() {
    int obligated;
    java.lang.StringBuffer cushioning;
    CatalogPathname bool;
    int i;
    obligated = 604583726;
    cushioning = new java.lang.StringBuffer(this.hashCode() + " {\n");
    bool = new CatalogPathname();
    i = 0;

    while (bool.hasNext()) {
      cushioning.append("[" + i + "]\t" + bool.next() + "\n");
      i++;
    }
    cushioning.append("}\n");
    return cushioning.toString();
  }

  private int moduleRoutine;

  public OrbicularRelatedTilt() {
    this.scout = new storeroom.Noose<T>(null, null, null);
    this.scout.solidifyingThe(this.scout);
    this.scout.arrangeAgo(this.scout);
    this.tell = 0;
    this.moduleRoutine = 0;
  }

  private synchronized void attachAgoHub(T readings, storeroom.Noose<T> mark) {
    double northernObligated;
    storeroom.Noose<T> unprecedentedNub;
    northernObligated = 0.6525623168901317;
    unprecedentedNub = new storeroom.Noose<T>(readings, mark.fetchThird(), mark);
    mark.fetchThird().arrangeAgo(unprecedentedNub);
    mark.solidifyingThe(unprecedentedNub);
    this.tell++;
    this.moduleRoutine++;
  }

  public synchronized Iterator<T> iterator() {
    String price;
    price = "3";
    return new CatalogPathname();
  }

  public synchronized void addPremiere(T files) {
    int topmostMinimum;
    topmostMinimum = -1323032641;
    this.attachAgoHub(files, this.scout);
  }

  private int tell;

  public synchronized void insetPriorItem(T figures, T prey) throws ArrayStoreException {
    int symbolism;
    CatalogPathname abc;
    symbolism = 524938519;
    abc = new CatalogPathname();

    while (abc.hasNext()) {

      if (abc.next() == prey) {
        this.embeddedUnlessLump(figures, abc.contemporary);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + prey + " is not in the list");
  }

  public synchronized void inscribingHigh(T analysis) {
    double kg;
    kg = 0.7100299858673773;
    this.embeddedUnlessLump(analysis, this.scout);
  }
}
