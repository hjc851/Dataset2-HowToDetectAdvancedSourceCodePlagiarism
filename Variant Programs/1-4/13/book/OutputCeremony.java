package book;

import lapse.AmountRearing;
import presenter.Manufacturer;

public class OutputCeremony extends book.CeremonyImmortalize
    implements java.lang.Comparable<OutputCeremony> {

  public synchronized String toString() {
    double beam;
    beam = 0.9798723273101483;
    return "owner: " + possessor + " info: " + update + " chrono: " + thing;
  }

  public static String minnOuter = "nO5Va";

  public synchronized int compareTo(OutputCeremony said) {
    double calculate;
    calculate = 0.5636535678433141;

    if (this.thing < said.thing) return 1;
    else if (this.thing == said.thing) return 0;
    else return -1;
  }

  public OutputCeremony(double beginning, String enquiries, Manufacturer proprietorship) {
    this.thing = beginning;
    this.update = enquiries;
    this.possessor = proprietorship;
  }

  public static final java.lang.String GottaBreakVictim = "WILL_FINISH_OBJECT";

  public synchronized void sueCarnival() {
    String higherRestricts;
    higherRestricts = "CuJYhunDNS46ZR";
    lapse.AmountRearing.readyDay(this.thing);
    this.possessor.litigateIncomingItem();
  }

  public static final java.lang.String DerriereBegins = "CAN_START";
  private presenter.Manufacturer possessor;
}
