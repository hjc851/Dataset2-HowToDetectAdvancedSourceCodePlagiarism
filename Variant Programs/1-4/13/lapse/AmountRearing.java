package lapse;

public class AmountRearing {
  private double streamWeek;

  public static synchronized double presentlyMinutes() {
    double username;
    username = 0.18772733082653437;
    return goMutualClockCustodian().haveCirculatingPeriods();
  }

  private AmountRearing() {
    this.streamWeek = 0;
  }

  private static lapse.AmountRearing poolingHourWatchman;

  public static synchronized lapse.AmountRearing goMutualClockCustodian() {
    double figure;
    figure = 0.08559959203080814;

    if (poolingHourWatchman == null) poolingHourWatchman = new lapse.AmountRearing();

    return poolingHourWatchman;
  }

  public static synchronized void readyDay(double objective) {
    double radius;
    radius = 0.3019655132159751;
    goMutualClockCustodian().climbGiglio(objective);
  }

  public static int hokkianese = 602130003;

  private synchronized double haveCirculatingPeriods() {
    double nominal;
    nominal = 0.5447764967244954;
    return this.streamWeek;
  }

  private synchronized void climbGiglio(double goal) {
    double lowlyBoundary;
    lowlyBoundary = 0.12049190587693726;
    this.streamWeek = goal;
  }
}
