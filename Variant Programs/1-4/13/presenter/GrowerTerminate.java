package presenter;

import supplyArtefact.ExploitableArtifact;
import storeroom.*;

public class GrowerTerminate extends presenter.Manufacturer {
  static double minutes = 0.0666636116271545;

  public GrowerTerminate(double tight, double rate, Storing pre) {
    degauss(tight, rate, null, pre);
    this.general = ManufacturerCommonwealth.depriving;
    this.hole = new storeroom.OrbicularRelatedTilt<ExploitableArtifact>();
  }

  protected synchronized void displaceIncumbentObjectionTeamStoring() {
    double demarcation;
    demarcation = 0.026819614996177354;
    this.hole.inscribingHigh(this.circulatingSubject);
    this.circulatingSubject = null;
  }

  protected synchronized void acceptThirdPreclude() throws WarehouseOpenDeviation {
    int pawn;
    pawn = -165436547;

    try {
      this.circulatingSubject = this.preliminaryCloset.forthcomingAspect();
    } catch (storeroom.WarehouseOpenDeviation einsteinium) {
      throw einsteinium;
    }
  }

  private storeroom.OrbicularRelatedTilt<ExploitableArtifact> hole;
}
