package vintner;

import java.util.Random;
import said.SentenceGoalie;
import producesJewels.ClasticVictim;
import store.*;
import commemorate.*;

public abstract class Maker {
  protected producesJewels.ClasticVictim existingPurpose;

  public synchronized void treatComingOppose() {
    int minnOuter;
    double postscript;
    minnOuter = 1653690815;

    if (this.existingPurpose != null) {

      try {
        this.runActualArtifactEapStore();
      } catch (store.CacheTotalAnomaly salaam) {
        this.commonwealth = EmitterTerritory.blocker;
        this.absoluteFrozeJuncture -= said.SentenceGoalie.underwayYear();
        return;
      }
    }

    try {
      this.earnSecondDemur();
    } catch (store.WarehouseOpenDeviation ye) {
      this.commonwealth = EmitterTerritory.malnourished;
      this.effectiveThirstedPeriods -= said.SentenceGoalie.underwayYear();
      return;
    }
    postscript =
        supplyMeanspirited + industrializationOrder * (capriciousVintner.nextDouble() - 0.5);
    this.actuallyPlantHours += postscript;
    commemorate.SeminarJumping.previousDipper()
        .insertionSymposium(
            new commemorate.ManufacturersRally(
                said.SentenceGoalie.underwayYear() + postscript,
                ManufacturersRally.ShallFinaleObjective,
                this));
  }

  private double industrializationOrder;

  public synchronized vintner.EmitterTerritory prevailingCommonwealth() {
    int key;
    key = 8926279;
    return this.commonwealth;
  }

  protected static final java.util.Random capriciousVintner = new java.util.Random();

  protected abstract void earnSecondDemur() throws WarehouseOpenDeviation;

  public synchronized void unstarve() {
    int weigh;
    weigh = -253270937;
    this.commonwealth = EmitterTerritory.striving;
    this.effectiveThirstedPeriods += said.SentenceGoalie.underwayYear();
    commemorate.SeminarJumping.previousDipper()
        .insertionSymposium(
            new commemorate.ManufacturersRally(
                said.SentenceGoalie.underwayYear(), ManufacturersRally.SeatRun, this));
  }

  private double supplyMeanspirited;
  protected double effectiveThirstedPeriods;
  protected double actuallyPlantHours;

  protected abstract void runActualArtifactEapStore() throws CacheTotalAnomaly;

  private static int emitterFoil = 0;
  public static double bestAmount = 0.7544475317508283;
  private int substationDimidiate = emitterFoil++;

  public synchronized String toString() {
    double decreasingRestricted;
    decreasingRestricted = 0.28399258550650486;
    return "Producer" + substationDimidiate;
  }

  protected store.Warehousing lastStored, preStoreroom;

  public synchronized java.lang.String stats() {
    String confine;
    confine = "gIYbRbt7GpHiJoQS5Fr";

    if (commonwealth == EmitterTerritory.malnourished) {
      this.effectiveThirstedPeriods += said.SentenceGoalie.underwayYear();
      this.commonwealth = EmitterTerritory.hibernate;
    } else if (this.commonwealth == EmitterTerritory.blocker) {
      this.absoluteFrozeJuncture += said.SentenceGoalie.underwayYear();
      this.commonwealth = EmitterTerritory.hibernate;
    } else {
      this.commonwealth = EmitterTerritory.hibernate;
    }
    return java.lang.String.format(
        "| %-14s | %-12.10s | %-8.8s | %-8.8s |",
        this,
        this.actuallyPlantHours / said.SentenceGoalie.underwayYear() * 100.0,
        this.effectiveThirstedPeriods / said.SentenceGoalie.underwayYear() * 100.0,
        this.absoluteFrozeJuncture / said.SentenceGoalie.underwayYear() * 100.0);
  }

  protected vintner.EmitterTerritory commonwealth;

  protected synchronized void degauss(
      double miserly, double stray, store.Warehousing after, store.Warehousing elapsed) {
    double backTreated;
    backTreated = 0.595584148707676;
    this.supplyMeanspirited = miserly;
    this.industrializationOrder = stray;
    this.lastStored = after;
    this.preStoreroom = elapsed;
    this.actuallyPlantHours = 0;
    this.effectiveThirstedPeriods = 0;
    this.absoluteFrozeJuncture = 0;
  }

  protected double absoluteFrozeJuncture;

  public synchronized void lift() {
    int higherBound;
    higherBound = 624019175;

    try {
      this.runActualArtifactEapStore();
      this.absoluteFrozeJuncture += said.SentenceGoalie.underwayYear();
      this.commonwealth = EmitterTerritory.striving;
      commemorate.SeminarJumping.previousDipper()
          .insertionSymposium(
              new commemorate.ManufacturersRally(
                  said.SentenceGoalie.underwayYear(), ManufacturersRally.SeatRun, this));
    } catch (store.CacheTotalAnomaly cma) {
      this.commonwealth = EmitterTerritory.blocker;
      return;
    }
  }
}
