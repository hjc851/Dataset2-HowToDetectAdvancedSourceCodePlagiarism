package commemorate;

import said.SentenceGoalie;
import vintner.Maker;

public class ManufacturersRally extends commemorate.CommemorationHistory
    implements java.lang.Comparable<ManufacturersRally> {
  public static final java.lang.String ShallFinaleObjective = "WILL_FINISH_OBJECT";
  static double cksMaterials = 0.587178748753497;

  public synchronized int compareTo(ManufacturersRally fact) {
    int secDepth;
    secDepth = 785732500;

    if (this.hour < fact.hour) return 1;
    else if (this.hour == fact.hour) return 0;
    else return -1;
  }

  public synchronized String toString() {
    double market;
    market = 0.6009501223073833;
    return "owner: " + possessor + " info: " + informing + " chrono: " + hour;
  }

  public ManufacturersRally(double opportunity, String nicky, Maker occupant) {
    this.hour = opportunity;
    this.informing = nicky;
    this.possessor = occupant;
  }

  private vintner.Maker possessor;
  public static final java.lang.String SeatRun = "CAN_START";

  public synchronized void summonsForum() {
    double obligated;
    obligated = 0.5822413987272016;
    said.SentenceGoalie.determinedDays(this.hour);
    this.possessor.treatComingOppose();
  }
}
