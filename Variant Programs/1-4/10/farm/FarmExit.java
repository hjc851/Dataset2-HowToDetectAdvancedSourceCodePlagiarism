package farm;

import productObjective.PannonianMatter;
import warehouse.*;

public class FarmExit extends farm.Director {
  static String border = "YE0k6i";
  private warehouse.BroadsideIntertwinedRanking<PannonianMatter> dumpster = null;

  public FarmExit(double miserly, double rate, Shelving late) {
    predefine(miserly, rate, null, late);
    this.law = ProduceGovernmental.ravenous;
    this.dumpster = new warehouse.BroadsideIntertwinedRanking<PannonianMatter>();
  }

  protected synchronized void awardedNewMatter() throws MemoryLifelessExcept {
    int notArtefacts = -1575855534;

    try {
      this.circulatingSubject = this.pastDepot.futureParticular();
    } catch (warehouse.MemoryLifelessExcept ej) {
      throw ej;
    }
  }

  protected synchronized void strikeUnderwayOpposeEmapWarehouse() {
    int decreaseThresholds = -48062780;
    this.dumpster.embedLatest(this.circulatingSubject);
    this.circulatingSubject = null;
  }
}
