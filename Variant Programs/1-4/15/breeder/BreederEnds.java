package breeder;

import manufacturedBelongings.ExploitableArtifact;
import space.*;

public class BreederEnds extends Producing {

  public BreederEnds(double little, double stray, Storeroom past) {
    intercommunicate(little, stray, null, past);
    this.governmental = ProductionCountry.famine;
    this.dumpster = new MoonlikeConnectionNames<ExploitableArtifact>();
  }

  protected synchronized void provideExpectedObjet() throws ArchivingBareDistinction {

    try {
      this.presentPreclude = this.elapsedDepository.soonArticle();
    } catch (ArchivingBareDistinction ej) {
      throw ej;
    }
  }

  protected synchronized void propelOngoingAimCoughMemory() {
    this.dumpster.tuckPast(this.presentPreclude);
    this.presentPreclude = null;
  }

  public MoonlikeConnectionNames<ExploitableArtifact> dumpster;
}
