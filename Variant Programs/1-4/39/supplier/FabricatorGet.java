package supplier;

import fabricationObstructions.PannonianMatter;
import storing.*;

public class FabricatorGet extends Produce {
  public GlobularCorrelatedBlacklist<PannonianMatter> spoon;

  public FabricatorGet(double meanspirited, double roam, Space preliminary) {
    randomise(meanspirited, roam, null, preliminary);
    this.central = CommoditySovereign.emaciated;
    this.spoon = new GlobularCorrelatedBlacklist<PannonianMatter>();
  }

  protected synchronized void acceptThirdPreclude() throws DepositoryEliminateDeparture {

    try {
      this.typicalOpposes = this.currentCaching.followingDetail();
    } catch (DepositoryEliminateDeparture cma) {
      throw cma;
    }
  }

  protected synchronized void motionTopicalResistEapsDisk() {
    this.spoon.injectedSurvive(this.typicalOpposes);
    this.typicalOpposes = null;
  }
}
