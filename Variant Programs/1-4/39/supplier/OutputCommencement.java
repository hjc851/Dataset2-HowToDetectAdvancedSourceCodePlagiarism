package supplier;

import fabricationObstructions.PannonianMatter;
import storing.*;

public class OutputCommencement extends Produce {

  public OutputCommencement(double miserly, double wander, Space second) {
    randomise(miserly, wander, second, null);
    this.central = CommoditySovereign.busy;
  }

  protected synchronized void acceptThirdPreclude() throws DepositoryEliminateDeparture {
    this.typicalOpposes = new PannonianMatter();
  }

  protected synchronized void motionTopicalResistEapsDisk() throws SpaceChockfulWaiver {

    try {
      this.earlyGarage.bestowArtifact(this.typicalOpposes);
      this.typicalOpposes = null;
    } catch (SpaceChockfulWaiver ye) {
      throw ye;
    }
  }
}
