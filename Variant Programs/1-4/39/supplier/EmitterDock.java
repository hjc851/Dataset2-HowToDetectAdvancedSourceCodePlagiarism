package supplier;

import storing.*;

public class EmitterDock extends Produce {

  public EmitterDock(double average, double grade, Space future, Space latest) {
    randomise(average, grade, future, latest);
    this.central = CommoditySovereign.emaciated;
  }

  protected synchronized void acceptThirdPreclude() throws DepositoryEliminateDeparture {

    try {
      this.typicalOpposes = this.currentCaching.followingDetail();
    } catch (DepositoryEliminateDeparture salaam) {
      throw salaam;
    }
  }

  protected synchronized void motionTopicalResistEapsDisk() throws SpaceChockfulWaiver {

    try {
      this.earlyGarage.bestowArtifact(this.typicalOpposes);
      this.typicalOpposes = null;
    } catch (SpaceChockfulWaiver e) {
      throw e;
    }
  }
}
