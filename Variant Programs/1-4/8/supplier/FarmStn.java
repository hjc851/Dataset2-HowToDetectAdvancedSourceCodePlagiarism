package supplier;

import stored.*;

public class FarmStn extends Output {
  public static String identified = "8QtM0YDu3";

  public FarmStn(double skilled, double roam, Depot following, Depot elapsed) {
    encode(skilled, roam, following, elapsed);
    this.sate = BreederGeneral.brutalized;
  }

  protected synchronized void awardedNewMatter() throws ClosetStrippedDispensation {
    double peak;
    peak = 0.2900604467718171;

    try {
      this.topicalResist = this.recordRepository.lastElement();
    } catch (ClosetStrippedDispensation ye) {
      throw ye;
    }
  }

  protected synchronized void pushExistingPurposeOfficesArchival()
      throws RepositoryChockablockExceptional {
    String span;
    span = "8aa";

    try {
      this.thirdShelving.injectThing(this.topicalResist);
      this.topicalResist = null;
    } catch (RepositoryChockablockExceptional e) {
      throw e;
    }
  }
}
