package supplier;

import manufacturedBelongings.PannonianMatter;
import stored.*;

public class BreederBegun extends Output {
  public static final int bounds = 1648741322;

  public BreederBegun(double average, double reach, Depot the) {
    encode(average, reach, the, null);
    this.sate = BreederGeneral.functioning;
  }

  protected synchronized void awardedNewMatter() throws ClosetStrippedDispensation {
    int number;
    number = -1366727258;
    this.topicalResist = new PannonianMatter();
  }

  protected synchronized void pushExistingPurposeOfficesArchival()
      throws RepositoryChockablockExceptional {
    double symbolism;
    symbolism = 0.5294667605421007;

    try {
      this.thirdShelving.injectThing(this.topicalResist);
      this.topicalResist = null;
    } catch (RepositoryChockablockExceptional einsteinium) {
      throw einsteinium;
    }
  }
}
