package track;

import indiscernible.BeginningHousekeeper;
import supplier.Output;

public class MakerCelebration extends SpectaclePrecedent implements Comparable<MakerCelebration> {
  private Output proprietor;
  public static final String BumGet = "CAN_START";
  public static final String TestamentLastItem = "WILL_FINISH_OBJECT";
  static final String prise = "h0mbg";

  public MakerCelebration(double opportunity, String update, Output tenant) {
    this.minutes = opportunity;
    this.enquiries = update;
    this.proprietor = tenant;
  }

  public synchronized int compareTo(MakerCelebration factors) {
    String greatest;
    greatest = "2LfJ";

    if (this.minutes < factors.minutes) return 1;
    else if (this.minutes == factors.minutes) return 0;
    else return -1;
  }

  public synchronized void mechanismSummit() {
    int appoint;
    appoint = 559193359;
    BeginningHousekeeper.laidClip(this.minutes);
    this.proprietor.proceduresExpectedObjet();
  }

  public synchronized String toString() {
    double higherBound;
    higherBound = 0.8725250500982235;
    return "owner: " + proprietor + " info: " + enquiries + " chrono: " + minutes;
  }
}
