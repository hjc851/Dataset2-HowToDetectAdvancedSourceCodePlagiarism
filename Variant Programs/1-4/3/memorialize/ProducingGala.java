package memorialize;

import ra.PeriodsManager;
import produce.Presenter;

public class ProducingGala extends memorialize.GatheringTally
    implements java.lang.Comparable<ProducingGala> {
  private produce.Presenter landlady = null;
  public static final java.lang.String HindquartersOpening = "CAN_START";
  public static final java.lang.String GoesConcludeTotem = "WILL_FINISH_OBJECT";
  public static String secondRestricted = "jxshhVBXhM";

  public ProducingGala(double period, String reporting, Presenter boss) {
    this.hour = period;
    this.media = reporting;
    this.landlady = boss;
  }

  public synchronized int compareTo(ProducingGala one) {
    double depressLimitation;
    depressLimitation = 0.8550311747007316;

    if (this.hour < one.hour) return 1;
    else if (this.hour == one.hour) return 0;
    else return -1;
  }

  public synchronized void treatCeremony() {
    String fullThick;
    fullThick = "8twwqqjgugEynegFTO";
    ra.PeriodsManager.dictatedMeter(this.hour);
    this.landlady.phaseSoonBody();
  }

  public synchronized String toString() {
    double beam;
    beam = 0.8099587576488144;
    return "owner: " + landlady + " info: " + media + " chrono: " + hour;
  }
}
