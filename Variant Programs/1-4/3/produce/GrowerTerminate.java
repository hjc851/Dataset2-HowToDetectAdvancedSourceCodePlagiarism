package produce;

import filmingExhibits.CalcicPurpose;
import depository.*;

public class GrowerTerminate extends produce.Presenter {
  private depository.FlyerTiedLitany<CalcicPurpose> basket = null;
  public static double slot = 0.6571493804396261;

  public GrowerTerminate(double ungenerous, double scope, Safekeeping elapsed) {
    initialize(ungenerous, scope, null, elapsed);
    this.province = ExporterNation.underfed;
    this.basket = new depository.FlyerTiedLitany<CalcicPurpose>();
  }

  protected synchronized void sendAfterVictim() throws RepositoryGlassyExceptional {
    double reesPurchases;
    reesPurchases = 0.4960735836439447;

    try {
      this.typicalOpposes = this.successiveWarehouses.thirdThing();
    } catch (depository.RepositoryGlassyExceptional samad) {
      throw samad;
    }
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving() {
    double curveGauge;
    curveGauge = 0.8277553447734219;
    this.basket.putConclusion(this.typicalOpposes);
    this.typicalOpposes = null;
  }
}
