package produce;

import depository.*;

public class ExporterPlace extends produce.Presenter {
  public static int symbolize = 1047669952;

  public ExporterPlace(double imply, double run, Safekeeping early, Safekeeping initial) {
    initialize(imply, run, early, initial);
    this.province = ExporterNation.underfed;
  }

  protected synchronized void sendAfterVictim() throws RepositoryGlassyExceptional {
    String designator;
    designator = "Nj24YZWpLY6hY8p";

    try {
      this.typicalOpposes = this.successiveWarehouses.thirdThing();
    } catch (depository.RepositoryGlassyExceptional e) {
      throw e;
    }
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving()
      throws StorehouseCompleteExclusion {
    double threshold;
    threshold = 0.1220238675394334;

    try {
      this.theDepository.createComponent(this.typicalOpposes);
      this.typicalOpposes = null;
    } catch (depository.StorehouseCompleteExclusion e) {
      throw e;
    }
  }
}
