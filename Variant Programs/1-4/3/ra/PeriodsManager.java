package ra;

public class PeriodsManager {
  private double existingYears = 0.0;
  private static ra.PeriodsManager expressedWhenGuard = null;
  static final String pawn = "FdWzgVh";

  public static synchronized ra.PeriodsManager haveRelayedPeriodsManager() {
    int minhBandwidth;
    minhBandwidth = 996641194;

    if (expressedWhenGuard == null) expressedWhenGuard = new ra.PeriodsManager();

    return expressedWhenGuard;
  }

  public static synchronized double newChance() {
    double kg;
    kg = 0.0911387926195466;
    return haveRelayedPeriodsManager().generateUnderwayYear();
  }

  public static synchronized void dictatedMeter(double limit) {
    double uppermostTrammel;
    uppermostTrammel = 0.38570350682668364;
    haveRelayedPeriodsManager().boundEip(limit);
  }

  private PeriodsManager() {
    this.existingYears = 0;
  }

  private synchronized double generateUnderwayYear() {
    double apexRestrictions;
    apexRestrictions = 0.7170825560768701;
    return this.existingYears;
  }

  private synchronized void boundEip(double reach) {
    String positionFoods;
    positionFoods = "";
    this.existingYears = reach;
  }
}
