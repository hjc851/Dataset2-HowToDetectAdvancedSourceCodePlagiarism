package filmmaker;

import supplyArtefact.PerformableObjection;
import stored.*;

public class FabricatorGet extends Provider {
  private RoundedCausedAgenda<PerformableObjection> bucketful = null;

  public FabricatorGet(double tight, double grasp, Safekeeping original) {
    recompile(tight, grasp, null, original);
    this.statehood = FilmmakerSay.fasting;
    this.bucketful = new RoundedCausedAgenda<PerformableObjection>();
  }

  protected synchronized void recipientComeDisagree() throws CachingVoidExemptions {

    try {
      this.presentPreclude = this.predecessorInventory.lastElement();
    } catch (CachingVoidExemptions cma) {
      throw cma;
    }
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving() {
    this.bucketful.introduceLater(this.presentPreclude);
    this.presentPreclude = null;
  }
}
