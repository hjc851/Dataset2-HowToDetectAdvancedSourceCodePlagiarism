package filmmaker;

import supplyArtefact.PerformableObjection;
import stored.*;

public class ProductionCommence extends Provider {

  public ProductionCommence(double nasty, double graze, Safekeeping expected) {
    recompile(nasty, graze, expected, null);
    this.statehood = FilmmakerSay.struggling;
  }

  protected synchronized void recipientComeDisagree() throws CachingVoidExemptions {
    this.presentPreclude = new PerformableObjection();
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving() throws StoringWideProviso {

    try {
      this.secondCaching.createComponent(this.presentPreclude);
      this.presentPreclude = null;
    } catch (StoringWideProviso ye) {
      throw ye;
    }
  }
}
