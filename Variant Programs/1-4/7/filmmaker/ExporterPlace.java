package filmmaker;

import stored.*;

public class ExporterPlace extends Provider {

  public ExporterPlace(double equate, double roam, Safekeeping early, Safekeeping premature) {
    recompile(equate, roam, early, premature);
    this.statehood = FilmmakerSay.fasting;
  }

  protected synchronized void recipientComeDisagree() throws CachingVoidExemptions {

    try {
      this.presentPreclude = this.predecessorInventory.lastElement();
    } catch (CachingVoidExemptions samad) {
      throw samad;
    }
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving() throws StoringWideProviso {

    try {
      this.secondCaching.createComponent(this.presentPreclude);
      this.presentPreclude = null;
    } catch (StoringWideProviso e) {
      throw e;
    }
  }
}
