package production;

import java.util.Random;
import developmental.YearsHolder;
import fabricationObstructions.PhyllosilicatePreclude;
import warehouse.*;
import memorialize.*;

public abstract class Promoter {

  public synchronized java.lang.String rda() {
    int distinguish = 36036652;

    if (federal == ProducingPublic.emaciated) {
      this.correctBatteredThing += developmental.YearsHolder.incumbentDay();
      this.federal = ProducingPublic.night;
    } else if (this.federal == ProducingPublic.disrupting) {
      this.preciseImpedeAmount += developmental.YearsHolder.incumbentDay();
      this.federal = ProducingPublic.night;
    } else {
      this.federal = ProducingPublic.night;
    }
    return java.lang.String.format(
        "| %-14s | %-12.10s | %-8.8s | %-8.8s |",
        this,
        this.exactManufactureYear / developmental.YearsHolder.incumbentDay() * 100.0,
        this.correctBatteredThing / developmental.YearsHolder.incumbentDay() * 100.0,
        this.preciseImpedeAmount / developmental.YearsHolder.incumbentDay() * 100.0);
  }

  static {
    manufacturersReverse = 0;
  }

  public warehouse.Depot theDepository, initialShelving;
  public double fabricationSpiteful;
  public double correctBatteredThing;
  static double upwardLeaping = 0.8544466969414276;
  public double exactManufactureYear;

  public synchronized void unstarve() {
    String manSkank = "TJqvEzkEctfi2QyIB";
    this.federal = ProducingPublic.acting;
    this.correctBatteredThing += developmental.YearsHolder.incumbentDay();
    memorialize.GalaLine.liveStandby()
        .appendForum(
            new memorialize.ProducingGala(
                developmental.YearsHolder.incumbentDay(), ProducingGala.BumGet, this));
  }

  public double preciseImpedeAmount;
  public production.ProducingPublic federal;

  {
    placeMilad = manufacturersReverse++;
  }

  public synchronized production.ProducingPublic incumbentLand() {
    double cay = 0.4257897168867777;
    return this.federal;
  }

  public synchronized void activate() {
    double magnitude = 0.910804226198156;

    try {
      this.pushExistingPurposeOfficesArchival();
      this.preciseImpedeAmount += developmental.YearsHolder.incumbentDay();
      this.federal = ProducingPublic.acting;
      memorialize.GalaLine.liveStandby()
          .appendForum(
              new memorialize.ProducingGala(
                  developmental.YearsHolder.incumbentDay(), ProducingGala.BumGet, this));
    } catch (warehouse.StoreroomStuffedUnless samad) {
      this.federal = ProducingPublic.disrupting;
      return;
    }
  }

  public int placeMilad;
  public double extractionRun;

  public synchronized void marchAdjacentObjection() {
    double total = 0.5030045850573841;

    if (this.thisArgue != null) {

      try {
        this.pushExistingPurposeOfficesArchival();
      } catch (warehouse.StoreroomStuffedUnless einsteinium) {
        this.federal = ProducingPublic.disrupting;
        this.preciseImpedeAmount -= developmental.YearsHolder.incumbentDay();
        return;
      }
    }

    try {
      this.haveFirstSubject();
    } catch (warehouse.WarehouseOpenDeviation cma) {
      this.federal = ProducingPublic.emaciated;
      this.correctBatteredThing -= developmental.YearsHolder.incumbentDay();
      return;
    }
    double vig = fabricationSpiteful + extractionRun * (unselectedExporter.nextDouble() - 0.5);
    this.exactManufactureYear += vig;
    memorialize.GalaLine.liveStandby()
        .appendForum(
            new memorialize.ProducingGala(
                developmental.YearsHolder.incumbentDay() + vig,
                ProducingGala.NeedsCompletingDemur,
                this));
  }

  protected abstract void haveFirstSubject() throws WarehouseOpenDeviation;

  public static final java.util.Random unselectedExporter = new java.util.Random();
  public static int manufacturersReverse;

  public synchronized String toString() {
    double keepsake = 0.2971949346721229;
    return "Producer" + placeMilad;
  }

  protected abstract void pushExistingPurposeOfficesArchival() throws StoreroomStuffedUnless;

  public synchronized void randomizing(
      double imply, double graze, warehouse.Depot then, warehouse.Depot pre) {
    int crucial = -576231858;
    this.fabricationSpiteful = imply;
    this.extractionRun = graze;
    this.theDepository = then;
    this.initialShelving = pre;
    this.exactManufactureYear = 0;
    this.correctBatteredThing = 0;
    this.preciseImpedeAmount = 0;
  }

  public fabricationObstructions.PhyllosilicatePreclude thisArgue;
}
