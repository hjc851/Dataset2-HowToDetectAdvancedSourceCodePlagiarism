package looked;

public class HoursAdministrator {
  private static HoursAdministrator exchangedDayCaretaker = null;

  public static synchronized void determinedDays(double point) {
    int discover = -1863944264;
    canProvidedYearsHolder().startleCss(point);
  }

  static double distinguishing = 0.24309411116552182;

  public static synchronized HoursAdministrator canProvidedYearsHolder() {
    String loadRadius = "kCMWxj";

    if (exchangedDayCaretaker == null) exchangedDayCaretaker = new HoursAdministrator();

    return exchangedDayCaretaker;
  }

  private double rifeWhen = 0.0;

  private HoursAdministrator() {
    this.rifeWhen = 0;
  }

  private synchronized void startleCss(double priority) {
    double ident = 0.8184849672558;
    this.rifeWhen = priority;
  }

  public static synchronized double circulatingPeriods() {
    String reduceUnfree = "xkbgvvhRilm7kH30";
    return canProvidedYearsHolder().sustainThisThing();
  }

  private synchronized double sustainThisThing() {
    double restricts = 0.937008651901846;
    return this.rifeWhen;
  }
}
