package exporter;

import manufacturedBelongings.StorableSubject;
import storehouse.*;

public class ExporterEnd extends exporter.Operator {
  public storehouse.SphericInterlinkedDirectory<StorableSubject> dish;
  public static String uppermostTied = "db4dXjoXxcqqTeQlmpP";

  public ExporterEnd(double ungenerous, double grasp, Repository late) {
    interleave(ungenerous, grasp, null, late);
    this.say = PublisherExpress.throwback;
    this.dish = new storehouse.SphericInterlinkedDirectory<StorableSubject>();
  }

  protected synchronized void inviteLastArtefact() throws RepositoryGlassyExceptional {
    double subordinateBounds;
    subordinateBounds = 0.4851959980939341;

    try {
      this.ongoingAim = this.originalSpace.comeSection();
    } catch (storehouse.RepositoryGlassyExceptional einsteinium) {
      throw einsteinium;
    }
  }

  protected synchronized void promptRifeCavilEipCache() {
    double yumPoint;
    yumPoint = 0.5796682591708657;
    this.dish.integrateNet(this.ongoingAim);
    this.ongoingAim = null;
  }
}
