package exporter;

import storehouse.*;

public class PromoterTrain extends exporter.Operator {
  static double boundary = 0.13370125713227343;

  public PromoterTrain(double beggarly, double crop, Repository incoming, Repository first) {
    interleave(beggarly, crop, incoming, first);
    this.say = PublisherExpress.throwback;
  }

  protected synchronized void inviteLastArtefact() throws RepositoryGlassyExceptional {
    double upstreamBorder;
    upstreamBorder = 0.9907274306591146;

    try {
      this.ongoingAim = this.originalSpace.comeSection();
    } catch (storehouse.RepositoryGlassyExceptional e) {
      throw e;
    }
  }

  protected synchronized void promptRifeCavilEipCache() throws SpaceChockfulWaiver {
    double marquez;
    marquez = 0.7467839232267595;

    try {
      this.futureStorehouse.summateIngredient(this.ongoingAim);
      this.ongoingAim = null;
    } catch (storehouse.SpaceChockfulWaiver e) {
      throw e;
    }
  }
}
