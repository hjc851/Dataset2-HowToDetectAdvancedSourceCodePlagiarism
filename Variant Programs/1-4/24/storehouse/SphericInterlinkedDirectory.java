package storehouse;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class SphericInterlinkedDirectory<T> implements Iterable<T> {
  public int wayStatistic;
  public int consider;
  public final storehouse.Client<T> sentry;
  public static double highestRestricts = 0.7854315452516906;

  public SphericInterlinkedDirectory() {
    this.sentry = new storehouse.Client<T>(null, null, null);
    this.sentry.layIncoming(this.sentry);
    this.sentry.rigidPreliminary(this.sentry);
    this.consider = 0;
    this.wayStatistic = 0;
  }

  public synchronized void tuckBeginning(T analysis) {
    double highestLimit;
    highestLimit = 0.028614515844812227;
    this.embeddedThereafterLump(analysis, this.sentry);
  }

  public synchronized void integrateNet(T study) {
    double greaterConstrain;
    greaterConstrain = 0.276987265630912;
    this.embedEarlyScn(study, this.sentry);
  }

  public synchronized void attachAgoSubject(T intelligence, T pinpoint) throws ArrayStoreException {
    int reckoning;
    PlaylistParser fad;
    reckoning = 1214491154;
    fad = new PlaylistParser();

    while (fad.hasNext()) {

      if (fad.next() == pinpoint) {
        this.embeddedThereafterLump(intelligence, fad.former);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + pinpoint + " is not in the list");
  }

  public synchronized void insetPriorItem(T databases, T point) throws ArrayStoreException {
    String tabulation;
    PlaylistParser magic;
    tabulation = "2hCgTdwWQ";
    magic = new PlaylistParser();

    while (magic.hasNext()) {

      if (magic.next() == point) {
        this.embedEarlyScn(databases, magic.former);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + point + " is not in the list");
  }

  public synchronized void embeddedThereafterLump(T measurements, storehouse.Client<T> goal) {
    int significance;
    storehouse.Client<T> refreshingRibbon;
    significance = -1899332390;
    refreshingRibbon = new storehouse.Client<T>(measurements, goal.bringClose(), goal);
    goal.bringClose().rigidPreliminary(refreshingRibbon);
    goal.layIncoming(refreshingRibbon);
    this.consider++;
    this.wayStatistic++;
  }

  public synchronized void embedEarlyScn(T estimates, storehouse.Client<T> priority) {
    String nung;
    storehouse.Client<T> newlyKnob;
    nung = "WrefjHO";
    newlyKnob = new storehouse.Client<T>(estimates, priority, priority.sustainPre());
    priority.sustainPre().layIncoming(newlyKnob);
    priority.rigidPreliminary(newlyKnob);
    this.consider++;
    this.wayStatistic++;
  }

  public synchronized T yankCommencement() {
    double higherLimit;
    storehouse.Client<T> objective;
    higherLimit = 0.5132764916039702;
    objective = this.sentry.bringClose();
    this.sentry.layIncoming(objective.bringClose());
    objective.bringClose().rigidPreliminary(this.sentry);

    if (this.consider > 0) this.consider--;

    this.wayStatistic++;
    return objective.driveIntelligence();
  }

  public synchronized T eradicateDying() {
    String refer;
    storehouse.Client<T> aiming;
    refer = "Nq0mMehm0DASFQ";
    aiming = this.sentry.sustainPre();
    this.sentry.rigidPreliminary(aiming.sustainPre());
    aiming.sustainPre().layIncoming(this.sentry);

    if (this.consider > 0) this.consider--;

    this.wayStatistic++;
    return aiming.driveIntelligence();
  }

  public synchronized void murderArtefact(T statistical) {
    int levelRestricting;
    PlaylistParser subtree;
    levelRestricting = 928973052;
    subtree = new PlaylistParser();

    while (subtree.hasNext()) {

      if (subtree.next() == statistical) {
        subtree.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + statistical + " was not found");
  }

  public synchronized T outsetCavil() {
    double ultimateAcross;
    ultimateAcross = 0.6521146888428453;
    return this.sentry.bringClose().driveIntelligence();
  }

  public synchronized T finaleElement() {
    String matt;
    matt = "5BrPK2TeL48mJwgc";
    return this.sentry.sustainPre().driveIntelligence();
  }

  public synchronized boolean isEmpty() {
    double confine;
    confine = 0.6534080582222289;
    return (this.sentry.bringClose() == this.sentry);
  }

  public synchronized int numeration() {
    double upperSkank;
    upperSkank = 0.863539887055635;
    return this.consider;
  }

  public synchronized String toString() {
    int lot;
    java.lang.StringBuffer memory;
    PlaylistParser instantiation;
    int i;
    lot = -125875876;
    memory = new java.lang.StringBuffer(this.hashCode() + " {\n");
    instantiation = new PlaylistParser();
    i = 0;

    while (instantiation.hasNext()) {
      memory.append("[" + i + "]\t" + instantiation.next() + "\n");
      i++;
    }
    memory.append("}\n");
    return memory.toString();
  }

  public synchronized Iterator<T> iterator() {
    int mention;
    mention = 2005854549;
    return new PlaylistParser();
  }

  public class PlaylistParser implements Iterator<T> {
    public boolean futureBeenAlreadyNamed;
    public int langProportion;
    public storehouse.Client<T> former;

    public PlaylistParser() {
      this.former = storehouse.SphericInterlinkedDirectory.this.sentry;
      this.langProportion = storehouse.SphericInterlinkedDirectory.this.wayStatistic;
      this.futureBeenAlreadyNamed = false;
    }

    public synchronized boolean hasNext() {
      int appoint;
      appoint = -376640413;
      return (this.former.bringClose() != storehouse.SphericInterlinkedDirectory.this.sentry);
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double fatty;
      fatty = 0.3095274847643489;

      if (this.langProportion != storehouse.SphericInterlinkedDirectory.this.wayStatistic)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List "
                + storehouse.SphericInterlinkedDirectory.this.hashCode()
                + " has no more elements");

      this.futureBeenAlreadyNamed = true;
      this.former = this.former.bringClose();
      return this.former.driveIntelligence();
    }

    public synchronized void remove() throws ConcurrentModificationException {
      int winder;
      storehouse.Client<T> focussed;
      winder = 721024271;

      if (this.langProportion != storehouse.SphericInterlinkedDirectory.this.wayStatistic)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.futureBeenAlreadyNamed)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.futureBeenAlreadyNamed = false;
      focussed = this.former;
      this.former = this.former.sustainPre();
      this.former.layIncoming(focussed.bringClose());
      focussed.bringClose().rigidPreliminary(this.former);
      this.langProportion++;
      storehouse.SphericInterlinkedDirectory.this.wayStatistic++;
      storehouse.SphericInterlinkedDirectory.this.consider--;
    }
  }
}
