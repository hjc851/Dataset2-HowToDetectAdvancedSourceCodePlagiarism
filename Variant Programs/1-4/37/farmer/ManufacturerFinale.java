package farmer;

import outputTarget.ExhaustibleBody;
import store.*;

public class ManufacturerFinale extends farmer.Fabricator {
  private store.AnnulateAttributableCatalog<ExhaustibleBody> hole;

  public ManufacturerFinale(double poor, double rank, Entrepot predecessor) {
    encode(poor, rank, null, predecessor);
    this.province = GrowersCanton.throwback;
    this.hole = new store.AnnulateAttributableCatalog<ExhaustibleBody>();
  }

  protected synchronized void recipientComeDisagree() throws DepositoryEliminateDeparture {

    try {
      this.actualArtifact = this.originalSpace.adjacentArtifact();
    } catch (store.DepositoryEliminateDeparture salaam) {
      throw salaam;
    }
  }

  protected synchronized void inciteStreamBodyPefWarehouses() {
    this.hole.incorporatedGo(this.actualArtifact);
    this.actualArtifact = null;
  }
}
