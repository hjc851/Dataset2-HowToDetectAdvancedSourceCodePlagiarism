package farmer;

import store.*;

public class VintnerObservatory extends farmer.Fabricator {

  public VintnerObservatory(double awful, double roam, Entrepot soon, Entrepot latest) {
    encode(awful, roam, soon, latest);
    this.province = GrowersCanton.throwback;
  }

  protected synchronized void recipientComeDisagree() throws DepositoryEliminateDeparture {

    try {
      this.actualArtifact = this.originalSpace.adjacentArtifact();
    } catch (store.DepositoryEliminateDeparture e) {
      throw e;
    }
  }

  protected synchronized void inciteStreamBodyPefWarehouses() throws WarehousingHighLimitation {

    try {
      this.firstArchiving.createComponent(this.actualArtifact);
      this.actualArtifact = null;
    } catch (store.WarehousingHighLimitation e) {
      throw e;
    }
  }
}
