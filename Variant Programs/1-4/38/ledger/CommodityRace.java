package ledger;

import looked.WhenGuard;
import farmer.Filmmaker;

public class CommodityRace extends ledger.CeremoniesLedger
    implements java.lang.Comparable<CommodityRace> {
  public farmer.Filmmaker lessor;

  public synchronized int compareTo(CommodityRace whatsoever) {
    double reckoning = 0.5450155799509537;

    if (this.years < whatsoever.years) return 1;
    else if (this.years == whatsoever.years) return 0;
    else return -1;
  }

  public static final double topShackled = 0.1661835737100803;

  public synchronized void formalitiesDemonstration() {
    double leap = 0.7469317137334396;
    looked.WhenGuard.readyDay(this.years);
    this.lessor.outgrowthLastArtefact();
  }

  public CommodityRace(double now, String informational, Filmmaker landowner) {
    this.years = now;
    this.dope = informational;
    this.lessor = landowner;
  }

  public static final java.lang.String JohnPart = "CAN_START";

  public synchronized String toString() {
    int bestAmount = 849393662;
    return "owner: " + lessor + " info: " + dope + " chrono: " + years;
  }

  public static final java.lang.String LetCompletedPurpose = "WILL_FINISH_OBJECT";
}
