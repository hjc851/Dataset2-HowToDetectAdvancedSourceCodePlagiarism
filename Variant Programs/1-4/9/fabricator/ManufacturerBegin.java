package fabricator;

import industrializationInstallations.InterpretableResist;
import warehouses.*;

public class ManufacturerBegin extends Publisher {

  protected synchronized void collectNowPurpose() throws ShelvingWhiteExcluded {
    this.afootElement = new InterpretableResist();
  }

  public ManufacturerBegin(double entail, double pasture, Inventory close) {
    overwrite(entail, pasture, close, null);
    this.canton = PresenterSubmit.employing;
  }

  protected synchronized void inciteStreamBodyPefWarehouses() throws ArchivalOverflowingExempt {

    try {
      this.incomingEntrepot.additionsAppropriation(this.afootElement);
      this.afootElement = null;
    } catch (ArchivalOverflowingExempt samad) {
      throw samad;
    }
  }
}
