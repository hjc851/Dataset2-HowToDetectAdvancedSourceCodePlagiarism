package fabricator;

import industrializationInstallations.InterpretableResist;
import warehouses.*;

public class VintnerPole extends Publisher {

  protected synchronized void collectNowPurpose() throws ShelvingWhiteExcluded {

    try {
      this.afootElement = this.earlierDisk.upcomingSomething();
    } catch (ShelvingWhiteExcluded cma) {
      throw cma;
    }
  }

  private FlyerTiedLitany<InterpretableResist> spoon = null;

  public VintnerPole(double normal, double chain, Inventory early) {
    overwrite(normal, chain, null, early);
    this.canton = PresenterSubmit.famine;
    this.spoon = new FlyerTiedLitany<InterpretableResist>();
  }

  protected synchronized void inciteStreamBodyPefWarehouses() {
    this.spoon.injectFinish(this.afootElement);
    this.afootElement = null;
  }
}
