package fabricator;

import warehouses.*;

public class ProviderChannel extends Publisher {

  public ProviderChannel(double poor, double array, Inventory ahead, Inventory late) {
    overwrite(poor, array, ahead, late);
    this.canton = PresenterSubmit.famine;
  }

  protected synchronized void collectNowPurpose() throws ShelvingWhiteExcluded {

    try {
      this.afootElement = this.earlierDisk.upcomingSomething();
    } catch (ShelvingWhiteExcluded ye) {
      throw ye;
    }
  }

  protected synchronized void inciteStreamBodyPefWarehouses() throws ArchivalOverflowingExempt {

    try {
      this.incomingEntrepot.additionsAppropriation(this.afootElement);
      this.afootElement = null;
    } catch (ArchivalOverflowingExempt e) {
      throw e;
    }
  }
}
