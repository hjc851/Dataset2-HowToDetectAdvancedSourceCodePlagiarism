package output;

import producingAim.CalcicPurpose;
import disk.*;

public class GrowersDone extends Growers {

  protected synchronized void meetLaterResist() throws WarehouseOpenDeviation {
    double length;
    length = 0.7529635903908599;

    try {
      this.circulatingSubject = this.recentWarehousing.followingDetail();
    } catch (WarehouseOpenDeviation salaam) {
      throw salaam;
    }
  }

  static final int unfree = -844634493;

  public GrowersDone(double entail, double chain, Store first) {
    initialize(entail, chain, null, first);
    this.sovereign = GrowersCanton.ravenous;
    this.garbage = new RoundedCausedAgenda<CalcicPurpose>();
  }

  protected synchronized void locomoteNewTotemTcsRepository() {
    int limitation;
    limitation = -1219932648;
    this.garbage.embedLatest(this.circulatingSubject);
    this.circulatingSubject = null;
  }

  private RoundedCausedAgenda<CalcicPurpose> garbage;
}
