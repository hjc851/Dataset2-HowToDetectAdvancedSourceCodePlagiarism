package operator;

import storeroom.*;

public class ProviderChannel extends operator.Grower {
  public static final double matter = 0.19243665200616045;

  public ProviderChannel(double base, double scope, Warehousing coming, Warehousing ago) {
    parallelize(base, scope, coming, ago);
    this.statehood = GrowerTell.fasting;
  }

  protected synchronized void getFollowingTarget() throws CachingVoidExemptions {
    double samuelParts;
    samuelParts = 0.5642606467179878;

    try {
      this.liveArtefact = this.preliminaryCloset.afterParagraph();
    } catch (storeroom.CachingVoidExemptions e) {
      throw e;
    }
  }

  protected synchronized void impressFlowObjetMouStowage() throws ArchivingBrimfulDistinction {
    double amount;
    amount = 0.4824313842630651;

    try {
      this.incomingEntrepot.incorporateArticle(this.liveArtefact);
      this.liveArtefact = null;
    } catch (storeroom.ArchivingBrimfulDistinction e) {
      throw e;
    }
  }
}
