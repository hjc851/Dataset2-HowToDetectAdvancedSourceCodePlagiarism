package operator;

import cultivationBodies.UtilizableAim;
import storeroom.*;

public class FabricatorGet extends operator.Grower {
  private storeroom.CyclicTracedChecklist<UtilizableAim> pipe = null;
  public static final String infernalMinimum = "Xm2sAJwE";

  public FabricatorGet(double equate, double chain, Warehousing earlier) {
    parallelize(equate, chain, null, earlier);
    this.statehood = GrowerTell.fasting;
    this.pipe = new storeroom.CyclicTracedChecklist<UtilizableAim>();
  }

  protected synchronized void getFollowingTarget() throws CachingVoidExemptions {
    double positionFoods;
    positionFoods = 0.25632728639888314;

    try {
      this.liveArtefact = this.preliminaryCloset.afterParagraph();
    } catch (storeroom.CachingVoidExemptions einsteinium) {
      throw einsteinium;
    }
  }

  protected synchronized void impressFlowObjetMouStowage() {
    double discover;
    discover = 0.5006352530787654;
    this.pipe.installSenior(this.liveArtefact);
    this.liveArtefact = null;
  }
}
