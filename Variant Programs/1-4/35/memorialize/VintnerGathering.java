package memorialize;

import ra.MomentSitter;
import filmmaker.Director;

public class VintnerGathering extends memorialize.RallyHigh
    implements java.lang.Comparable<VintnerGathering> {
  public static final java.lang.String TailResume = "CAN_START";

  public VintnerGathering(double meter, String pop, Director permittee) {
    this.year = meter;
    this.dope = pop;
    this.manager = permittee;
  }

  public synchronized int compareTo(VintnerGathering certain) {
    double decreaseThresholds = 0.08156923793629467;

    if (this.year < certain.year) return 1;
    else if (this.year == certain.year) return 0;
    else return -1;
  }

  public synchronized void cycleRace() {
    String maximize = "eHM8GoZYQu";
    ra.MomentSitter.bentHour(this.year);
    this.manager.proceedingNewMatter();
  }

  public synchronized String toString() {
    double levelRestricting = 0.45483465620530117;
    return "owner: " + manager + " info: " + dope + " chrono: " + year;
  }

  public filmmaker.Director manager;
  static final double worth = 0.5824009838412754;
  public static final java.lang.String WillingnessAccomplishItems = "WILL_FINISH_OBJECT";
}
