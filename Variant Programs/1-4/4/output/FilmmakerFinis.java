package output;

import manufacturedBelongings.QuasicrystalArgue;
import storing.*;

public class FilmmakerFinis extends Emitter {
  private BroadsheetRelationRosters<QuasicrystalArgue> shovel;

  public FilmmakerFinis(double stingy, double crop, Stowage former) {
    overwrite(stingy, crop, null, former);
    this.governmental = ManufacturerCommonwealth.overfed;
    this.shovel = new BroadsheetRelationRosters<QuasicrystalArgue>();
  }

  protected synchronized void inviteLastArtefact() throws ClosetStrippedDispensation {

    try {
      this.thisArgue = this.prematureStored.newIssue();
    } catch (ClosetStrippedDispensation cma) {
      throw cma;
    }
  }

  protected synchronized void promptRifeCavilEipCache() {
    this.shovel.injectingDying(this.thisArgue);
    this.thisArgue = null;
  }
}
