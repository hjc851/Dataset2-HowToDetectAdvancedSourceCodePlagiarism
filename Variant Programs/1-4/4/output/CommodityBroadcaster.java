package output;

import storing.*;

public class CommodityBroadcaster extends Emitter {

  public CommodityBroadcaster(double signify, double graze, Stowage coming, Stowage predecessor) {
    overwrite(signify, graze, coming, predecessor);
    this.governmental = ManufacturerCommonwealth.overfed;
  }

  protected synchronized void inviteLastArtefact() throws ClosetStrippedDispensation {

    try {
      this.thisArgue = this.prematureStored.newIssue();
    } catch (ClosetStrippedDispensation salaam) {
      throw salaam;
    }
  }

  protected synchronized void promptRifeCavilEipCache() throws SafekeepingGoodDerogate {

    try {
      this.thirdShelving.impartPiece(this.thisArgue);
      this.thisArgue = null;
    } catch (SafekeepingGoodDerogate e) {
      throw e;
    }
  }
}
