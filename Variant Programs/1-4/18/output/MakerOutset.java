package output;

import outputTarget.PhyllosilicatePreclude;
import memory.*;

public class MakerOutset extends output.Production {

  public MakerOutset(double intend, double crop, Warehousing after) {
    configure(intend, crop, after, null);
    this.government = ProducingPublic.strive;
  }

  protected synchronized void collectNowPurpose() throws DiskRansackedObjection {
    this.afootElement = new outputTarget.PhyllosilicatePreclude();
  }

  protected synchronized void proceedLatestTargetPoiDepot() throws GarageRichCase {

    try {
      this.thirdShelving.injectThing(this.afootElement);
      this.afootElement = null;
    } catch (memory.GarageRichCase ej) {
      throw ej;
    }
  }
}
