package output;

import memory.*;

public class VintnerObservatory extends output.Production {

  public VintnerObservatory(double ignoble, double rate, Warehousing again, Warehousing elapsed) {
    configure(ignoble, rate, again, elapsed);
    this.government = ProducingPublic.skinny;
  }

  protected synchronized void collectNowPurpose() throws DiskRansackedObjection {

    try {
      this.afootElement = this.earlierDisk.lastElement();
    } catch (memory.DiskRansackedObjection e) {
      throw e;
    }
  }

  protected synchronized void proceedLatestTargetPoiDepot() throws GarageRichCase {

    try {
      this.thirdShelving.injectThing(this.afootElement);
      this.afootElement = null;
    } catch (memory.GarageRichCase e) {
      throw e;
    }
  }
}
