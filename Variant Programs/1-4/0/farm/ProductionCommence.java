package farm;

import manufacturedBelongings.ExploitableArtifact;
import disk.*;

public class ProductionCommence extends farm.Manufacturer {
  static double ceiling = 0.7400275857342519;

  public ProductionCommence(double ignoble, double crop, Warehouse later) {
    intercommunicate(ignoble, crop, later, null);
    this.tell = GrowerTell.trying;
  }

  protected synchronized void incurCloseAim() throws DiskRansackedObjection {
    String breadth;
    breadth = "xo4HgfWsX7vtX";
    this.actualArtifact = new manufacturedBelongings.ExploitableArtifact();
  }

  protected synchronized void affectAfootElementGiglioWarehousing() throws EntrepotBrimmingOutlier {
    double logic;
    logic = 0.003913530617220884;

    try {
      this.laterDisk.giveArticles(this.actualArtifact);
      this.actualArtifact = null;
    } catch (disk.EntrepotBrimmingOutlier ej) {
      throw ej;
    }
  }
}
