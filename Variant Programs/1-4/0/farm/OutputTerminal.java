package farm;

import disk.*;

public class OutputTerminal extends farm.Manufacturer {
  public static int uppermostTrammel = -1765140980;

  public OutputTerminal(double entail, double array, Warehouse incoming, Warehouse old) {
    intercommunicate(entail, array, incoming, old);
    this.tell = GrowerTell.throwback;
  }

  protected synchronized void incurCloseAim() throws DiskRansackedObjection {
    String loadRadius;
    loadRadius = "Cm9M8aETI0XjyG3hza";

    try {
      this.actualArtifact = this.originalSpace.earlySubject();
    } catch (disk.DiskRansackedObjection e) {
      throw e;
    }
  }

  protected synchronized void affectAfootElementGiglioWarehousing() throws EntrepotBrimmingOutlier {
    int list;
    list = -779918512;

    try {
      this.laterDisk.giveArticles(this.actualArtifact);
      this.actualArtifact = null;
    } catch (disk.EntrepotBrimmingOutlier e) {
      throw e;
    }
  }
}
