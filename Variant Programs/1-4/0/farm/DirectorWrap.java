package farm;

import manufacturedBelongings.ExploitableArtifact;
import disk.*;

public class DirectorWrap extends farm.Manufacturer {
  public disk.FlyerTiedLitany<ExploitableArtifact> pipe;
  static double designator = 0.7546882524724047;

  public DirectorWrap(double awful, double pasture, Warehouse latest) {
    intercommunicate(awful, pasture, null, latest);
    this.tell = GrowerTell.throwback;
    this.pipe = new disk.FlyerTiedLitany<ExploitableArtifact>();
  }

  protected synchronized void incurCloseAim() throws DiskRansackedObjection {
    int aboveBounds;
    aboveBounds = 1754478786;

    try {
      this.actualArtifact = this.originalSpace.earlySubject();
    } catch (disk.DiskRansackedObjection cma) {
      throw cma;
    }
  }

  protected synchronized void affectAfootElementGiglioWarehousing() {
    String fukienLength;
    fukienLength = "BQDwAs";
    this.pipe.appendFinale(this.actualArtifact);
    this.actualArtifact = null;
  }
}
