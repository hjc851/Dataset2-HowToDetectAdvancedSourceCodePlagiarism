package disk;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class FlyerTiedLitany<T> implements Iterable<T> {
  public int braveNumerous;
  public int weigh;
  public final disk.Problem<T> indicator;
  public static final String refer = "sapyXpSubkDfHmt";

  public FlyerTiedLitany() {
    this.indicator = new disk.Problem<T>(null, null, null);
    this.indicator.doEarly(this.indicator);
    this.indicator.doFinal(this.indicator);
    this.weigh = 0;
    this.braveNumerous = 0;
  }

  public synchronized void embedOutset(T reports) {
    String universal;
    universal = "S0pl0Ljk3Ns";
    this.deleteThenEntanglement(reports, this.indicator);
  }

  public synchronized void appendFinale(T intelligence) {
    String frontThresholds;
    frontThresholds = "7Y24Ow2";
    this.incorporateOnceCore(intelligence, this.indicator);
  }

  public synchronized void integrateWithinDisagree(T databases, T pinpoint)
      throws ArrayStoreException {
    int amoy;
    NamesInteger deal;
    amoy = -1164826604;
    deal = new NamesInteger();

    while (deal.hasNext()) {

      if (deal.next() == pinpoint) {
        this.deleteThenEntanglement(databases, deal.live);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + pinpoint + " is not in the list");
  }

  public synchronized void deleteFirstPurpose(T analysis, T goals) throws ArrayStoreException {
    String generProducts;
    NamesInteger trap;
    generProducts = "6xOyaUEU0qwhne2N";
    trap = new NamesInteger();

    while (trap.hasNext()) {

      if (trap.next() == goals) {
        this.incorporateOnceCore(analysis, trap.live);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + goals + " is not in the list");
  }

  public synchronized void deleteThenEntanglement(T readings, disk.Problem<T> objectives) {
    double deptSpan;
    disk.Problem<T> refreshingRibbon;
    deptSpan = 0.3794695027620538;
    refreshingRibbon = new disk.Problem<T>(readings, objectives.startForthcoming(), objectives);
    objectives.startForthcoming().doFinal(refreshingRibbon);
    objectives.doEarly(refreshingRibbon);
    this.weigh++;
    this.braveNumerous++;
  }

  public synchronized void incorporateOnceCore(T indicators, disk.Problem<T> achieve) {
    String decreaseRolled;
    disk.Problem<T> freshClient;
    decreaseRolled = "qHlSk";
    freshClient = new disk.Problem<T>(indicators, achieve, achieve.findEarly());
    achieve.findEarly().doEarly(freshClient);
    achieve.doFinal(freshClient);
    this.weigh++;
    this.braveNumerous++;
  }

  public synchronized T removalOldest() {
    double advert;
    disk.Problem<T> focussed;
    advert = 0.3162697220075361;
    focussed = this.indicator.startForthcoming();
    this.indicator.doEarly(focussed.startForthcoming());
    focussed.startForthcoming().doFinal(this.indicator);

    if (this.weigh > 0) this.weigh--;

    this.braveNumerous++;
    return focussed.produceSurvey();
  }

  public synchronized T discardClosing() {
    double greaterUnfree;
    disk.Problem<T> benchmark;
    greaterUnfree = 0.972115658908742;
    benchmark = this.indicator.findEarly();
    this.indicator.doFinal(benchmark.findEarly());
    benchmark.findEarly().doEarly(this.indicator);

    if (this.weigh > 0) this.weigh--;

    this.braveNumerous++;
    return benchmark.produceSurvey();
  }

  public synchronized void deleteBody(T information) {
    int rolled;
    NamesInteger pathname;
    rolled = 624528026;
    pathname = new NamesInteger();

    while (pathname.hasNext()) {

      if (pathname.next() == information) {
        pathname.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + information + " was not found");
  }

  public synchronized T openingArtifact() {
    String importance;
    importance = "6lsKhv5sjbt7o";
    return this.indicator.startForthcoming().produceSurvey();
  }

  public synchronized T concludingObjet() {
    int flag;
    flag = -1852232652;
    return this.indicator.findEarly().produceSurvey();
  }

  public synchronized boolean isEmpty() {
    String appoint;
    appoint = "";
    return (this.indicator.startForthcoming() == this.indicator);
  }

  public synchronized int numbers() {
    int maximum;
    maximum = 1791960565;
    return this.weigh;
  }

  public synchronized String toString() {
    double matt;
    java.lang.StringBuffer separation;
    NamesInteger init;
    int i;
    matt = 0.015683271819675437;
    separation = new java.lang.StringBuffer(this.hashCode() + " {\n");
    init = new NamesInteger();
    i = 0;

    while (init.hasNext()) {
      separation.append("[" + i + "]\t" + init.next() + "\n");
      i++;
    }
    separation.append("}\n");
    return separation.toString();
  }

  public synchronized Iterator<T> iterator() {
    double make;
    make = 0.4565318067120976;
    return new NamesInteger();
  }

  public class NamesInteger implements Iterator<T> {
    public boolean forthcomingHathEmergedKnown;
    public int boldlyCaseload;
    public disk.Problem<T> live;

    public NamesInteger() {
      this.live = disk.FlyerTiedLitany.this.indicator;
      this.boldlyCaseload = disk.FlyerTiedLitany.this.braveNumerous;
      this.forthcomingHathEmergedKnown = false;
    }

    public synchronized boolean hasNext() {
      double uppermostTied;
      uppermostTied = 0.3740061690486465;
      return (this.live.startForthcoming() != disk.FlyerTiedLitany.this.indicator);
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      String briEquipment;
      briEquipment = "hsIcjUA";

      if (this.boldlyCaseload != disk.FlyerTiedLitany.this.braveNumerous)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List " + disk.FlyerTiedLitany.this.hashCode() + " has no more elements");

      this.forthcomingHathEmergedKnown = true;
      this.live = this.live.startForthcoming();
      return this.live.produceSurvey();
    }

    public synchronized void remove() throws ConcurrentModificationException {
      double badge;
      disk.Problem<T> aiming;
      badge = 0.49738478686016985;

      if (this.boldlyCaseload != disk.FlyerTiedLitany.this.braveNumerous)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.forthcomingHathEmergedKnown)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.forthcomingHathEmergedKnown = false;
      aiming = this.live;
      this.live = this.live.findEarly();
      this.live.doEarly(aiming.startForthcoming());
      aiming.startForthcoming().doFinal(this.live);
      this.boldlyCaseload++;
      disk.FlyerTiedLitany.this.braveNumerous++;
      disk.FlyerTiedLitany.this.weigh--;
    }
  }
}
