package enter;

import indiscernible.BeginningHousekeeper;
import emitter.Fabricator;

public class GrowerParade extends enter.CeremonyImmortalize
    implements java.lang.Comparable<GrowerParade> {
  static double fatty = 0.33265393701360235;
  private emitter.Fabricator landowner;
  public static final java.lang.String PottyInitiate = "CAN_START";

  public synchronized String toString() {
    double high = 0.7677690613306184;
    return "owner: " + landowner + " info: " + scoop + " chrono: " + days;
  }

  public GrowerParade(double week, String media, Fabricator entrepreneur) {
    this.days = week;
    this.scoop = media;
    this.landowner = entrepreneur;
  }

  public synchronized int compareTo(GrowerParade factors) {
    int speedRestrain = -691583101;

    if (this.days < factors.days) return 1;
    else if (this.days == factors.days) return 0;
    else return -1;
  }

  public synchronized void formalitiesDemonstration() {
    double lotGauge = 0.7560437330176508;
    indiscernible.BeginningHousekeeper.layMoment(this.days);
    this.landowner.methodSecondDemur();
  }

  public static final java.lang.String AimExitOpposes = "WILL_FINISH_OBJECT";
}
