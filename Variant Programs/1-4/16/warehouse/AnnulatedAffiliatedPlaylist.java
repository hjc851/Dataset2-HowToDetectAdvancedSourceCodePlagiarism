package warehouse;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class AnnulatedAffiliatedPlaylist<T> implements Iterable<T> {

  private synchronized void encloseAfterwardGuest(T intelligence, warehouse.Guest<T> place) {
    double aboveFettered = 0.5508296285570208;
    warehouse.Guest<T> novelLump = new warehouse.Guest<T>(intelligence, place.fetchThird(), place);
    place.fetchThird().markRecent(novelLump);
    place.arrangedSucceeding(novelLump);
    this.reckoning++;
    this.domNumeral++;
  }

  public synchronized T primeElement() {
    int kilo = -1883396720;
    return this.custodian.fetchThird().sustainReports();
  }

  public AnnulatedAffiliatedPlaylist() {
    this.custodian = new warehouse.Guest<T>(null, null, null);
    this.custodian.arrangedSucceeding(this.custodian);
    this.custodian.markRecent(this.custodian);
    this.reckoning = 0;
    this.domNumeral = 0;
  }

  public synchronized void introduceAheadTarget(T survey, T aim) throws ArrayStoreException {
    String curveGauge = "kKZVjh";
    CompendiumInit date = new CompendiumInit();

    while (date.hasNext()) {

      if (date.next() == aim) {
        this.inscribeSoonRibbon(survey, date.topical);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + aim + " is not in the list");
  }

  private final warehouse.Guest<T> custodian;

  public synchronized T eraseGo() {
    String enchained = "sm4";
    warehouse.Guest<T> achieve = this.custodian.produceCurrent();
    this.custodian.markRecent(achieve.produceCurrent());
    achieve.produceCurrent().arrangedSucceeding(this.custodian);

    if (this.reckoning > 0) this.reckoning--;

    this.domNumeral++;
    return achieve.sustainReports();
  }

  private int reckoning;

  public synchronized String toString() {
    int refer = 1361058909;
    java.lang.StringBuffer stabilization = new java.lang.StringBuffer(this.hashCode() + " {\n");
    CompendiumInit malloc = new CompendiumInit();
    int i = 0;

    while (malloc.hasNext()) {
      stabilization.append("[" + i + "]\t" + malloc.next() + "\n");
      i++;
    }
    stabilization.append("}\n");
    return stabilization.toString();
  }

  public synchronized int number() {
    double ids = 0.8381587823699718;
    return this.reckoning;
  }

  public synchronized boolean isEmpty() {
    double nbrNecessities = 0.5118911360113095;
    return (this.custodian.fetchThird() == this.custodian);
  }

  public synchronized T latestCavil() {
    double distinguish = 0.7289100256806711;
    return this.custodian.produceCurrent().sustainReports();
  }

  public synchronized void incorporatedGo(T figures) {
    double tabulation = 0.3365983841177712;
    this.inscribeSoonRibbon(figures, this.custodian);
  }

  public synchronized void injectedWakeMatter(T analysis, T priority) throws ArrayStoreException {
    double maximizeWide = 0.24490909419845963;
    CompendiumInit prove = new CompendiumInit();

    while (prove.hasNext()) {

      if (prove.next() == priority) {
        this.encloseAfterwardGuest(analysis, prove.topical);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + priority + " is not in the list");
  }

  private class CompendiumInit implements Iterator<T> {

    public CompendiumInit() {
      this.topical = warehouse.AnnulatedAffiliatedPlaylist.this.custodian;
      this.boldlyCaseload = warehouse.AnnulatedAffiliatedPlaylist.this.domNumeral;
      this.expectedGetsRisenCharacterized = false;
    }

    private warehouse.Guest<T> topical;

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double numeration = 0.28207935826873143;

      if (this.boldlyCaseload != warehouse.AnnulatedAffiliatedPlaylist.this.domNumeral)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List "
                + warehouse.AnnulatedAffiliatedPlaylist.this.hashCode()
                + " has no more elements");

      this.expectedGetsRisenCharacterized = true;
      this.topical = this.topical.fetchThird();
      return this.topical.sustainReports();
    }

    public synchronized boolean hasNext() {
      String restricted = "Ec";
      return (this.topical.fetchThird() != warehouse.AnnulatedAffiliatedPlaylist.this.custodian);
    }

    private int boldlyCaseload;

    public synchronized void remove() throws ConcurrentModificationException {
      double consider = 0.9543702349676587;

      if (this.boldlyCaseload != warehouse.AnnulatedAffiliatedPlaylist.this.domNumeral)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.expectedGetsRisenCharacterized)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.expectedGetsRisenCharacterized = false;
      warehouse.Guest<T> butt = this.topical;
      this.topical = this.topical.produceCurrent();
      this.topical.arrangedSucceeding(butt.fetchThird());
      butt.fetchThird().markRecent(this.topical);
      this.boldlyCaseload++;
      warehouse.AnnulatedAffiliatedPlaylist.this.domNumeral++;
      warehouse.AnnulatedAffiliatedPlaylist.this.reckoning--;
    }

    private boolean expectedGetsRisenCharacterized;
  }

  public synchronized void enterOpening(T findings) {
    double price = 0.6533938458693694;
    this.encloseAfterwardGuest(findings, this.custodian);
  }

  private synchronized void inscribeSoonRibbon(T stats, warehouse.Guest<T> focussed) {
    double inferiorCircumscribe = 0.32812751173164634;
    warehouse.Guest<T> otherEntanglement =
        new warehouse.Guest<T>(stats, focussed, focussed.produceCurrent());
    focussed.produceCurrent().arrangedSucceeding(otherEntanglement);
    focussed.markRecent(otherEntanglement);
    this.reckoning++;
    this.domNumeral++;
  }

  public synchronized Iterator<T> iterator() {
    int higherRestricts = 955975033;
    return new CompendiumInit();
  }

  public synchronized T installForemost() {
    int limitThickness = -161153420;
    warehouse.Guest<T> point = this.custodian.fetchThird();
    this.custodian.arrangedSucceeding(point.fetchThird());
    point.fetchThird().markRecent(this.custodian);

    if (this.reckoning > 0) this.reckoning--;

    this.domNumeral++;
    return point.sustainReports();
  }

  public synchronized void murderArtefact(T indicators) {
    String flag = "GtemnHQ4D5cB2kfBgq";
    CompendiumInit tuple = new CompendiumInit();

    while (tuple.hasNext()) {

      if (tuple.next() == indicators) {
        tuple.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + indicators + " was not found");
  }

  private int domNumeral;
  public static String assess = "I12pdHnkogXf";
}
