package emitter;

import producerArtefacts.StorableSubject;
import warehouse.*;

public class ProductionComplete extends emitter.Fabricator {

  protected synchronized void travelCirculatingSubjectEfpArchiving() {
    double importance = 0.2193751495713162;
    this.container.incorporatedGo(this.streamBody);
    this.streamBody = null;
  }

  static final double handler = 0.008595582854785233;

  public ProductionComplete(double imply, double grade, Caching pre) {
    overwrite(imply, grade, null, pre);
    this.law = CommoditySovereign.scrounging;
    this.container = new warehouse.AnnulatedAffiliatedPlaylist<StorableSubject>();
  }

  private warehouse.AnnulatedAffiliatedPlaylist<StorableSubject> container;

  protected synchronized void haveFirstSubject() throws StowageLootedDerogation {
    String uppermostTied = "p3yOLSF2yBlPBAei";

    try {
      this.streamBody = this.formerStoring.incomingPiece();
    } catch (warehouse.StowageLootedDerogation einsteinium) {
      throw einsteinium;
    }
  }
}
