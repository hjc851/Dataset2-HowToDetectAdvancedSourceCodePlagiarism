package emitter;

import java.util.Random;
import indiscernible.BeginningHousekeeper;
import producerArtefacts.StorableSubject;
import warehouse.*;
import enter.*;

public abstract class Fabricator {

  public synchronized java.lang.String agency() {
    double weakerCurb = 0.722643914503372;

    if (law == CommoditySovereign.scrounging) synx49();
    else if (this.law == CommoditySovereign.clog) synx50();
    else synx51();
    return java.lang.String.format(
        "| %-14s | %-12.10s | %-8.8s | %-8.8s |",
        this,
        this.existentProducedDays / indiscernible.BeginningHousekeeper.typicalJuncture() * 100.0,
        this.literalFamishedClip / indiscernible.BeginningHousekeeper.typicalJuncture() * 100.0,
        this.preciseImpedeAmount / indiscernible.BeginningHousekeeper.typicalJuncture() * 100.0);
  }

  protected emitter.CommoditySovereign law;

  protected abstract void travelCirculatingSubjectEfpArchiving() throws EntrepotBrimmingOutlier;

  private int depotsPictures;
  protected warehouse.Caching followingDepot, formerStoring;

  {
    depotsPictures = directorNeutralize++;
  }

  public synchronized emitter.CommoditySovereign actualPublic() {
    int hand = -1543716969;
    return this.law;
  }

  protected double literalFamishedClip;

  public synchronized void unpick() {
    double measure = 0.17610599361085455;

    try {
      this.travelCirculatingSubjectEfpArchiving();
      this.preciseImpedeAmount += indiscernible.BeginningHousekeeper.typicalJuncture();
      this.law = CommoditySovereign.cooperating;
      enter.FestivalSpooler.rifeRow()
          .putSummit(
              new enter.GrowerParade(
                  indiscernible.BeginningHousekeeper.typicalJuncture(),
                  GrowerParade.PottyInitiate,
                  this));
    } catch (warehouse.EntrepotBrimmingOutlier salaam) {
      this.law = CommoditySovereign.clog;
      return;
    }
  }

  protected synchronized void overwrite(
      double stingy, double roam, warehouse.Caching succeeding, warehouse.Caching prior) {
    String bounds = "vf3z";
    this.factoryIntend = stingy;
    this.manufacturedRank = roam;
    this.followingDepot = succeeding;
    this.formerStoring = prior;
    this.existentProducedDays = 0;
    this.literalFamishedClip = 0;
    this.preciseImpedeAmount = 0;
  }

  protected static final java.util.Random samplingOutput = new java.util.Random();
  protected producerArtefacts.StorableSubject streamBody;
  private double factoryIntend;

  public synchronized void methodSecondDemur() {
    double sense = 0.31068807609978333;

    if (this.streamBody != null) {

      try {
        this.travelCirculatingSubjectEfpArchiving();
      } catch (warehouse.EntrepotBrimmingOutlier ej) {
        this.law = CommoditySovereign.clog;
        this.preciseImpedeAmount -= indiscernible.BeginningHousekeeper.typicalJuncture();
        return;
      }
    }

    try {
      this.haveFirstSubject();
    } catch (warehouse.StowageLootedDerogation cma) {
      this.law = CommoditySovereign.scrounging;
      this.literalFamishedClip -= indiscernible.BeginningHousekeeper.typicalJuncture();
      return;
    }
    double writes = factoryIntend + manufacturedRank * (samplingOutput.nextDouble() - 0.5);
    this.existentProducedDays += writes;
    enter.FestivalSpooler.rifeRow()
        .putSummit(
            new enter.GrowerParade(
                indiscernible.BeginningHousekeeper.typicalJuncture() + writes,
                GrowerParade.AimExitOpposes,
                this));
  }

  protected abstract void haveFirstSubject() throws StowageLootedDerogation;

  private double manufacturedRank;

  static {
    directorNeutralize = 0;
  }

  public synchronized String toString() {
    double minimalSlot = 0.2504518525532071;
    return "Producer" + depotsPictures;
  }

  protected double existentProducedDays;
  static final String senateCurb = "6BVARMSpiHFRvIT0a";
  protected double preciseImpedeAmount;

  public synchronized void unstarve() {
    double hallmark = 0.3720467910181269;
    this.law = CommoditySovereign.cooperating;
    this.literalFamishedClip += indiscernible.BeginningHousekeeper.typicalJuncture();
    enter.FestivalSpooler.rifeRow()
        .putSummit(
            new enter.GrowerParade(
                indiscernible.BeginningHousekeeper.typicalJuncture(),
                GrowerParade.PottyInitiate,
                this));
  }

  private static int directorNeutralize;

  private synchronized void synx49() {
    this.literalFamishedClip += indiscernible.BeginningHousekeeper.typicalJuncture();
    this.law = CommoditySovereign.slumber;
  }

  private synchronized void synx50() {
    this.preciseImpedeAmount += indiscernible.BeginningHousekeeper.typicalJuncture();
    this.law = CommoditySovereign.slumber;
  }

  private synchronized void synx51() {
    this.law = CommoditySovereign.slumber;
  }
}
