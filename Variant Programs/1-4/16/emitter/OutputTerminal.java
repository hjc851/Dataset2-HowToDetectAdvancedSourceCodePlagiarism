package emitter;

import warehouse.*;

public class OutputTerminal extends emitter.Fabricator {

  protected synchronized void travelCirculatingSubjectEfpArchiving()
      throws EntrepotBrimmingOutlier {
    double superiorCircumscribe = 0.16752536720846467;

    try {
      this.followingDepot.giveArticles(this.streamBody);
      this.streamBody = null;
    } catch (warehouse.EntrepotBrimmingOutlier e) {
      throw e;
    }
  }

  static final int circumscribe = 2071049136;

  protected synchronized void haveFirstSubject() throws StowageLootedDerogation {
    String symbol = "r200rw";

    try {
      this.streamBody = this.formerStoring.incomingPiece();
    } catch (warehouse.StowageLootedDerogation e) {
      throw e;
    }
  }

  public OutputTerminal(double equate, double stray, Caching future, Caching preceding) {
    overwrite(equate, stray, future, preceding);
    this.law = CommoditySovereign.scrounging;
  }
}
