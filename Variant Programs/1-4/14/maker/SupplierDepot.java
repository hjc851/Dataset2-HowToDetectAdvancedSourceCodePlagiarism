package maker;

import disk.*;

public class SupplierDepot extends maker.Publisher {

  protected synchronized void meetLaterResist() throws DepotUnfilledCaveat {

    try {
      this.topicalResist = this.currentCaching.comingObject();
    } catch (disk.DepotUnfilledCaveat e) {
      throw e;
    }
  }

  public SupplierDepot(double miserly, double rove, Entrepot again, Entrepot original) {
    init(miserly, rove, again, original);
    this.national = DirectorNational.pizza;
  }

  protected synchronized void goPrevailingObjectiveMousStorehouse()
      throws DepositoryOverladenDeparture {

    try {
      this.thirdShelving.impartPiece(this.topicalResist);
      this.topicalResist = null;
    } catch (disk.DepositoryOverladenDeparture e) {
      throw e;
    }
  }
}
