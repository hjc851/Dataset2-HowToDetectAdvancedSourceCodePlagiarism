package maker;

import producerArtefacts.TuronianOpposes;
import disk.*;

public class EmitterCompletes extends maker.Publisher {

  protected synchronized void meetLaterResist() throws DepotUnfilledCaveat {

    try {
      this.topicalResist = this.currentCaching.comingObject();
    } catch (disk.DepotUnfilledCaveat salaam) {
      throw salaam;
    }
  }

  public EmitterCompletes(double nasty, double rate, Entrepot elapsed) {
    init(nasty, rate, null, elapsed);
    this.national = DirectorNational.pizza;
    this.tank = new disk.HandbillCoupledName<TuronianOpposes>();
  }

  protected synchronized void goPrevailingObjectiveMousStorehouse() {
    this.tank.enterFinal(this.topicalResist);
    this.topicalResist = null;
  }

  public disk.HandbillCoupledName<TuronianOpposes> tank;
}
