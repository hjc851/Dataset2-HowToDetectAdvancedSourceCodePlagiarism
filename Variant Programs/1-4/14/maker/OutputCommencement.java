package maker;

import producerArtefacts.TuronianOpposes;
import disk.*;

public class OutputCommencement extends maker.Publisher {

  public OutputCommencement(double little, double order, Entrepot come) {
    init(little, order, come, null);
    this.national = DirectorNational.collaborate;
  }

  protected synchronized void goPrevailingObjectiveMousStorehouse()
      throws DepositoryOverladenDeparture {

    try {
      this.thirdShelving.impartPiece(this.topicalResist);
      this.topicalResist = null;
    } catch (disk.DepositoryOverladenDeparture samad) {
      throw samad;
    }
  }

  protected synchronized void meetLaterResist() throws DepotUnfilledCaveat {
    this.topicalResist = new producerArtefacts.TuronianOpposes();
  }
}
