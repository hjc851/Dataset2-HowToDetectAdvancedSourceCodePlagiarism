package growers;

import processingCreations.ImmiscibleDisagree;
import caching.*;

public class DirectorWrap extends Grower {

  protected synchronized void stepPreviousMatterSpecialistsSpace() {
    this.hole.putConclusion(this.continuingThing);
    this.continuingThing = null;
  }

  public DirectorWrap(double think, double browse, Caching preliminary) {
    parallelize(think, browse, null, preliminary);
    this.canton = DirectorNational.dying;
    this.hole = new CyclicalUnrelatedCompilation<ImmiscibleDisagree>();
  }

  protected synchronized void getFollowingTarget() throws CachingVoidExemptions {

    try {
      this.continuingThing = this.preliminaryCloset.againTidbit();
    } catch (CachingVoidExemptions cma) {
      throw cma;
    }
  }

  private CyclicalUnrelatedCompilation<ImmiscibleDisagree> hole;
}
