package growers;

import processingCreations.ImmiscibleDisagree;
import caching.*;

public class ManufacturerBegin extends Grower {

  protected synchronized void getFollowingTarget() throws CachingVoidExemptions {
    this.continuingThing = new ImmiscibleDisagree();
  }

  protected synchronized void stepPreviousMatterSpecialistsSpace() throws StoreroomStuffedUnless {

    try {
      this.theDepository.bestowArtifact(this.continuingThing);
      this.continuingThing = null;
    } catch (StoreroomStuffedUnless salaam) {
      throw salaam;
    }
  }

  public ManufacturerBegin(double meanspirited, double scope, Caching then) {
    parallelize(meanspirited, scope, then, null);
    this.canton = DirectorNational.practical;
  }
}
