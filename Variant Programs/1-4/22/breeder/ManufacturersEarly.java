package breeder;

import productivityDevices.RealizableArtefact;
import memory.*;

public class ManufacturersEarly extends breeder.Breeder {

  protected synchronized void provideExpectedObjet() throws StoreroomPillagedUnless {
    this.actualArtifact = new productivityDevices.RealizableArtefact();
  }

  public ManufacturersEarly(double ungenerous, double rank, Store after) {
    configure(ungenerous, rank, after, null);
    this.tell = EmitterTerritory.doing;
  }

  protected synchronized void runActualArtifactEapStore() throws DepotEntireCaveat {

    try {
      this.laterDisk.extendSubject(this.actualArtifact);
      this.actualArtifact = null;
    } catch (memory.DepotEntireCaveat ye) {
      throw ye;
    }
  }
}
