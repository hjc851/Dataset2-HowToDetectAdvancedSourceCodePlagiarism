package production;

import manufacturedBelongings.MinableOppose;
import warehouses.*;

public class EmitterOpening extends Presenter {

  public EmitterOpening(double normal, double rank, Repository future) {
    parallelize(normal, rank, future, null);
    this.central = EmitterTerritory.employing;
  }

  protected synchronized void eligibleSoonBody() throws DepositoryEliminateDeparture {
    this.presentPreclude = new MinableOppose();
  }

  protected synchronized void propelOngoingAimCoughMemory() throws DiskWholeObjection {

    try {
      this.adjacentStoring.enhanceParagraph(this.presentPreclude);
      this.presentPreclude = null;
    } catch (DiskWholeObjection einsteinium) {
      throw einsteinium;
    }
  }
}
