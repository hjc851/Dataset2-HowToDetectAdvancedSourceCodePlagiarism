package production;

import warehouses.*;

public class PresenterOutpost extends Presenter {

  public PresenterOutpost(double ignoble, double scope, Repository come, Repository former) {
    parallelize(ignoble, scope, come, former);
    this.central = EmitterTerritory.overpopulated;
  }

  protected synchronized void eligibleSoonBody() throws DepositoryEliminateDeparture {

    try {
      this.presentPreclude = this.firstStowage.afterParagraph();
    } catch (DepositoryEliminateDeparture ej) {
      throw ej;
    }
  }

  protected synchronized void propelOngoingAimCoughMemory() throws DiskWholeObjection {

    try {
      this.adjacentStoring.enhanceParagraph(this.presentPreclude);
      this.presentPreclude = null;
    } catch (DiskWholeObjection e) {
      throw e;
    }
  }
}
