package filmmaker;

import productivityDevices.HypabyssalTotem;
import store.*;

public class MakerOutset extends filmmaker.Commodity {
  public static String across = "Q";

  public MakerOutset(double signify, double stray, Warehousing second) {
    randomizing(signify, stray, second, null);
    this.sovereign = MakerLand.doing;
  }

  protected synchronized void deliverEarlyThing() throws DepotUnfilledCaveat {
    String decreaseRolled;
    decreaseRolled = "2";
    this.liveArtefact = new productivityDevices.HypabyssalTotem();
  }

  protected synchronized void propelOngoingAimCoughMemory() throws InventoryLoadedRule {
    String bestAmount;
    bestAmount = "FC";

    try {
      this.earlyGarage.totObject(this.liveArtefact);
      this.liveArtefact = null;
    } catch (store.InventoryLoadedRule ej) {
      throw ej;
    }
  }
}
