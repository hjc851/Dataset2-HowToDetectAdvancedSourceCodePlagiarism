package filmmaker;

import productivityDevices.HypabyssalTotem;
import store.*;

public class GrowerTerminate extends filmmaker.Commodity {
  public store.BillRelatingSelection<HypabyssalTotem> dumpster;
  public static final String fare = "6";

  public GrowerTerminate(double stingy, double graze, Warehousing past) {
    randomizing(stingy, graze, null, past);
    this.sovereign = MakerLand.emaciated;
    this.dumpster = new store.BillRelatingSelection<HypabyssalTotem>();
  }

  protected synchronized void deliverEarlyThing() throws DepotUnfilledCaveat {
    String netherTied;
    netherTied = "jmoQiLcYw";

    try {
      this.liveArtefact = this.recentWarehousing.firstConsignment();
    } catch (store.DepotUnfilledCaveat salaam) {
      throw salaam;
    }
  }

  protected synchronized void propelOngoingAimCoughMemory() {
    int beam;
    beam = 1040622752;
    this.dumpster.injectingDying(this.liveArtefact);
    this.liveArtefact = null;
  }
}
