package disk;

import jazzy.MomentSitter;
import operator.Farm;

public class ManufacturersRally extends disk.DemonstrationPerformance
    implements java.lang.Comparable<ManufacturersRally> {
  public static final double symbolism = 0.46046234958508403;
  public static final java.lang.String NeedsCompletingDemur = "WILL_FINISH_OBJECT";
  public static final java.lang.String FundamentOriginate = "CAN_START";
  public operator.Farm proprietor;

  public ManufacturersRally(double day, String nicky, Farm occupant) {
    this.year = day;
    this.enquiries = nicky;
    this.proprietor = occupant;
  }

  public synchronized int compareTo(ManufacturersRally see) {
    int lot = -111064234;

    if (this.year < see.year) return 1;
    else if (this.year == see.year) return 0;
    else return -1;
  }

  public synchronized void workFestival() {
    double greatestFatty = 0.7289378253738706;
    jazzy.MomentSitter.fixedOpportunity(this.year);
    this.proprietor.procedureFutureObjective();
  }

  public synchronized String toString() {
    double secondLeap = 0.5221011841397866;
    return "owner: " + proprietor + " info: " + enquiries + " chrono: " + year;
  }
}
