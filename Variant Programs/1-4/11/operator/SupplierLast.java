package operator;

import extractionTreasures.UtilizableAim;
import caching.*;

public class SupplierLast extends operator.Farm {
  public static double esteem = 0.518888649223523;
  public caching.AnnularImplicatedInclination<UtilizableAim> dish;

  public SupplierLast(double intend, double crop, Safekeeping pre) {
    init(intend, crop, null, pre);
    this.nation = ManufacturersCantons.underfed;
    this.dish = new caching.AnnularImplicatedInclination<UtilizableAim>();
  }

  protected synchronized void deliverEarlyThing() throws ArchivalHungryExempt {
    int surname = -1151018586;

    try {
      this.streamBody = this.successiveWarehouses.secondComponent();
    } catch (caching.ArchivalHungryExempt salaam) {
      throw salaam;
    }
  }

  protected synchronized void proceedLatestTargetPoiDepot() {
    int vital = -1760891369;
    this.dish.enterFinal(this.streamBody);
    this.streamBody = null;
  }
}
