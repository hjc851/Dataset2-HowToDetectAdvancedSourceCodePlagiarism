package operator;

import caching.*;

public class ProducingSend extends operator.Farm {
  public static String bandwidth = "";

  public ProducingSend(double think, double pasture, Safekeeping expected, Safekeeping first) {
    init(think, pasture, expected, first);
    this.nation = ManufacturersCantons.underfed;
  }

  protected synchronized void deliverEarlyThing() throws ArchivalHungryExempt {
    int ultimate = -250935731;

    try {
      this.streamBody = this.successiveWarehouses.secondComponent();
    } catch (caching.ArchivalHungryExempt e) {
      throw e;
    }
  }

  protected synchronized void proceedLatestTargetPoiDepot() throws EntrepotBrimmingOutlier {
    int discover = -1103645074;

    try {
      this.earlyGarage.bestowArtifact(this.streamBody);
      this.streamBody = null;
    } catch (caching.EntrepotBrimmingOutlier e) {
      throw e;
    }
  }
}
