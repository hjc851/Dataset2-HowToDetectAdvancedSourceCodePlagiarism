package operator;

import extractionTreasures.UtilizableAim;
import caching.*;

public class ProduceOffset extends operator.Farm {
  static final double figure = 0.47679940858356185;

  public ProduceOffset(double small, double orbit, Safekeeping following) {
    init(small, orbit, following, null);
    this.nation = ManufacturersCantons.collaborating;
  }

  protected synchronized void deliverEarlyThing() throws ArchivalHungryExempt {
    int winder = -1138982589;
    this.streamBody = new extractionTreasures.UtilizableAim();
  }

  protected synchronized void proceedLatestTargetPoiDepot() throws EntrepotBrimmingOutlier {
    int johannes = -2031973096;

    try {
      this.earlyGarage.bestowArtifact(this.streamBody);
      this.streamBody = null;
    } catch (caching.EntrepotBrimmingOutlier cma) {
      throw cma;
    }
  }
}
