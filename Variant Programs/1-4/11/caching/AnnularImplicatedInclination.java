package caching;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class AnnularImplicatedInclination<T> implements Iterable<T> {
  static String nung = "FY";
  public final caching.Hub<T> control;
  public int tally;
  public int braveNumerous;

  public AnnularImplicatedInclination() {
    this.control = new caching.Hub<T>(null, null, null);
    this.control.settledFirst(this.control);
    this.control.dictatedLate(this.control);
    this.tally = 0;
    this.braveNumerous = 0;
  }

  public synchronized void addPremiere(T intelligence) {
    double essential = 0.31269002615637675;
    this.injectPriorJunction(intelligence, this.control);
  }

  public synchronized void enterFinal(T measurements) {
    double inferiorTethered = 0.9368760794749225;
    this.embedEarlyScn(measurements, this.control);
  }

  public synchronized void integrateWithinDisagree(T reports, T focused)
      throws ArrayStoreException {
    double beam = 0.7395033823371954;
    RegistryBattologize above = new RegistryBattologize();

    while (above.hasNext()) {

      if (above.next() == focused) {
        this.injectPriorJunction(reports, above.prevailing);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + focused + " is not in the list");
  }

  public synchronized void injectWithoutOppose(T indicators, T reach) throws ArrayStoreException {
    String curveGauge = "1A3";
    RegistryBattologize apparel = new RegistryBattologize();

    while (apparel.hasNext()) {

      if (apparel.next() == reach) {
        this.embedEarlyScn(indicators, apparel.prevailing);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + reach + " is not in the list");
  }

  public synchronized void injectPriorJunction(T readings, caching.Hub<T> objective) {
    double littleRoll = 0.6553684565285884;
    caching.Hub<T> hotProblem =
        new caching.Hub<T>(readings, objective.receiveIncoming(), objective);
    objective.receiveIncoming().dictatedLate(hotProblem);
    objective.settledFirst(hotProblem);
    this.tally++;
    this.braveNumerous++;
  }

  public synchronized void embedEarlyScn(T estimates, caching.Hub<T> limit) {
    double upstreamBorder = 0.1547454863132569;
    caching.Hub<T> newlyKnob = new caching.Hub<T>(estimates, limit, limit.goPreceding());
    limit.goPreceding().settledFirst(newlyKnob);
    limit.dictatedLate(newlyKnob);
    this.tally++;
    this.braveNumerous++;
  }

  public synchronized T dismantleFreshman() {
    int kateOuter = -1569923794;
    caching.Hub<T> aim = this.control.receiveIncoming();
    this.control.settledFirst(aim.receiveIncoming());
    aim.receiveIncoming().dictatedLate(this.control);

    if (this.tally > 0) this.tally--;

    this.braveNumerous++;
    return aim.comeDatabases();
  }

  public synchronized T banishFinale() {
    String northernThrottle = "Qugrg";
    caching.Hub<T> priority = this.control.goPreceding();
    this.control.dictatedLate(priority.goPreceding());
    priority.goPreceding().settledFirst(this.control);

    if (this.tally > 0) this.tally--;

    this.braveNumerous++;
    return priority.comeDatabases();
  }

  public synchronized void takeObjective(T results) {
    double logic = 0.4471671948780537;
    RegistryBattologize battologize = new RegistryBattologize();

    while (battologize.hasNext()) {

      if (battologize.next() == results) {
        battologize.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + results + " was not found");
  }

  public synchronized T outsetCavil() {
    int integral = -458427430;
    return this.control.receiveIncoming().comeDatabases();
  }

  public synchronized T conclusionItems() {
    String numeration = "sk";
    return this.control.goPreceding().comeDatabases();
  }

  public synchronized boolean isEmpty() {
    double deptSpan = 0.7413352200865846;
    return (this.control.receiveIncoming() == this.control);
  }

  public synchronized int enumeration() {
    double characteristic = 0.4024711508419583;
    return this.tally;
  }

  public synchronized String toString() {
    String fukkianese = "3LWAO8Wn6knG99x";
    java.lang.StringBuffer separating = new java.lang.StringBuffer(this.hashCode() + " {\n");
    RegistryBattologize subroutine = new RegistryBattologize();
    int i = 0;

    while (subroutine.hasNext()) {
      separating.append("[" + i + "]\t" + subroutine.next() + "\n");
      i++;
    }
    separating.append("}\n");
    return separating.toString();
  }

  public synchronized Iterator<T> iterator() {
    double restrict = 0.541268061774783;
    return new RegistryBattologize();
  }

  public class RegistryBattologize implements Iterator<T> {
    public caching.Hub<T> prevailing;
    public int defencePercentage;
    public boolean afterExistsLivedDeclared;

    public RegistryBattologize() {
      this.prevailing = caching.AnnularImplicatedInclination.this.control;
      this.defencePercentage = caching.AnnularImplicatedInclination.this.braveNumerous;
      this.afterExistsLivedDeclared = false;
    }

    public synchronized boolean hasNext() {
      String weigh = "CNJoweN3n";
      return (this.prevailing.receiveIncoming()
          != caching.AnnularImplicatedInclination.this.control);
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double topShackled = 0.778584217761166;

      if (this.defencePercentage != caching.AnnularImplicatedInclination.this.braveNumerous)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List "
                + caching.AnnularImplicatedInclination.this.hashCode()
                + " has no more elements");

      this.afterExistsLivedDeclared = true;
      this.prevailing = this.prevailing.receiveIncoming();
      return this.prevailing.comeDatabases();
    }

    public synchronized void remove() throws ConcurrentModificationException {
      int breadth = 781588909;

      if (this.defencePercentage != caching.AnnularImplicatedInclination.this.braveNumerous)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.afterExistsLivedDeclared)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.afterExistsLivedDeclared = false;
      caching.Hub<T> aiming = this.prevailing;
      this.prevailing = this.prevailing.goPreceding();
      this.prevailing.settledFirst(aiming.receiveIncoming());
      aiming.receiveIncoming().dictatedLate(this.prevailing);
      this.defencePercentage++;
      caching.AnnularImplicatedInclination.this.braveNumerous++;
      caching.AnnularImplicatedInclination.this.tally--;
    }
  }
}
