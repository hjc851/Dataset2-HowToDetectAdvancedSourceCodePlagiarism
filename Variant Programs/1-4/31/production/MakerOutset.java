package production;

import filmingExhibits.UseableItem;
import warehouse.*;

public class MakerOutset extends Commodity {

  protected synchronized void proceedLatestTargetPoiDepot() throws CachingBroadExemptions {

    try {
      this.laterDisk.introduceAspect(this.afootElement);
      this.afootElement = null;
    } catch (CachingBroadExemptions cma) {
      throw cma;
    }
  }

  protected synchronized void giveAgainTotem() throws ArchivalHungryExempt {
    this.afootElement = new UseableItem();
  }

  public MakerOutset(double meanspirited, double ambit, Disk soon) {
    degauss(meanspirited, ambit, soon, null);
    this.government = GrowerTell.functioning;
  }
}
