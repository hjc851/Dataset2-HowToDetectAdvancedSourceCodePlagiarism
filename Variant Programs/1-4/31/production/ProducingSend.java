package production;

import warehouse.*;

public class ProducingSend extends Commodity {

  protected synchronized void proceedLatestTargetPoiDepot() throws CachingBroadExemptions {

    try {
      this.laterDisk.introduceAspect(this.afootElement);
      this.afootElement = null;
    } catch (CachingBroadExemptions einsteinium) {
      throw einsteinium;
    }
  }

  public ProducingSend(double little, double orbit, Disk upcoming, Disk pervious) {
    degauss(little, orbit, upcoming, pervious);
    this.government = GrowerTell.brutalized;
  }

  protected synchronized void giveAgainTotem() throws ArchivalHungryExempt {

    try {
      this.afootElement = this.originalSpace.upcomingSomething();
    } catch (ArchivalHungryExempt e) {
      throw e;
    }
  }
}
