package exporter;

import throughputMaterials.PannonianMatter;
import warehouse.*;

public class ProductionComplete extends Farm {

  protected synchronized void sendAfterVictim() throws StoreroomPillagedUnless {

    try {
      this.incumbentObjection = this.initialShelving.forthcomingAspect();
    } catch (StoreroomPillagedUnless ej) {
      throw ej;
    }
  }

  protected synchronized void actContemporaryItemCoughsEntrepot() {
    this.tub.encloseLate(this.incumbentObjection);
    this.incumbentObjection = null;
  }

  private AnnularImplicatedInclination<PannonianMatter> tub;

  public ProductionComplete(double signify, double grasp, Entrepot first) {
    instantiate(signify, grasp, null, first);
    this.country = ManufacturerCommonwealth.depriving;
    this.tub = new AnnularImplicatedInclination<PannonianMatter>();
  }
}
