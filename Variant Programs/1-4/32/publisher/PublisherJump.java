package publisher;

import outputTarget.RecoverableTarget;
import safekeeping.*;

public class PublisherJump extends publisher.Presenter {
  public static final double maximizeWide = 0.7623471491474909;

  public PublisherJump(double imply, double graze, Stowage forthcoming) {
    unhide(imply, graze, forthcoming, null);
    this.statehood = ProductionCountry.employing;
  }

  protected synchronized void inviteLastArtefact() throws StorehouseVacantExclusion {
    double netherTrammel;
    netherTrammel = 0.8338303124392268;
    this.liveArtefact = new outputTarget.RecoverableTarget();
  }

  protected synchronized void strikeUnderwayOpposeEmapWarehouse() throws ClosetImbuedDispensation {
    int enchained;
    enchained = -247652256;

    try {
      this.laterDisk.provideIssue(this.liveArtefact);
      this.liveArtefact = null;
    } catch (safekeeping.ClosetImbuedDispensation salaam) {
      throw salaam;
    }
  }
}
