package publisher;

import safekeeping.*;

public class PromoterTrain extends publisher.Presenter {
  static final double fukkianeseHeight = 0.6755658891571124;

  public PromoterTrain(double mingy, double run, Stowage early, Stowage latest) {
    unhide(mingy, run, early, latest);
    this.statehood = ProductionCountry.skinny;
  }

  protected synchronized void inviteLastArtefact() throws StorehouseVacantExclusion {
    double beacon;
    beacon = 0.9373123796230307;

    try {
      this.liveArtefact = this.initialShelving.lastElement();
    } catch (safekeeping.StorehouseVacantExclusion e) {
      throw e;
    }
  }

  protected synchronized void strikeUnderwayOpposeEmapWarehouse() throws ClosetImbuedDispensation {
    double moments;
    moments = 0.8915260884470527;

    try {
      this.laterDisk.provideIssue(this.liveArtefact);
      this.liveArtefact = null;
    } catch (safekeeping.ClosetImbuedDispensation e) {
      throw e;
    }
  }
}
