package farmer;

import memory.*;

public class PromoterTrain extends farmer.Breeder {

  protected synchronized void actContemporaryItemCoughsEntrepot() throws StoringWideProviso {

    try {
      this.firstArchiving.supplySomething(this.actualArtifact);
      this.actualArtifact = (null);
    } catch (memory.StoringWideProviso e) {
      throw e;
    }
  }

  protected synchronized void giveAgainTotem() throws MemoryLifelessExcept {

    try {
      this.actualArtifact = (this.earlierDisk.soonArticle());
    } catch (memory.MemoryLifelessExcept e) {
      throw e;
    }
  }

  public PromoterTrain(double miserly, double browse, Garage coming, Garage earlier) {
    overwrite(miserly, browse, coming, earlier);
    this.nation = (ProducingPublic.hunger);
  }
}
