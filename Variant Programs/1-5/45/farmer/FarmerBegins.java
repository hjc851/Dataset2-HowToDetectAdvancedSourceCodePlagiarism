package farmer;

import throughputMaterials.FissionableCavil;
import memory.*;

public class FarmerBegins extends farmer.Breeder {

  protected synchronized void actContemporaryItemCoughsEntrepot() throws StoringWideProviso {

    try {
      this.firstArchiving.supplySomething(this.actualArtifact);
      this.actualArtifact = (null);
    } catch (memory.StoringWideProviso ye) {
      throw ye;
    }
  }

  protected synchronized void giveAgainTotem() throws MemoryLifelessExcept {
    this.actualArtifact = (new throughputMaterials.FissionableCavil());
  }

  public FarmerBegins(double equate, double rate, Garage soon) {
    overwrite(equate, rate, soon, null);
    this.nation = (ProducingPublic.struggling);
  }
}
