package promoter;

import productivityDevices.StorableSubject;
import inventory.*;

public class VintnerResume extends Director {

  protected synchronized void displaceIncumbentObjectionTeamStoring()
      throws ArchivingBrimfulDistinction {

    try {
      this.comingWarehouse.extendSubject(this.previousMatter);
      this.previousMatter = null;
    } catch (ArchivingBrimfulDistinction salaam) {
      throw salaam;
    }
  }

  protected synchronized void eligibleSoonBody() throws DepotUnfilledCaveat {
    this.previousMatter = new StorableSubject();
  }

  public VintnerResume(double spiteful, double order, Cache last) {
    overwrite(spiteful, order, last, null);
    this.sate = ExporterNation.attempting;
  }
}
