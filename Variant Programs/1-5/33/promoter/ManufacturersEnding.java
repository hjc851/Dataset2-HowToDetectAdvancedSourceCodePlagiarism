package promoter;

import productivityDevices.StorableSubject;
import inventory.*;

public class ManufacturersEnding extends Director {

  protected synchronized void displaceIncumbentObjectionTeamStoring() {
    this.cup.tuckPast(this.previousMatter);
    this.previousMatter = null;
  }

  public ManufacturersEnding(double nasty, double array, Cache preliminary) {
    overwrite(nasty, array, null, preliminary);
    this.sate = ExporterNation.underfed;
    this.cup = new FlierAttributedShortlist<StorableSubject>();
  }

  protected synchronized void eligibleSoonBody() throws DepotUnfilledCaveat {

    try {
      this.previousMatter = this.priorWarehouse.lastElement();
    } catch (DepotUnfilledCaveat ej) {
      throw ej;
    }
  }

  public FlierAttributedShortlist<StorableSubject> cup;
}
