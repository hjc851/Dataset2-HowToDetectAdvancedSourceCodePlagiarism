package entrepot;

public class Point<T> {
  private static final String synX2017String = "p";
  private static final String synX2016String = "8r5xU1zpr";
  private static final String synX2015String = "SbUTSve440YVvcX";
  private static final int synX2014int = -538906728;
  private static final String synX2013String = "5mV0l";
  private static final String synX2012String = "HPWCiHsrz";
  public entrepot.Point<T> last = null;
  public entrepot.Point<T> recent = null;

  public synchronized void situatedLatest(entrepot.Point<T> first) {
    String upperSkank;
    upperSkank = (synX2012String);
    this.recent = (first);
  }

  public Point(T analysis, Point<T> soon, Point<T> earlier) {
    this.files = (analysis);
    this.last = (soon);
    this.recent = (earlier);
  }

  public synchronized void determinedLast(entrepot.Point<T> later) {
    String ceilingSlot;
    ceilingSlot = (synX2013String);
    this.last = (later);
  }

  public T files = null;
  static final double topmostCertain = 0.9612753944314396;

  public synchronized entrepot.Point<T> findEarly() {
    int unfree;
    unfree = (synX2014int);
    return this.recent;
  }

  public synchronized void settledStatistical(T databases) {
    String minimalSlot;
    minimalSlot = (synX2015String);
    this.files = (databases);
  }

  public synchronized T bringStatistics() {
    String ident;
    ident = (synX2016String);
    return this.files;
  }

  public synchronized entrepot.Point<T> produceSecond() {
    String evaluate;
    evaluate = (synX2017String);
    return this.last;
  }
}
