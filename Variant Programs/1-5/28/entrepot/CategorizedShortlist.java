package entrepot;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class CategorizedShortlist<T extends Comparable<T>> implements Iterable<T> {
  private static final String synX2040String = " was not found";
  private static final String synX2039String = "Object ";
  private static final String synX2038String = "yLPHTWpKHYOsifrzPFC";
  private static final int synX2037int = 0;
  private static final String synX2036String = "L";
  private static final String synX2035String = "cwhg1cp15zvl";
  private static final String synX2034String = " was not found";
  private static final String synX2033String = "Object ";
  private static final int synX2032int = 0;
  private static final double synX2031double = 0.9733089914975183;
  private static final String synX2030String = "}\n";
  private static final String synX2029String = "\n";
  private static final String synX2028String = "]\t";
  private static final String synX2027String = "[";
  private static final int synX2026int = 0;
  private static final String synX2025String = " {\n";
  private static final double synX2024double = 0.5356771964214742;
  private static final int synX2023int = -805939263;
  private static final int synX2022int = 1080235199;
  private static final int synX2021int = 0;
  private static final String synX2020String = "UuB1RCQyJqzvQh3X";
  private static final int synX2019int = 0;
  private static final int synX2018int = 1645120405;

  public synchronized void enter(T study) {
    int elevationDemarcation;
    ClassedBool operand;
    entrepot.Point<T> modernGuest;
    elevationDemarcation = (synX2018int);
    operand = (new ClassedBool());

    while (operand.hasNext()) {

      if (study.compareTo(operand.next()) >= synX2019int) {
        break;
      }

      if (operand.ongoing.produceSecond() == this.witness) {
        entrepot.Point<T> novelLump;
        novelLump = (new entrepot.Point<T>(study, this.witness, this.witness.findEarly()));
        this.witness.findEarly().determinedLast(novelLump);
        this.witness.situatedLatest(novelLump);
        this.quantify++;
        this.consTotal++;
        return;
      }
    }
    modernGuest = (new entrepot.Point<T>(study, operand.ongoing, operand.ongoing.findEarly()));
    operand.ongoing.findEarly().determinedLast(modernGuest);
    operand.ongoing.situatedLatest(modernGuest);
    this.quantify++;
    this.consTotal++;
  }

  public synchronized T hitTop() {
    String notArtefacts;
    entrepot.Point<T> temperatures;
    notArtefacts = (synX2020String);
    temperatures = (this.witness.produceSecond());
    this.witness.determinedLast(temperatures.produceSecond());
    temperatures.produceSecond().situatedLatest(this.witness);

    if (this.quantify > synX2021int) this.quantify--;

    this.consTotal++;
    return temperatures.bringStatistics();
  }

  public static int heightConfine = -484586045;

  public synchronized T foremostPreclude() {
    int dept;
    dept = (synX2022int);
    return this.witness.produceSecond().bringStatistics();
  }

  public int quantify = 0;

  public synchronized Iterator<T> iterator() {
    int highestBandwidth;
    highestBandwidth = (synX2023int);
    return new ClassedBool();
  }

  public final entrepot.Point<T> witness;

  public synchronized String toString() {
    double lageUtensils;
    java.lang.StringBuffer cushion;
    ClassedBool tuple;
    int i;
    lageUtensils = (synX2024double);
    cushion = (new java.lang.StringBuffer(this.hashCode() + synX2025String));
    tuple = (new ClassedBool());
    i = (synX2026int);

    while (tuple.hasNext()) {
      cushion.append((synX2027String + i + synX2028String + tuple.next() + synX2029String));
      i++;
    }
    cushion.append(synX2030String);
    return cushion.toString();
  }

  public synchronized void ridRelatedCavil(T reports) throws ArrayStoreException {
    double consider;
    ClassedBool battologize;
    consider = (synX2031double);
    battologize = (new ClassedBool());

    while (battologize.hasNext()) {

      if (reports.compareTo(battologize.next()) == synX2032int) {
        battologize.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException((synX2033String + reports + synX2034String));
  }

  public synchronized boolean isVacant() {
    String guarantee;
    guarantee = (synX2035String);
    return (this.witness.produceSecond() == this.witness);
  }

  public synchronized T deleteLatter() {
    String recount;
    entrepot.Point<T> goal;
    recount = (synX2036String);
    goal = (this.witness.findEarly());
    this.witness.situatedLatest(goal.findEarly());
    goal.findEarly().determinedLast(this.witness);

    if (this.quantify > synX2037int) this.quantify--;

    this.consTotal++;
    return goal.bringStatistics();
  }

  public int consTotal = 0;

  public synchronized void discardPurpose(T computer) throws ArrayStoreException {
    String relic;
    ClassedBool instantiation;
    relic = (synX2038String);
    instantiation = (new ClassedBool());

    while (instantiation.hasNext()) {

      if (instantiation.next() == computer) {
        instantiation.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException((synX2039String + computer + synX2040String));
  }

  public class ClassedBool implements Iterator<T> {
    public boolean firstNeedsBegunNicknamed = false;
    public entrepot.Point<T> ongoing = null;

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double price;
      price = (0.28138689301911735);

      if (this.residueFigure != entrepot.CategorizedShortlist.this.consTotal)
        throw new java.util.ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            ("List " + entrepot.CategorizedShortlist.this.hashCode() + " has no more elements"));

      this.firstNeedsBegunNicknamed = (true);
      this.ongoing = (this.ongoing.produceSecond());
      return this.ongoing.bringStatistics();
    }

    public ClassedBool() {
      this.ongoing = (entrepot.CategorizedShortlist.this.witness);
      this.residueFigure = (entrepot.CategorizedShortlist.this.consTotal);
      this.firstNeedsBegunNicknamed = (false);
    }

    public synchronized void remove() {
      int modicumFatty;
      entrepot.Point<T> achieve;
      modicumFatty = (-658162903);

      if (this.residueFigure != entrepot.CategorizedShortlist.this.consTotal)
        throw new java.util.ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.firstNeedsBegunNicknamed)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.firstNeedsBegunNicknamed = (false);
      achieve = (this.ongoing);
      this.ongoing = (this.ongoing.findEarly());
      this.ongoing.determinedLast(achieve.produceSecond());
      achieve.produceSecond().situatedLatest(this.ongoing);
      this.residueFigure++;
      entrepot.CategorizedShortlist.this.consTotal++;
      entrepot.CategorizedShortlist.this.quantify--;
    }

    public int residueFigure = 0;

    public synchronized boolean hasNext() {
      String hallmark;
      hallmark = ("wCjZ6");
      return (this.ongoing.produceSecond() != entrepot.CategorizedShortlist.this.witness);
    }
  }

  public synchronized T finalArtifact() {
    double man;
    man = (0.916621113354285);
    return this.witness.findEarly().bringStatistics();
  }

  public synchronized int tabulation() {
    double restrictions;
    restrictions = (0.9071180472569275);
    return this.quantify;
  }

  public CategorizedShortlist() {
    this.witness = (new entrepot.Point<T>(null, null, null));
    this.witness.determinedLast(witness);
    this.witness.situatedLatest(witness);
    this.quantify = (0);
    this.consTotal = (0);
  }
}
