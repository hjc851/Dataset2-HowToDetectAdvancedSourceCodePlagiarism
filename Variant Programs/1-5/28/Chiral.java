import java.util.Scanner;
import entrepot.Safekeeping;
import analog.Pretense;
import java.lang.String;
import presenter.Provider;
import static java.lang.System.out;
import static java.lang.System.in;

public class Chiral {
  private static final int synX2055int = 10000000;
  private static final String synX2054String =
      "       Values should be in the form mean (double) rng (double) storageLimit (int)";
  private static final String synX2053String =
      "ERROR: There are not enough values or the values passed are in the incorrect format";
  private static final int synX2052int = 0;
  private static final int synX2051int = 0;
  private static final int synX2050int = 0;
  private static final String synX2049String =
      "ERROR: System.in is empty, no file was passed or it is empty";
  private static final int synX2048int = 0;
  private static final int synX2047int = 0;
  private static final String synX2046String = "\f";
  private static final String synX2045String = "";
  private static final String synX2044String = "";
  private static final String synX2043String = "";
  private static final String synX2042String = "3SiEjFMQh7Ce32";
  private static final String synX2041String = "";

  {
    q = (synX2041String);
    r = (null);
    fh = (null);
    dhfr = (null);
  }

  public Provider dhfr = null;
  static final double rolled = 0.012123154612706188;
  public String q = null;
  public Provider fh = null;

  public static synchronized void main(String[] param) {
    String lessDestined;
    String bcl;
    String yt;
    String z;
    Provider spain;
    Provider sb;
    Provider stan;
    Provider pardi;
    int ak;
    lessDestined = (synX2042String);
    bcl = (synX2043String);
    yt = (synX2044String);
    z = (synX2045String);
    spain = (null);
    sb = (null);
    stan = (null);
    pardi = (null);
    out.print(synX2046String);
    ak = (synX2047int);

    try {
      ak = (in.available());
    } catch (java.lang.Exception ej) {
    }

    if (ak <= synX2048int) {
      out.println(synX2049String);
    } else {
      double westCompass, revisedSkilled;
      int wordStoredBoundary;
      analog.Pretense indeed;
      westCompass = (synX2050int);
      revisedSkilled = (synX2051int);
      wordStoredBoundary = (synX2052int);

      try {
        java.util.Scanner touchscreen;
        touchscreen = (new java.util.Scanner(in));
        revisedSkilled = (touchscreen.nextDouble());
        westCompass = (touchscreen.nextDouble());
        wordStoredBoundary = (touchscreen.nextInt());
      } catch (java.lang.Exception combatants) {
        out.println(synX2053String);
        out.println(synX2054String);
        return;
      }
      indeed = (new analog.Pretense(synX2055int, revisedSkilled, westCompass));
      entrepot.Safekeeping.readyStoringBounds(wordStoredBoundary);
      indeed.originate();
    }
  }

  public Provider r = null;
}
