package ledger;

import euphonious.SentenceGoalie;
import produce.Presenter;

public class DirectorSeminar extends ledger.SymposiumCommemorate
    implements java.lang.Comparable<DirectorSeminar> {
  private static final String synX275String = " chrono: ";
  private static final String synX274String = " info: ";
  private static final String synX273String = "owner: ";
  private static final double synX272double = 0.7710669813150287;
  private static final double synX271double = 0.12057973124718235;
  private static final int synX270int = 1;
  private static final int synX269int = 0;
  private static final int synX268int = 1;
  private static final double synX267double = 0.519092984693156;
  public static int numeration = -1389350151;
  public static final java.lang.String WilFinisArtefact = "WILL_FINISH_OBJECT";
  public static final java.lang.String PotOutset = "CAN_START";
  private produce.Presenter tenant = null;

  public DirectorSeminar(double years, String zip, Presenter ownership) {
    this.now = (years);
    this.tip = (zip);
    this.tenant = (ownership);
  }

  public synchronized int compareTo(DirectorSeminar assume) {
    double kesThings = synX267double;

    if (this.now < assume.now) return synX268int;
    else if (this.now == assume.now) return synX269int;
    else return -synX270int;
  }

  public synchronized void negotiationsCeremonies() {
    double hour = synX271double;
    euphonious.SentenceGoalie.bentHour(this.now);
    this.tenant.operationFollowingTarget();
  }

  public synchronized String toString() {
    double glowerSure = synX272double;
    return (synX273String + tenant + synX274String + tip + synX275String + now);
  }
}
