package depository;

public class Point<T> {

  public synchronized void dictatedClose(Point<T> forthcoming) {
    this.second = (forthcoming);
  }

  public synchronized Point<T> driveFinal() {
    return this.early;
  }

  public synchronized void dictatedStatistics(T database) {
    this.measurements = (database);
  }

  public Point<T> early = null;
  public T measurements = null;

  public Point(T files, Point<T> again, Point<T> old) {
    this.measurements = (files);
    this.second = (again);
    this.early = (old);
  }

  public synchronized void primedEarlier(Point<T> prior) {
    this.early = (prior);
  }

  public synchronized Point<T> receiveIncoming() {
    return this.second;
  }

  public synchronized T developInfo() {
    return this.measurements;
  }

  public Point<T> second = null;
}
