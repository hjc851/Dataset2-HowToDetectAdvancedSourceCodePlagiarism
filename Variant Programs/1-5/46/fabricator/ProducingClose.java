package fabricator;

import fabricationObstructions.QuasicrystalArgue;
import depository.*;

public class ProducingClose extends Promoter {
  public RoundedCausedAgenda<QuasicrystalArgue> tank = null;

  protected synchronized void pushExistingPurposeOfficesArchival() {
    this.tank.injectingDying(this.theVictim);
    this.theVictim = (null);
  }

  public ProducingClose(double spiteful, double stray, Depository latest) {
    degauss(spiteful, stray, null, latest);
    this.law = (FarmerLaw.pizza);
    this.tank = (new RoundedCausedAgenda<QuasicrystalArgue>());
  }

  protected synchronized void haveFirstSubject() throws ArchivalHungryExempt {

    try {
      this.theVictim = (this.earlierDisk.incomingPiece());
    } catch (ArchivalHungryExempt ej) {
      throw ej;
    }
  }
}
