package fabricator;

import fabricationObstructions.QuasicrystalArgue;
import depository.*;

public class CommodityBegan extends Promoter {

  protected synchronized void pushExistingPurposeOfficesArchival() throws SpaceChockfulWaiver {

    try {
      this.aheadWarehousing.sumGoods(this.theVictim);
      this.theVictim = (null);
    } catch (SpaceChockfulWaiver einsteinium) {
      throw einsteinium;
    }
  }

  protected synchronized void haveFirstSubject() throws ArchivalHungryExempt {
    this.theVictim = (new QuasicrystalArgue());
  }

  public CommodityBegan(double intend, double ambit, Depository the) {
    degauss(intend, ambit, the, null);
    this.law = (FarmerLaw.preparing);
  }
}
