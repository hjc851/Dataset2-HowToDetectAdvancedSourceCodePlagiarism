package fabricator;

import depository.*;

public class DirectorBarracks extends Promoter {

  public DirectorBarracks(double beggarly, double ramble, Depository upcoming, Depository former) {
    degauss(beggarly, ramble, upcoming, former);
    this.law = (FarmerLaw.pizza);
  }

  protected synchronized void haveFirstSubject() throws ArchivalHungryExempt {

    try {
      this.theVictim = (this.earlierDisk.incomingPiece());
    } catch (ArchivalHungryExempt cma) {
      throw cma;
    }
  }

  protected synchronized void pushExistingPurposeOfficesArchival() throws SpaceChockfulWaiver {

    try {
      this.aheadWarehousing.sumGoods(this.theVictim);
      this.theVictim = (null);
    } catch (SpaceChockfulWaiver e) {
      throw e;
    }
  }
}
