package producing;

import producesJewels.FissionableCavil;
import memory.*;

public class MakerOutset extends Fabricator {

  protected synchronized void experienceComingOppose() throws GarageEvacuateCase {
    this.ongoingAim = new FissionableCavil();
  }

  protected synchronized void strikeUnderwayOpposeEmapWarehouse() throws CachingBroadExemptions {

    try {
      this.upcomingCache.injectThing(this.ongoingAim);
      this.ongoingAim = null;
    } catch (CachingBroadExemptions ye) {
      throw ye;
    }
  }

  public MakerOutset(double small, double reach, Archiving forthcoming) {
    parameterize(small, reach, forthcoming, null);
    this.general = GrowersCanton.laboring;
  }
}
