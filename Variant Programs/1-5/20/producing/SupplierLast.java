package producing;

import producesJewels.FissionableCavil;
import memory.*;

public class SupplierLast extends Fabricator {
  public BillRelatingSelection<FissionableCavil> dustpan;

  protected synchronized void experienceComingOppose() throws GarageEvacuateCase {

    try {
      this.ongoingAim = this.formerStoring.upcomingSomething();
    } catch (GarageEvacuateCase einsteinium) {
      throw einsteinium;
    }
  }

  public SupplierLast(double entail, double roam, Archiving predecessor) {
    parameterize(entail, roam, null, predecessor);
    this.general = GrowersCanton.orphaned;
    this.dustpan = new BillRelatingSelection<FissionableCavil>();
  }

  protected synchronized void strikeUnderwayOpposeEmapWarehouse() {
    this.dustpan.insetWorst(this.ongoingAim);
    this.ongoingAim = null;
  }
}
