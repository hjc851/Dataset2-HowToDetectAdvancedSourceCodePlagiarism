package producing;

import memory.*;

public class ProviderChannel extends Fabricator {

  public ProviderChannel(double intend, double run, Archiving then, Archiving pre) {
    parameterize(intend, run, then, pre);
    this.general = GrowersCanton.orphaned;
  }

  protected synchronized void strikeUnderwayOpposeEmapWarehouse() throws CachingBroadExemptions {

    try {
      this.upcomingCache.injectThing(this.ongoingAim);
      this.ongoingAim = null;
    } catch (CachingBroadExemptions samad) {
      throw samad;
    }
  }

  protected synchronized void experienceComingOppose() throws GarageEvacuateCase {

    try {
      this.ongoingAim = this.formerStoring.upcomingSomething();
    } catch (GarageEvacuateCase e) {
      throw e;
    }
  }
}
