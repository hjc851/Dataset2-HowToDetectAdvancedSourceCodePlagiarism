package farm;

import inventory.*;

public class EmitterDock extends farm.Producing {
  private static final int synX509int = -1466215430;
  private static final double synX508double = 0.7611478141380555;

  public EmitterDock(double normal, double array, Caching soon, Caching old) {
    initialisation(normal, array, soon, old);
    this.central = (BreederGeneral.malnourished);
  }

  protected synchronized void obtainFutureObjective() throws StowageLootedDerogation {
    double speedTrussed = synX508double;

    try {
      this.ongoingAim = (this.priorWarehouse.forthcomingAspect());
    } catch (inventory.StowageLootedDerogation e) {
      throw e;
    }
  }

  protected synchronized void impressFlowObjetMouStowage() throws ArchivalOverflowingExempt {
    int lessMagnitude = synX509int;

    try {
      this.secondCaching.bringParticular(this.ongoingAim);
      this.ongoingAim = (null);
    } catch (inventory.ArchivalOverflowingExempt e) {
      throw e;
    }
  }

  public static int modicumFatty = -80813506;
}
