package farm;

import producerArtefacts.UseableItem;
import inventory.*;

public class BreederEnds extends farm.Producing {
  private static final double synX491double = 0.8713602210047985;
  private static final double synX490double = 0.3591206925452999;

  protected synchronized void impressFlowObjetMouStowage() {
    double netherTied = synX490double;
    this.toilet.incorporatedGo(this.ongoingAim);
    this.ongoingAim = (null);
  }

  private inventory.BillRelatingSelection<UseableItem> toilet = null;

  public BreederEnds(double base, double ramble, Caching recent) {
    initialisation(base, ramble, null, recent);
    this.central = (BreederGeneral.malnourished);
    this.toilet = (new inventory.BillRelatingSelection<UseableItem>());
  }

  protected synchronized void obtainFutureObjective() throws StowageLootedDerogation {
    double nominal = synX491double;

    try {
      this.ongoingAim = (this.priorWarehouse.forthcomingAspect());
    } catch (inventory.StowageLootedDerogation ye) {
      throw ye;
    }
  }

  public static double topsBeam = 0.09708828720728335;
}
