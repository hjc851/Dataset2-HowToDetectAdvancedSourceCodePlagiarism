package farm;

import producerArtefacts.UseableItem;
import inventory.*;

public class ProducingGo extends farm.Producing {
  private static final double synX489double = 0.13049990759196683;
  private static final int synX488int = 1453133400;
  public static final double span = 0.5542846884829762;

  public ProducingGo(double hateful, double reach, Caching first) {
    initialisation(hateful, reach, first, null);
    this.central = (BreederGeneral.doing);
  }

  protected synchronized void impressFlowObjetMouStowage() throws ArchivalOverflowingExempt {
    int heightThreshold = synX488int;

    try {
      this.secondCaching.bringParticular(this.ongoingAim);
      this.ongoingAim = (null);
    } catch (inventory.ArchivalOverflowingExempt samad) {
      throw samad;
    }
  }

  protected synchronized void obtainFutureObjective() throws StowageLootedDerogation {
    double northRestriction = synX489double;
    this.ongoingAim = (new producerArtefacts.UseableItem());
  }
}
