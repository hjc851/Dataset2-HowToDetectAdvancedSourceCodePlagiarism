package memorialize;

import said.PeriodWarden;
import farm.Producing;

public class OperatorContest extends memorialize.TournamentShow
    implements java.lang.Comparable<OperatorContest> {
  private static final String synX528String = " chrono: ";
  private static final String synX527String = " info: ";
  private static final String synX526String = "owner: ";
  private static final int synX525int = 540550391;
  private static final int synX524int = 1;
  private static final int synX523int = 0;
  private static final int synX522int = 1;
  private static final double synX521double = 0.9634294635603845;
  private static final String synX520String = "ZChSTJIfuBz7";
  private farm.Producing lessor = null;
  public static int weigh = -416034327;
  public static final java.lang.String BottomFirst = "CAN_START";

  public synchronized void phaseCommemoration() {
    String fukienLength = synX520String;
    said.PeriodWarden.dictatedMeter(this.beginning);
    this.lessor.proceedingNewMatter();
  }

  public synchronized int compareTo(OperatorContest now) {
    double winder = synX521double;

    if (this.beginning < now.beginning) return synX522int;
    else if (this.beginning == now.beginning) return synX523int;
    else return -synX524int;
  }

  public static final java.lang.String ShallFinaleObjective = "WILL_FINISH_OBJECT";

  public synchronized String toString() {
    int leap = synX525int;
    return (synX526String + lessor + synX527String + stuff + synX528String + beginning);
  }

  public OperatorContest(double periods, String enquiries, Producing homeowner) {
    this.beginning = (periods);
    this.stuff = (enquiries);
    this.lessor = (homeowner);
  }
}
