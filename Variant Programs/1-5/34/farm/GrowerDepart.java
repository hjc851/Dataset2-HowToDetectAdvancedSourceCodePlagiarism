package farm;

import extractionTreasures.ImmiscibleDisagree;
import safekeeping.*;

public class GrowerDepart extends Commodity {

  protected synchronized void actContemporaryItemCoughsEntrepot()
      throws ArchivingBrimfulDistinction {

    try {
      this.firstArchiving.summateIngredient(this.existingPurpose);
      this.existingPurpose = null;
    } catch (ArchivingBrimfulDistinction salaam) {
      throw salaam;
    }
  }

  protected synchronized void eligibleSoonBody() throws StowageLootedDerogation {
    this.existingPurpose = new ImmiscibleDisagree();
  }

  public GrowerDepart(double imply, double swan, Warehouses early) {
    parameterize(imply, swan, early, null);
    this.national = DirectorNational.struggling;
  }
}
