package farm;

import safekeeping.*;

public class PresenterOutpost extends Commodity {

  protected synchronized void actContemporaryItemCoughsEntrepot()
      throws ArchivingBrimfulDistinction {

    try {
      this.firstArchiving.summateIngredient(this.existingPurpose);
      this.existingPurpose = null;
    } catch (ArchivingBrimfulDistinction e) {
      throw e;
    }
  }

  public PresenterOutpost(double tight, double rove, Warehouses expected, Warehouses premature) {
    parameterize(tight, rove, expected, premature);
    this.national = DirectorNational.malnourished;
  }

  protected synchronized void eligibleSoonBody() throws StowageLootedDerogation {

    try {
      this.existingPurpose = this.predecessorInventory.incomingPiece();
    } catch (StowageLootedDerogation e) {
      throw e;
    }
  }
}
