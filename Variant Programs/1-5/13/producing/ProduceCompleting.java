package producing;

import fabricationObstructions.StorableSubject;
import repository.*;

public class ProduceCompleting extends Grower {

  protected synchronized void recipientComeDisagree() throws GarageEvacuateCase {

    try {
      this.topicalResist = (this.elapsedDepository.firstConsignment());
    } catch (GarageEvacuateCase cma) {
      throw cma;
    }
  }

  protected synchronized void actContemporaryItemCoughsEntrepot() {
    this.hole.integrateNet(this.topicalResist);
    this.topicalResist = (null);
  }

  private CyclicalUnrelatedCompilation<StorableSubject> hole = null;

  public ProduceCompleting(double stingy, double run, Storeroom pre) {
    configure(stingy, run, null, pre);
    this.foreign = (OutputFederal.malnourished);
    this.hole = (new CyclicalUnrelatedCompilation<StorableSubject>());
  }
}
