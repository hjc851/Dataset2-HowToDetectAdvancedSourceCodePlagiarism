package producing;

import fabricationObstructions.StorableSubject;
import repository.*;

public class DirectorPart extends Grower {

  public DirectorPart(double ignoble, double array, Storeroom early) {
    configure(ignoble, array, early, null);
    this.foreign = (OutputFederal.employing);
  }

  protected synchronized void actContemporaryItemCoughsEntrepot()
      throws RepositoryChockablockExceptional {

    try {
      this.comingWarehouse.combineAgenda(this.topicalResist);
      this.topicalResist = (null);
    } catch (RepositoryChockablockExceptional samad) {
      throw samad;
    }
  }

  protected synchronized void recipientComeDisagree() throws GarageEvacuateCase {
    this.topicalResist = (new StorableSubject());
  }
}
