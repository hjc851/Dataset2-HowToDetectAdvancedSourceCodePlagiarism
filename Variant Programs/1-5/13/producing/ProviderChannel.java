package producing;

import repository.*;

public class ProviderChannel extends Grower {

  protected synchronized void actContemporaryItemCoughsEntrepot()
      throws RepositoryChockablockExceptional {

    try {
      this.comingWarehouse.combineAgenda(this.topicalResist);
      this.topicalResist = (null);
    } catch (RepositoryChockablockExceptional ye) {
      throw ye;
    }
  }

  protected synchronized void recipientComeDisagree() throws GarageEvacuateCase {

    try {
      this.topicalResist = (this.elapsedDepository.firstConsignment());
    } catch (GarageEvacuateCase e) {
      throw e;
    }
  }

  public ProviderChannel(double think, double straddle, Storeroom upcoming, Storeroom preliminary) {
    configure(think, straddle, upcoming, preliminary);
    this.foreign = (OutputFederal.malnourished);
  }
}
