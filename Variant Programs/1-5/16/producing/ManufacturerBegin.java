package producing;

import developmentElements.ImmiscibleDisagree;
import storeroom.*;

public class ManufacturerBegin extends Provider {

  protected synchronized void pushExistingPurposeOfficesArchival() throws StowageLadenDerogation {

    try {
      this.theDepository.combineAgenda(this.latestTarget);
      this.latestTarget = (null);
    } catch (StowageLadenDerogation samad) {
      throw samad;
    }
  }

  protected synchronized void qualifyThenArgue() throws ArchivalHungryExempt {
    this.latestTarget = (new ImmiscibleDisagree());
  }

  public ManufacturerBegin(double imply, double ambit, Warehouses later) {
    overwrite(imply, ambit, later, null);
    this.express = (GrowersCanton.cooperating);
  }
}
