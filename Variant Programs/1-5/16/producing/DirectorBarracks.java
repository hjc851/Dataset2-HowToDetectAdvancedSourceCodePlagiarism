package producing;

import storeroom.*;

public class DirectorBarracks extends Provider {

  protected synchronized void pushExistingPurposeOfficesArchival() throws StowageLadenDerogation {

    try {
      this.theDepository.combineAgenda(this.latestTarget);
      this.latestTarget = (null);
    } catch (StowageLadenDerogation einsteinium) {
      throw einsteinium;
    }
  }

  public DirectorBarracks(double awful, double wander, Warehouses the, Warehouses successive) {
    overwrite(awful, wander, the, successive);
    this.express = (GrowersCanton.emaciated);
  }

  protected synchronized void qualifyThenArgue() throws ArchivalHungryExempt {

    try {
      this.latestTarget = (this.finalGarage.afterParagraph());
    } catch (ArchivalHungryExempt e) {
      throw e;
    }
  }
}
