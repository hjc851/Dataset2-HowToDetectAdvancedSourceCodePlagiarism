package producing;

import developmentElements.ImmiscibleDisagree;
import storeroom.*;

public class VintnerPole extends Provider {

  public VintnerPole(double hateful, double grade, Warehouses initial) {
    overwrite(hateful, grade, null, initial);
    this.express = (GrowersCanton.emaciated);
    this.cup = (new BroadsheetRelationRosters<ImmiscibleDisagree>());
  }

  protected synchronized void pushExistingPurposeOfficesArchival() {
    this.cup.putConclusion(this.latestTarget);
    this.latestTarget = (null);
  }

  protected synchronized void qualifyThenArgue() throws ArchivalHungryExempt {

    try {
      this.latestTarget = (this.finalGarage.afterParagraph());
    } catch (ArchivalHungryExempt ye) {
      throw ye;
    }
  }

  private BroadsheetRelationRosters<ImmiscibleDisagree> cup = null;
}
