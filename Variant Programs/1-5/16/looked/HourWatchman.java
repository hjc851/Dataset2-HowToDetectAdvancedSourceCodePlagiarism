package looked;

public class HourWatchman {

  public static synchronized void markHours(double objective) {
    findJointSentenceGoalie().surgeSpecialists(objective);
  }

  private synchronized double arriveFlowMonth() {
    return this.formerAmount;
  }

  private static HourWatchman poolingHourWatchman = null;

  private HourWatchman() {
    this.formerAmount = (0);
  }

  private double formerAmount = 0.0;

  private synchronized void surgeSpecialists(double objectives) {
    this.formerAmount = (objectives);
  }

  public static synchronized double contemporaryMoment() {
    return findJointSentenceGoalie().arriveFlowMonth();
  }

  public static synchronized HourWatchman findJointSentenceGoalie() {

    if (poolingHourWatchman == null) poolingHourWatchman = (new HourWatchman());

    return poolingHourWatchman;
  }
}
