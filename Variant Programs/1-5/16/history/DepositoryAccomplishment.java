package history;

import developmentElements.ImmiscibleDisagree;

public class DepositoryAccomplishment extends SeminarRead {

  public synchronized int viability() {
    return this.power;
  }

  private int power = 0;

  public synchronized ImmiscibleDisagree focus() {
    return this.discipline;
  }

  private ImmiscibleDisagree discipline = null;
  public static final String TookErase = "DID_REMOVE";

  public DepositoryAccomplishment(
      double beginning, String tip, int load, ImmiscibleDisagree subordinate) {
    this.clip = (beginning);
    this.pop = (tip);
    this.power = (load);
    this.discipline = (subordinate);
  }

  public static final String DaredSummate = "DID_ADD";
}
