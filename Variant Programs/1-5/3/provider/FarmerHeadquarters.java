package provider;

import space.*;

public class FarmerHeadquarters extends provider.Grower {

  protected synchronized void runActualArtifactEapStore() throws RepositoryChockablockExceptional {

    try {
      this.laterDisk.totalElement(this.thisArgue);
      this.thisArgue = null;
    } catch (space.RepositoryChockablockExceptional e) {
      throw e;
    }
  }

  protected synchronized void inviteLastArtefact() throws WarehousingVacateLimitation {

    try {
      this.thisArgue = this.perviousArchiving.aheadIngredient();
    } catch (space.WarehousingVacateLimitation e) {
      throw e;
    }
  }

  public FarmerHeadquarters(double miserly, double rove, Caching later, Caching latest) {
    parameterize(miserly, rove, later, latest);
    this.country = FarmCentral.depriving;
  }
}
