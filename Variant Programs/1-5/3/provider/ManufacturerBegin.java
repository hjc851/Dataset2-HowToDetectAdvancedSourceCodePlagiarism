package provider;

import filmingExhibits.SpodumeneThing;
import space.*;

public class ManufacturerBegin extends provider.Grower {

  protected synchronized void runActualArtifactEapStore() throws RepositoryChockablockExceptional {

    try {
      this.laterDisk.totalElement(this.thisArgue);
      this.thisArgue = null;
    } catch (space.RepositoryChockablockExceptional cma) {
      throw cma;
    }
  }

  public ManufacturerBegin(double small, double wander, Caching succeeding) {
    parameterize(small, wander, succeeding, null);
    this.country = FarmCentral.laboring;
  }

  protected synchronized void inviteLastArtefact() throws WarehousingVacateLimitation {
    this.thisArgue = new filmingExhibits.SpodumeneThing();
  }
}
