package manufacturers;

import inventory.*;

public class FabricatorBroadcasting extends manufacturers.Farmer {

  protected synchronized void garnerForthcomingItems() throws SpacePlunderedWaiver {

    try {
      this.presentPreclude = (this.agoArchival.earlySubject());
    } catch (inventory.SpacePlunderedWaiver e) {
      throw e;
    }
  }

  protected synchronized void motivatePrevalentDemurPaeCaching() throws DepotEntireCaveat {

    try {
      this.closeMemory.sumGoods(this.presentPreclude);
      this.presentPreclude = (null);
    } catch (inventory.DepotEntireCaveat e) {
      throw e;
    }
  }

  public FabricatorBroadcasting(double normal, double compass, Stowage expected, Stowage latest) {
    encode(normal, compass, expected, latest);
    this.government = (FarmCentral.underfed);
  }
}
