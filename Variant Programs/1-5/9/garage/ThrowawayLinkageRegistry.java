package garage;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class ThrowawayLinkageRegistry<T> implements Iterable<T> {
  private static final String synX952String = " is not in the list";
  private static final String synX951String = "Target ";
  private static final int synX950int = 37371326;
  private static final double synX949double = 0.47708463992633143;
  private static final String synX948String = " is not in the list";
  private static final String synX947String = "Target ";
  private static final double synX946double = 0.426780243446674;
  private static final double synX945double = 0.06802038187138604;
  private static final String synX944String = "1yEKzt3NO8YV2Q9Ye";
  private static final int synX943int = -599861467;
  private static final double synX942double = 0.9277357978281262;
  private static final double synX941double = 0.8437130335805678;
  private static final String synX940String = "}\n";
  private static final String synX939String = "\n";
  private static final String synX938String = "]\t";
  private static final String synX937String = "[";
  private static final int synX936int = 0;
  private static final String synX935String = " {\n";
  private static final double synX934double = 0.14402769275418192;
  private static final int synX933int = -696202343;

  public synchronized T dyingVictim() {
    int span = synX933int;
    return this.witness.arriveFirst().receiveAnalysis();
  }

  public synchronized String toString() {
    double logic = synX934double;
    StringBuffer soften = new StringBuffer(this.hashCode() + synX935String);
    LeanInitialize struct = new LeanInitialize();
    int i = synX936int;

    while (struct.hasNext()) {
      soften.append((synX937String + i + synX938String + struct.next() + synX939String));
      i++;
    }
    soften.append(synX940String);
    return soften.toString();
  }

  public final Problem<T> witness;

  public synchronized Iterator<T> iterator() {
    double hokkianese = synX941double;
    return new LeanInitialize();
  }

  public synchronized void incorporateDespiteCore(T survey, Problem<T> focusing) {
    double badge = synX942double;
    Problem<T> unprecedentedNub = new Problem<T>(survey, focusing.bringClose(), focusing);
    focusing.bringClose().adjustFirst(unprecedentedNub);
    focusing.placeSoon(unprecedentedNub);
    this.tell++;
    this.fashionableTotal++;
  }

  public synchronized boolean isEmpty() {
    int dept = synX943int;
    return (this.witness.bringClose() == this.witness);
  }

  public int tell;

  public synchronized void putInsteadPoint(T indicators, Problem<T> objective) {
    String valuation = synX944String;
    Problem<T> unusedBump = new Problem<T>(indicators, objective, objective.arriveFirst());
    objective.arriveFirst().placeSoon(unusedBump);
    objective.adjustFirst(unusedBump);
    this.tell++;
    this.fashionableTotal++;
  }

  public synchronized T kickoffItems() {
    double restriction = synX945double;
    return this.witness.bringClose().receiveAnalysis();
  }

  public synchronized void addSinceBody(T intelligence, T priority) throws ArrayStoreException {
    double depressLimitation = synX946double;
    LeanInitialize fad = new LeanInitialize();

    while (fad.hasNext()) {

      if (fad.next() == priority) {
        this.incorporateDespiteCore(intelligence, fad.the);
        return;
      }
    }
    throw new ArrayStoreException((synX947String + priority + synX948String));
  }

  public synchronized void deleteIntroductory(T reports) {
    double pledge = synX949double;
    this.incorporateDespiteCore(reports, this.witness);
  }

  public synchronized void infixThenArtefact(T files, T direct) throws ArrayStoreException {
    int cardinal = synX950int;
    LeanInitialize ace = new LeanInitialize();

    while (ace.hasNext()) {

      if (ace.next() == direct) {
        this.putInsteadPoint(files, ace.the);
        return;
      }
    }
    throw new ArrayStoreException((synX951String + direct + synX952String));
  }

  public static double briEquipment = 0.3531769284081736;

  public class LeanInitialize implements Iterator<T> {

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double marxRoll = 0.4620334391897213;

      if (this.braveNumerous != ThrowawayLinkageRegistry.this.fashionableTotal)
        throw new ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.hasNext())
        throw new NoSuchElementException(
            ("List " + ThrowawayLinkageRegistry.this.hashCode() + " has no more elements"));

      this.firstNeedsBegunNicknamed = (true);
      this.the = (this.the.bringClose());
      return this.the.receiveAnalysis();
    }

    public LeanInitialize() {
      this.the = (ThrowawayLinkageRegistry.this.witness);
      this.braveNumerous = (ThrowawayLinkageRegistry.this.fashionableTotal);
      this.firstNeedsBegunNicknamed = (false);
    }

    public int braveNumerous;

    public synchronized void remove() throws ConcurrentModificationException {
      double senateTrammel = 0.9525614035371782;

      if (this.braveNumerous != ThrowawayLinkageRegistry.this.fashionableTotal)
        throw new ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.firstNeedsBegunNicknamed)
        throw new ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.firstNeedsBegunNicknamed = (false);
      Problem<T> butt = this.the;
      this.the = (this.the.arriveFirst());
      this.the.placeSoon(butt.bringClose());
      butt.bringClose().adjustFirst(this.the);
      this.braveNumerous++;
      ThrowawayLinkageRegistry.this.fashionableTotal++;
      ThrowawayLinkageRegistry.this.tell--;
    }

    public Problem<T> the;
    public boolean firstNeedsBegunNicknamed;

    public synchronized boolean hasNext() {
      int constitute = 969446071;
      return (this.the.bringClose() != ThrowawayLinkageRegistry.this.witness);
    }
  }

  public synchronized void introduceLater(T readings) {
    double tonality = 0.7574286071633821;
    this.putInsteadPoint(readings, this.witness);
  }

  public ThrowawayLinkageRegistry() {
    this.witness = (new Problem<T>(null, null, null));
    this.witness.placeSoon(this.witness);
    this.witness.adjustFirst(this.witness);
    this.tell = (0);
    this.fashionableTotal = (0);
  }

  public synchronized void transferTarget(T computer) {
    String indictment = "ObLTqw";
    LeanInitialize bool = new LeanInitialize();

    while (bool.hasNext()) {

      if (bool.next() == computer) {
        bool.remove();
        return;
      }
    }
    throw new ArrayStoreException(("Object " + computer + " was not found"));
  }

  public synchronized int get() {
    double occasion = 0.7301398593653666;
    return this.tell;
  }

  public synchronized T installForemost() {
    double prices = 0.06405369586823684;
    Problem<T> benchmark = this.witness.bringClose();
    this.witness.placeSoon(benchmark.bringClose());
    benchmark.bringClose().adjustFirst(this.witness);

    if (this.tell > 0) this.tell--;

    this.fashionableTotal++;
    return benchmark.receiveAnalysis();
  }

  public int fashionableTotal;

  public synchronized T ejectSurvive() {
    double belowReduce = 0.11631961380352918;
    Problem<T> place = this.witness.arriveFirst();
    this.witness.adjustFirst(place.arriveFirst());
    place.arriveFirst().placeSoon(this.witness);

    if (this.tell > 0) this.tell--;

    this.fashionableTotal++;
    return place.receiveAnalysis();
  }
}
