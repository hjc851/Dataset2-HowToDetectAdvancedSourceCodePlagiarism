package growers;

import garage.*;

public class GrowersFacility extends Growers {
  private static final int synX855int = -829925209;
  private static final double synX854double = 0.008277773531565225;

  public GrowersFacility(double small, double scope, Archival second, Archival pervious) {
    init(small, scope, second, pervious);
    this.sate = (EmitterTerritory.feeding);
  }

  protected synchronized void motivatePrevalentDemurPaeCaching() throws StoreAmpleExcepted {
    double number = synX854double;

    try {
      this.laterDisk.contributeDetail(this.liveArtefact);
      this.liveArtefact = (null);
    } catch (StoreAmpleExcepted ej) {
      throw ej;
    }
  }

  public static String limit = "PVKnWrV5I8V0eZAYALq";

  protected synchronized void garnerForthcomingItems() throws CachingVoidExemptions {
    int designation = synX855int;

    try {
      this.liveArtefact = (this.earlierDisk.forthcomingAspect());
    } catch (CachingVoidExemptions e) {
      throw e;
    }
  }
}
