package growers;

import outputTarget.FissionableCavil;
import garage.*;

public class PresenterCoating extends Growers {
  private static final int synX837int = -1454529529;
  private static final double synX836double = 0.9130747572419029;
  static double distinguish = 0.6343620395010438;

  public PresenterCoating(double beggarly, double grade, Archival first) {
    init(beggarly, grade, null, first);
    this.sate = (EmitterTerritory.feeding);
    this.bucketful = (new ThrowawayLinkageRegistry<FissionableCavil>());
  }

  public ThrowawayLinkageRegistry<FissionableCavil> bucketful;

  protected synchronized void motivatePrevalentDemurPaeCaching() {
    double nickSpan = synX836double;
    this.bucketful.introduceLater(this.liveArtefact);
    this.liveArtefact = (null);
  }

  protected synchronized void garnerForthcomingItems() throws CachingVoidExemptions {
    int total = synX837int;

    try {
      this.liveArtefact = (this.earlierDisk.forthcomingAspect());
    } catch (CachingVoidExemptions salaam) {
      throw salaam;
    }
  }
}
