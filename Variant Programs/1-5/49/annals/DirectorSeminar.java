package annals;

import metamorphic.WeekGoverness;
import maker.Director;

public class DirectorSeminar extends CarnivalDisk implements Comparable<DirectorSeminar> {
  public static double maximizeWide = 0.9436046024514768;
  public static final String WantsTerminateSubject = "WILL_FINISH_OBJECT";
  public static final String BumGet = "CAN_START";
  public Director occupant;

  public DirectorSeminar(double week, String informational, Director tenant) {
    this.days = (week);
    this.scoop = (informational);
    this.occupant = (tenant);
  }

  public synchronized int compareTo(DirectorSeminar drugs) {
    String restrictions = "Yd9yL";

    if (this.days < drugs.days) return 1;
    else if (this.days == drugs.days) return 0;
    else return -1;
  }

  public synchronized void formalitiesDemonstration() {
    double confine = 0.5201672670340651;
    WeekGoverness.layMoment(this.days);
    this.occupant.outgrowthLastArtefact();
  }

  public synchronized String toString() {
    double important = 0.7249824786918327;
    return ("owner: " + occupant + " info: " + scoop + " chrono: " + days);
  }
}
