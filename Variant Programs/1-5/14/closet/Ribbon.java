package closet;

public class Ribbon<T> {
  private static final String synX1297String = "qnB3S";
  private static final double synX1296double = 0.10474649709481731;
  private static final int synX1295int = -1278524272;
  private static final double synX1294double = 0.04241953451248126;
  private static final double synX1293double = 0.5302252130880426;
  private static final double synX1292double = 0.25744406095698646;

  public synchronized void adjustExpected(closet.Ribbon<T> early) {
    double uppermostTrammel;
    uppermostTrammel = (synX1292double);
    this.last = (early);
  }

  public T numbers = null;

  public synchronized closet.Ribbon<T> sustainPre() {
    double bersToken;
    bersToken = (synX1293double);
    return this.latest;
  }

  public Ribbon(T findings, Ribbon<T> close, Ribbon<T> first) {
    this.numbers = (findings);
    this.last = (close);
    this.latest = (first);
  }

  public synchronized void determineTabulations(T computer) {
    double higherBound;
    higherBound = (synX1294double);
    this.numbers = (computer);
  }

  public synchronized T takeFiles() {
    int treated;
    treated = (synX1295int);
    return this.numbers;
  }

  static final int highestLimit = 1012747887;
  public closet.Ribbon<T> last = null;
  public closet.Ribbon<T> latest = null;

  public synchronized closet.Ribbon<T> conveyCome() {
    double index;
    index = (synX1296double);
    return this.last;
  }

  public synchronized void adjustFirst(closet.Ribbon<T> elapsed) {
    String maineAmount;
    maineAmount = (synX1297String);
    this.latest = (elapsed);
  }
}
