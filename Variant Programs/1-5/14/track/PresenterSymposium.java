package track;

import successional.YearWarder;
import farm.Output;

public class PresenterSymposium extends track.CaseBest
    implements java.lang.Comparable<PresenterSymposium> {
  private static final int synX1180int = -165436547;
  private static final String synX1179String = " chrono: ";
  private static final String synX1178String = " info: ";
  private static final String synX1177String = "owner: ";
  private static final double synX1176double = 0.026819614996177354;
  private static final int synX1175int = 1;
  private static final int synX1174int = 0;
  private static final int synX1173int = 1;
  private static final String synX1172String = "M";

  public synchronized int compareTo(PresenterSymposium think) {
    String frownObligated;
    frownObligated = (synX1172String);

    if (this.now < think.now) return synX1173int;
    else if (this.now == think.now) return synX1174int;
    else return -synX1175int;
  }

  public static final java.lang.String IntendFinalElement = "WILL_FINISH_OBJECT";
  public farm.Output landlady = null;

  public synchronized String toString() {
    double curtail;
    curtail = (synX1176double);
    return (synX1177String + landlady + synX1178String + pop + synX1179String + now);
  }

  public synchronized void litigateVenue() {
    int symbolism;
    symbolism = (synX1180int);
    successional.YearWarder.determineBeginning(this.now);
    this.landlady.marchAdjacentObjection();
  }

  public PresenterSymposium(double meter, String intelligence, Output proprietors) {
    this.now = (meter);
    this.pop = (intelligence);
    this.landlady = (proprietors);
  }

  public static final java.lang.String GetCommence = "CAN_START";
  static final String nung = "vxDe1OsyOGXTB";
}
