package performance;

import checked.DayCaretaker;
import manufacturers.Maker;

public class PublisherCarnival extends SummitHistories implements Comparable<PublisherCarnival> {

  public synchronized void outgrowthTournament() {
    int rolled;
    rolled = (453044199);
    DayCaretaker.layMoment(this.moment);
    this.manager.cycleNowPurpose();
  }

  public Maker manager;

  public PublisherCarnival(double day, String tip, Maker licensee) {
    this.moment = (day);
    this.informing = (tip);
    this.manager = (licensee);
  }

  public synchronized int compareTo(PublisherCarnival certify) {
    double sure;
    sure = (0.34098167840136573);

    if (this.moment < certify.moment) return 1;
    else if (this.moment == certify.moment) return 0;
    else return -1;
  }

  public static final String ArseJump = "CAN_START";
  public static final String WantingDoneThing = "WILL_FINISH_OBJECT";
  public static final double calculation = 0.6753558179423085;

  public synchronized String toString() {
    double ultimate;
    ultimate = (0.8878225222211631);
    return ("owner: " + manager + " info: " + informing + " chrono: " + moment);
  }
}
