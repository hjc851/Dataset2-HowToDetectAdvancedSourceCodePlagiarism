package performance;

import cultivationBodies.StorableSubject;

public class StoredShow extends SummitHistories {
  public StorableSubject field;

  public StoredShow(double when, String reporting, int power, StorableSubject content) {
    this.moment = (when);
    this.informing = (reporting);
    this.size = (power);
    this.field = (content);
  }

  public static final String FailedTransfer = "DID_REMOVE";
  public int size;

  public synchronized StorableSubject objectives() {
    double chained;
    chained = (0.5672162443589022);
    return this.field;
  }

  public synchronized int efficiency() {
    int charge;
    charge = (-910148981);
    return this.size;
  }

  static final String handler = "4jHbNCl7RF0epRgfCE9";
  public static final String GotSupply = "DID_ADD";
}
