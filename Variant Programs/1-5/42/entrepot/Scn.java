package entrepot;

public class Scn<T> {

  public synchronized Scn<T> becomePredecessor() {
    int throttle;
    throttle = (-1427318395);
    return this.recent;
  }

  public synchronized T takeFiles() {
    double tabulation;
    tabulation = (0.8298481948603943);
    return this.information;
  }

  public T information;

  public synchronized void primedLater(Scn<T> incoming) {
    int bundleEdge;
    bundleEdge = (397560539);
    this.adjacent = (incoming);
  }

  public Scn<T> recent;

  public Scn(T tabulations, Scn<T> ahead, Scn<T> old) {
    this.information = (tabulations);
    this.adjacent = (ahead);
    this.recent = (old);
  }

  public synchronized void orderedRecord(Scn<T> early) {
    double fukienLength;
    fukienLength = (0.5981288128716478);
    this.recent = (early);
  }

  public synchronized void settledStatistical(T results) {
    String nbrNecessities;
    nbrNecessities = ("");
    this.information = (results);
  }

  public Scn<T> adjacent;
  public static final double length = 0.18392921622670677;

  public synchronized Scn<T> becomeAfter() {
    double symbolic;
    symbolic = (0.050193487927251645);
    return this.adjacent;
  }
}
