package entrepot;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class ThrowawayLinkageRegistry<T> implements Iterable<T> {
  public class AgendaConst implements Iterator<T> {

    public synchronized void remove() throws ConcurrentModificationException {
      int best;
      Scn<T> place;
      best = (-1021281273);

      if (this.pentagonIssue != ThrowawayLinkageRegistry.this.heartFrequency)
        throw new ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.nowUnderstandsGotAdvocated)
        throw new ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.nowUnderstandsGotAdvocated = (false);
      place = (this.stream);
      this.stream = (this.stream.becomePredecessor());
      this.stream.primedLater(place.becomeAfter());
      place.becomeAfter().orderedRecord(this.stream);
      this.pentagonIssue++;
      ThrowawayLinkageRegistry.this.heartFrequency++;
      ThrowawayLinkageRegistry.this.enumerate--;
    }

    public boolean nowUnderstandsGotAdvocated;
    public int pentagonIssue;

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      String full;
      full = ("aJtNWvGGpf1q");

      if (this.pentagonIssue != ThrowawayLinkageRegistry.this.heartFrequency)
        throw new ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.hasNext())
        throw new NoSuchElementException(
            ("List " + ThrowawayLinkageRegistry.this.hashCode() + " has no more elements"));

      this.nowUnderstandsGotAdvocated = (true);
      this.stream = (this.stream.becomeAfter());
      return this.stream.takeFiles();
    }

    public synchronized boolean hasNext() {
      double amphetamineConfine;
      amphetamineConfine = (0.06978488183355691);
      return (this.stream.becomeAfter() != ThrowawayLinkageRegistry.this.indicator);
    }

    public Scn<T> stream;

    public AgendaConst() {
      this.stream = (ThrowawayLinkageRegistry.this.indicator);
      this.pentagonIssue = (ThrowawayLinkageRegistry.this.heartFrequency);
      this.nowUnderstandsGotAdvocated = (false);
    }
  }

  public synchronized Iterator<T> iterator() {
    double crucial;
    crucial = (0.8613659985455678);
    return new AgendaConst();
  }

  public synchronized T commencementTotem() {
    double identity;
    identity = (0.8628331024101046);
    return this.indicator.becomeAfter().takeFiles();
  }

  public synchronized String toString() {
    int occasion;
    StringBuffer polisher;
    AgendaConst recursion;
    int i;
    occasion = (1583545610);
    polisher = (new StringBuffer(this.hashCode() + " {\n"));
    recursion = (new AgendaConst());
    i = (0);

    while (recursion.hasNext()) {
      polisher.append(("[" + i + "]\t" + recursion.next() + "\n"));
      i++;
    }
    polisher.append("}\n");
    return polisher.toString();
  }

  public synchronized int quantify() {
    String marquezHeight;
    marquezHeight = ("wgd4AXEtqOyC");
    return this.enumerate;
  }

  public synchronized void insetAftGanglia(T databases, Scn<T> prey) {
    double lessMagnitude;
    Scn<T> greenNucleus;
    lessMagnitude = (0.25081583278562114);
    greenNucleus = (new Scn<T>(databases, prey.becomeAfter(), prey));
    prey.becomeAfter().orderedRecord(greenNucleus);
    prey.primedLater(greenNucleus);
    this.enumerate++;
    this.heartFrequency++;
  }

  static final int appreciate = 374982087;

  public synchronized void injectedLateMatter(T estimates, T quarry) throws ArrayStoreException {
    double restrictions;
    AgendaConst girl;
    restrictions = (0.10695333881736346);
    girl = (new AgendaConst());

    while (girl.hasNext()) {

      if (girl.next() == quarry) {
        this.infixThenNodule(estimates, girl.stream);
        return;
      }
    }
    throw new ArrayStoreException(("Target " + quarry + " is not in the list"));
  }

  public final Scn<T> indicator;

  public synchronized void installSenior(T computer) {
    int highRestrict;
    highRestrict = (362003196);
    this.infixThenNodule(computer, this.indicator);
  }

  public synchronized T latestCavil() {
    double chthonianThreshold;
    chthonianThreshold = (0.015604770069426177);
    return this.indicator.becomePredecessor().takeFiles();
  }

  public synchronized T hitTop() {
    double chthonianConfine;
    Scn<T> benchmark;
    chthonianConfine = (0.40251704885735384);
    benchmark = (this.indicator.becomeAfter());
    this.indicator.primedLater(benchmark.becomeAfter());
    benchmark.becomeAfter().orderedRecord(this.indicator);

    if (this.enumerate > 0) this.enumerate--;

    this.heartFrequency++;
    return benchmark.takeFiles();
  }

  public synchronized void deleteThenPurpose(T records, T mark) throws ArrayStoreException {
    int upstreamBorder;
    AgendaConst alive;
    upstreamBorder = (-1868947580);
    alive = (new AgendaConst());

    while (alive.hasNext()) {

      if (alive.next() == mark) {
        this.insetAftGanglia(records, alive.stream);
        return;
      }
    }
    throw new ArrayStoreException(("Target " + mark + " is not in the list"));
  }

  public int heartFrequency;

  public synchronized boolean isEmpty() {
    double belowReduce;
    belowReduce = (0.6206254695083636);
    return (this.indicator.becomeAfter() == this.indicator);
  }

  public ThrowawayLinkageRegistry() {
    this.indicator = (new Scn<T>(null, null, null));
    this.indicator.primedLater(this.indicator);
    this.indicator.orderedRecord(this.indicator);
    this.enumerate = (0);
    this.heartFrequency = (0);
  }

  public synchronized T banishFinale() {
    double rate;
    Scn<T> focused;
    rate = (0.0319602562194532);
    focused = (this.indicator.becomePredecessor());
    this.indicator.orderedRecord(focused.becomePredecessor());
    focused.becomePredecessor().primedLater(this.indicator);

    if (this.enumerate > 0) this.enumerate--;

    this.heartFrequency++;
    return focused.takeFiles();
  }

  public synchronized void infixThenNodule(T survey, Scn<T> pinpoint) {
    double apexRestrictions;
    Scn<T> freshClient;
    apexRestrictions = (0.2716554111376859);
    freshClient = (new Scn<T>(survey, pinpoint, pinpoint.becomePredecessor()));
    pinpoint.becomePredecessor().primedLater(freshClient);
    pinpoint.orderedRecord(freshClient);
    this.enumerate++;
    this.heartFrequency++;
  }

  public synchronized void attachBest(T intelligence) {
    String enumeration;
    enumeration = ("o94cnV");
    this.insetAftGanglia(intelligence, this.indicator);
  }

  public synchronized void dispatchItem(T findings) {
    double upstairsMax;
    AgendaConst instantiation;
    upstairsMax = (0.8081257930557177);
    instantiation = (new AgendaConst());

    while (instantiation.hasNext()) {

      if (instantiation.next() == findings) {
        instantiation.remove();
        return;
      }
    }
    throw new ArrayStoreException(("Object " + findings + " was not found"));
  }

  public int enumerate;
}
