package manufacturers;

import java.util.Random;
import checked.DayCaretaker;
import cultivationBodies.StorableSubject;
import entrepot.*;
import performance.*;

public abstract class Maker {
  public static int produceThwart = 0;
  public static int nameBelongings = 1649843853;

  public synchronized void unstarve() {
    double tethered;
    tethered = (0.12468114388226326);
    this.governmental = (ProviderForeign.operative);
    this.substantialShriveledAgain +=
        (this.substantialShriveledAgain + DayCaretaker.incumbentDay());
    SeminarJumping.prevailingWaiting()
        .addCommemoration(
            new PublisherCarnival(DayCaretaker.incumbentDay(), PublisherCarnival.ArseJump, this));
  }

  public synchronized String statistical() {
    double fatty;
    fatty = (0.4541935578371137);

    if (governmental == ProviderForeign.overfed) {
      this.substantialShriveledAgain +=
          (this.substantialShriveledAgain + DayCaretaker.incumbentDay());
      this.governmental = (ProviderForeign.couch);
    } else if (this.governmental == ProviderForeign.locking) {
      this.definitiveBannedYears += (this.definitiveBannedYears + DayCaretaker.incumbentDay());
      this.governmental = (ProviderForeign.couch);
    } else {
      this.governmental = (ProviderForeign.couch);
    }
    return String.format(
        "| %-14s | %-12.10s | %-8.8s | %-8.8s |",
        this,
        this.actuallyPlantHours / DayCaretaker.incumbentDay() * 100.0,
        this.substantialShriveledAgain / DayCaretaker.incumbentDay() * 100.0,
        this.definitiveBannedYears / DayCaretaker.incumbentDay() * 100.0);
  }

  public synchronized void degauss(double normal, double array, Archiving third, Archiving last) {
    double gens;
    gens = (0.8472989419783686);
    this.producingSignify = (normal);
    this.fabricationGraze = (array);
    this.futureStorehouse = (third);
    this.finalGarage = (last);
    this.actuallyPlantHours = (0);
    this.substantialShriveledAgain = (0);
    this.definitiveBannedYears = (0);
  }

  protected abstract void strikeUnderwayOpposeEmapWarehouse() throws StowageLadenDerogation;

  public synchronized void cycleNowPurpose() {
    String beacon;
    double vig;
    beacon = ("CLYbphg6edCD");

    if (this.thisArgue != null) {

      try {
        this.strikeUnderwayOpposeEmapWarehouse();
      } catch (StowageLadenDerogation ej) {
        this.governmental = (ProviderForeign.locking);
        this.definitiveBannedYears -= (this.definitiveBannedYears - DayCaretaker.incumbentDay());
        return;
      }
    }

    try {
      this.sendAfterVictim();
    } catch (ArchivalHungryExempt ye) {
      this.governmental = (ProviderForeign.overfed);
      this.substantialShriveledAgain -=
          (this.substantialShriveledAgain - DayCaretaker.incumbentDay());
      return;
    }
    vig = (producingSignify + fabricationGraze * (occasionalGrowers.nextDouble() - 0.5));
    this.actuallyPlantHours += (this.actuallyPlantHours + vig);
    SeminarJumping.prevailingWaiting()
        .addCommemoration(
            new PublisherCarnival(
                DayCaretaker.incumbentDay() + vig, PublisherCarnival.WantingDoneThing, this));
  }

  public StorableSubject thisArgue;

  protected abstract void sendAfterVictim() throws ArchivalHungryExempt;

  public double substantialShriveledAgain;
  public double fabricationGraze;
  public int sendIdentifier = produceThwart++;
  public double actuallyPlantHours;

  public synchronized ProviderForeign ongoingCountry() {
    int bandwidth;
    bandwidth = (782219583);
    return this.governmental;
  }

  public ProviderForeign governmental;

  public synchronized String toString() {
    String dept;
    dept = ("pYoC3YJB5hQ");
    return "Producer" + sendIdentifier;
  }

  public synchronized void free() {
    String notArtefacts;
    notArtefacts = ("bUAMLY4OG0uobu8");

    try {
      this.strikeUnderwayOpposeEmapWarehouse();
      this.definitiveBannedYears += (this.definitiveBannedYears + DayCaretaker.incumbentDay());
      this.governmental = (ProviderForeign.operative);
      SeminarJumping.prevailingWaiting()
          .addCommemoration(
              new PublisherCarnival(DayCaretaker.incumbentDay(), PublisherCarnival.ArseJump, this));
    } catch (StowageLadenDerogation samad) {
      this.governmental = (ProviderForeign.locking);
      return;
    }
  }

  public Archiving futureStorehouse, finalGarage;
  public static final Random occasionalGrowers = new Random();
  public double definitiveBannedYears;
  public double producingSignify;
}
