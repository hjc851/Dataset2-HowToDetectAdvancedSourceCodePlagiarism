package breeder;

import manufactureOrnaments.NeocomianElement;
import warehouses.*;

public class ProduceCompleting extends Maker {

  public ProduceCompleting(double imply, double compass, Caching ago) {
    instantiate(imply, compass, null, ago);
    this.tell = BreederGeneral.malnourished;
    this.pipe = new GlobularCorrelatedBlacklist<NeocomianElement>();
  }

  public GlobularCorrelatedBlacklist<NeocomianElement> pipe;

  protected synchronized void meetLaterResist() throws DepotUnfilledCaveat {

    try {
      this.continuingThing = this.perviousArchiving.thirdThing();
    } catch (DepotUnfilledCaveat samad) {
      throw samad;
    }
  }

  protected synchronized void propelOngoingAimCoughMemory() {
    this.pipe.deleteClosing(this.continuingThing);
    this.continuingThing = null;
  }
}
