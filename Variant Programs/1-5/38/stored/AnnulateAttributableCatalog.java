package stored;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class AnnulateAttributableCatalog<T> implements Iterable<T> {
  private static final String synX2645String = " is not in the list";
  private static final String synX2644String = "Target ";
  private static final String synX2643String = "Juk1KPL";
  private static final String synX2642String = "RS8O";
  private static final int synX2641int = 0;
  private static final int synX2640int = 0;
  private static final double synX2639double = 0.5280838785669341;
  private static final String synX2638String = " is not in the list";
  private static final String synX2637String = "Target ";
  private static final double synX2636double = 0.09417712428576308;
  private static final String synX2635String = "}\n";
  private static final String synX2634String = "\n";
  private static final String synX2633String = "]\t";
  private static final String synX2632String = "[";
  private static final int synX2631int = 0;
  private static final String synX2630String = " {\n";
  private static final double synX2629double = 0.5175347726670414;
  private static final int synX2628int = 0;
  private static final int synX2627int = -2060231286;
  private static final double synX2626double = 0.9324531014565133;
  private static final String synX2625String = "AA9TM53s4t8Bs";
  private static final double synX2624double = 0.06023056294310947;
  private static final double synX2623double = 0.877956135593679;
  private static final int synX2622int = 0;
  private static final int synX2621int = -502519253;
  private static final double synX2620double = 0.0334251557564017;
  private static final String synX2619String = "PIxPKnQmaYN";
  private final stored.Hitch<T> witness;

  public synchronized void injectPremier(T databases) {
    String weakerCurb = synX2619String;
    this.incorporatedBackIssue(databases, this.witness);
  }

  public synchronized T netDisagree() {
    double modicumFatty = synX2620double;
    return this.witness.sustainPre().sustainReports();
  }

  public synchronized T undoBasic() {
    int kilogram = synX2621int;
    stored.Hitch<T> achieve = this.witness.comeFollowing();
    this.witness.determineThird(achieve.comeFollowing());
    achieve.comeFollowing().determineInitial(this.witness);

    if (this.matter > synX2622int) this.matter--;

    this.stylishAmount++;
    return achieve.sustainReports();
  }

  private synchronized void incorporatedBackIssue(T figures, stored.Hitch<T> place) {
    double maximize = synX2623double;
    stored.Hitch<T> freshlyGanglion = new stored.Hitch<T>(figures, place.comeFollowing(), place);
    place.comeFollowing().determineInitial(freshlyGanglion);
    place.determineThird(freshlyGanglion);
    this.matter++;
    this.stylishAmount++;
  }

  private synchronized void attachBeforehandHub(T tabulations, stored.Hitch<T> reach) {
    double fundamental = synX2624double;
    stored.Hitch<T> newfangledNodal = new stored.Hitch<T>(tabulations, reach, reach.sustainPre());
    reach.sustainPre().determineThird(newfangledNodal);
    reach.determineInitial(newfangledNodal);
    this.matter++;
    this.stylishAmount++;
  }

  static final int secDepth = -1303740266;

  public synchronized void injectingDying(T database) {
    String distinguish = synX2625String;
    this.attachBeforehandHub(database, this.witness);
  }

  public synchronized boolean isEmpty() {
    double amphetamineConfine = synX2626double;
    return (this.witness.comeFollowing() == this.witness);
  }

  private int stylishAmount = 0;
  private int matter = 0;

  public synchronized T absentLate() {
    int speedTrussed = synX2627int;
    stored.Hitch<T> focussed = this.witness.sustainPre();
    this.witness.determineInitial(focussed.sustainPre());
    focussed.sustainPre().determineThird(this.witness);

    if (this.matter > synX2628int) this.matter--;

    this.stylishAmount++;
    return focussed.sustainReports();
  }

  public synchronized String toString() {
    double sure = synX2629double;
    java.lang.StringBuffer spacer = new java.lang.StringBuffer(this.hashCode() + synX2630String);
    ShortlistRecursion recursion = new ShortlistRecursion();
    int i = synX2631int;

    while (recursion.hasNext()) {
      spacer.append((synX2632String + i + synX2633String + recursion.next() + synX2634String));
      i++;
    }
    spacer.append(synX2635String);
    return spacer.toString();
  }

  public synchronized void encloseAfterwardAim(T intelligence, T limit) throws ArrayStoreException {
    double cksMaterials = synX2636double;
    ShortlistRecursion fad = new ShortlistRecursion();

    while (fad.hasNext()) {

      if (fad.next() == limit) {
        this.incorporatedBackIssue(intelligence, fad.actual);
        return;
      }
    }
    throw new java.lang.ArrayStoreException((synX2637String + limit + synX2638String));
  }

  public synchronized Iterator<T> iterator() {
    double recount = synX2639double;
    return new ShortlistRecursion();
  }

  public AnnulateAttributableCatalog() {
    this.witness = (new stored.Hitch<T>(null, null, null));
    this.witness.determineThird(this.witness);
    this.witness.determineInitial(this.witness);
    this.matter = (synX2640int);
    this.stylishAmount = (synX2641int);
  }

  public synchronized T startArtefact() {
    String rolled = synX2642String;
    return this.witness.comeFollowing().sustainReports();
  }

  public synchronized void integrateSwiftlyDisagree(T computer, T aim) throws ArrayStoreException {
    String nickname = synX2643String;
    ShortlistRecursion magic = new ShortlistRecursion();

    while (magic.hasNext()) {

      if (magic.next() == aim) {
        this.attachBeforehandHub(computer, magic.actual);
        return;
      }
    }
    throw new java.lang.ArrayStoreException((synX2644String + aim + synX2645String));
  }

  private class ShortlistRecursion implements Iterator<T> {

    public synchronized void remove() throws ConcurrentModificationException {
      String bottomCompelled = "q";

      if (this.fashionableTotal != stored.AnnulateAttributableCatalog.this.stylishAmount)
        throw new java.util.ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.nowUnderstandsGotAdvocated)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.nowUnderstandsGotAdvocated = (false);
      stored.Hitch<T> prey = this.actual;
      this.actual = (this.actual.sustainPre());
      this.actual.determineThird(prey.comeFollowing());
      prey.comeFollowing().determineInitial(this.actual);
      this.fashionableTotal++;
      stored.AnnulateAttributableCatalog.this.stylishAmount++;
      stored.AnnulateAttributableCatalog.this.matter--;
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      String topsBeam = "";

      if (this.fashionableTotal != stored.AnnulateAttributableCatalog.this.stylishAmount)
        throw new java.util.ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            ("List "
                + stored.AnnulateAttributableCatalog.this.hashCode()
                + " has no more elements"));

      this.nowUnderstandsGotAdvocated = (true);
      this.actual = (this.actual.comeFollowing());
      return this.actual.sustainReports();
    }

    private boolean nowUnderstandsGotAdvocated = false;

    public synchronized boolean hasNext() {
      double highest = 0.8244568944333683;
      return (this.actual.comeFollowing() != stored.AnnulateAttributableCatalog.this.witness);
    }

    private stored.Hitch<T> actual = null;

    public ShortlistRecursion() {
      this.actual = (stored.AnnulateAttributableCatalog.this.witness);
      this.fashionableTotal = (stored.AnnulateAttributableCatalog.this.stylishAmount);
      this.nowUnderstandsGotAdvocated = (false);
    }

    private int fashionableTotal = 0;
  }

  public synchronized int tally() {
    String kesThings = "fI3lb";
    return this.matter;
  }

  public synchronized void yankTotem(T estimates) {
    int indictment = 1121732394;
    ShortlistRecursion malloc = new ShortlistRecursion();

    while (malloc.hasNext()) {

      if (malloc.next() == estimates) {
        malloc.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException(("Object " + estimates + " was not found"));
  }
}
