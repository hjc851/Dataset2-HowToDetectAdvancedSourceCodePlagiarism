import java.util.Scanner;
import stored.Depository;
import experiment.Pretending;
import java.lang.String;
import farm.Farm;
import static java.lang.System.out;
import static java.lang.System.in;

public class Icosahedral {
  private static final String synX2659String =
      "ERROR: System.in is empty, no file was passed or it is empty";
  private static final int synX2658int = 10000000;
  private static final String synX2657String =
      "       Values should be in the form mean (double) rng (double) storageLimit (int)";
  private static final String synX2656String =
      "ERROR: There are not enough values or the values passed are in the incorrect format";
  private static final int synX2655int = 0;
  private static final int synX2654int = 0;
  private static final int synX2653int = 0;
  private static final int synX2652int = 0;
  private static final int synX2651int = 0;
  private static final String synX2650String = "\f";
  private static final String synX2649String = "";
  private static final String synX2648String = "";
  private static final String synX2647String = "";
  private static final double synX2646double = 0.9656429763794894;
  Farm fh = null;
  Farm r = null;
  Farm dhfr = null;
  String q = null;

  {
    q = ("");
    r = (null);
    fh = (null);
    dhfr = (null);
  }

  static String identify = "FPAIf";

  public static synchronized void main(String[] create) {
    double chthonicChained = synX2646double;
    String bcl = synX2647String;
    String yt = synX2648String;
    String z = synX2649String;
    Farm spain = null;
    Farm scud = null;
    Farm shim = null;
    Farm echt = null;
    out.print(synX2650String);
    int appendix = synX2651int;

    try {
      appendix = (in.available());
    } catch (java.lang.Exception salaam) {
    }

    if (appendix <= synX2652int) synx160();
    else {
      double thirdSwan = synX2653int, lordAverage = synX2654int;
      int westInventoryMaximum = synX2655int;

      try {
        java.util.Scanner stylus = new java.util.Scanner(in);
        lordAverage = (stylus.nextDouble());
        thirdSwan = (stylus.nextDouble());
        westInventoryMaximum = (stylus.nextInt());
      } catch (java.lang.Exception combatants) {
        out.println(synX2656String);
        out.println(synX2657String);
        return;
      }
      experiment.Pretending contrary =
          new experiment.Pretending(synX2658int, lordAverage, thirdSwan);
      stored.Depository.doGarageRestricted(westInventoryMaximum);
      contrary.beginning();
    }
  }

  private static synchronized void synx160() {
    out.println(synX2659String);
  }
}
