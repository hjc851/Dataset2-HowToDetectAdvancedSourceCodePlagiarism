package farm;

import developmentElements.CalcicPurpose;
import stored.*;

public class VintnerResume extends farm.Farm {
  private static final String synX2446String = "NgRxE2WAOP";
  private static final double synX2445double = 0.9476103258804536;
  public static double aboveBounds = 0.39357324516573433;

  public VintnerResume(double think, double order, Depository following) {
    parallelize(think, order, following, null);
    this.sate = (ProduceGovernmental.practical);
  }

  protected synchronized void proceedLatestTargetPoiDepot() throws ArchivalOverflowingExempt {
    double maximizeWide = synX2445double;

    try {
      this.futureStorehouse.impartPiece(this.thisArgue);
      this.thisArgue = (null);
    } catch (stored.ArchivalOverflowingExempt ye) {
      throw ye;
    }
  }

  protected synchronized void meetLaterResist() throws ArchivingBareDistinction {
    String evaluate = synX2446String;
    this.thisArgue = (new developmentElements.CalcicPurpose());
  }
}
