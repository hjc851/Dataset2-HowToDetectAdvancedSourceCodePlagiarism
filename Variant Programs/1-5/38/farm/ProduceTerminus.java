package farm;

import stored.*;

public class ProduceTerminus extends farm.Farm {
  private static final double synX2466double = 0.7455822184916637;
  private static final double synX2465double = 0.9716430409309107;

  protected synchronized void proceedLatestTargetPoiDepot() throws ArchivalOverflowingExempt {
    double ultimateAcross = synX2465double;

    try {
      this.futureStorehouse.impartPiece(this.thisArgue);
      this.thisArgue = (null);
    } catch (stored.ArchivalOverflowingExempt e) {
      throw e;
    }
  }

  public ProduceTerminus(double stingy, double orbit, Depository again, Depository preceding) {
    parallelize(stingy, orbit, again, preceding);
    this.sate = (ProduceGovernmental.hunger);
  }

  protected synchronized void meetLaterResist() throws ArchivingBareDistinction {
    double fukkianese = synX2466double;

    try {
      this.thisArgue = (this.latestSafekeeping.soonArticle());
    } catch (stored.ArchivingBareDistinction e) {
      throw e;
    }
  }

  static double topShackled = 0.14771789618761266;
}
