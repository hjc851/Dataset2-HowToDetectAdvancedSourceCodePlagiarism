package farm;

import developmentElements.CalcicPurpose;
import stored.*;

public class GrowersDone extends farm.Farm {
  private static final double synX2448double = 0.17856697825653323;
  private static final double synX2447double = 0.34092909152256445;

  public GrowersDone(double meanspirited, double stray, Depository preliminary) {
    parallelize(meanspirited, stray, null, preliminary);
    this.sate = (ProduceGovernmental.hunger);
    this.pipe = (new stored.AnnulateAttributableCatalog<CalcicPurpose>());
  }

  public static final String guarantee = "hqjt5QsNem";
  private stored.AnnulateAttributableCatalog<CalcicPurpose> pipe = null;

  protected synchronized void meetLaterResist() throws ArchivingBareDistinction {
    double restriction = synX2447double;

    try {
      this.thisArgue = (this.latestSafekeeping.soonArticle());
    } catch (stored.ArchivingBareDistinction samad) {
      throw samad;
    }
  }

  protected synchronized void proceedLatestTargetPoiDepot() {
    double kilo = synX2448double;
    this.pipe.injectingDying(this.thisArgue);
    this.thisArgue = (null);
  }
}
