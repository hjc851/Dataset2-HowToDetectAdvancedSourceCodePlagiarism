package provider;

import shelving.*;

public class ManufacturerBase extends Growers {

  public ManufacturerBase(double spiteful, double drift, Stored later, Stored recent) {
    synchronising(spiteful, drift, later, recent);
    this.cantons = (ExporterNation.famished);
  }

  protected synchronized void sendAfterVictim() throws WarehousesGlazedExempted {

    try {
      this.prevailingObjective = (this.precedingStorehouse.comeSection());
    } catch (WarehousesGlazedExempted e) {
      throw e;
    }
  }

  protected synchronized void impressFlowObjetMouStowage() throws DepotEntireCaveat {

    try {
      this.succeedingStore.bestowArtifact(this.prevailingObjective);
      this.prevailingObjective = (null);
    } catch (DepotEntireCaveat e) {
      throw e;
    }
  }
}
