package provider;

import manufacturedBelongings.PannonianMatter;
import shelving.*;

public class OperatorOriginate extends Growers {

  protected synchronized void impressFlowObjetMouStowage() throws DepotEntireCaveat {

    try {
      this.succeedingStore.bestowArtifact(this.prevailingObjective);
      this.prevailingObjective = (null);
    } catch (DepotEntireCaveat einsteinium) {
      throw einsteinium;
    }
  }

  protected synchronized void sendAfterVictim() throws WarehousesGlazedExempted {
    this.prevailingObjective = (new PannonianMatter());
  }

  public OperatorOriginate(double small, double straddle, Stored expected) {
    synchronising(small, straddle, expected, null);
    this.cantons = (ExporterNation.trying);
  }
}
