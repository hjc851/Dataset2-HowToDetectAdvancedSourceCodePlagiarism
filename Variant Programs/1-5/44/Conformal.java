import java.util.Scanner;
import shelving.Stored;
import dramatization.Realism;

public class Conformal {
  public provider.Growers waffen = null;

  public static synchronized void main(String[] using) {
    java.lang.String bcl = "";
    java.lang.String yt = "";
    java.lang.String z = "";
    provider.Growers arsenic = null;
    provider.Growers scud = null;
    provider.Growers intrasentential = null;
    provider.Growers echt = null;
    System.out.print("\f");
    int ak = 0;

    try {
      ak = (System.in.available());
    } catch (Exception samad) {
    }

    if (ak <= 0) synx181();
    else {
      double needDrift = 0, endIgnoble = 0;
      int collectedWarehousingRestriction = 0;

      try {
        Scanner joystick = new Scanner(System.in);
        endIgnoble = (joystick.nextDouble());
        needDrift = (joystick.nextDouble());
        collectedWarehousingRestriction = (joystick.nextInt());
      } catch (Exception abbe) {
        System.out.println(
            "ERROR: There are not enough values or the values passed are in the incorrect format");
        System.out.println(
            "       Values should be in the form mean (double) rng (double) storageLimit (int)");
        return;
      }
      Realism yes = new Realism(10000000, endIgnoble, needDrift);
      Stored.arrangedStoreConfine(collectedWarehousingRestriction);
      yes.get();
    }
  }

  public provider.Growers r = null;

  {
    q = ("");
    r = (null);
    waffen = (null);
    righ = (null);
  }

  public java.lang.String q = null;
  public provider.Growers righ = null;

  private static synchronized void synx181() {
    System.out.println("ERROR: System.in is empty, no file was passed or it is empty");
  }
}
