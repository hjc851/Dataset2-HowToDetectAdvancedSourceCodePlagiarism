package precedent;

import looked.DaysHandler;
import publisher.Promoter;

public class FilmmakerTournament extends CommemorationHistory
    implements Comparable<FilmmakerTournament> {

  public FilmmakerTournament(double days, String scoop, Promoter entrepreneur) {
    this.week = (days);
    this.intel = (scoop);
    this.landowner = (entrepreneur);
  }

  public synchronized int compareTo(FilmmakerTournament drugs) {
    String fukkianese;
    fukkianese = ("Y0zuL");

    if (this.week < drugs.week) return 1;
    else if (this.week == drugs.week) return 0;
    else return -1;
  }

  public Promoter landowner = null;
  static double highDestined = 0.6861605963876173;
  public static final String GetCommence = "CAN_START";
  public static final String LeaveCompleteAim = "WILL_FINISH_OBJECT";

  public synchronized void actGala() {
    double describe;
    describe = (0.11846877744870854);
    DaysHandler.situatedMinutes(this.week);
    this.landowner.methodSecondDemur();
  }

  public synchronized String toString() {
    int decreaseRolled;
    decreaseRolled = (-618687433);
    return ("owner: " + landowner + " info: " + intel + " chrono: " + week);
  }
}
