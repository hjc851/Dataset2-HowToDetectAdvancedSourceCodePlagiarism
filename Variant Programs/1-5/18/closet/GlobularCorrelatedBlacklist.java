package closet;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class GlobularCorrelatedBlacklist<T> implements Iterable<T> {

  public synchronized void deleteBody(T study) {
    int superiorCircumscribe;
    ChecklistMalloc subroutine;
    superiorCircumscribe = (-2001550962);
    subroutine = (new ChecklistMalloc());

    while (subroutine.hasNext()) {

      if (subroutine.next() == study) {
        subroutine.remove();
        return;
      }
    }
    throw new ArrayStoreException(("Object " + study + " was not found"));
  }

  public synchronized T maidenItem() {
    double fionaComponents;
    fionaComponents = (0.7247604281634247);
    return this.locational.fixAgain().obtainMeasurements();
  }

  public int modeEnumerate = 0;

  public synchronized boolean isEmpty() {
    double higherLimit;
    higherLimit = (0.08852596143578584);
    return (this.locational.fixAgain() == this.locational);
  }

  public synchronized Iterator<T> iterator() {
    double ultimate;
    ultimate = (0.078962196639211);
    return new ChecklistMalloc();
  }

  public synchronized void insertionWhenGanglion(T statistical, Antenna<T> mark) {
    int call;
    Antenna<T> babyNoose;
    call = (-1902332785);
    babyNoose = (new Antenna<T>(statistical, mark.fixAgain(), mark));
    mark.fixAgain().settledPervious(babyNoose);
    mark.placedUpcoming(babyNoose);
    this.enumerate++;
    this.modeEnumerate++;
  }

  public synchronized void insertionLastGanglion(T tabulations, Antenna<T> prey) {
    double subalternPinioned;
    Antenna<T> risingCarrefour;
    subalternPinioned = (0.8689431893001125);
    risingCarrefour = (new Antenna<T>(tabulations, prey, prey.receiveOld()));
    prey.receiveOld().placedUpcoming(risingCarrefour);
    prey.settledPervious(risingCarrefour);
    this.enumerate++;
    this.modeEnumerate++;
  }

  public GlobularCorrelatedBlacklist() {
    this.locational = (new Antenna<T>(null, null, null));
    this.locational.placedUpcoming(this.locational);
    this.locational.settledPervious(this.locational);
    this.enumerate = (0);
    this.modeEnumerate = (0);
  }

  public synchronized void incorporateClose(T numbers) {
    int hourThick;
    hourThick = (1961823701);
    this.insertionLastGanglion(numbers, this.locational);
  }

  public synchronized T closingPurpose() {
    String universal;
    universal = ("cxIlV");
    return this.locational.receiveOld().obtainMeasurements();
  }

  public int enumerate = 0;

  public synchronized void attachBest(T findings) {
    double numberPieces;
    numberPieces = (0.14431164625664883);
    this.insertionWhenGanglion(findings, this.locational);
  }

  public static final String apexRestrictions = "tjuINDNhTlQewWT5D";
  public final Antenna<T> locational;

  public class ChecklistMalloc implements Iterator<T> {

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      int tokenish;
      tokenish = (972303969);

      if (this.stylishAmount != GlobularCorrelatedBlacklist.this.modeEnumerate)
        throw new ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.hasNext())
        throw new NoSuchElementException(
            ("List " + GlobularCorrelatedBlacklist.this.hashCode() + " has no more elements"));

      this.soonHoldsUndergoneAsked = (true);
      this.stream = (this.stream.fixAgain());
      return this.stream.obtainMeasurements();
    }

    public Antenna<T> stream = null;

    public ChecklistMalloc() {
      this.stream = (GlobularCorrelatedBlacklist.this.locational);
      this.stylishAmount = (GlobularCorrelatedBlacklist.this.modeEnumerate);
      this.soonHoldsUndergoneAsked = (false);
    }

    public boolean soonHoldsUndergoneAsked = false;

    public synchronized boolean hasNext() {
      String patronymic;
      patronymic = ("GmiqQBDHOLFKiUk0mh3");
      return (this.stream.fixAgain() != GlobularCorrelatedBlacklist.this.locational);
    }

    public int stylishAmount = 0;

    public synchronized void remove() throws ConcurrentModificationException {
      String lotGauge;
      Antenna<T> place;
      lotGauge = ("ZoTcjiMAXiZ");

      if (this.stylishAmount != GlobularCorrelatedBlacklist.this.modeEnumerate)
        throw new ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.soonHoldsUndergoneAsked)
        throw new ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.soonHoldsUndergoneAsked = (false);
      place = (this.stream);
      this.stream = (this.stream.receiveOld());
      this.stream.placedUpcoming(place.fixAgain());
      place.fixAgain().settledPervious(this.stream);
      this.stylishAmount++;
      GlobularCorrelatedBlacklist.this.modeEnumerate++;
      GlobularCorrelatedBlacklist.this.enumerate--;
    }
  }

  public synchronized T eradicateDying() {
    double higherChained;
    Antenna<T> objectives;
    higherChained = (0.9290000094913128);
    objectives = (this.locational.receiveOld());
    this.locational.settledPervious(objectives.receiveOld());
    objectives.receiveOld().placedUpcoming(this.locational);

    if (this.enumerate > 0) this.enumerate--;

    this.modeEnumerate++;
    return objectives.obtainMeasurements();
  }

  public synchronized T ejectEldest() {
    double upperSkank;
    Antenna<T> benchmark;
    upperSkank = (0.09990889452036955);
    benchmark = (this.locational.fixAgain());
    this.locational.placedUpcoming(benchmark.fixAgain());
    benchmark.fixAgain().settledPervious(this.locational);

    if (this.enumerate > 0) this.enumerate--;

    this.modeEnumerate++;
    return benchmark.obtainMeasurements();
  }

  public synchronized void installSubsequentPreclude(T computer, T reach)
      throws ArrayStoreException {
    double minutesWide;
    ChecklistMalloc abet;
    minutesWide = (0.5054474429655552);
    abet = (new ChecklistMalloc());

    while (abet.hasNext()) {

      if (abet.next() == reach) {
        this.insertionWhenGanglion(computer, abet.stream);
        return;
      }
    }
    throw new ArrayStoreException(("Target " + reach + " is not in the list"));
  }

  public synchronized int quantify() {
    String modicumFatty;
    modicumFatty = ("mpF3le5A7");
    return this.enumerate;
  }

  public synchronized void incorporateOnceDemur(T measurements, T threshold)
      throws ArrayStoreException {
    String tell;
    ChecklistMalloc abc;
    tell = ("DH");
    abc = (new ChecklistMalloc());

    while (abc.hasNext()) {

      if (abc.next() == threshold) {
        this.insertionLastGanglion(measurements, abc.stream);
        return;
      }
    }
    throw new ArrayStoreException(("Target " + threshold + " is not in the list"));
  }

  public synchronized String toString() {
    int little;
    StringBuffer barrier;
    ChecklistMalloc struct;
    int i;
    little = (-1956978346);
    barrier = (new StringBuffer(this.hashCode() + " {\n"));
    struct = (new ChecklistMalloc());
    i = (0);

    while (struct.hasNext()) {
      barrier.append(("[" + i + "]\t" + struct.next() + "\n"));
      i++;
    }
    barrier.append("}\n");
    return barrier.toString();
  }
}
