package publisher;

import java.util.Random;
import looked.DaysHandler;
import manufactureOrnaments.EvaporiteDemur;
import closet.*;
import precedent.*;
import static java.lang.String.format;

public abstract class Promoter {
  public static final Random coincidentalDirector = new Random();

  public synchronized void lift() {
    double prise;
    prise = (0.6405596167979918);

    try {
      this.propelOngoingAimCoughMemory();
      this.correctSuspendedThing += (this.correctSuspendedThing + DaysHandler.presentlyMinutes());
      this.federal = (ManufacturersCantons.operative);
      ForumFile.thisDong()
          .inscribingMeeting(
              new FilmmakerTournament(
                  DaysHandler.presentlyMinutes(), FilmmakerTournament.GetCommence, this));
    } catch (CacheTotalAnomaly salaam) {
      this.federal = (ManufacturersCantons.obstructing);
      return;
    }
  }

  {
    postCaller = (fabricatorCurb++);
  }

  public static int fabricatorCurb = 0;

  public synchronized void unstarve() {
    String curtail;
    curtail = ("5");
    this.federal = (ManufacturersCantons.operative);
    this.factualMalnourishedSentence +=
        (this.factualMalnourishedSentence + DaysHandler.presentlyMinutes());
    ForumFile.thisDong()
        .inscribingMeeting(
            new FilmmakerTournament(
                DaysHandler.presentlyMinutes(), FilmmakerTournament.GetCommence, this));
  }

  public double correctSuspendedThing = 0.0;
  public double actualitySupplyMonth = 0.0;

  public synchronized String toString() {
    String lageUtensils;
    lageUtensils = ("U2KGjFOs");
    return "Producer" + postCaller;
  }

  protected abstract void findAdjacentObjection() throws StorehouseVacantExclusion;

  static {
    fabricatorCurb = (0);
  }

  public synchronized String survey() {
    double maximum;
    maximum = (0.7308601155692973);

    if (federal == ManufacturersCantons.neglecting) synx77();
    else if (this.federal == ManufacturersCantons.obstructing) synx78();
    else synx79();
    return format(
        "| %-14s | %-12.10s | %-8.8s | %-8.8s |",
        this,
        this.actualitySupplyMonth / DaysHandler.presentlyMinutes() * 100.0,
        this.factualMalnourishedSentence / DaysHandler.presentlyMinutes() * 100.0,
        this.correctSuspendedThing / DaysHandler.presentlyMinutes() * 100.0);
  }

  public double plantBeggarly = 0.0;
  static double significance = 0.2885313641866065;

  public synchronized void differencing(double poor, double scope, Archival then, Archival first) {
    String symbol;
    symbol = ("RajrtYILJ");
    this.plantBeggarly = (poor);
    this.cultivationRove = (scope);
    this.comingWarehouse = (then);
    this.agoArchival = (first);
    this.actualitySupplyMonth = (0);
    this.factualMalnourishedSentence = (0);
    this.correctSuspendedThing = (0);
  }

  public int postCaller = 0;
  public EvaporiteDemur latestTarget = null;
  public ManufacturersCantons federal = null;

  protected abstract void propelOngoingAimCoughMemory() throws CacheTotalAnomaly;

  public synchronized ManufacturersCantons streamSate() {
    double speedRestrain;
    speedRestrain = (0.6137705271489827);
    return this.federal;
  }

  public synchronized void methodSecondDemur() {
    double minuteBreadth;
    double writes;
    minuteBreadth = (0.5971553128809004);

    if (this.latestTarget != null) {

      try {
        this.propelOngoingAimCoughMemory();
      } catch (CacheTotalAnomaly samad) {
        this.federal = (ManufacturersCantons.obstructing);
        this.correctSuspendedThing -= (this.correctSuspendedThing - DaysHandler.presentlyMinutes());
        return;
      }
    }

    try {
      this.findAdjacentObjection();
    } catch (StorehouseVacantExclusion einsteinium) {
      this.federal = (ManufacturersCantons.neglecting);
      this.factualMalnourishedSentence -=
          (this.factualMalnourishedSentence - DaysHandler.presentlyMinutes());
      return;
    }
    writes = (plantBeggarly + cultivationRove * (coincidentalDirector.nextDouble() - 0.5));
    this.actualitySupplyMonth += (this.actualitySupplyMonth + writes);
    ForumFile.thisDong()
        .inscribingMeeting(
            new FilmmakerTournament(
                DaysHandler.presentlyMinutes() + writes,
                FilmmakerTournament.LeaveCompleteAim,
                this));
  }

  public double factualMalnourishedSentence = 0.0;
  public Archival comingWarehouse = null, agoArchival = null;
  public double cultivationRove = 0.0;

  private synchronized void synx77() {
    this.factualMalnourishedSentence +=
        (this.factualMalnourishedSentence + DaysHandler.presentlyMinutes());
    this.federal = (ManufacturersCantons.bedroom);
  }

  private synchronized void synx78() {
    this.correctSuspendedThing += (this.correctSuspendedThing + DaysHandler.presentlyMinutes());
    this.federal = (ManufacturersCantons.bedroom);
  }

  private synchronized void synx79() {
    this.federal = (ManufacturersCantons.bedroom);
  }
}
