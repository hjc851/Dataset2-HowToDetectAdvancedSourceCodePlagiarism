package producing;

import cultivationBodies.DiamondiferousItems;
import depot.*;

public class ProduceCompleting extends Supplier {

  protected synchronized void payTheOpposes() throws WarehouseOpenDeviation {

    try {
      this.rifeCavil = (this.lateMemory.secondComponent());
    } catch (WarehouseOpenDeviation samad) {
      throw samad;
    }
  }

  public ProduceCompleting(double intend, double graze, Stored former) {
    modulo(intend, graze, null, former);
    this.law = (GrowersCanton.dying);
    this.basket = (new RoundishJoinedLean<DiamondiferousItems>());
  }

  public RoundishJoinedLean<DiamondiferousItems> basket = null;

  protected synchronized void affectAfootElementGiglioWarehousing() {
    this.basket.addLatter(this.rifeCavil);
    this.rifeCavil = (null);
  }
}
