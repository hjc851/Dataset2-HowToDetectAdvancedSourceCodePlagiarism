package producing;

import cultivationBodies.DiamondiferousItems;
import depot.*;

public class ProduceOffset extends Supplier {

  public ProduceOffset(double ungenerous, double rank, Stored soon) {
    modulo(ungenerous, rank, soon, null);
    this.law = (GrowersCanton.employing);
  }

  protected synchronized void payTheOpposes() throws WarehouseOpenDeviation {
    this.rifeCavil = (new DiamondiferousItems());
  }

  protected synchronized void affectAfootElementGiglioWarehousing() throws CachingBroadExemptions {

    try {
      this.aheadWarehousing.extendSubject(this.rifeCavil);
      this.rifeCavil = (null);
    } catch (CachingBroadExemptions cma) {
      throw cma;
    }
  }
}
