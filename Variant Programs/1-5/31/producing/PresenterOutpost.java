package producing;

import depot.*;

public class PresenterOutpost extends Supplier {

  public PresenterOutpost(double stingy, double grade, Stored ahead, Stored successive) {
    modulo(stingy, grade, ahead, successive);
    this.law = (GrowersCanton.dying);
  }

  protected synchronized void payTheOpposes() throws WarehouseOpenDeviation {

    try {
      this.rifeCavil = (this.lateMemory.secondComponent());
    } catch (WarehouseOpenDeviation einsteinium) {
      throw einsteinium;
    }
  }

  protected synchronized void affectAfootElementGiglioWarehousing() throws CachingBroadExemptions {

    try {
      this.aheadWarehousing.extendSubject(this.rifeCavil);
      this.rifeCavil = (null);
    } catch (CachingBroadExemptions e) {
      throw e;
    }
  }
}
