package output;

import producingAim.DepositionalObjet;
import storeroom.*;

public class GrowerDepart extends output.Emitter {

  public GrowerDepart(double beggarly, double browse, Storeroom upcoming) {
    unhide(beggarly, browse, upcoming, null);
    this.national = (ProductionCountry.doing);
  }

  protected synchronized void awardedNewMatter() throws DiskRansackedObjection {
    this.topicalResist = (new producingAim.DepositionalObjet());
  }

  protected synchronized void strikeUnderwayOpposeEmapWarehouse() throws StoreAmpleExcepted {

    try {
      this.againRepository.totalElement(this.topicalResist);
      this.topicalResist = (null);
    } catch (storeroom.StoreAmpleExcepted cma) {
      throw cma;
    }
  }
}
