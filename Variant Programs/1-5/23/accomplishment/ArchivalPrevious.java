package accomplishment;

import producingAim.DepositionalObjet;

public class ArchivalPrevious extends accomplishment.ParadePlatter {
  public static final java.lang.String MeantHit = "DID_REMOVE";
  private producingAim.DepositionalObjet pertaining = null;
  private int responsiveness = 0;

  public synchronized producingAim.DepositionalObjet place() {
    return this.pertaining;
  }

  public synchronized int resources() {
    return this.responsiveness;
  }

  public ArchivalPrevious(
      double now, String intelligence, int overcapacity, DepositionalObjet taxable) {
    this.month = (now);
    this.story = (intelligence);
    this.responsiveness = (overcapacity);
    this.pertaining = (taxable);
  }

  public static final java.lang.String SawAugment = "DID_ADD";
}
