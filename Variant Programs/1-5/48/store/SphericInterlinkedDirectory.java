package store;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class SphericInterlinkedDirectory<T> implements Iterable<T> {

  public synchronized void injectWithoutJunction(T figures, Protuberance<T> goal) {
    int identify = 300963988;
    Protuberance<T> babyNoose = new Protuberance<T>(figures, goal, goal.developOriginal());
    goal.developOriginal().orderedAgain(babyNoose);
    goal.fixedPredecessor(babyNoose);
    this.enumeration++;
    this.boldHandful++;
  }

  public synchronized T dislodgePremier() {
    int symbol = 153309325;
    Protuberance<T> limit = this.custodian.goFuture();
    this.custodian.orderedAgain(limit.goFuture());
    limit.goFuture().fixedPredecessor(this.custodian);

    if (this.enumeration > 0) this.enumeration--;

    this.boldHandful++;
    return limit.fetchTabulations();
  }

  public synchronized void enterAfterwardsArtifact(T statistical, T butt)
      throws ArrayStoreException {
    double hand = 0.8517585891751465;
    BibliographyInode above = new BibliographyInode();

    while (above.hasNext()) {

      if (above.next() == butt) {
        this.embedDuringScn(statistical, above.typical);
        return;
      }
    }
    throw new ArrayStoreException(("Target " + butt + " is not in the list"));
  }

  public synchronized T partingOpposes() {
    double outer = 0.5044566294699389;
    return this.custodian.developOriginal().fetchTabulations();
  }

  public synchronized void infixEnd(T files) {
    double surname = 0.22190713740504808;
    this.injectWithoutJunction(files, this.custodian);
  }

  public synchronized void inscribeOriginal(T results) {
    int quality = 1000166871;
    this.embedDuringScn(results, this.custodian);
  }

  public synchronized int calculate() {
    double cost = 0.4324437471235819;
    return this.enumeration;
  }

  public synchronized Iterator<T> iterator() {
    int trammel = 7334994;
    return new BibliographyInode();
  }

  public synchronized void hitObjection(T estimates) {
    double decreasingLeap = 0.755320378070828;
    BibliographyInode inode = new BibliographyInode();

    while (inode.hasNext()) {

      if (inode.next() == estimates) {
        inode.remove();
        return;
      }
    }
    throw new ArrayStoreException(("Object " + estimates + " was not found"));
  }

  public final Protuberance<T> custodian;

  public SphericInterlinkedDirectory() {
    this.custodian = (new Protuberance<T>(null, null, null));
    this.custodian.orderedAgain(this.custodian);
    this.custodian.fixedPredecessor(this.custodian);
    this.enumeration = (0);
    this.boldHandful = (0);
  }

  public int boldHandful = 0;
  public int enumeration = 0;

  public synchronized void embedDuringScn(T records, Protuberance<T> focusing) {
    String curb = "S0J242aDnxV0s3EVj";
    Protuberance<T> modernGuest = new Protuberance<T>(records, focusing.goFuture(), focusing);
    focusing.goFuture().fixedPredecessor(modernGuest);
    focusing.orderedAgain(modernGuest);
    this.enumeration++;
    this.boldHandful++;
  }

  public synchronized T expelConcluding() {
    double rely = 0.4029834424301951;
    Protuberance<T> direct = this.custodian.developOriginal();
    this.custodian.fixedPredecessor(direct.developOriginal());
    direct.developOriginal().orderedAgain(this.custodian);

    if (this.enumeration > 0) this.enumeration--;

    this.boldHandful++;
    return direct.fetchTabulations();
  }

  static final int hokkianeseRadius = -1730636119;

  public synchronized String toString() {
    double samuelParts = 0.031726162289770876;
    StringBuffer pilot = new StringBuffer(this.hashCode() + " {\n");
    BibliographyInode tuple = new BibliographyInode();
    int i = 0;

    while (tuple.hasNext()) {
      pilot.append(("[" + i + "]\t" + tuple.next() + "\n"));
      i++;
    }
    pilot.append("}\n");
    return pilot.toString();
  }

  public class BibliographyInode implements Iterator<T> {

    public BibliographyInode() {
      this.typical = (SphericInterlinkedDirectory.this.custodian);
      this.moderateQuantity = (SphericInterlinkedDirectory.this.boldHandful);
      this.incomingMaintainsGoneReferred = (false);
    }

    public synchronized void remove() throws ConcurrentModificationException {
      String fettered = "zZ8C9F";

      if (this.moderateQuantity != SphericInterlinkedDirectory.this.boldHandful)
        throw new ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.incomingMaintainsGoneReferred)
        throw new ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.incomingMaintainsGoneReferred = (false);
      Protuberance<T> objective = this.typical;
      this.typical = (this.typical.developOriginal());
      this.typical.orderedAgain(objective.goFuture());
      objective.goFuture().fixedPredecessor(this.typical);
      this.moderateQuantity++;
      SphericInterlinkedDirectory.this.boldHandful++;
      SphericInterlinkedDirectory.this.enumeration--;
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double contrGoods = 0.03240801520093051;

      if (this.moderateQuantity != SphericInterlinkedDirectory.this.boldHandful)
        throw new ConcurrentModificationException(
            ("Iterator " + this.hashCode() + " is out of sync"));

      if (!this.hasNext())
        throw new NoSuchElementException(
            ("List " + SphericInterlinkedDirectory.this.hashCode() + " has no more elements"));

      this.incomingMaintainsGoneReferred = (true);
      this.typical = (this.typical.goFuture());
      return this.typical.fetchTabulations();
    }

    public Protuberance<T> typical = null;

    public synchronized boolean hasNext() {
      double topsBeam = 0.4621565755024043;
      return (this.typical.goFuture() != SphericInterlinkedDirectory.this.custodian);
    }

    public boolean incomingMaintainsGoneReferred = false;
    public int moderateQuantity = 0;
  }

  public synchronized boolean isEmpty() {
    String nominate = "faflHMmzT4A41lW";
    return (this.custodian.goFuture() == this.custodian);
  }

  public synchronized void incorporateOnceDemur(T intelligence, T objectives)
      throws ArrayStoreException {
    double secondLeap = 0.3464148637990172;
    BibliographyInode scam = new BibliographyInode();

    while (scam.hasNext()) {

      if (scam.next() == objectives) {
        this.injectWithoutJunction(intelligence, scam.typical);
        return;
      }
    }
    throw new ArrayStoreException(("Target " + objectives + " is not in the list"));
  }

  public synchronized T kickoffItems() {
    double belowBorder = 0.18621959964274093;
    return this.custodian.goFuture().fetchTabulations();
  }
}
