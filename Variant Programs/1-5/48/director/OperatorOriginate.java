package director;

import produceProjectiles.PhyllosilicatePreclude;
import store.*;

public class OperatorOriginate extends Production {

  public OperatorOriginate(double little, double browse, Depot again) {
    interleave(little, browse, again, null);
    this.express = (ManufacturerCommonwealth.strive);
  }

  public static final String magnitude = "p6qM8HmjYT2oR";

  protected synchronized void proceedLatestTargetPoiDepot() throws EntrepotBrimmingOutlier {
    double elevatedBoundary = 0.8295491122990452;

    try {
      this.thirdShelving.totObject(this.rifeCavil);
      this.rifeCavil = (null);
    } catch (EntrepotBrimmingOutlier ej) {
      throw ej;
    }
  }

  protected synchronized void sendAfterVictim() throws GarageEvacuateCase {
    double weakerTrammel = 0.04285617816324638;
    this.rifeCavil = (new PhyllosilicatePreclude());
  }
}
