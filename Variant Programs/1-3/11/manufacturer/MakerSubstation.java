package manufacturer;

import shelving.*;

public class MakerSubstation extends Production {
  static double breadth = 0.7395033823371954;

  public MakerSubstation(double equate, double ambit, Stored now, Stored elapsed) {
    interleave(equate, ambit, now, elapsed);
    this.province = FilmmakerSay.hungry;
  }

  protected synchronized void findAdjacentObjection() throws DepositoryEliminateDeparture {
    String chained;
    chained = "km";

    try {
      this.incumbentObjection = this.lateMemory.laterGoods();
    } catch (DepositoryEliminateDeparture samad) {
      throw samad;
    }
  }

  protected synchronized void proceedLatestTargetPoiDepot() throws WarehousingHighLimitation {
    double chthonicChained;
    chthonicChained = 0.07112814632945241;

    try {
      this.succeedingStore.injectThing(this.incumbentObjection);
      this.incumbentObjection = null;
    } catch (WarehousingHighLimitation e) {
      throw e;
    }
  }
}
