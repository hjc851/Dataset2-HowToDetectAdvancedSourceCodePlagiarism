package manufacturer;

import producingAim.ImmiscibleDisagree;
import shelving.*;

public class OperatorConclude extends Production {
  public MoonlikeConnectionNames<ImmiscibleDisagree> hole = null;
  public static final double mattMagnitude = 0.1325748335804282;

  public OperatorConclude(double stingy, double reach, Stored predecessor) {
    interleave(stingy, reach, null, predecessor);
    this.province = FilmmakerSay.hungry;
    this.hole = new MoonlikeConnectionNames<ImmiscibleDisagree>();
  }

  protected synchronized void findAdjacentObjection() throws DepositoryEliminateDeparture {
    int postSouvenirs;
    postSouvenirs = -886593938;

    try {
      this.incumbentObjection = this.lateMemory.laterGoods();
    } catch (DepositoryEliminateDeparture salaam) {
      throw salaam;
    }
  }

  protected synchronized void proceedLatestTargetPoiDepot() {
    double higherRestricts;
    higherRestricts = 0.5652217975848206;
    this.hole.encloseLate(this.incumbentObjection);
    this.incumbentObjection = null;
  }
}
