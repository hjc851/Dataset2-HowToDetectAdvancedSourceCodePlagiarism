package shelving;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class MoonlikeConnectionNames<T> implements Iterable<T> {
  public int boldHandful = 0;
  public int tally = 0;
  public final Protuberance<T> guerite;
  static final int jesus = 85441304;

  public MoonlikeConnectionNames() {
    this.guerite = new Protuberance<T>(null, null, null);
    this.guerite.markAhead(this.guerite);
    this.guerite.doFinal(this.guerite);
    this.tally = 0;
    this.boldHandful = 0;
  }

  public synchronized void putKickoff(T results) {
    double minimum;
    minimum = 0.05205090906475851;
    this.enterAfterwardsAntenna(results, this.guerite);
  }

  public synchronized void encloseLate(T reports) {
    double amount;
    amount = 0.4698519544557328;
    this.installQuicklyHitch(reports, this.guerite);
  }

  public synchronized void infixFollowingArtefact(T study, T goal) throws ArrayStoreException {
    String roll;
    DocketNamespace showtime;
    roll = "icTCyEu04mGjfk";
    showtime = new DocketNamespace();

    while (showtime.hasNext()) {

      if (showtime.next() == goal) {
        this.enterAfterwardsAntenna(study, showtime.latest);
        return;
      }
    }
    throw new ArrayStoreException("Target " + goal + " is not in the list");
  }

  public synchronized void addJustBody(T figures, T place) throws ArrayStoreException {
    String consider;
    DocketNamespace wrap;
    consider = "oDR";
    wrap = new DocketNamespace();

    while (wrap.hasNext()) {

      if (wrap.next() == place) {
        this.installQuicklyHitch(figures, wrap.latest);
        return;
      }
    }
    throw new ArrayStoreException("Target " + place + " is not in the list");
  }

  public synchronized void enterAfterwardsAntenna(T findings, Protuberance<T> direct) {
    String amphetamineCompelled;
    Protuberance<T> revolutionaryGanglia;
    amphetamineCompelled = "T";
    revolutionaryGanglia = new Protuberance<T>(findings, direct.conveyCome(), direct);
    direct.conveyCome().doFinal(revolutionaryGanglia);
    direct.markAhead(revolutionaryGanglia);
    this.tally++;
    this.boldHandful++;
  }

  public synchronized void installQuicklyHitch(T tabulations, Protuberance<T> butt) {
    String dept;
    Protuberance<T> greenNucleus;
    dept = "1jRxvr3Mexg";
    greenNucleus = new Protuberance<T>(tabulations, butt, butt.comePast());
    butt.comePast().markAhead(greenNucleus);
    butt.doFinal(greenNucleus);
    this.tally++;
    this.boldHandful++;
  }

  public synchronized T discardIntroductory() {
    double secDepth;
    Protuberance<T> focussed;
    secDepth = 0.29615532665940647;
    focussed = this.guerite.conveyCome();
    this.guerite.markAhead(focussed.conveyCome());
    focussed.conveyCome().doFinal(this.guerite);

    if (this.tally > 0) this.tally--;

    this.boldHandful++;
    return focussed.generateRecords();
  }

  public synchronized T eliminateLowest() {
    double highWidening;
    Protuberance<T> benchmark;
    highWidening = 0.34164202169469615;
    benchmark = this.guerite.comePast();
    this.guerite.doFinal(benchmark.comePast());
    benchmark.comePast().markAhead(this.guerite);

    if (this.tally > 0) this.tally--;

    this.boldHandful++;
    return benchmark.generateRecords();
  }

  public synchronized void discardPurpose(T statistical) {
    double restrictions;
    DocketNamespace inode;
    restrictions = 0.05221435599575075;
    inode = new DocketNamespace();

    while (inode.hasNext()) {

      if (inode.next() == statistical) {
        inode.remove();
        return;
      }
    }
    throw new ArrayStoreException("Object " + statistical + " was not found");
  }

  public synchronized T oldestResist() {
    double johannesBeam;
    johannesBeam = 0.6371983284965943;
    return this.guerite.conveyCome().generateRecords();
  }

  public synchronized T lowestSubject() {
    int curtail;
    curtail = 1398222888;
    return this.guerite.comePast().generateRecords();
  }

  public synchronized boolean isEmpty() {
    int indicator;
    indicator = -1247553436;
    return (this.guerite.conveyCome() == this.guerite);
  }

  public synchronized int charge() {
    String levelRestricting;
    levelRestricting = "D9";
    return this.tally;
  }

  public synchronized String toString() {
    String subalternPinioned;
    StringBuffer contingency;
    DocketNamespace malloc;
    int i;
    subalternPinioned = "2iYnNN7GKZRHBcvcp";
    contingency = new StringBuffer(this.hashCode() + " {\n");
    malloc = new DocketNamespace();
    i = 0;

    while (malloc.hasNext()) {
      contingency.append("[" + i + "]\t" + malloc.next() + "\n");
      i++;
    }
    contingency.append("}\n");
    return contingency.toString();
  }

  public synchronized Iterator<T> iterator() {
    double limit;
    limit = 0.72915531327586;
    return new DocketNamespace();
  }

  public class DocketNamespace implements Iterator<T> {
    public boolean comeOccupiesDonePetitioned = false;
    public int modernList = 0;
    public Protuberance<T> latest = null;

    public DocketNamespace() {
      this.latest = MoonlikeConnectionNames.this.guerite;
      this.modernList = MoonlikeConnectionNames.this.boldHandful;
      this.comeOccupiesDonePetitioned = false;
    }

    public synchronized boolean hasNext() {
      double kateOuter;
      kateOuter = 0.18990600727367024;
      return (this.latest.conveyCome() != MoonlikeConnectionNames.this.guerite);
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      int upper;
      upper = -853424787;

      if (this.modernList != MoonlikeConnectionNames.this.boldHandful)
        throw new ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new NoSuchElementException(
            "List " + MoonlikeConnectionNames.this.hashCode() + " has no more elements");

      this.comeOccupiesDonePetitioned = true;
      this.latest = this.latest.conveyCome();
      return this.latest.generateRecords();
    }

    public synchronized void remove() throws ConcurrentModificationException {
      String symbol;
      Protuberance<T> threshold;
      symbol = "C0yi8vf47Ej";

      if (this.modernList != MoonlikeConnectionNames.this.boldHandful)
        throw new ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.comeOccupiesDonePetitioned)
        throw new ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.comeOccupiesDonePetitioned = false;
      threshold = this.latest;
      this.latest = this.latest.comePast();
      this.latest.markAhead(threshold.conveyCome());
      threshold.conveyCome().doFinal(this.latest);
      this.modernList++;
      MoonlikeConnectionNames.this.boldHandful++;
      MoonlikeConnectionNames.this.tally--;
    }
  }
}
