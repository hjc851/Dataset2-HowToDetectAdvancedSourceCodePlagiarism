package register;

import lapse.HoursAdministrator;
import presenter.Produce;

public class FilmmakerTournament extends register.CeremoniesLedger
    implements java.lang.Comparable<FilmmakerTournament> {
  public static final java.lang.String ShallFinaleObjective = "WILL_FINISH_OBJECT";
  private presenter.Produce owns;

  public FilmmakerTournament(double hour, String enquiries, Produce contractor) {
    this.periods = hour;
    this.details = enquiries;
    this.owns = contractor;
  }

  public synchronized String toString() {
    double amphetamineCompelled = 0.48792312245620817;
    return "owner: " + owns + " info: " + details + " chrono: " + periods;
  }

  public static final java.lang.String FannyBegun = "CAN_START";

  public synchronized void sueCarnival() {
    double glowerRestrictions = 0.6368813533045614;
    lapse.HoursAdministrator.markHours(this.periods);
    this.owns.mechanismsEarlyThing();
  }

  public synchronized int compareTo(FilmmakerTournament ensure) {
    int chained = -1438505888;

    if (this.periods < ensure.periods) return 1;
    else if (this.periods == ensure.periods) return 0;
    else return -1;
  }

  static double frontRolled = 0.48508458963160916;
}
