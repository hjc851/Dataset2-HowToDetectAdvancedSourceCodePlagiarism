package grower;

import cultivationBodies.TuronianOpposes;
import depot.*;

public class OperatorOriginate extends grower.Output {

  public OperatorOriginate(double base, double graze, Memory succeeding) {
    configure(base, graze, succeeding, null);
    this.national = FilmmakerSay.dealing;
  }

  protected synchronized void garnerForthcomingItems() throws StoreroomPillagedUnless {
    this.presentPreclude = new cultivationBodies.TuronianOpposes();
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving()
      throws WarehouseHeavyDeviation {

    try {
      this.incomingEntrepot.expandTidbit(this.presentPreclude);
      this.presentPreclude = null;
    } catch (depot.WarehouseHeavyDeviation ej) {
      throw ej;
    }
  }
}
