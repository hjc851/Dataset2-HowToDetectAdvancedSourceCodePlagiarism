package grower;

import cultivationBodies.TuronianOpposes;
import depot.*;

public class MakerConclusion extends grower.Output {
  public depot.RoundAlignedListings<TuronianOpposes> shovel;

  public MakerConclusion(double signify, double stray, Memory first) {
    configure(signify, stray, null, first);
    this.national = FilmmakerSay.emaciated;
    this.shovel = new depot.RoundAlignedListings<TuronianOpposes>();
  }

  protected synchronized void garnerForthcomingItems() throws StoreroomPillagedUnless {

    try {
      this.presentPreclude = this.originalSpace.soonArticle();
    } catch (depot.StoreroomPillagedUnless samad) {
      throw samad;
    }
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving() {
    this.shovel.insetWorst(this.presentPreclude);
    this.presentPreclude = null;
  }
}
