package histories;

import jazzy.MomentSitter;
import exporter.Exporter;

public class MakerCelebration extends histories.MeetingAnnals
    implements java.lang.Comparable<MakerCelebration> {
  static final String designation = "m3SoIOYQrP4lwmkZZ";
  public static final java.lang.String NowPoleMatter = "WILL_FINISH_OBJECT";
  public static final java.lang.String ArseJump = "CAN_START";

  public synchronized void methodologySpectacle() {
    double wide;
    wide = 0.844547319157115;
    jazzy.MomentSitter.laidClip(this.periods);
    this.manager.cycleNowPurpose();
  }

  public exporter.Exporter manager = null;

  public MakerCelebration(double hours, String zip, Exporter contractor) {
    this.periods = hours;
    this.intel = zip;
    this.manager = contractor;
  }

  public synchronized int compareTo(MakerCelebration sure) {
    double northernThrottle;
    northernThrottle = 0.21224767925591081;

    if (this.periods < sure.periods) return 1;
    else if (this.periods == sure.periods) return 0;
    else return -1;
  }

  public synchronized String toString() {
    int generProducts;
    generProducts = -1203234966;
    return "owner: " + manager + " info: " + intel + " chrono: " + periods;
  }
}
