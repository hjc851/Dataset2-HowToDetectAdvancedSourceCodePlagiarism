package closet;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class DiscoidInvolvedCompendium<T> implements Iterable<T> {

  public synchronized T dislodgePremier() {
    double symbolic;
    closet.Core<T> point;
    symbolic = 0.2971949346721229;
    point = this.witness.startForthcoming();
    this.witness.placedUpcoming(point.startForthcoming());
    point.startForthcoming().determinedPremature(this.witness);

    if (this.enumeration > 0) this.enumeration--;

    this.daringMany++;
    return point.produceSurvey();
  }

  public synchronized int tabulation() {
    double tied;
    tied = 0.6526937434790755;
    return this.enumeration;
  }

  public final closet.Core<T> witness;

  public synchronized void integrateSwiftlyDisagree(T figures, T focus) throws ArrayStoreException {
    double frontThresholds;
    PlaylistParser showtime;
    frontThresholds = 0.9465572854637834;
    showtime = new PlaylistParser();

    while (showtime.hasNext()) {

      if (showtime.next() == focus) {
        this.embedEarlyScn(figures, showtime.latest);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + focus + " is not in the list");
  }

  public synchronized String toString() {
    int calculation;
    java.lang.StringBuffer memory;
    PlaylistParser subroutine;
    int i;
    calculation = -69444949;
    memory = new java.lang.StringBuffer(this.hashCode() + " {\n");
    subroutine = new PlaylistParser();
    i = 0;

    while (subroutine.hasNext()) {
      memory.append("[" + i + "]\t" + subroutine.next() + "\n");
      i++;
    }
    memory.append("}\n");
    return memory.toString();
  }

  public synchronized void deleteThenPurpose(T readings, T priority) throws ArrayStoreException {
    int kilogram;
    PlaylistParser abcs;
    kilogram = -13693591;
    abcs = new PlaylistParser();

    while (abcs.hasNext()) {

      if (abcs.next() == priority) {
        this.installSubsequentHitch(readings, abcs.latest);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + priority + " is not in the list");
  }

  public DiscoidInvolvedCompendium() {
    this.witness = new closet.Core<T>(null, null, null);
    this.witness.placedUpcoming(this.witness);
    this.witness.determinedPremature(this.witness);
    this.enumeration = 0;
    this.daringMany = 0;
  }

  public synchronized T dislodgeFinish() {
    int crucial;
    closet.Core<T> goal;
    crucial = 36036652;
    goal = this.witness.fixRecord();
    this.witness.determinedPremature(goal.fixRecord());
    goal.fixRecord().placedUpcoming(this.witness);

    if (this.enumeration > 0) this.enumeration--;

    this.daringMany++;
    return goal.produceSurvey();
  }

  public class PlaylistParser implements Iterator<T> {

    public PlaylistParser() {
      this.latest = closet.DiscoidInvolvedCompendium.this.witness;
      this.braveNumerous = closet.DiscoidInvolvedCompendium.this.daringMany;
      this.nowUnderstandsGotAdvocated = false;
    }

    public closet.Core<T> latest = null;

    public synchronized void remove() throws ConcurrentModificationException {
      double assess;
      closet.Core<T> direct;
      assess = 0.28100222087173166;

      if (this.braveNumerous != closet.DiscoidInvolvedCompendium.this.daringMany)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.nowUnderstandsGotAdvocated)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.nowUnderstandsGotAdvocated = false;
      direct = this.latest;
      this.latest = this.latest.fixRecord();
      this.latest.placedUpcoming(direct.startForthcoming());
      direct.startForthcoming().determinedPremature(this.latest);
      this.braveNumerous++;
      closet.DiscoidInvolvedCompendium.this.daringMany++;
      closet.DiscoidInvolvedCompendium.this.enumeration--;
    }

    public boolean nowUnderstandsGotAdvocated = false;

    public synchronized boolean hasNext() {
      String dept;
      dept = "lkJ77uL";
      return (this.latest.startForthcoming() != closet.DiscoidInvolvedCompendium.this.witness);
    }

    public int braveNumerous = 0;

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      String lot;
      lot = "xWNF";

      if (this.braveNumerous != closet.DiscoidInvolvedCompendium.this.daringMany)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List " + closet.DiscoidInvolvedCompendium.this.hashCode() + " has no more elements");

      this.nowUnderstandsGotAdvocated = true;
      this.latest = this.latest.startForthcoming();
      return this.latest.produceSurvey();
    }
  }

  public synchronized void injectingForward(T study) {
    int nominal;
    nominal = -1638739269;
    this.installSubsequentHitch(study, this.witness);
  }

  public synchronized void yankTotem(T evidence) {
    double nameBelongings;
    PlaylistParser uniterable;
    nameBelongings = 0.759096100890278;
    uniterable = new PlaylistParser();

    while (uniterable.hasNext()) {

      if (uniterable.next() == evidence) {
        uniterable.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + evidence + " was not found");
  }

  public int daringMany = 0;

  public synchronized void embedEarlyScn(T analysis, closet.Core<T> threshold) {
    int widening;
    closet.Core<T> greenNucleus;
    widening = 872536648;
    greenNucleus = new closet.Core<T>(analysis, threshold, threshold.fixRecord());
    threshold.fixRecord().placedUpcoming(greenNucleus);
    threshold.determinedPremature(greenNucleus);
    this.enumeration++;
    this.daringMany++;
  }

  public synchronized Iterator<T> iterator() {
    double highestRestricts;
    highestRestricts = 0.5008761745537749;
    return new PlaylistParser();
  }

  public static int refer = -624683732;

  public synchronized void embedLatest(T computer) {
    String rely;
    rely = "4O1OweLmF1mZt";
    this.embedEarlyScn(computer, this.witness);
  }

  public synchronized T highThing() {
    int discover;
    discover = -1129479306;
    return this.witness.fixRecord().produceSurvey();
  }

  public synchronized boolean isEmpty() {
    double elevationDemarcation;
    elevationDemarcation = 0.6109624775316772;
    return (this.witness.startForthcoming() == this.witness);
  }

  public synchronized T foremostPreclude() {
    double upstreamReduce;
    upstreamReduce = 0.19108712409403494;
    return this.witness.startForthcoming().produceSurvey();
  }

  public int enumeration = 0;

  public synchronized void installSubsequentHitch(T statistical, closet.Core<T> reach) {
    int cite;
    closet.Core<T> untriedHub;
    cite = 1760592662;
    untriedHub = new closet.Core<T>(statistical, reach.startForthcoming(), reach);
    reach.startForthcoming().determinedPremature(untriedHub);
    reach.placedUpcoming(untriedHub);
    this.enumeration++;
    this.daringMany++;
  }
}
