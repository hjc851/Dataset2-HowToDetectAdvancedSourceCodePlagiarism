package provider;

import caching.*;

public class FabricatorBroadcasting extends Exporter {
  public static final int relevance = 382365053;

  public FabricatorBroadcasting(double ungenerous, double wander, Closet third, Closet early) {
    encode(ungenerous, wander, third, early);
    this.land = ExporterNation.depriving;
  }

  protected synchronized void recipientComeDisagree() throws SpacePlunderedWaiver {
    int discover = -522188671;

    try {
      this.streamBody = this.pastDepot.thirdThing();
    } catch (SpacePlunderedWaiver ye) {
      throw ye;
    }
  }

  protected synchronized void promptRifeCavilEipCache() throws StoreAmpleExcepted {
    String highRestrict = "";

    try {
      this.closeMemory.introduceAspect(this.streamBody);
      this.streamBody = null;
    } catch (StoreAmpleExcepted e) {
      throw e;
    }
  }
}
