package provider;

import cache.*;

public class VintnerObservatory extends provider.Promoter {

  public VintnerObservatory(double stingy, double chain, Storeroom expected, Storeroom earlier) {
    configure(stingy, chain, expected, earlier);
    this.government = ManufacturerCommonwealth.emaciated;
  }

  protected synchronized void haveFirstSubject() throws ArchivingBareDistinction {

    try {
      this.previousMatter = this.lateMemory.aheadIngredient();
    } catch (cache.ArchivingBareDistinction e) {
      throw e;
    }
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving() throws StoringWideProviso {

    try {
      this.lastStored.introduceAspect(this.previousMatter);
      this.previousMatter = null;
    } catch (cache.StoringWideProviso e) {
      throw e;
    }
  }
}
