package provider;

import manufacturingPieces.PhyllosilicatePreclude;
import cache.*;

public class FabricatorGet extends provider.Promoter {
  private cache.AnnulatedAffiliatedPlaylist<PhyllosilicatePreclude> shovel = null;

  public FabricatorGet(double nasty, double straddle, Storeroom original) {
    configure(nasty, straddle, null, original);
    this.government = ManufacturerCommonwealth.emaciated;
    this.shovel = new cache.AnnulatedAffiliatedPlaylist<PhyllosilicatePreclude>();
  }

  protected synchronized void haveFirstSubject() throws ArchivingBareDistinction {

    try {
      this.previousMatter = this.lateMemory.aheadIngredient();
    } catch (cache.ArchivingBareDistinction ye) {
      throw ye;
    }
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving() {
    this.shovel.inscribingHigh(this.previousMatter);
    this.previousMatter = null;
  }
}
