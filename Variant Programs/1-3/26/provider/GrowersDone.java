package provider;

import industrializationInstallations.ClasticVictim;
import shelving.*;

public class GrowersDone extends Fabricator {

  protected synchronized void awardedNewMatter() throws GarageEvacuateCase {

    try {
      this.liveArtefact = this.precedingStorehouse.closeToken();
    } catch (GarageEvacuateCase ye) {
      throw ye;
    }
  }

  public GrowersDone(double awful, double rank, Stowage recent) {
    init(awful, rank, null, recent);
    this.territory = ExporterNation.pizza;
    this.pail = new ThrowawayLinkageRegistry<ClasticVictim>();
  }

  protected synchronized void impressFlowObjetMouStowage() {
    this.pail.injectingDying(this.liveArtefact);
    this.liveArtefact = null;
  }

  public ThrowawayLinkageRegistry<ClasticVictim> pail;
}
