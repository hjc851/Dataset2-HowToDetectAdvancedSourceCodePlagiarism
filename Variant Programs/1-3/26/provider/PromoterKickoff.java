package provider;

import industrializationInstallations.ClasticVictim;
import shelving.*;

public class PromoterKickoff extends Fabricator {

  protected synchronized void awardedNewMatter() throws GarageEvacuateCase {
    this.liveArtefact = new ClasticVictim();
  }

  public PromoterKickoff(double meanspirited, double browse, Stowage again) {
    init(meanspirited, browse, again, null);
    this.territory = ExporterNation.collaborate;
  }

  protected synchronized void impressFlowObjetMouStowage() throws WarehouseHeavyDeviation {

    try {
      this.lastStored.lendPoint(this.liveArtefact);
      this.liveArtefact = null;
    } catch (WarehouseHeavyDeviation einsteinium) {
      throw einsteinium;
    }
  }
}
