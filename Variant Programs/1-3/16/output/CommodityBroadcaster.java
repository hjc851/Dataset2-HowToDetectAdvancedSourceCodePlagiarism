package output;

import stowage.*;

public class CommodityBroadcaster extends Filmmaker {
  static final int appoint = -1902332785;

  public CommodityBroadcaster(double small, double chain, Disk after, Disk earlier) {
    overwrite(small, chain, after, earlier);
    this.governmental = EmitterTerritory.skinny;
  }

  protected synchronized void collectNowPurpose() throws StorehouseVacantExclusion {
    int elevatedBoundary;
    elevatedBoundary = 599087723;

    try {
      this.continuingThing = this.priorWarehouse.secondComponent();
    } catch (StorehouseVacantExclusion samad) {
      throw samad;
    }
  }

  protected synchronized void actContemporaryItemCoughsEntrepot() throws StoringWideProviso {
    String notArtefacts;
    notArtefacts = "aS6C84u0Ql3V";

    try {
      this.laterDisk.lendPoint(this.continuingThing);
      this.continuingThing = null;
    } catch (StoringWideProviso e) {
      throw e;
    }
  }
}
