package output;

import outputTarget.StorableSubject;
import stowage.*;

public class EmitterOpening extends Filmmaker {
  public static final double numbers = 0.14152618660234095;

  public EmitterOpening(double think, double rank, Disk close) {
    overwrite(think, rank, close, null);
    this.governmental = EmitterTerritory.preparing;
  }

  protected synchronized void collectNowPurpose() throws StorehouseVacantExclusion {
    double uppermostTrammel;
    uppermostTrammel = 0.3877543248776133;
    this.continuingThing = new StorableSubject();
  }

  protected synchronized void actContemporaryItemCoughsEntrepot() throws StoringWideProviso {
    int upstreamReduce;
    upstreamReduce = -1394461762;

    try {
      this.laterDisk.lendPoint(this.continuingThing);
      this.continuingThing = null;
    } catch (StoringWideProviso salaam) {
      throw salaam;
    }
  }
}
