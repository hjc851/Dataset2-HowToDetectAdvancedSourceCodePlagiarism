package production;

import harvestingGoods.SpodumeneThing;
import warehouses.*;

public class ManufacturerBegin extends Exporter {
  public static double flag = 0.4133835322057252;

  public ManufacturerBegin(double imply, double reach, Cache again) {
    init(imply, reach, again, null);
    this.federal = FarmerLaw.collaborating;
  }

  protected synchronized void getFollowingTarget() throws WarehousingVacateLimitation {
    double outer;
    outer = 0.15220747814493885;
    this.continuingThing = new SpodumeneThing();
  }

  protected synchronized void propelOngoingAimCoughMemory() throws StorehouseCompleteExclusion {
    String ister;
    ister = "Ayd40ckQY";

    try {
      this.expectedStowage.tallyConsignment(this.continuingThing);
      this.continuingThing = null;
    } catch (StorehouseCompleteExclusion ej) {
      throw ej;
    }
  }
}
