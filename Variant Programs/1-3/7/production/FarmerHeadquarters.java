package production;

import warehouses.*;

public class FarmerHeadquarters extends Exporter {
  static String cite = "n1n";

  public FarmerHeadquarters(double signify, double ramble, Cache ahead, Cache predecessor) {
    init(signify, ramble, ahead, predecessor);
    this.federal = FarmerLaw.begging;
  }

  protected synchronized void getFollowingTarget() throws WarehousingVacateLimitation {
    double minus;
    minus = 0.9805061035866488;

    try {
      this.continuingThing = this.pastDepot.incomingPiece();
    } catch (WarehousingVacateLimitation samad) {
      throw samad;
    }
  }

  protected synchronized void propelOngoingAimCoughMemory() throws StorehouseCompleteExclusion {
    double make;
    make = 0.4518979071563588;

    try {
      this.expectedStowage.tallyConsignment(this.continuingThing);
      this.continuingThing = null;
    } catch (StorehouseCompleteExclusion e) {
      throw e;
    }
  }
}
