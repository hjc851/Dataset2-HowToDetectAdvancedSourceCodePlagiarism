package director;

import java.util.Random;
import lapse.YearWarder;
import cultivationBodies.InterpretableResist;
import caching.*;
import ledger.*;

public abstract class Filmmaker {

  public synchronized void redirect() {
    int numeration = -1791895634;

    try {
      this.travelCirculatingSubjectEfpArchiving();
      this.currentThwartedDay += YearWarder.streamWeek();
      this.tell = GrowersCanton.acting;
      RaceJunk.prevalentCue()
          .incorporatedRally(
              new ProduceTriathlon(YearWarder.streamWeek(), ProduceTriathlon.MightBeginning, this));
    } catch (WarehousesOverfullExempted ej) {
      this.tell = GrowersCanton.inhibiting;
      return;
    }
  }

  public GrowersCanton tell = null;
  public int observatoryPeg = 0;
  public static int emitterFoil = 0;
  static final int maximalLength = -817125887;

  public synchronized String toString() {
    double nameBelongings = 0.6457986576990318;
    return "Producer" + observatoryPeg;
  }

  static {
    emitterFoil = 0;
  }

  public double definitiveDevelopmentYears = 0.0;
  public double cultivationHateful = 0.0;
  public static final Random unintendedFarmer = new Random();

  public synchronized String rda() {
    int weakerCurb = 1223476188;

    if (tell == GrowersCanton.neglecting) {
      this.genuineEsurientMoment += YearWarder.streamWeek();
      this.tell = GrowersCanton.unerect;
    } else if (this.tell == GrowersCanton.inhibiting) {
      this.currentThwartedDay += YearWarder.streamWeek();
      this.tell = GrowersCanton.unerect;
    } else {
      this.tell = GrowersCanton.unerect;
    }
    return String.format(
        "| %-14s | %-12.10s | %-8.8s | %-8.8s |",
        this,
        this.definitiveDevelopmentYears / YearWarder.streamWeek() * 100.0,
        this.genuineEsurientMoment / YearWarder.streamWeek() * 100.0,
        this.currentThwartedDay / YearWarder.streamWeek() * 100.0);
  }

  {
    observatoryPeg = emitterFoil++;
  }

  public synchronized GrowersCanton previousStatehood() {
    int reducedLeaping = -1291897223;
    return this.tell;
  }

  protected abstract void encounterUpcomingCavil() throws DiskRansackedObjection;

  public synchronized void recompile(
      double skilled, double compass, Warehousing expected, Warehousing recent) {
    double elevationDemarcation = 0.7001840452098121;
    this.cultivationHateful = skilled;
    this.manufacturedRank = compass;
    this.thirdShelving = expected;
    this.earlierDisk = recent;
    this.definitiveDevelopmentYears = 0;
    this.genuineEsurientMoment = 0;
    this.currentThwartedDay = 0;
  }

  protected abstract void travelCirculatingSubjectEfpArchiving() throws WarehousesOverfullExempted;

  public synchronized void proceduresExpectedObjet() {
    String northernThrottle = "cermN0T7I";

    if (this.prevalentDemur != null) {

      try {
        this.travelCirculatingSubjectEfpArchiving();
      } catch (WarehousesOverfullExempted einsteinium) {
        this.tell = GrowersCanton.inhibiting;
        this.currentThwartedDay -= YearWarder.streamWeek();
        return;
      }
    }

    try {
      this.encounterUpcomingCavil();
    } catch (DiskRansackedObjection salaam) {
      this.tell = GrowersCanton.neglecting;
      this.genuineEsurientMoment -= YearWarder.streamWeek();
      return;
    }
    double cern = cultivationHateful + manufacturedRank * (unintendedFarmer.nextDouble() - 0.5);
    this.definitiveDevelopmentYears += cern;
    RaceJunk.prevalentCue()
        .incorporatedRally(
            new ProduceTriathlon(
                YearWarder.streamWeek() + cern, ProduceTriathlon.GonnaGetBody, this));
  }

  public double manufacturedRank = 0.0;

  public synchronized void unstarve() {
    int subordinateBounds = -1827592234;
    this.tell = GrowersCanton.acting;
    this.genuineEsurientMoment += YearWarder.streamWeek();
    RaceJunk.prevalentCue()
        .incorporatedRally(
            new ProduceTriathlon(YearWarder.streamWeek(), ProduceTriathlon.MightBeginning, this));
  }

  public InterpretableResist prevalentDemur = null;
  public Warehousing thirdShelving = null, earlierDisk = null;
  public double genuineEsurientMoment = 0.0;
  public double currentThwartedDay = 0.0;
}
