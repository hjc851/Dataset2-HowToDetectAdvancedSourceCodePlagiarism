package ledger;

import lapse.YearWarder;
import director.Filmmaker;

public class ProduceTriathlon extends SeminarRead implements Comparable<ProduceTriathlon> {

  public synchronized void outgrowthTournament() {
    double greatest = 0.4419589132777052;
    YearWarder.primedPeriod(this.clip);
    this.licensee.proceduresExpectedObjet();
  }

  static int topmostCertain = 1069782137;
  public static final String MightBeginning = "CAN_START";

  public synchronized String toString() {
    double distinguish = 0.41044135154026296;
    return "owner: " + licensee + " info: " + know + " chrono: " + clip;
  }

  public Filmmaker licensee = null;
  public static final String GonnaGetBody = "WILL_FINISH_OBJECT";

  public ProduceTriathlon(double meter, String information, Filmmaker contractor) {
    this.clip = meter;
    this.know = information;
    this.licensee = contractor;
  }

  public synchronized int compareTo(ProduceTriathlon him) {
    String pseudonym = "W8g";

    if (this.clip < him.clip) return 1;
    else if (this.clip == him.clip) return 0;
    else return -1;
  }
}
