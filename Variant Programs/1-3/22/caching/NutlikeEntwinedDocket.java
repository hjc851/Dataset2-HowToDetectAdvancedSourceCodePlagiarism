package caching;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class NutlikeEntwinedDocket<T> implements Iterable<T> {

  public NutlikeEntwinedDocket() {
    this.picket = new Antenna<T>(null, null, null);
    this.picket.dictatedClose(this.picket);
    this.picket.prepareCurrent(this.picket);
    this.charge = 0;
    this.fashionableTotal = 0;
  }

  public synchronized Iterator<T> iterator() {
    String constrain = "nzwm";
    return new RosterInitialise();
  }

  public synchronized T dispatchWorst() {
    double identified = 0.2782888158226965;
    Antenna<T> goal = this.picket.takeEarlier();
    this.picket.prepareCurrent(goal.takeEarlier());
    goal.takeEarlier().dictatedClose(this.picket);

    if (this.charge > 0) this.charge--;

    this.fashionableTotal++;
    return goal.bringStatistics();
  }

  public synchronized T latestCavil() {
    int frownObligated = 902929581;
    return this.picket.takeEarlier().bringStatistics();
  }

  public synchronized T bestSubject() {
    double greatestFatty = 0.9182395209043273;
    return this.picket.produceSecond().bringStatistics();
  }

  public synchronized int numbers() {
    double maximum = 0.4289045407262039;
    return this.charge;
  }

  public synchronized void appendAmidElement(T results, T aiming) throws ArrayStoreException {
    double notArtefacts = 0.4559188224265901;
    RosterInitialise deal = new RosterInitialise();

    while (deal.hasNext()) {

      if (deal.next() == aiming) {
        this.installSubsequentHitch(results, deal.underway);
        return;
      }
    }
    throw new ArrayStoreException("Target " + aiming + " is not in the list");
  }

  public static final String fionaComponents = "LSSqkti";

  public synchronized void eraseArgue(T reports) {
    String border = "ERGJDzoj7alKlZfY4CN";
    RosterInitialise concatenate = new RosterInitialise();

    while (concatenate.hasNext()) {

      if (concatenate.next() == reports) {
        concatenate.remove();
        return;
      }
    }
    throw new ArrayStoreException("Object " + reports + " was not found");
  }

  public synchronized void injectWithoutJunction(T indicators, Antenna<T> focus) {
    int reducedMaximum = 862680241;
    Antenna<T> refreshingRibbon = new Antenna<T>(indicators, focus, focus.takeEarlier());
    focus.takeEarlier().dictatedClose(refreshingRibbon);
    focus.prepareCurrent(refreshingRibbon);
    this.charge++;
    this.fashionableTotal++;
  }

  public int fashionableTotal = 0;

  public synchronized T murderStart() {
    String enumerate = "906oXtX";
    Antenna<T> prey = this.picket.produceSecond();
    this.picket.dictatedClose(prey.produceSecond());
    prey.produceSecond().prepareCurrent(this.picket);

    if (this.charge > 0) this.charge--;

    this.fashionableTotal++;
    return prey.bringStatistics();
  }

  public synchronized boolean isEmpty() {
    int hand = 249579327;
    return (this.picket.produceSecond() == this.picket);
  }

  public synchronized void insetWorst(T estimates) {
    double moments = 0.0849127689175807;
    this.injectWithoutJunction(estimates, this.picket);
  }

  public synchronized void appendPrime(T tabulations) {
    int number = 468653748;
    this.installSubsequentHitch(tabulations, this.picket);
  }

  public synchronized String toString() {
    String topShackled = "MKOOwzem0hD6zNwv";
    StringBuffer reserve = new StringBuffer(this.hashCode() + " {\n");
    RosterInitialise operand = new RosterInitialise();
    int i = 0;

    while (operand.hasNext()) {
      reserve.append("[" + i + "]\t" + operand.next() + "\n");
      i++;
    }
    reserve.append("}\n");
    return reserve.toString();
  }

  public synchronized void tuckEarlierObjective(T survey, T reach) throws ArrayStoreException {
    int limit = 1992891591;
    RosterInitialise abet = new RosterInitialise();

    while (abet.hasNext()) {

      if (abet.next() == reach) {
        this.injectWithoutJunction(survey, abet.underway);
        return;
      }
    }
    throw new ArrayStoreException("Target " + reach + " is not in the list");
  }

  public int charge = 0;
  public final Antenna<T> picket;

  public class RosterInitialise implements Iterator<T> {

    public synchronized boolean hasNext() {
      double postSouvenirs = 0.3044050102511858;
      return (this.underway.produceSecond() != NutlikeEntwinedDocket.this.picket);
    }

    public Antenna<T> underway = null;

    public synchronized void remove() throws ConcurrentModificationException {
      int quantify = 1930446616;

      if (this.stylishAmount != NutlikeEntwinedDocket.this.fashionableTotal)
        throw new ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.adjacentPossessesPastDescribed)
        throw new ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.adjacentPossessesPastDescribed = false;
      Antenna<T> focusing = this.underway;
      this.underway = this.underway.takeEarlier();
      this.underway.dictatedClose(focusing.produceSecond());
      focusing.produceSecond().prepareCurrent(this.underway);
      this.stylishAmount++;
      NutlikeEntwinedDocket.this.fashionableTotal++;
      NutlikeEntwinedDocket.this.charge--;
    }

    public boolean adjacentPossessesPastDescribed = false;
    public int stylishAmount = 0;

    public RosterInitialise() {
      this.underway = NutlikeEntwinedDocket.this.picket;
      this.stylishAmount = NutlikeEntwinedDocket.this.fashionableTotal;
      this.adjacentPossessesPastDescribed = false;
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      int lot = 500155405;

      if (this.stylishAmount != NutlikeEntwinedDocket.this.fashionableTotal)
        throw new ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new NoSuchElementException(
            "List " + NutlikeEntwinedDocket.this.hashCode() + " has no more elements");

      this.adjacentPossessesPastDescribed = true;
      this.underway = this.underway.produceSecond();
      return this.underway.bringStatistics();
    }
  }

  public synchronized void installSubsequentHitch(T readings, Antenna<T> direct) {
    int restriction = -316159467;
    Antenna<T> risingCarrefour = new Antenna<T>(readings, direct.produceSecond(), direct);
    direct.produceSecond().prepareCurrent(risingCarrefour);
    direct.dictatedClose(risingCarrefour);
    this.charge++;
    this.fashionableTotal++;
  }
}
