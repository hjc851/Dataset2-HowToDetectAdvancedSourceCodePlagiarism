package promoter;

import manufacturingPieces.PannonianMatter;
import space.*;

public class FabricatorStartle extends promoter.Manufacturers {

  public FabricatorStartle(double hateful, double wander, Warehouse adjacent) {
    differencing(hateful, wander, adjacent, null);
    this.nation = MakerLand.preparing;
  }

  protected synchronized void garnerForthcomingItems() throws EntrepotHollowOutlier {
    this.theVictim = new manufacturingPieces.PannonianMatter();
  }

  protected synchronized void movementLiveArtefactCssStored() throws ClosetImbuedDispensation {

    try {
      this.thirdShelving.introduceAspect(this.theVictim);
      this.theVictim = null;
    } catch (space.ClosetImbuedDispensation samad) {
      throw samad;
    }
  }
}
