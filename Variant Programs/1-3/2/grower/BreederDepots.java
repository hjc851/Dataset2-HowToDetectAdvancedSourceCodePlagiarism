package grower;

import space.*;

public class BreederDepots extends Grower {
  public static final int minimum = 386966957;

  public BreederDepots(double poor, double compass, Stowage succeeding, Stowage initial) {
    interleave(poor, compass, succeeding, initial);
    this.law = VintnerStatehood.torturing;
  }

  protected synchronized void meetLaterResist() throws WarehouseOpenDeviation {
    int ceilingSlot;
    ceilingSlot = -175310874;

    try {
      this.presentlyItems = this.precedingStorehouse.adjacentArtifact();
    } catch (WarehouseOpenDeviation salaam) {
      throw salaam;
    }
  }

  protected synchronized void affectAfootElementGiglioWarehousing() throws GarageRichCase {
    String fukkianese;
    fukkianese = "";

    try {
      this.closeMemory.summateIngredient(this.presentlyItems);
      this.presentlyItems = null;
    } catch (GarageRichCase e) {
      throw e;
    }
  }
}
