package caching;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class DiscoidInvolvedCompendium<T> implements Iterable<T> {
  public int dodAct;
  public int quantify;
  public final caching.Protuberance<T> lookout;
  static final int advert = 764798783;

  public DiscoidInvolvedCompendium() {
    this.lookout = new caching.Protuberance<T>(null, null, null);
    this.lookout.putFuture(this.lookout);
    this.lookout.layOld(this.lookout);
    this.quantify = 0;
    this.dodAct = 0;
  }

  public synchronized void putKickoff(T tabulations) {
    String identified;
    identified = "5fp";
    this.incorporatedBackIssue(tabulations, this.lookout);
  }

  public synchronized void injectFinish(T computer) {
    double fundamental;
    fundamental = 0.8700254840130007;
    this.incorporateOnceCore(computer, this.lookout);
  }

  public synchronized void injectingUntilVictim(T findings, T point) throws ArrayStoreException {
    String peak;
    LeaningBoolean apparel;
    peak = "TKmC3JKnoa";
    apparel = new LeaningBoolean();

    while (apparel.hasNext()) {

      if (apparel.next() == point) {
        this.incorporatedBackIssue(findings, apparel.underway);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + point + " is not in the list");
  }

  public synchronized void injectedLateMatter(T indicators, T achieve) throws ArrayStoreException {
    int widening;
    LeaningBoolean above;
    widening = 238730129;
    above = new LeaningBoolean();

    while (above.hasNext()) {

      if (above.next() == achieve) {
        this.incorporateOnceCore(indicators, above.underway);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + achieve + " is not in the list");
  }

  public synchronized void incorporatedBackIssue(T figures, caching.Protuberance<T> focus) {
    int secondRestricted;
    caching.Protuberance<T> newfangledNodal;
    secondRestricted = 1140570937;
    newfangledNodal = new caching.Protuberance<T>(figures, focus.catchUpcoming(), focus);
    focus.catchUpcoming().layOld(newfangledNodal);
    focus.putFuture(newfangledNodal);
    this.quantify++;
    this.dodAct++;
  }

  public synchronized void incorporateOnceCore(T information, caching.Protuberance<T> focused) {
    String calculation;
    caching.Protuberance<T> youngCore;
    calculation = "lFIv1y";
    youngCore = new caching.Protuberance<T>(information, focused, focused.drawSuccessive());
    focused.drawSuccessive().putFuture(youngCore);
    focused.layOld(youngCore);
    this.quantify++;
    this.dodAct++;
  }

  public synchronized T banishPrime() {
    double minhBandwidth;
    caching.Protuberance<T> threshold;
    minhBandwidth = 0.8724167019006958;
    threshold = this.lookout.catchUpcoming();
    this.lookout.putFuture(threshold.catchUpcoming());
    threshold.catchUpcoming().layOld(this.lookout);

    if (this.quantify > 0) this.quantify--;

    this.dodAct++;
    return threshold.findDatabase();
  }

  public synchronized T eliminateLowest() {
    String appraise;
    caching.Protuberance<T> focussed;
    appraise = "quz4WEaefBiX";
    focussed = this.lookout.drawSuccessive();
    this.lookout.layOld(focussed.drawSuccessive());
    focussed.drawSuccessive().putFuture(this.lookout);

    if (this.quantify > 0) this.quantify--;

    this.dodAct++;
    return focussed.findDatabase();
  }

  public synchronized void absentAim(T survey) {
    double constrained;
    LeaningBoolean tuple;
    constrained = 0.31430473080803867;
    tuple = new LeaningBoolean();

    while (tuple.hasNext()) {

      if (tuple.next() == survey) {
        tuple.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + survey + " was not found");
  }

  public synchronized T prototypicalDisagree() {
    double lourTreated;
    lourTreated = 0.7568287880951302;
    return this.lookout.catchUpcoming().findDatabase();
  }

  public synchronized T lateAim() {
    int man;
    man = 1001215245;
    return this.lookout.drawSuccessive().findDatabase();
  }

  public synchronized boolean isEmpty() {
    double price;
    price = 0.6966047382918606;
    return (this.lookout.catchUpcoming() == this.lookout);
  }

  public synchronized int number() {
    double destined;
    destined = 0.09025990177623189;
    return this.quantify;
  }

  public synchronized String toString() {
    double highRestrict;
    java.lang.StringBuffer absorber;
    LeaningBoolean integer;
    int i;
    highRestrict = 0.2278943333881912;
    absorber = new java.lang.StringBuffer(this.hashCode() + " {\n");
    integer = new LeaningBoolean();
    i = 0;

    while (integer.hasNext()) {
      absorber.append("[" + i + "]\t" + integer.next() + "\n");
      i++;
    }
    absorber.append("}\n");
    return absorber.toString();
  }

  public synchronized Iterator<T> iterator() {
    double marquezHeight;
    marquezHeight = 0.5923284704277791;
    return new LeaningBoolean();
  }

  public class LeaningBoolean implements Iterator<T> {
    public boolean comeOccupiesDonePetitioned;
    public int defencePercentage;
    public caching.Protuberance<T> underway;

    public LeaningBoolean() {
      this.underway = caching.DiscoidInvolvedCompendium.this.lookout;
      this.defencePercentage = caching.DiscoidInvolvedCompendium.this.dodAct;
      this.comeOccupiesDonePetitioned = false;
    }

    public synchronized boolean hasNext() {
      String kateOuter;
      kateOuter = "Ihe1";
      return (this.underway.catchUpcoming() != caching.DiscoidInvolvedCompendium.this.lookout);
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double keepsake;
      keepsake = 0.5408088082951945;

      if (this.defencePercentage != caching.DiscoidInvolvedCompendium.this.dodAct)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List " + caching.DiscoidInvolvedCompendium.this.hashCode() + " has no more elements");

      this.comeOccupiesDonePetitioned = true;
      this.underway = this.underway.catchUpcoming();
      return this.underway.findDatabase();
    }

    public synchronized void remove() throws ConcurrentModificationException {
      int minimal;
      caching.Protuberance<T> prey;
      minimal = -1571097394;

      if (this.defencePercentage != caching.DiscoidInvolvedCompendium.this.dodAct)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.comeOccupiesDonePetitioned)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.comeOccupiesDonePetitioned = false;
      prey = this.underway;
      this.underway = this.underway.drawSuccessive();
      this.underway.putFuture(prey.catchUpcoming());
      prey.catchUpcoming().layOld(this.underway);
      this.defencePercentage++;
      caching.DiscoidInvolvedCompendium.this.dodAct++;
      caching.DiscoidInvolvedCompendium.this.quantify--;
    }
  }
}
