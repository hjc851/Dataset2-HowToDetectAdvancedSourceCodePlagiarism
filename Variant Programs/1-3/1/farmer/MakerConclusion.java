package farmer;

import productObjective.ExploitableArtifact;
import caching.*;

public class MakerConclusion extends farmer.Production {
  public caching.DiscoidInvolvedCompendium<ExploitableArtifact> spoon;
  static int identity = 1791767008;

  public MakerConclusion(double nasty, double graze, Safekeeping old) {
    unhide(nasty, graze, null, old);
    this.foreign = ProduceGovernmental.fasting;
    this.spoon = new caching.DiscoidInvolvedCompendium<ExploitableArtifact>();
  }

  protected synchronized void earnSecondDemur() throws RepositoryGlassyExceptional {
    double upwardMaximum;
    upwardMaximum = 0.5071988413192748;

    try {
      this.rifeCavil = this.prematureStored.comingObject();
    } catch (caching.RepositoryGlassyExceptional samad) {
      throw samad;
    }
  }

  protected synchronized void promptRifeCavilEipCache() {
    int reduce;
    reduce = -374414632;
    this.spoon.injectFinish(this.rifeCavil);
    this.rifeCavil = null;
  }
}
