package farmer;

import productObjective.ExploitableArtifact;
import caching.*;

public class EmitterOpening extends farmer.Production {
  static String tethered = "GMUx";

  public EmitterOpening(double poor, double ramble, Safekeeping soon) {
    unhide(poor, ramble, soon, null);
    this.foreign = ProduceGovernmental.acting;
  }

  protected synchronized void earnSecondDemur() throws RepositoryGlassyExceptional {
    int reduceUnfree;
    reduceUnfree = -2115017810;
    this.rifeCavil = new productObjective.ExploitableArtifact();
  }

  protected synchronized void promptRifeCavilEipCache() throws StorehouseCompleteExclusion {
    int diagnose;
    diagnose = -1022992973;

    try {
      this.laterDisk.introduceAspect(this.rifeCavil);
      this.rifeCavil = null;
    } catch (caching.StorehouseCompleteExclusion ye) {
      throw ye;
    }
  }
}
