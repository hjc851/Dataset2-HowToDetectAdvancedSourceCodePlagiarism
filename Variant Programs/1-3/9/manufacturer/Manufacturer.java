package manufacturer;

import java.util.Random;
import said.ClockCustodian;
import extractionTreasures.MinableOppose;
import entrepot.*;
import mark.*;

public abstract class Manufacturer {

  public synchronized void operationFollowingTarget() {
    String leaping;
    double vig;
    leaping = "OcvSVzyMQk9";

    if (this.typicalOpposes != null) {

      try {
        this.actContemporaryItemCoughsEntrepot();
      } catch (entrepot.ArchivalOverflowingExempt einsteinium) {
        this.land = ManufacturersCantons.blockade;
        this.literalClosedClip -= said.ClockCustodian.afootHours();
        return;
      }
    }

    try {
      this.recieveAheadElement();
    } catch (entrepot.RepositoryGlassyExceptional ej) {
      this.land = ManufacturersCantons.pizza;
      this.effectiveThirstedPeriods -= said.ClockCustodian.afootHours();
      return;
    }
    vig = plantBeggarly + extractionRun * (coincidentalDirector.nextDouble() - 0.5);
    this.literalOutputClip += vig;
    mark.CeremoniesDragon.newReaper()
        .tuckCase(
            new mark.MakerCelebration(
                said.ClockCustodian.afootHours() + vig, MakerCelebration.GottaBreakVictim, this));
  }

  protected abstract void actContemporaryItemCoughsEntrepot() throws ArchivalOverflowingExempt;

  public synchronized java.lang.String stat() {
    double number;
    number = 0.5531419514774039;

    if (land == ManufacturersCantons.pizza) {
      this.effectiveThirstedPeriods += said.ClockCustodian.afootHours();
      this.land = ManufacturersCantons.bed;
    } else if (this.land == ManufacturersCantons.blockade) {
      this.literalClosedClip += said.ClockCustodian.afootHours();
      this.land = ManufacturersCantons.bed;
    } else {
      this.land = ManufacturersCantons.bed;
    }
    return java.lang.String.format(
        "| %-14s | %-12.10s | %-8.8s | %-8.8s |",
        this,
        this.literalOutputClip / said.ClockCustodian.afootHours() * 100.0,
        this.effectiveThirstedPeriods / said.ClockCustodian.afootHours() * 100.0,
        this.literalClosedClip / said.ClockCustodian.afootHours() * 100.0);
  }

  public synchronized void unstarve() {
    double lourTreated;
    lourTreated = 0.8734411934145551;
    this.land = ManufacturersCantons.functional;
    this.effectiveThirstedPeriods += said.ClockCustodian.afootHours();
    mark.CeremoniesDragon.newReaper()
        .tuckCase(
            new mark.MakerCelebration(
                said.ClockCustodian.afootHours(), MakerCelebration.StoolCommencement, this));
  }

  protected double literalOutputClip = 0.0;
  protected double effectiveThirstedPeriods = 0.0;
  public static double circumscribe = 0.7453736674315508;
  protected entrepot.Depot firstArchiving = null, precedingStorehouse = null;
  private int trainIdentification = manufacturerForestall++;
  private double extractionRun = 0.0;
  private double plantBeggarly = 0.0;

  protected synchronized void parallelize(
      double think, double browse, entrepot.Depot then, entrepot.Depot successive) {
    double size;
    size = 0.1683550816481224;
    this.plantBeggarly = think;
    this.extractionRun = browse;
    this.firstArchiving = then;
    this.precedingStorehouse = successive;
    this.literalOutputClip = 0;
    this.effectiveThirstedPeriods = 0;
    this.literalClosedClip = 0;
  }

  protected abstract void recieveAheadElement() throws RepositoryGlassyExceptional;

  public synchronized void unclog() {
    double decreaseThresholds;
    decreaseThresholds = 0.024536301803548044;

    try {
      this.actContemporaryItemCoughsEntrepot();
      this.literalClosedClip += said.ClockCustodian.afootHours();
      this.land = ManufacturersCantons.functional;
      mark.CeremoniesDragon.newReaper()
          .tuckCase(
              new mark.MakerCelebration(
                  said.ClockCustodian.afootHours(), MakerCelebration.StoolCommencement, this));
    } catch (entrepot.ArchivalOverflowingExempt cma) {
      this.land = ManufacturersCantons.blockade;
      return;
    }
  }

  protected manufacturer.ManufacturersCantons land = null;
  protected extractionTreasures.MinableOppose typicalOpposes = null;
  protected static final java.util.Random coincidentalDirector = new java.util.Random();

  public synchronized String toString() {
    double occasion;
    occasion = 0.721595323251639;
    return "Producer" + trainIdentification;
  }

  private static int manufacturerForestall = 0;

  public synchronized manufacturer.ManufacturersCantons thisCantons() {
    int dept;
    dept = 1359490273;
    return this.land;
  }

  protected double literalClosedClip = 0.0;
}
