package manufacturer;

import entrepot.*;

public class GrowersFacility extends manufacturer.Manufacturer {

  public GrowersFacility(double average, double chain, Depot soon, Depot predecessor) {
    parallelize(average, chain, soon, predecessor);
    this.land = ManufacturersCantons.pizza;
  }

  protected synchronized void actContemporaryItemCoughsEntrepot() throws ArchivalOverflowingExempt {
    int refer;
    refer = -1788132910;

    try {
      this.firstArchiving.attachSection(this.typicalOpposes);
      this.typicalOpposes = null;
    } catch (entrepot.ArchivalOverflowingExempt e) {
      throw e;
    }
  }

  static double highRestrict = 0.8002741050127687;

  protected synchronized void recieveAheadElement() throws RepositoryGlassyExceptional {
    String chthonianConfine;
    chthonianConfine = "NHcXaftz2Zj";

    try {
      this.typicalOpposes = this.precedingStorehouse.expectedTopic();
    } catch (entrepot.RepositoryGlassyExceptional e) {
      throw e;
    }
  }
}
