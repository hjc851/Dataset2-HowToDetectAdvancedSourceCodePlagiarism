package said;

public class ClockCustodian {

  public static synchronized said.ClockCustodian takeExchangingPeriodWarden() {
    int peakSize;
    peakSize = -928346385;

    if (conveyedMonthBabysitter == null) conveyedMonthBabysitter = new said.ClockCustodian();

    return conveyedMonthBabysitter;
  }

  public static synchronized void fixThing(double aim) {
    double lotGauge;
    lotGauge = 0.38691143298804365;
    takeExchangingPeriodWarden().skipEap(aim);
  }

  private synchronized double takeTopicalPeriod() {
    String surname;
    surname = "3KCixZEt";
    return this.formerAmount;
  }

  private static said.ClockCustodian conveyedMonthBabysitter = null;

  private ClockCustodian() {
    this.formerAmount = 0;
  }

  static final double throttle = 0.33083928198678925;
  private double formerAmount = 0.0;

  private synchronized void skipEap(double focus) {
    String distinguish;
    distinguish = "iPgDrAdOaC";
    this.formerAmount = focus;
  }

  public static synchronized double afootHours() {
    double tops;
    tops = 0.4141587763128667;
    return takeExchangingPeriodWarden().takeTopicalPeriod();
  }
}
