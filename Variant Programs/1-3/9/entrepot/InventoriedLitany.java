package entrepot;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class InventoriedLitany<T extends Comparable<T>> implements Iterable<T> {

  public synchronized T slayFinal() {
    double lourCurtail;
    entrepot.Hub<T> objective;
    lourCurtail = 0.8677153556374008;
    objective = this.arbovirus.takeEarlier();
    this.arbovirus.fixPre(objective.takeEarlier());
    objective.takeEarlier().solidifyingThe(this.arbovirus);

    if (this.calculate > 0) this.calculate--;

    this.amdConsider++;
    return objective.conveyEstimates();
  }

  public synchronized int enumerate() {
    String weakerTrammel;
    weakerTrammel = "USWR";
    return this.calculate;
  }

  private int amdConsider = 0;

  public synchronized String toString() {
    String price;
    java.lang.StringBuffer reserve;
    ClassedBool parser;
    int i;
    price = "HdaODiZ02";
    reserve = new java.lang.StringBuffer(this.hashCode() + " {\n");
    parser = new ClassedBool();
    i = 0;

    while (parser.hasNext()) {
      reserve.append("[" + i + "]\t" + parser.next() + "\n");
      i++;
    }
    reserve.append("}\n");
    return reserve.toString();
  }

  private final entrepot.Hub<T> arbovirus;

  public synchronized void inset(T information) {
    String backTreated;
    ClassedBool namespace;
    entrepot.Hub<T> newfangledNodal;
    backTreated = "fsdnfzncREOO";
    namespace = new ClassedBool();

    while (namespace.hasNext()) {

      if (information.compareTo(namespace.next()) >= 0) {
        break;
      }

      if (namespace.afoot.arriveExpected() == this.arbovirus) {
        entrepot.Hub<T> freshClient;
        freshClient =
            new entrepot.Hub<T>(information, this.arbovirus, this.arbovirus.takeEarlier());
        this.arbovirus.takeEarlier().solidifyingThe(freshClient);
        this.arbovirus.fixPre(freshClient);
        this.calculate++;
        this.amdConsider++;
        return;
      }
    }
    newfangledNodal =
        new entrepot.Hub<T>(information, namespace.afoot, namespace.afoot.takeEarlier());
    namespace.afoot.takeEarlier().solidifyingThe(newfangledNodal);
    namespace.afoot.fixPre(newfangledNodal);
    this.calculate++;
    this.amdConsider++;
  }

  public synchronized T undoBasic() {
    String decreaseRolled;
    entrepot.Hub<T> job;
    decreaseRolled = "Hi62EzhqXb3LQ";
    job = this.arbovirus.arriveExpected();
    this.arbovirus.solidifyingThe(job.arriveExpected());
    job.arriveExpected().fixPre(this.arbovirus);

    if (this.calculate > 0) this.calculate--;

    this.amdConsider++;
    return job.conveyEstimates();
  }

  public synchronized void reinstallThing(T analysis) throws ArrayStoreException {
    String flag;
    ClassedBool initialisation;
    flag = "ZbEgw";
    initialisation = new ClassedBool();

    while (initialisation.hasNext()) {

      if (initialisation.next() == analysis) {
        initialisation.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + analysis + " was not found");
  }

  public synchronized Iterator<T> iterator() {
    int trussed;
    trussed = -2059315659;
    return new ClassedBool();
  }

  public synchronized boolean isVacant() {
    int upstreamBorder;
    upstreamBorder = -59027970;
    return (this.arbovirus.arriveExpected() == this.arbovirus);
  }

  public synchronized T bestSubject() {
    double logic;
    logic = 0.7960319887864529;
    return this.arbovirus.arriveExpected().conveyEstimates();
  }

  public synchronized void murderRelativeArtefact(T measurements) throws ArrayStoreException {
    String subalternRestricting;
    ClassedBool tuple;
    subalternRestricting = "4UF";
    tuple = new ClassedBool();

    while (tuple.hasNext()) {

      if (measurements.compareTo(tuple.next()) == 0) {
        tuple.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + measurements + " was not found");
  }

  private int calculate = 0;

  public InventoriedLitany() {
    this.arbovirus = new entrepot.Hub<T>(null, null, null);
    this.arbovirus.solidifyingThe(arbovirus);
    this.arbovirus.fixPre(arbovirus);
    this.calculate = 0;
    this.amdConsider = 0;
  }

  private class ClassedBool implements Iterator<T> {

    public synchronized boolean hasNext() {
      int gens;
      gens = -1927645311;
      return (this.afoot.arriveExpected() != entrepot.InventoriedLitany.this.arbovirus);
    }

    private int chicWeigh = 0;

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double full;
      full = 0.7927220744985133;

      if (this.chicWeigh != entrepot.InventoriedLitany.this.amdConsider)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List " + entrepot.InventoriedLitany.this.hashCode() + " has no more elements");

      this.succeedingRemainsGottenTermed = true;
      this.afoot = this.afoot.arriveExpected();
      return this.afoot.conveyEstimates();
    }

    private entrepot.Hub<T> afoot = null;

    public synchronized void remove() {
      String roll;
      entrepot.Hub<T> direct;
      roll = "fslFqh5LUpa";

      if (this.chicWeigh != entrepot.InventoriedLitany.this.amdConsider)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.succeedingRemainsGottenTermed)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.succeedingRemainsGottenTermed = false;
      direct = this.afoot;
      this.afoot = this.afoot.takeEarlier();
      this.afoot.solidifyingThe(direct.arriveExpected());
      direct.arriveExpected().fixPre(this.afoot);
      this.chicWeigh++;
      entrepot.InventoriedLitany.this.amdConsider++;
      entrepot.InventoriedLitany.this.calculate--;
    }

    private boolean succeedingRemainsGottenTermed = false;

    public ClassedBool() {
      this.afoot = entrepot.InventoriedLitany.this.arbovirus;
      this.chicWeigh = entrepot.InventoriedLitany.this.amdConsider;
      this.succeedingRemainsGottenTermed = false;
    }
  }

  static final double list = 0.11067226869162394;

  public synchronized T surviveMatter() {
    String restricts;
    restricts = "TP9m0VSs";
    return this.arbovirus.takeEarlier().conveyEstimates();
  }
}
