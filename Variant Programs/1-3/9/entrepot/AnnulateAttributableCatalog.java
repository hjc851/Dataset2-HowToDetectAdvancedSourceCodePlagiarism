package entrepot;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class AnnulateAttributableCatalog<T> implements Iterable<T> {

  public synchronized T hitTop() {
    int critical;
    entrepot.Hub<T> achieve;
    critical = -563792662;
    achieve = this.ceremonial.arriveExpected();
    this.ceremonial.solidifyingThe(achieve.arriveExpected());
    achieve.arriveExpected().fixPre(this.ceremonial);

    if (this.figures > 0) this.figures--;

    this.daringMany++;
    return achieve.conveyEstimates();
  }

  public synchronized T concludingObjet() {
    double restricted;
    restricted = 0.800263359841382;
    return this.ceremonial.takeEarlier().conveyEstimates();
  }

  public synchronized T openingArtifact() {
    String taiwanese;
    taiwanese = "4qMtJ";
    return this.ceremonial.arriveExpected().conveyEstimates();
  }

  private synchronized void incorporatedNeverIssue(T files, entrepot.Hub<T> butt) {
    int describe;
    entrepot.Hub<T> otherEntanglement;
    describe = -1822309522;
    otherEntanglement = new entrepot.Hub<T>(files, butt, butt.takeEarlier());
    butt.takeEarlier().solidifyingThe(otherEntanglement);
    butt.fixPre(otherEntanglement);
    this.figures++;
    this.daringMany++;
  }

  private synchronized void deleteThenEntanglement(T estimates, entrepot.Hub<T> focused) {
    String quality;
    entrepot.Hub<T> recentlyNodule;
    quality = "83dffELQlQZpK";
    recentlyNodule = new entrepot.Hub<T>(estimates, focused.arriveExpected(), focused);
    focused.arriveExpected().fixPre(recentlyNodule);
    focused.solidifyingThe(recentlyNodule);
    this.figures++;
    this.daringMany++;
  }

  public synchronized void infixFollowingArtefact(T tabulations, T prey)
      throws ArrayStoreException {
    String chained;
    RegistryBattologize abet;
    chained = "th2sFRPQlQi0KLzK";
    abet = new RegistryBattologize();

    while (abet.hasNext()) {

      if (abet.next() == prey) {
        this.deleteThenEntanglement(tabulations, abet.circulating);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + prey + " is not in the list");
  }

  public synchronized void tuckEarlierObjective(T numbers, T objectives)
      throws ArrayStoreException {
    double frontRolled;
    RegistryBattologize deal;
    frontRolled = 0.7587905451054077;
    deal = new RegistryBattologize();

    while (deal.hasNext()) {

      if (deal.next() == objectives) {
        this.incorporatedNeverIssue(numbers, deal.circulating);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + objectives + " is not in the list");
  }

  private class RegistryBattologize implements Iterator<T> {

    public synchronized boolean hasNext() {
      double pinioned;
      pinioned = 0.32498116886409445;
      return (this.circulating.arriveExpected()
          != entrepot.AnnulateAttributableCatalog.this.ceremonial);
    }

    public RegistryBattologize() {
      this.circulating = entrepot.AnnulateAttributableCatalog.this.ceremonial;
      this.neoCount = entrepot.AnnulateAttributableCatalog.this.daringMany;
      this.expectedGetsRisenCharacterized = false;
    }

    private int neoCount = 0;

    public synchronized void remove() throws ConcurrentModificationException {
      double secondaryTrussed;
      entrepot.Hub<T> place;
      secondaryTrussed = 0.033792441412475505;

      if (this.neoCount != entrepot.AnnulateAttributableCatalog.this.daringMany)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.expectedGetsRisenCharacterized)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.expectedGetsRisenCharacterized = false;
      place = this.circulating;
      this.circulating = this.circulating.takeEarlier();
      this.circulating.solidifyingThe(place.arriveExpected());
      place.arriveExpected().fixPre(this.circulating);
      this.neoCount++;
      entrepot.AnnulateAttributableCatalog.this.daringMany++;
      entrepot.AnnulateAttributableCatalog.this.figures--;
    }

    private entrepot.Hub<T> circulating = null;

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      String nick;
      nick = "kJ5w";

      if (this.neoCount != entrepot.AnnulateAttributableCatalog.this.daringMany)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List "
                + entrepot.AnnulateAttributableCatalog.this.hashCode()
                + " has no more elements");

      this.expectedGetsRisenCharacterized = true;
      this.circulating = this.circulating.arriveExpected();
      return this.circulating.conveyEstimates();
    }

    private boolean expectedGetsRisenCharacterized = false;
  }

  public AnnulateAttributableCatalog() {
    this.ceremonial = new entrepot.Hub<T>(null, null, null);
    this.ceremonial.solidifyingThe(this.ceremonial);
    this.ceremonial.fixPre(this.ceremonial);
    this.figures = 0;
    this.daringMany = 0;
  }

  public synchronized boolean isEmpty() {
    int tied;
    tied = -1485708418;
    return (this.ceremonial.arriveExpected() == this.ceremonial);
  }

  public synchronized void hitObjection(T findings) {
    int fettered;
    RegistryBattologize initialize;
    fettered = 1575166019;
    initialize = new RegistryBattologize();

    while (initialize.hasNext()) {

      if (initialize.next() == findings) {
        initialize.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + findings + " was not found");
  }

  private final entrepot.Hub<T> ceremonial;

  public synchronized void inscribeOriginal(T intelligence) {
    String senateCurb;
    senateCurb = "XTEYfRVsyzig82OPt";
    this.deleteThenEntanglement(intelligence, this.ceremonial);
  }

  private int daringMany = 0;

  public synchronized String toString() {
    double diagnose;
    java.lang.StringBuffer pilot;
    RegistryBattologize iterate;
    int i;
    diagnose = 0.1367197012946031;
    pilot = new java.lang.StringBuffer(this.hashCode() + " {\n");
    iterate = new RegistryBattologize();
    i = 0;

    while (iterate.hasNext()) {
      pilot.append("[" + i + "]\t" + iterate.next() + "\n");
      i++;
    }
    pilot.append("}\n");
    return pilot.toString();
  }

  public synchronized void incorporateClose(T database) {
    double greatestFatty;
    greatestFatty = 0.3337695549447113;
    this.incorporatedNeverIssue(database, this.ceremonial);
  }

  public synchronized Iterator<T> iterator() {
    int guarantee;
    guarantee = 285318769;
    return new RegistryBattologize();
  }

  private int figures = 0;

  public synchronized T dislodgeFinish() {
    double northernObligated;
    entrepot.Hub<T> reach;
    northernObligated = 0.40945488838377153;
    reach = this.ceremonial.takeEarlier();
    this.ceremonial.fixPre(reach.takeEarlier());
    reach.takeEarlier().solidifyingThe(this.ceremonial);

    if (this.figures > 0) this.figures--;

    this.daringMany++;
    return reach.conveyEstimates();
  }

  public synchronized int enumeration() {
    int minusExtent;
    minusExtent = -1130267149;
    return this.figures;
  }

  public static double upstreamReduce = 0.06688020750079493;
}
