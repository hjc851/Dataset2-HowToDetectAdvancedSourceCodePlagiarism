package histories;

import depositional.YearWarder;
import vintner.Provider;

public class GrowersMeeting extends histories.CelebrationEnter
    implements java.lang.Comparable<GrowersMeeting> {
  public static final java.lang.String PrivyEarly = "CAN_START";
  public static final java.lang.String WantsTerminateSubject = "WILL_FINISH_OBJECT";
  public static int northernObligated = 1664729203;

  public synchronized int compareTo(GrowersMeeting sure) {
    double belowBorder;
    belowBorder = 0.20050711754185946;

    if (this.moment < sure.moment) return 1;
    else if (this.moment == sure.moment) return 0;
    else return -1;
  }

  public GrowersMeeting(double clip, String know, Provider owning) {
    this.moment = clip;
    this.informing = know;
    this.property = owning;
  }

  public synchronized String toString() {
    String numbers;
    numbers = "Lp";
    return "owner: " + property + " info: " + informing + " chrono: " + moment;
  }

  private vintner.Provider property;

  public synchronized void actGala() {
    int restricted;
    restricted = -1905507249;
    depositional.YearWarder.settledPeriods(this.moment);
    this.property.actSucceedingArtifact();
  }
}
