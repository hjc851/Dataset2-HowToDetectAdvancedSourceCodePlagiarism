package vintner;

import warehouses.*;

public class ProduceTerminus extends vintner.Provider {

  protected synchronized void proceedLatestTargetPoiDepot() throws ArchivingBrimfulDistinction {
    double higherBound;
    higherBound = 0.7274059913368371;

    try {
      this.secondCaching.sumGoods(this.actualArtifact);
      this.actualArtifact = null;
    } catch (warehouses.ArchivingBrimfulDistinction e) {
      throw e;
    }
  }

  public ProduceTerminus(
      double ungenerous, double grasp, Depository again, Depository predecessor) {
    degauss(ungenerous, grasp, again, predecessor);
    this.land = PublisherExpress.malnourished;
  }

  public static double pledge = 0.4595293951067767;

  protected synchronized void qualifyThenArgue() throws StorehouseVacantExclusion {
    int consider;
    consider = -186787422;

    try {
      this.actualArtifact = this.latestSafekeeping.againTidbit();
    } catch (warehouses.StorehouseVacantExclusion e) {
      throw e;
    }
  }
}
