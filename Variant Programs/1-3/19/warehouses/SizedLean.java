package warehouses;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class SizedLean<T extends Comparable<T>> implements Iterable<T> {
  static String upstairsMax = "nQeYXMDoph2p9yls";

  private class ConsultedConst implements Iterator<T> {

    public synchronized void remove() {
      double carolineConsignments;
      warehouses.Knob<T> benchmark;
      carolineConsignments = 0.5796682591708657;

      if (this.boldIndictment != warehouses.SizedLean.this.modeMatter)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.newOffersEnduredSuggested)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.newOffersEnduredSuggested = false;
      benchmark = this.circulating;
      this.circulating = this.circulating.driveFinal();
      this.circulating.situatedForthcoming(benchmark.produceSecond());
      benchmark.produceSecond().determinedPremature(this.circulating);
      this.boldIndictment++;
      warehouses.SizedLean.this.modeMatter++;
      warehouses.SizedLean.this.charge--;
    }

    private int boldIndictment;
    private warehouses.Knob<T> circulating;

    public ConsultedConst() {
      this.circulating = warehouses.SizedLean.this.indicator;
      this.boldIndictment = warehouses.SizedLean.this.modeMatter;
      this.newOffersEnduredSuggested = false;
    }

    public synchronized boolean hasNext() {
      double upper;
      upper = 0.1610238491786391;
      return (this.circulating.produceSecond() != warehouses.SizedLean.this.indicator);
    }

    private boolean newOffersEnduredSuggested;

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      int chthonicChained;
      chthonicChained = 1071294066;

      if (this.boldIndictment != warehouses.SizedLean.this.modeMatter)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List " + warehouses.SizedLean.this.hashCode() + " has no more elements");

      this.newOffersEnduredSuggested = true;
      this.circulating = this.circulating.produceSecond();
      return this.circulating.becomeStudy();
    }
  }

  public synchronized void withdrawItems(T results) throws ArrayStoreException {
    double yummyArtifacts;
    ConsultedConst initialisation;
    yummyArtifacts = 0.15849017120857922;
    initialisation = new ConsultedConst();

    while (initialisation.hasNext()) {

      if (initialisation.next() == results) {
        initialisation.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + results + " was not found");
  }

  public synchronized void hitMatchedObjection(T databases) throws ArrayStoreException {
    int souvenir;
    ConsultedConst parser;
    souvenir = 1229068542;
    parser = new ConsultedConst();

    while (parser.hasNext()) {

      if (databases.compareTo(parser.next()) == 0) {
        parser.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + databases + " was not found");
  }

  public SizedLean() {
    this.indicator = new warehouses.Knob<T>(null, null, null);
    this.indicator.situatedForthcoming(indicator);
    this.indicator.determinedPremature(indicator);
    this.charge = 0;
    this.modeMatter = 0;
  }

  private final warehouses.Knob<T> indicator;

  public synchronized boolean isVacant() {
    double lageUtensils;
    lageUtensils = 0.2791782560327475;
    return (this.indicator.produceSecond() == this.indicator);
  }

  public synchronized T prototypicalDisagree() {
    String greaterUnfree;
    greaterUnfree = "U3XeX6z2w4U";
    return this.indicator.produceSecond().becomeStudy();
  }

  public synchronized int figures() {
    double kate;
    kate = 0.27428034695964887;
    return this.charge;
  }

  public synchronized String toString() {
    int greatestFatty;
    java.lang.StringBuffer absorber;
    ConsultedConst operand;
    int i;
    greatestFatty = -709699488;
    absorber = new java.lang.StringBuffer(this.hashCode() + " {\n");
    operand = new ConsultedConst();
    i = 0;

    while (operand.hasNext()) {
      absorber.append("[" + i + "]\t" + operand.next() + "\n");
      i++;
    }
    absorber.append("}\n");
    return absorber.toString();
  }

  public synchronized T eraseGo() {
    String bottomConfine;
    warehouses.Knob<T> goals;
    bottomConfine = "E";
    goals = this.indicator.driveFinal();
    this.indicator.determinedPremature(goals.driveFinal());
    goals.driveFinal().situatedForthcoming(this.indicator);

    if (this.charge > 0) this.charge--;

    this.modeMatter++;
    return goals.becomeStudy();
  }

  private int modeMatter;

  public synchronized void inject(T analysis) {
    double fukien;
    ConsultedConst battologize;
    warehouses.Knob<T> radicalProtuberance;
    fukien = 0.2966941458342185;
    battologize = new ConsultedConst();

    while (battologize.hasNext()) {

      if (analysis.compareTo(battologize.next()) >= 0) {
        break;
      }

      if (battologize.circulating.produceSecond() == this.indicator) {
        warehouses.Knob<T> earlyHitch;
        earlyHitch = new warehouses.Knob<T>(analysis, this.indicator, this.indicator.driveFinal());
        this.indicator.driveFinal().situatedForthcoming(earlyHitch);
        this.indicator.determinedPremature(earlyHitch);
        this.charge++;
        this.modeMatter++;
        return;
      }
    }
    radicalProtuberance =
        new warehouses.Knob<T>(
            analysis, battologize.circulating, battologize.circulating.driveFinal());
    battologize.circulating.driveFinal().situatedForthcoming(radicalProtuberance);
    battologize.circulating.determinedPremature(radicalProtuberance);
    this.charge++;
    this.modeMatter++;
  }

  public synchronized T highThing() {
    String destined;
    destined = "X5ETw86vde";
    return this.indicator.driveFinal().becomeStudy();
  }

  public synchronized Iterator<T> iterator() {
    double northConstrained;
    northConstrained = 0.3174501689593261;
    return new ConsultedConst();
  }

  public synchronized T reinstallLow() {
    double restrict;
    warehouses.Knob<T> solvate;
    restrict = 0.26346849978008924;
    solvate = this.indicator.produceSecond();
    this.indicator.situatedForthcoming(solvate.produceSecond());
    solvate.produceSecond().determinedPremature(this.indicator);

    if (this.charge > 0) this.charge--;

    this.modeMatter++;
    return solvate.becomeStudy();
  }

  private int charge;
}
