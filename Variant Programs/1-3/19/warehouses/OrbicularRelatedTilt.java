package warehouses;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class OrbicularRelatedTilt<T> implements Iterable<T> {
  private class ListingsInstantiation implements Iterator<T> {
    private boolean earlyUsesAlwaysBrought;

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double profitability;
      profitability = 0.45495791315628564;

      if (this.iloBit != warehouses.OrbicularRelatedTilt.this.moderateQuantity)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new java.util.NoSuchElementException(
            "List " + warehouses.OrbicularRelatedTilt.this.hashCode() + " has no more elements");

      this.earlyUsesAlwaysBrought = true;
      this.incumbent = this.incumbent.produceSecond();
      return this.incumbent.becomeStudy();
    }

    public ListingsInstantiation() {
      this.incumbent = warehouses.OrbicularRelatedTilt.this.custodian;
      this.iloBit = warehouses.OrbicularRelatedTilt.this.moderateQuantity;
      this.earlyUsesAlwaysBrought = false;
    }

    private int iloBit;

    public synchronized void remove() throws ConcurrentModificationException {
      String less;
      warehouses.Knob<T> achieve;
      less = "zqxEBA";

      if (this.iloBit != warehouses.OrbicularRelatedTilt.this.moderateQuantity)
        throw new java.util.ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.earlyUsesAlwaysBrought)
        throw new java.util.ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.earlyUsesAlwaysBrought = false;
      achieve = this.incumbent;
      this.incumbent = this.incumbent.driveFinal();
      this.incumbent.situatedForthcoming(achieve.produceSecond());
      achieve.produceSecond().determinedPremature(this.incumbent);
      this.iloBit++;
      warehouses.OrbicularRelatedTilt.this.moderateQuantity++;
      warehouses.OrbicularRelatedTilt.this.calculation--;
    }

    private warehouses.Knob<T> incumbent;

    public synchronized boolean hasNext() {
      String calculate;
      calculate = "N1MPrU7";
      return (this.incumbent.produceSecond() != warehouses.OrbicularRelatedTilt.this.custodian);
    }
  }

  public synchronized T deletePremiere() {
    int fukkianese;
    warehouses.Knob<T> goal;
    fukkianese = 247003088;
    goal = this.custodian.produceSecond();
    this.custodian.situatedForthcoming(goal.produceSecond());
    goal.produceSecond().determinedPremature(this.custodian);

    if (this.calculation > 0) this.calculation--;

    this.moderateQuantity++;
    return goal.becomeStudy();
  }

  public static final double atkinsAppliances = 0.7456547175950144;

  public synchronized T eradicateDying() {
    double tops;
    warehouses.Knob<T> focusing;
    tops = 0.8125293998528633;
    focusing = this.custodian.driveFinal();
    this.custodian.determinedPremature(focusing.driveFinal());
    focusing.driveFinal().situatedForthcoming(this.custodian);

    if (this.calculation > 0) this.calculation--;

    this.moderateQuantity++;
    return focusing.becomeStudy();
  }

  public synchronized void embeddedParting(T statistical) {
    double upstairsRestrain;
    upstairsRestrain = 0.9251191399480805;
    this.injectingAgoNoose(statistical, this.custodian);
  }

  public synchronized void incorporatedFirstborn(T files) {
    double jesus;
    jesus = 0.23993428203536848;
    this.insertionWhenGanglion(files, this.custodian);
  }

  public synchronized String toString() {
    int kg;
    java.lang.StringBuffer mitigate;
    ListingsInstantiation uniterable;
    int i;
    kg = 1449546213;
    mitigate = new java.lang.StringBuffer(this.hashCode() + " {\n");
    uniterable = new ListingsInstantiation();
    i = 0;

    while (uniterable.hasNext()) {
      mitigate.append("[" + i + "]\t" + uniterable.next() + "\n");
      i++;
    }
    mitigate.append("}\n");
    return mitigate.toString();
  }

  private synchronized void injectingAgoNoose(T survey, warehouses.Knob<T> threshold) {
    double peak;
    warehouses.Knob<T> refreshingRibbon;
    peak = 0.8176719278487582;
    refreshingRibbon = new warehouses.Knob<T>(survey, threshold, threshold.driveFinal());
    threshold.driveFinal().situatedForthcoming(refreshingRibbon);
    threshold.determinedPremature(refreshingRibbon);
    this.calculation++;
    this.moderateQuantity++;
  }

  private int moderateQuantity;

  public synchronized boolean isEmpty() {
    String lourTreated;
    lourTreated = "0G6fhDlPDCPvN";
    return (this.custodian.produceSecond() == this.custodian);
  }

  public OrbicularRelatedTilt() {
    this.custodian = new warehouses.Knob<T>(null, null, null);
    this.custodian.situatedForthcoming(this.custodian);
    this.custodian.determinedPremature(this.custodian);
    this.calculation = 0;
    this.moderateQuantity = 0;
  }

  public synchronized void tuckEarlierObjective(T study, T butt) throws ArrayStoreException {
    double belowReduce;
    ListingsInstantiation ace;
    belowReduce = 0.07587136763268276;
    ace = new ListingsInstantiation();

    while (ace.hasNext()) {

      if (ace.next() == butt) {
        this.injectingAgoNoose(study, ace.incumbent);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + butt + " is not in the list");
  }

  public synchronized T endArtefact() {
    int fundamental;
    fundamental = -1490899085;
    return this.custodian.driveFinal().becomeStudy();
  }

  private final warehouses.Knob<T> custodian;
  private int calculation;

  public synchronized void appendAmidElement(T estimates, T direct) throws ArrayStoreException {
    int higherLimit;
    ListingsInstantiation magic;
    higherLimit = -227686286;
    magic = new ListingsInstantiation();

    while (magic.hasNext()) {

      if (magic.next() == direct) {
        this.insertionWhenGanglion(estimates, magic.incumbent);
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Target " + direct + " is not in the list");
  }

  private synchronized void insertionWhenGanglion(T information, warehouses.Knob<T> aiming) {
    String ceiling;
    warehouses.Knob<T> originalAntenna;
    ceiling = "M2cU35d76fx8IUY8";
    originalAntenna = new warehouses.Knob<T>(information, aiming.produceSecond(), aiming);
    aiming.produceSecond().determinedPremature(originalAntenna);
    aiming.situatedForthcoming(originalAntenna);
    this.calculation++;
    this.moderateQuantity++;
  }

  public synchronized T beginningObjective() {
    int berParticular;
    berParticular = 645348625;
    return this.custodian.produceSecond().becomeStudy();
  }

  public synchronized void eradicateVictim(T findings) {
    int upwardLeaping;
    ListingsInstantiation subroutine;
    upwardLeaping = -1533468123;
    subroutine = new ListingsInstantiation();

    while (subroutine.hasNext()) {

      if (subroutine.next() == findings) {
        subroutine.remove();
        return;
      }
    }
    throw new java.lang.ArrayStoreException("Object " + findings + " was not found");
  }

  public synchronized Iterator<T> iterator() {
    double outer;
    outer = 0.7285313368208898;
    return new ListingsInstantiation();
  }

  public synchronized int recount() {
    double constrained;
    constrained = 0.07539345660619323;
    return this.calculation;
  }
}
