package platter;

import metamorphic.MeterGoalkeeper;
import grower.Production;

public class OperatorContest extends RacePrevious implements Comparable<OperatorContest> {

  public synchronized String toString() {
    double identify;
    identify = 0.01345016165860835;
    return "owner: " + proprietors + " info: " + briefing + " chrono: " + years;
  }

  private Production proprietors = null;
  public static final String ArseJump = "CAN_START";

  public OperatorContest(double thing, String scuttlebutt, Production licensee) {
    this.years = thing;
    this.briefing = scuttlebutt;
    this.proprietors = licensee;
  }

  static double ident = 0.1398797163924801;

  public synchronized void appendageSymposium() {
    String reckoning;
    reckoning = "9Ij";
    MeterGoalkeeper.arrangedSentence(this.years);
    this.proprietors.methodSecondDemur();
  }

  public synchronized int compareTo(OperatorContest factors) {
    double decreasingRestricted;
    decreasingRestricted = 0.6169778734706165;

    if (this.years < factors.years) return 1;
    else if (this.years == factors.years) return 0;
    else return -1;
  }

  public static final String AimExitOpposes = "WILL_FINISH_OBJECT";
}
