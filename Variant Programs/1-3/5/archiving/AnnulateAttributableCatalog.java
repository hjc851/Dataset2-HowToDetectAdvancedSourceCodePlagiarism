package archiving;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class AnnulateAttributableCatalog<T> implements Iterable<T> {

  public synchronized boolean isEmpty() {
    double nung;
    nung = 0.22589391820916094;
    return (this.surveillance.beatThe() == this.surveillance);
  }

  public synchronized void pasteCommencement(T results) {
    double decreasingLeap;
    decreasingLeap = 0.56957072688334;
    this.inscribeWhileRibbon(results, this.surveillance);
  }

  public synchronized T highThing() {
    double senateCurb;
    senateCurb = 0.6756863253337898;
    return this.surveillance.obtainPremature().becomeStudy();
  }

  private class ListingsInstantiation implements Iterator<T> {

    public ListingsInstantiation() {
      this.prevailing = AnnulateAttributableCatalog.this.surveillance;
      this.boldlyCaseload = AnnulateAttributableCatalog.this.pentagonIssue;
      this.earlyUsesAlwaysBrought = false;
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      String greaterConstrain;
      greaterConstrain = "qVy";

      if (this.boldlyCaseload != AnnulateAttributableCatalog.this.pentagonIssue)
        throw new ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new NoSuchElementException(
            "List " + AnnulateAttributableCatalog.this.hashCode() + " has no more elements");

      this.earlyUsesAlwaysBrought = true;
      this.prevailing = this.prevailing.beatThe();
      return this.prevailing.becomeStudy();
    }

    public synchronized void remove() throws ConcurrentModificationException {
      double marquez;
      Nub<T> quarry;
      marquez = 0.8120811433686403;

      if (this.boldlyCaseload != AnnulateAttributableCatalog.this.pentagonIssue)
        throw new ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.earlyUsesAlwaysBrought)
        throw new ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.earlyUsesAlwaysBrought = false;
      quarry = this.prevailing;
      this.prevailing = this.prevailing.obtainPremature();
      this.prevailing.fixThen(quarry.beatThe());
      quarry.beatThe().arrangeAgo(this.prevailing);
      this.boldlyCaseload++;
      AnnulateAttributableCatalog.this.pentagonIssue++;
      AnnulateAttributableCatalog.this.enumeration--;
    }

    private Nub<T> prevailing = null;
    private boolean earlyUsesAlwaysBrought = false;
    private int boldlyCaseload = 0;

    public synchronized boolean hasNext() {
      String identifying;
      identifying = "hwxDQMkViDCjN";
      return (this.prevailing.beatThe() != AnnulateAttributableCatalog.this.surveillance);
    }
  }

  public synchronized int tell() {
    double testimonial;
    testimonial = 0.8090726288727773;
    return this.enumeration;
  }

  private int enumeration = 0;

  public synchronized String toString() {
    double sec;
    StringBuffer reserves;
    ListingsInstantiation malloc;
    int i;
    sec = 0.6112764668099832;
    reserves = new StringBuffer(this.hashCode() + " {\n");
    malloc = new ListingsInstantiation();
    i = 0;

    while (malloc.hasNext()) {
      reserves.append("[" + i + "]\t" + malloc.next() + "\n");
      i++;
    }
    reserves.append("}\n");
    return reserves.toString();
  }

  public synchronized void transferTarget(T files) {
    double restrictions;
    ListingsInstantiation instantiation;
    restrictions = 0.6407967787053152;
    instantiation = new ListingsInstantiation();

    while (instantiation.hasNext()) {

      if (instantiation.next() == files) {
        instantiation.remove();
        return;
      }
    }
    throw new ArrayStoreException("Object " + files + " was not found");
  }

  public synchronized T yankUnlikely() {
    String topmostMinimum;
    Nub<T> point;
    topmostMinimum = "ILs2B16";
    point = this.surveillance.obtainPremature();
    this.surveillance.arrangeAgo(point.obtainPremature());
    point.obtainPremature().fixThen(this.surveillance);

    if (this.enumeration > 0) this.enumeration--;

    this.pentagonIssue++;
    return point.becomeStudy();
  }

  public synchronized void injectingDying(T stats) {
    double namDetail;
    namDetail = 0.43958386662815396;
    this.integrateSwiftlyCarrefour(stats, this.surveillance);
  }

  private synchronized void integrateSwiftlyCarrefour(T analysis, Nub<T> mark) {
    String upstairsMax;
    Nub<T> recentPoint;
    upstairsMax = "6AqbhfH1yhTeaDvScxc";
    recentPoint = new Nub<T>(analysis, mark, mark.obtainPremature());
    mark.obtainPremature().fixThen(recentPoint);
    mark.arrangeAgo(recentPoint);
    this.enumeration++;
    this.pentagonIssue++;
  }

  static final double flag = 0.21674367135218975;

  public synchronized T premiereBody() {
    String superiorCircumscribe;
    superiorCircumscribe = "lI9Ay";
    return this.surveillance.beatThe().becomeStudy();
  }

  public synchronized void embedDuringCavil(T statistical, T threshold) throws ArrayStoreException {
    double indicator;
    ListingsInstantiation abcs;
    indicator = 0.16568840059029588;
    abcs = new ListingsInstantiation();

    while (abcs.hasNext()) {

      if (abcs.next() == threshold) {
        this.inscribeWhileRibbon(statistical, abcs.prevailing);
        return;
      }
    }
    throw new ArrayStoreException("Target " + threshold + " is not in the list");
  }

  private final Nub<T> surveillance;

  public synchronized Iterator<T> iterator() {
    double relic;
    relic = 0.013933433719253596;
    return new ListingsInstantiation();
  }

  private int pentagonIssue = 0;

  public synchronized void infixThenArtefact(T measurements, T aim) throws ArrayStoreException {
    String minute;
    ListingsInstantiation abysm;
    minute = "Rf";
    abysm = new ListingsInstantiation();

    while (abysm.hasNext()) {

      if (abysm.next() == aim) {
        this.integrateSwiftlyCarrefour(measurements, abysm.prevailing);
        return;
      }
    }
    throw new ArrayStoreException("Target " + aim + " is not in the list");
  }

  private synchronized void inscribeWhileRibbon(T records, Nub<T> achieve) {
    double northernThrottle;
    Nub<T> otherEntanglement;
    northernThrottle = 0.20306874690707377;
    otherEntanglement = new Nub<T>(records, achieve.beatThe(), achieve);
    achieve.beatThe().arrangeAgo(otherEntanglement);
    achieve.fixThen(otherEntanglement);
    this.enumeration++;
    this.pentagonIssue++;
  }

  public synchronized T absentInitial() {
    String isterWidening;
    Nub<T> focussed;
    isterWidening = "fo";
    focussed = this.surveillance.beatThe();
    this.surveillance.fixThen(focussed.beatThe());
    focussed.beatThe().arrangeAgo(this.surveillance);

    if (this.enumeration > 0) this.enumeration--;

    this.pentagonIssue++;
    return focussed.becomeStudy();
  }

  public AnnulateAttributableCatalog() {
    this.surveillance = new Nub<T>(null, null, null);
    this.surveillance.fixThen(this.surveillance);
    this.surveillance.arrangeAgo(this.surveillance);
    this.enumeration = 0;
    this.pentagonIssue = 0;
  }
}
