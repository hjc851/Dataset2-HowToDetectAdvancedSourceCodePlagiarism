package grower;

import archiving.*;

public class OperatorPolice extends Production {

  protected synchronized void displaceIncumbentObjectionTeamStoring()
      throws ShelvingRepleteExcluded {
    double widening;
    widening = 0.32462080934148163;

    try {
      this.lastStored.lendPoint(this.rifeCavil);
      this.rifeCavil = null;
    } catch (ShelvingRepleteExcluded salaam) {
      throw salaam;
    }
  }

  protected synchronized void encounterUpcomingCavil() throws StoreBlankExcepted {
    double marxRoll;
    marxRoll = 0.4896283465966613;

    try {
      this.rifeCavil = this.originalSpace.followingDetail();
    } catch (StoreBlankExcepted e) {
      throw e;
    }
  }

  public OperatorPolice(double normal, double chain, Disk ahead, Disk latest) {
    instantiate(normal, chain, ahead, latest);
    this.canton = OperatorRepublic.emaciated;
  }

  static final int carolineConsignments = -1286786940;
}
