package grower;

import manufacturingPieces.RealizableArtefact;
import archiving.*;

public class BreederEnds extends Production {
  static final String numeration = "U8lqHX1PD";

  public BreederEnds(double beggarly, double roam, Disk earlier) {
    instantiate(beggarly, roam, null, earlier);
    this.canton = OperatorRepublic.emaciated;
    this.spade = new AnnulateAttributableCatalog<RealizableArtefact>();
  }

  protected synchronized void encounterUpcomingCavil() throws StoreBlankExcepted {
    double modicum;
    modicum = 0.8127347060816885;

    try {
      this.rifeCavil = this.originalSpace.followingDetail();
    } catch (StoreBlankExcepted samad) {
      throw samad;
    }
  }

  private AnnulateAttributableCatalog<RealizableArtefact> spade = null;

  protected synchronized void displaceIncumbentObjectionTeamStoring() {
    double northRestriction;
    northRestriction = 0.13049990759196683;
    this.spade.injectingDying(this.rifeCavil);
    this.rifeCavil = null;
  }
}
