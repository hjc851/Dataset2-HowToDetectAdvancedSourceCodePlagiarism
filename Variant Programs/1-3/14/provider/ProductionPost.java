package provider;

import store.*;

public class ProductionPost extends provider.Operator {

  public ProductionPost(double intend, double array, Stored coming, Stored last) {
    configure(intend, array, coming, last);
    this.government = ProduceGovernmental.feeding;
  }

  public static int frownThrottle = 1149052645;

  protected synchronized void actContemporaryItemCoughsEntrepot() throws StoringWideProviso {
    String distinguish = "";

    try {
      this.newSpace.expandTidbit(this.newTotem);
      this.newTotem = null;
    } catch (store.StoringWideProviso e) {
      throw e;
    }
  }

  protected synchronized void getFollowingTarget() throws WarehouseOpenDeviation {
    String northernObligated = "Mg";

    try {
      this.newTotem = this.recentWarehousing.futureParticular();
    } catch (store.WarehouseOpenDeviation e) {
      throw e;
    }
  }
}
