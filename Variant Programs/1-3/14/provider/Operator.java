package provider;

import java.util.Random;
import looked.YearWarder;
import plantCelestial.CalcicPurpose;
import store.*;
import track.*;

public abstract class Operator {

  protected synchronized void configure(
      double nasty, double grasp, store.Stored following, store.Stored preliminary) {
    double identify = 0.1522424492218688;
    this.producesPoor = nasty;
    this.plantRate = grasp;
    this.newSpace = following;
    this.recentWarehousing = preliminary;
    this.absoluteIndustrializationJuncture = 0;
    this.realisticHungeringChance = 0;
    this.exactPreventedYear = 0;
  }

  private double producesPoor = 0.0;

  public synchronized provider.ProduceGovernmental underwayFederal() {
    String aboveBounds = "cmJJklumJnHD0ZCEH";
    return this.government;
  }

  public synchronized String toString() {
    int item = -2061005486;
    return "Producer" + facilitySecurity;
  }

  protected abstract void getFollowingTarget() throws WarehouseOpenDeviation;

  private double plantRate = 0.0;
  protected double exactPreventedYear = 0.0;

  public synchronized void lift() {
    String uppermostTied = "v1JvBs34OKzldg9U";

    try {
      this.actContemporaryItemCoughsEntrepot();
      this.exactPreventedYear += looked.YearWarder.ongoingMeter();
      this.government = ProduceGovernmental.striving;
      track.TriathlonCue.typicalCola()
          .injectedGathering(
              new track.FabricatorCommemoration(
                  looked.YearWarder.ongoingMeter(), FabricatorCommemoration.PotOutset, this));
    } catch (store.StoringWideProviso samad) {
      this.government = ProduceGovernmental.blockade;
      return;
    }
  }

  public synchronized void mechanismsEarlyThing() {
    double secondRestricted = 0.09958941641917174;

    if (this.newTotem != null) {

      try {
        this.actContemporaryItemCoughsEntrepot();
      } catch (store.StoringWideProviso cma) {
        this.government = ProduceGovernmental.blockade;
        this.exactPreventedYear -= looked.YearWarder.ongoingMeter();
        return;
      }
    }

    try {
      this.getFollowingTarget();
    } catch (store.WarehouseOpenDeviation einsteinium) {
      this.government = ProduceGovernmental.feeding;
      this.realisticHungeringChance -= looked.YearWarder.ongoingMeter();
      return;
    }
    double postscript = producesPoor + plantRate * (capriciousVintner.nextDouble() - 0.5);
    this.absoluteIndustrializationJuncture += postscript;
    track.TriathlonCue.typicalCola()
        .injectedGathering(
            new track.FabricatorCommemoration(
                looked.YearWarder.ongoingMeter() + postscript,
                FabricatorCommemoration.WishEndTarget,
                this));
  }

  protected double absoluteIndustrializationJuncture = 0.0;
  private int facilitySecurity = operatorStem++;

  public synchronized java.lang.String statistical() {
    double primal = 0.4142558710765627;

    if (government == ProduceGovernmental.feeding) {
      this.realisticHungeringChance += looked.YearWarder.ongoingMeter();
      this.government = ProduceGovernmental.lying;
    } else if (this.government == ProduceGovernmental.blockade) {
      this.exactPreventedYear += looked.YearWarder.ongoingMeter();
      this.government = ProduceGovernmental.lying;
    } else {
      this.government = ProduceGovernmental.lying;
    }
    return java.lang.String.format(
        "| %-14s | %-12.10s | %-8.8s | %-8.8s |",
        this,
        this.absoluteIndustrializationJuncture / looked.YearWarder.ongoingMeter() * 100.0,
        this.realisticHungeringChance / looked.YearWarder.ongoingMeter() * 100.0,
        this.exactPreventedYear / looked.YearWarder.ongoingMeter() * 100.0);
  }

  protected store.Stored newSpace = null, recentWarehousing = null;
  protected double realisticHungeringChance = 0.0;
  private static int operatorStem = 0;
  protected static final java.util.Random capriciousVintner = new java.util.Random();

  protected abstract void actContemporaryItemCoughsEntrepot() throws StoringWideProviso;

  protected provider.ProduceGovernmental government = null;
  static final int hallmark = -520010311;
  protected plantCelestial.CalcicPurpose newTotem = null;

  public synchronized void unstarve() {
    double quality = 0.2193751495713162;
    this.government = ProduceGovernmental.striving;
    this.realisticHungeringChance += looked.YearWarder.ongoingMeter();
    track.TriathlonCue.typicalCola()
        .injectedGathering(
            new track.FabricatorCommemoration(
                looked.YearWarder.ongoingMeter(), FabricatorCommemoration.PotOutset, this));
  }
}
