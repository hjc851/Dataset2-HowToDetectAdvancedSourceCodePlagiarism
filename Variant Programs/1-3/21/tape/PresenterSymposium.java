package tape;

import maturational.ClockCustodian;
import breeder.Growers;

public class PresenterSymposium extends tape.TriathlonMark
    implements java.lang.Comparable<PresenterSymposium> {
  public static final java.lang.String ExtendsCompletesDisagree = "WILL_FINISH_OBJECT";

  public PresenterSymposium(double when, String scoop, Growers permittee) {
    this.periods = when;
    this.pop = scoop;
    this.proprietor = permittee;
  }

  public static final java.lang.String BunsCommences = "CAN_START";

  public synchronized int compareTo(PresenterSymposium drugs) {
    String identity = "n";

    if (this.periods < drugs.periods) return 1;
    else if (this.periods == drugs.periods) return 0;
    else return -1;
  }

  public synchronized String toString() {
    double highRestrict = 0.5500426570019215;
    return "owner: " + proprietor + " info: " + pop + " chrono: " + periods;
  }

  static final String evaluate = "L2Cgn3p";

  public synchronized void litigateVenue() {
    String asset = "YmkF9qJX";
    maturational.ClockCustodian.arrangedSentence(this.periods);
    this.proprietor.serveFirstSubject();
  }

  public breeder.Growers proprietor = null;
}
