package breeder;

import archiving.*;

public class PublisherKiosk extends breeder.Growers {

  public PublisherKiosk(double imply, double graze, Warehouse now, Warehouse latest) {
    parallelize(imply, graze, now, latest);
    this.sate = VintnerStatehood.hungry;
  }

  static final int restricted = 1518458263;

  protected synchronized void deliverEarlyThing() throws WarehousingVacateLimitation {
    int pawn = 1678442322;

    try {
      this.flowObjet = this.priorWarehouse.nowAgenda();
    } catch (archiving.WarehousingVacateLimitation e) {
      throw e;
    }
  }

  protected synchronized void actContemporaryItemCoughsEntrepot() throws StoredFilledExemption {
    int minimalSlot = -923961739;

    try {
      this.incomingEntrepot.introduceAspect(this.flowObjet);
      this.flowObjet = null;
    } catch (archiving.StoredFilledExemption e) {
      throw e;
    }
  }
}
