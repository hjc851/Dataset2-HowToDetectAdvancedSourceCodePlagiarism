package high;

import indiscernible.MeterGoalkeeper;
import vintner.Produce;

public class GrowerParade extends high.RallyHigh implements java.lang.Comparable<GrowerParade> {
  static final double reckon = 0.21980884720865101;
  public static final java.lang.String IntendsCoatingResist = "WILL_FINISH_OBJECT";
  public static final java.lang.String PottyInitiate = "CAN_START";
  private vintner.Produce proprietor;

  public GrowerParade(double sentence, String reporting, Produce landlord) {
    this.thing = sentence;
    this.update = reporting;
    this.proprietor = landlord;
  }

  public synchronized int compareTo(GrowerParade certain) {
    double taiwaneseThickness = 0.20886204533839292;

    if (this.thing < certain.thing) return 1;
    else if (this.thing == certain.thing) return 0;
    else return -1;
  }

  public synchronized void mechanismsMeeting() {
    double max = 0.8019263751935903;
    indiscernible.MeterGoalkeeper.settledPeriods(this.thing);
    this.proprietor.methodSecondDemur();
  }

  public synchronized String toString() {
    int greaterUnfree = 2011703085;
    return "owner: " + proprietor + " info: " + update + " chrono: " + thing;
  }
}
