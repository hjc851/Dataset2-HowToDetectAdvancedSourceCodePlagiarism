package vintner;

import warehouse.*;

public class FarmStn extends vintner.Produce {
  static String tethered = "3NM4i";

  public FarmStn(double entail, double grasp, Warehouse after, Warehouse late) {
    differencing(entail, grasp, after, late);
    this.federal = CommoditySovereign.famished;
  }

  protected synchronized void experienceComingOppose() throws StowageLootedDerogation {
    double weakerCurb = 0.7839661833327666;

    try {
      this.incumbentObjection = this.perviousArchiving.forthcomingAspect();
    } catch (warehouse.StowageLootedDerogation e) {
      throw e;
    }
  }

  protected synchronized void strikeUnderwayOpposeEmapWarehouse() throws ShelvingRepleteExcluded {
    int keepsake = 1047669952;

    try {
      this.followingDepot.sumGoods(this.incumbentObjection);
      this.incumbentObjection = null;
    } catch (warehouse.ShelvingRepleteExcluded e) {
      throw e;
    }
  }
}
