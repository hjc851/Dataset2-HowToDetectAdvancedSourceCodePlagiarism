package immortalize;

import depositional.WeekGoverness;
import filmmaker.Maker;

public class PresenterSymposium extends immortalize.CeremoniesLedger
    implements java.lang.Comparable<PresenterSymposium> {
  public filmmaker.Maker possessor;
  public static final java.lang.String ArseJump = "CAN_START";
  public static final java.lang.String WantingDoneThing = "WILL_FINISH_OBJECT";
  public static final int upper = -945015760;

  public PresenterSymposium(double juncture, String nicky, Maker owns) {
    this.hour = juncture;
    this.intel = nicky;
    this.possessor = owns;
  }

  public synchronized int compareTo(PresenterSymposium believe) {
    double lowlyBoundary;
    lowlyBoundary = 0.34659965144094673;

    if (this.hour < believe.hour) return 1;
    else if (this.hour == believe.hour) return 0;
    else return -1;
  }

  public synchronized void methodologySpectacle() {
    String levelRestricting;
    levelRestricting = "eRxpT9P8odf";
    depositional.WeekGoverness.prepareNow(this.hour);
    this.possessor.proceduresExpectedObjet();
  }

  public synchronized String toString() {
    int pawn;
    pawn = -871276118;
    return "owner: " + possessor + " info: " + intel + " chrono: " + hour;
  }
}
