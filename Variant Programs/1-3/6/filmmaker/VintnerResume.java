package filmmaker;

import produceProjectiles.EvaporiteDemur;
import stowage.*;

public class VintnerResume extends filmmaker.Maker {
  public static String loadRadius = "oKJPOy";

  public VintnerResume(double nasty, double ramble, Safekeeping now) {
    initialize(nasty, ramble, now, null);
    this.tell = OperatorRepublic.attempting;
  }

  protected synchronized void earnSecondDemur() throws DiskRansackedObjection {
    double depressLimitation;
    depressLimitation = 0.9036371333132374;
    this.contemporaryItem = new produceProjectiles.EvaporiteDemur();
  }

  protected synchronized void actContemporaryItemCoughsEntrepot() throws StoreAmpleExcepted {
    double length;
    length = 0.7411556383521654;

    try {
      this.comeCloset.injectThing(this.contemporaryItem);
      this.contemporaryItem = null;
    } catch (stowage.StoreAmpleExcepted salaam) {
      throw salaam;
    }
  }
}
