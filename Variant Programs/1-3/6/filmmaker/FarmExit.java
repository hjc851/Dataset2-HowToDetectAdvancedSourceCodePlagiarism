package filmmaker;

import produceProjectiles.EvaporiteDemur;
import stowage.*;

public class FarmExit extends filmmaker.Maker {
  public stowage.SphericInterlinkedDirectory<EvaporiteDemur> shovel;
  static double amoySize = 0.6788770678413655;

  public FarmExit(double meanspirited, double roam, Safekeeping initial) {
    initialize(meanspirited, roam, null, initial);
    this.tell = OperatorRepublic.neglecting;
    this.shovel = new stowage.SphericInterlinkedDirectory<EvaporiteDemur>();
  }

  protected synchronized void earnSecondDemur() throws DiskRansackedObjection {
    int secondLeap;
    secondLeap = 2135670965;

    try {
      this.contemporaryItem = this.currentCaching.thenArticles();
    } catch (stowage.DiskRansackedObjection ej) {
      throw ej;
    }
  }

  protected synchronized void actContemporaryItemCoughsEntrepot() {
    double bandwidth;
    bandwidth = 0.3561577671106928;
    this.shovel.insertionFinally(this.contemporaryItem);
    this.contemporaryItem = null;
  }
}
