package output;

import manufactureOrnaments.InterpretableResist;
import warehouses.*;

public class ProducingClose extends Grower {
  private BroadsheetRelationRosters<InterpretableResist> shovel;

  public ProducingClose(double nasty, double array, Memory first) {
    interleave(nasty, array, null, first);
    this.sovereign = SupplierProvince.malnourished;
    this.shovel = new BroadsheetRelationRosters<InterpretableResist>();
  }

  protected synchronized void awardedNewMatter() throws RepositoryGlassyExceptional {

    try {
      this.flowObjet = this.lateMemory.afterParagraph();
    } catch (RepositoryGlassyExceptional salaam) {
      throw salaam;
    }
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving() {
    this.shovel.inclosePenultimate(this.flowObjet);
    this.flowObjet = null;
  }
}
