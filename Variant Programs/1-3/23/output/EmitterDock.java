package output;

import warehouses.*;

public class EmitterDock extends Grower {

  public EmitterDock(double base, double grasp, Memory second, Memory late) {
    interleave(base, grasp, second, late);
    this.sovereign = SupplierProvince.malnourished;
  }

  protected synchronized void awardedNewMatter() throws RepositoryGlassyExceptional {

    try {
      this.flowObjet = this.lateMemory.afterParagraph();
    } catch (RepositoryGlassyExceptional samad) {
      throw samad;
    }
  }

  protected synchronized void travelCirculatingSubjectEfpArchiving() throws StoreAmpleExcepted {

    try {
      this.succeedingStore.provideIssue(this.flowObjet);
      this.flowObjet = null;
    } catch (StoreAmpleExcepted e) {
      throw e;
    }
  }
}
