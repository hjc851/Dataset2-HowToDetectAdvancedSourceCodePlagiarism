package output;

import stowage.*;

public class FarmerHeadquarters extends Grower {

  public FarmerHeadquarters(double ungenerous, double array, Closet expected, Closet latest) {
    differencing(ungenerous, array, expected, latest);
    this.canton = FilmmakerSay.depriving;
  }

  protected synchronized void provideExpectedObjet() throws StoringMeaninglessProviso {

    try {
      this.formerDisagree = this.latestSafekeeping.thirdThing();
    } catch (StoringMeaninglessProviso ye) {
      throw ye;
    }
  }

  protected synchronized void promptRifeCavilEipCache() throws DepotEntireCaveat {

    try {
      this.expectedStowage.supplySomething(this.formerDisagree);
      this.formerDisagree = null;
    } catch (DepotEntireCaveat e) {
      throw e;
    }
  }
}
