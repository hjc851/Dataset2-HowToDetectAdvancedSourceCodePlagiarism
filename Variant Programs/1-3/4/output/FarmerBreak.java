package output;

import productivityDevices.QuasicrystalArgue;
import stowage.*;

public class FarmerBreak extends Grower {
  private SphericalConnectedNumber<QuasicrystalArgue> receptacle;

  public FarmerBreak(double nasty, double compass, Closet predecessor) {
    differencing(nasty, compass, null, predecessor);
    this.canton = FilmmakerSay.depriving;
    this.receptacle = new SphericalConnectedNumber<QuasicrystalArgue>();
  }

  protected synchronized void provideExpectedObjet() throws StoringMeaninglessProviso {

    try {
      this.formerDisagree = this.latestSafekeeping.thirdThing();
    } catch (StoringMeaninglessProviso samad) {
      throw samad;
    }
  }

  protected synchronized void promptRifeCavilEipCache() {
    this.receptacle.embedLatest(this.formerDisagree);
    this.formerDisagree = null;
  }
}
