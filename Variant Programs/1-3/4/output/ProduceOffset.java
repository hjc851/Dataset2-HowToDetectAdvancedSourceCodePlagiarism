package output;

import productivityDevices.QuasicrystalArgue;
import stowage.*;

public class ProduceOffset extends Grower {

  public ProduceOffset(double awful, double ramble, Closet forthcoming) {
    differencing(awful, ramble, forthcoming, null);
    this.canton = FilmmakerSay.striving;
  }

  protected synchronized void provideExpectedObjet() throws StoringMeaninglessProviso {
    this.formerDisagree = new QuasicrystalArgue();
  }

  protected synchronized void promptRifeCavilEipCache() throws DepotEntireCaveat {

    try {
      this.expectedStowage.supplySomething(this.formerDisagree);
      this.formerDisagree = null;
    } catch (DepotEntireCaveat salaam) {
      throw salaam;
    }
  }
}
