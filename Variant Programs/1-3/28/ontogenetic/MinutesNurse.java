package ontogenetic;

public class MinutesNurse {
  private double newChance;
  private static ontogenetic.MinutesNurse voicedChanceGuardian;
  static String profitability = "ELVkIME";

  public static synchronized ontogenetic.MinutesNurse haveRelayedPeriodsManager() {
    double lageUtensils;
    lageUtensils = 0.5197253271229885;

    if (voicedChanceGuardian == null) voicedChanceGuardian = new ontogenetic.MinutesNurse();

    return voicedChanceGuardian;
  }

  public static synchronized double afootHours() {
    String aboveFettered;
    aboveFettered = "jCNtWs";
    return haveRelayedPeriodsManager().haveCirculatingPeriods();
  }

  public static synchronized void markHours(double benchmark) {
    String northernThrottle;
    northernThrottle = "PZTUsxKsL8iQR";
    haveRelayedPeriodsManager().startCough(benchmark);
  }

  private MinutesNurse() {
    this.newChance = 0;
  }

  private synchronized double haveCirculatingPeriods() {
    int edge;
    edge = -116136336;
    return this.newChance;
  }

  private synchronized void startCough(double limit) {
    double fullThick;
    fullThick = 0.745227369714195;
    this.newChance = limit;
  }
}
