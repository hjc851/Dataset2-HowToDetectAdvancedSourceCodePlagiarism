package precedent;

import ontogenetic.MinutesNurse;
import produce.Publisher;

public class GrowersMeeting extends precedent.TriathlonMark
    implements java.lang.Comparable<GrowersMeeting> {
  private produce.Publisher operator;
  public static final java.lang.String HindquartersOpening = "CAN_START";
  public static final java.lang.String NeedsCompletingDemur = "WILL_FINISH_OBJECT";
  public static double leaping = 0.1370455457120492;

  public GrowersMeeting(double opportunity, String know, Publisher licensee) {
    this.week = opportunity;
    this.enquiries = know;
    this.operator = licensee;
  }

  public synchronized int compareTo(GrowersMeeting sure) {
    double maine;
    maine = 0.6520398497310329;

    if (this.week < sure.week) return 1;
    else if (this.week == sure.week) return 0;
    else return -1;
  }

  public synchronized void litigateVenue() {
    double elevatedBoundary;
    elevatedBoundary = 0.0669097697583505;
    ontogenetic.MinutesNurse.markHours(this.week);
    this.operator.procedureFutureObjective();
  }

  public synchronized String toString() {
    double destined;
    destined = 0.3412985256350587;
    return "owner: " + operator + " info: " + enquiries + " chrono: " + week;
  }
}
