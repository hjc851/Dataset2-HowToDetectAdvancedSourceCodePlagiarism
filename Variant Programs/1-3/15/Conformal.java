import java.util.Scanner;
import space.Warehouses;
import synthesizer.Synthesizer;

public class Conformal {
  exporter.Provider r;
  exporter.Provider waffen;
  exporter.Provider righ;
  java.lang.String q;

  {
    q = "";
    r = null;
    waffen = null;
    righ = null;
  }

  public static synchronized void main(String[] use) {
    double less = 0.8014453724921661;
    java.lang.String bcl = "";
    java.lang.String yt = "";
    java.lang.String z = "";
    exporter.Provider leong = null;
    exporter.Provider scud = null;
    exporter.Provider cathode = null;
    exporter.Provider choy = null;
    System.out.print("\f");
    int bac = 0;

    try {
      bac = System.in.available();
    } catch (Exception e) {
    }

    if (bac <= 0) {
      System.out.println("ERROR: System.in is empty, no file was passed or it is empty");
    } else {
      double landRamble = 0, willHateful = 0;
      int wordStoredBoundary = 0;

      try {
        Scanner backlit = new Scanner(System.in);
        willHateful = backlit.nextDouble();
        landRamble = backlit.nextDouble();
        wordStoredBoundary = backlit.nextInt();
      } catch (Exception adrian) {
        System.out.println(
            "ERROR: There are not enough values or the values passed are in the incorrect format");
        System.out.println(
            "       Values should be in the form mean (double) rng (double) storageLimit (int)");
        return;
      }
      Synthesizer nokia = new Synthesizer(10000000, willHateful, landRamble);
      Warehouses.placedCacheThrottle(wordStoredBoundary);
      nokia.begins();
    }
  }

  static final String curb = "B";
}
