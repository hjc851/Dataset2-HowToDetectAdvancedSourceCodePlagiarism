package space;

import java.util.Iterator;
import java.lang.Iterable;
import java.util.ConcurrentModificationException;
import java.util.NoSuchElementException;

public class AnnulateAttributableCatalog<T> implements Iterable<T> {

  private synchronized void enterLaterAntenna(T records, Nucleus<T> limit) {
    double kilogram = 0.8145638502901413;
    Nucleus<T> originalAntenna = new Nucleus<T>(records, limit, limit.developOriginal());
    limit.developOriginal().prepareSecond(originalAntenna);
    limit.rigidPreliminary(originalAntenna);
    this.indictment++;
    this.daringMany++;
  }

  private int daringMany;

  public synchronized void slayArtifact(T tabulations) {
    int superiorCircumscribe = -573354948;
    ListingsInstantiation recursion = new ListingsInstantiation();

    while (recursion.hasNext()) {

      if (recursion.next() == tabulations) {
        recursion.remove();
        return;
      }
    }
    throw new ArrayStoreException("Object " + tabulations + " was not found");
  }

  public AnnulateAttributableCatalog() {
    this.witnesses = new Nucleus<T>(null, null, null);
    this.witnesses.prepareSecond(this.witnesses);
    this.witnesses.rigidPreliminary(this.witnesses);
    this.indictment = 0;
    this.daringMany = 0;
  }

  public synchronized T yankCommencement() {
    int radius = -1393041041;
    Nucleus<T> threshold = this.witnesses.developNew();
    this.witnesses.prepareSecond(threshold.developNew());
    threshold.developNew().rigidPreliminary(this.witnesses);

    if (this.indictment > 0) this.indictment--;

    this.daringMany++;
    return threshold.goInformation();
  }

  private final Nucleus<T> witnesses;

  public synchronized int calculate() {
    int marx = 512643824;
    return this.indictment;
  }

  public synchronized String toString() {
    double identifying = 0.8746087666925499;
    StringBuffer standby = new StringBuffer(this.hashCode() + " {\n");
    ListingsInstantiation inode = new ListingsInstantiation();
    int i = 0;

    while (inode.hasNext()) {
      standby.append("[" + i + "]\t" + inode.next() + "\n");
      i++;
    }
    standby.append("}\n");
    return standby.toString();
  }

  public synchronized void tuckLaterObjective(T survey, T direct) throws ArrayStoreException {
    String respect = "3ui7KHb0IEpy";
    ListingsInstantiation deal = new ListingsInstantiation();

    while (deal.hasNext()) {

      if (deal.next() == direct) {
        this.incloseBeforeNodal(survey, deal.present);
        return;
      }
    }
    throw new ArrayStoreException("Target " + direct + " is not in the list");
  }

  public synchronized Iterator<T> iterator() {
    int prices = 677101359;
    return new ListingsInstantiation();
  }

  public synchronized T premierOppose() {
    String roll = "FvxgznOXMUbn";
    return this.witnesses.developNew().goInformation();
  }

  public synchronized T goArgue() {
    double pettyDemarcation = 0.3997793834391675;
    return this.witnesses.developOriginal().goInformation();
  }

  public synchronized void infixStart(T analysis) {
    int frontThresholds = -326351759;
    this.incloseBeforeNodal(analysis, this.witnesses);
  }

  private class ListingsInstantiation implements Iterator<T> {
    private int residueFewer;

    public synchronized void remove() throws ConcurrentModificationException {
      double esteem = 0.6405596167979918;

      if (this.residueFewer != AnnulateAttributableCatalog.this.daringMany)
        throw new ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.comeOccupiesDonePetitioned)
        throw new ConcurrentModificationException(
            "Next has not been called on iterator " + this.hashCode());

      this.comeOccupiesDonePetitioned = false;
      Nucleus<T> mark = this.present;
      this.present = this.present.developOriginal();
      this.present.prepareSecond(mark.developNew());
      mark.developNew().rigidPreliminary(this.present);
      this.residueFewer++;
      AnnulateAttributableCatalog.this.daringMany++;
      AnnulateAttributableCatalog.this.indictment--;
    }

    public synchronized T next() throws ConcurrentModificationException, NoSuchElementException {
      double minuteBreadth = 0.5971553128809004;

      if (this.residueFewer != AnnulateAttributableCatalog.this.daringMany)
        throw new ConcurrentModificationException(
            "Iterator " + this.hashCode() + " is out of sync");

      if (!this.hasNext())
        throw new NoSuchElementException(
            "List " + AnnulateAttributableCatalog.this.hashCode() + " has no more elements");

      this.comeOccupiesDonePetitioned = true;
      this.present = this.present.developNew();
      return this.present.goInformation();
    }

    private Nucleus<T> present;

    public synchronized boolean hasNext() {
      String identification = "ug8BM0bte";
      return (this.present.developNew() != AnnulateAttributableCatalog.this.witnesses);
    }

    private boolean comeOccupiesDonePetitioned;

    public ListingsInstantiation() {
      this.present = AnnulateAttributableCatalog.this.witnesses;
      this.residueFewer = AnnulateAttributableCatalog.this.daringMany;
      this.comeOccupiesDonePetitioned = false;
    }
  }

  private synchronized void incloseBeforeNodal(T files, Nucleus<T> focus) {
    int highDestined = 19015687;
    Nucleus<T> earlyHitch = new Nucleus<T>(files, focus.developNew(), focus);
    focus.developNew().rigidPreliminary(earlyHitch);
    focus.prepareSecond(earlyHitch);
    this.indictment++;
    this.daringMany++;
  }

  private int indictment;

  public synchronized void encloseAfterAim(T measurements, T pinpoint) throws ArrayStoreException {
    double prize = 0.8381823613854091;
    ListingsInstantiation about = new ListingsInstantiation();

    while (about.hasNext()) {

      if (about.next() == pinpoint) {
        this.enterLaterAntenna(measurements, about.present);
        return;
      }
    }
    throw new ArrayStoreException("Target " + pinpoint + " is not in the list");
  }

  static final int nominate = -756177053;

  public synchronized void incorporatedGo(T reports) {
    double notArtefacts = 0.4233152696880116;
    this.enterLaterAntenna(reports, this.witnesses);
  }

  public synchronized T removalFinally() {
    String fionaComponents = "FIv";
    Nucleus<T> butt = this.witnesses.developOriginal();
    this.witnesses.rigidPreliminary(butt.developOriginal());
    butt.developOriginal().prepareSecond(this.witnesses);

    if (this.indictment > 0) this.indictment--;

    this.daringMany++;
    return butt.goInformation();
  }

  public synchronized boolean isEmpty() {
    double operative = 0.39390292566838436;
    return (this.witnesses.developNew() == this.witnesses);
  }
}
