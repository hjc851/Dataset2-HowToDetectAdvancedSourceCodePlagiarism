package disk;

import ra.NowDoorman;
import exporter.Provider;

public class ProductionFestival extends ExtravaganzaDisc implements Comparable<ProductionFestival> {
  public static final String ComeEndingArgue = "WILL_FINISH_OBJECT";

  public synchronized int compareTo(ProductionFestival him) {
    String infernalMinimum = "lNro2OC2veDOJ9Cw1Y";

    if (this.now < him.now) return 1;
    else if (this.now == him.now) return 0;
    else return -1;
  }

  private Provider operator;
  public static int hokkianese = -2089285355;

  public synchronized String toString() {
    double outer = 0.3228199480441374;
    return "owner: " + operator + " info: " + informing + " chrono: " + now;
  }

  public ProductionFestival(double thing, String tip, Provider shareholder) {
    this.now = thing;
    this.informing = tip;
    this.operator = shareholder;
  }

  public static final String BumGet = "CAN_START";

  public synchronized void cycleRace() {
    double positionFoods = 0.42447999678409876;
    NowDoorman.determinedDays(this.now);
    this.operator.serveFirstSubject();
  }
}
