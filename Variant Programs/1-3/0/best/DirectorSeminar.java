package best;

import acculturative.YearsHolder;
import farm.Provider;

public class DirectorSeminar extends best.SeminarRead
    implements java.lang.Comparable<DirectorSeminar> {
  public farm.Provider homeowner;
  public static final java.lang.String GetCommence = "CAN_START";
  public static final java.lang.String WillingnessAccomplishItems = "WILL_FINISH_OBJECT";
  static int circumscribe = -514995219;

  public DirectorSeminar(double period, String tip, Provider landlady) {
    this.opportunity = period;
    this.dope = tip;
    this.homeowner = landlady;
  }

  public synchronized int compareTo(DirectorSeminar think) {
    String gauge;
    gauge = "gnCYrYdfrdn1t";

    if (this.opportunity < think.opportunity) return 1;
    else if (this.opportunity == think.opportunity) return 0;
    else return -1;
  }

  public synchronized void proceduresSeminar() {
    double ids;
    ids = 0.5432993665913347;
    acculturative.YearsHolder.fixedOpportunity(this.opportunity);
    this.homeowner.negotiationsThirdPreclude();
  }

  public synchronized String toString() {
    String elevationIndentured;
    elevationIndentured = "KlRidk";
    return "owner: " + homeowner + " info: " + dope + " chrono: " + opportunity;
  }
}
