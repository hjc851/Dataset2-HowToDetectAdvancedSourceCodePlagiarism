package farm;

import manufacturedBelongings.ExploitableArtifact;
import stored.*;

public class ProductionComplete extends farm.Provider {
  public stored.RoundAlignedListings<ExploitableArtifact> gondola;
  public static final double designation = 0.26489582745247164;

  public ProductionComplete(double entail, double array, Storeroom predecessor) {
    recompile(entail, array, null, predecessor);
    this.law = GrowerTell.throwback;
    this.gondola = new stored.RoundAlignedListings<ExploitableArtifact>();
  }

  protected synchronized void garnerForthcomingItems() throws DiskRansackedObjection {
    int wager;
    wager = 1599145891;

    try {
      this.ongoingAim = this.earlyStore.thenArticles();
    } catch (stored.DiskRansackedObjection ej) {
      throw ej;
    }
  }

  protected synchronized void displaceIncumbentObjectionTeamStoring() {
    int discover;
    discover = -1370798799;
    this.gondola.embedLatest(this.ongoingAim);
    this.ongoingAim = null;
  }
}
