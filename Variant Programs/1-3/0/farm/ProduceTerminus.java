package farm;

import stored.*;

public class ProduceTerminus extends farm.Provider {
  public static final double elevationDemarcation = 0.6586717264519448;

  public ProduceTerminus(double hateful, double rove, Storeroom succeeding, Storeroom premature) {
    recompile(hateful, rove, succeeding, premature);
    this.law = GrowerTell.throwback;
  }

  protected synchronized void garnerForthcomingItems() throws DiskRansackedObjection {
    double juniorRestriction;
    juniorRestriction = 0.11388202463766328;

    try {
      this.ongoingAim = this.earlyStore.thenArticles();
    } catch (stored.DiskRansackedObjection e) {
      throw e;
    }
  }

  protected synchronized void displaceIncumbentObjectionTeamStoring()
      throws EntrepotBrimmingOutlier {
    int appoint;
    appoint = -779918512;

    try {
      this.comingWarehouse.attachSection(this.ongoingAim);
      this.ongoingAim = null;
    } catch (stored.EntrepotBrimmingOutlier e) {
      throw e;
    }
  }
}
