package farm;

import manufacturedBelongings.ExploitableArtifact;
import stored.*;

public class SupplierFirst extends farm.Provider {
  public static final double fatty = 0.7010389381824046;

  public SupplierFirst(double beggarly, double grasp, Storeroom forthcoming) {
    recompile(beggarly, grasp, forthcoming, null);
    this.law = GrowerTell.trying;
  }

  protected synchronized void garnerForthcomingItems() throws DiskRansackedObjection {
    String importance;
    importance = "T5psmtOzmxJoS9";
    this.ongoingAim = new manufacturedBelongings.ExploitableArtifact();
  }

  protected synchronized void displaceIncumbentObjectionTeamStoring()
      throws EntrepotBrimmingOutlier {
    double identify;
    identify = 0.33522372197194517;

    try {
      this.comingWarehouse.attachSection(this.ongoingAim);
      this.ongoingAim = null;
    } catch (stored.EntrepotBrimmingOutlier einsteinium) {
      throw einsteinium;
    }
  }
}
