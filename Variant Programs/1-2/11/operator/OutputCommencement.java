package operator;

import industrializationInstallations.ClasticVictim;
import warehouse.*;

public class OutputCommencement extends Produce {

  public OutputCommencement(double ungenerous, double scope, Memory early) {
    uncompress(ungenerous, scope, early, null);
    this.government = MakerLand.running;
  }

  protected void eligibleSoonBody() throws MemoryLifelessExcept {
    this.presentPreclude = new ClasticVictim();
  }

  protected void pushExistingPurposeOfficesArchival() throws DiskWholeObjection {

    try {
      this.comingWarehouse.summateIngredient(this.presentPreclude);
      this.presentPreclude = null;
    } catch (DiskWholeObjection ej) {
      throw ej;
    }
  }
}
