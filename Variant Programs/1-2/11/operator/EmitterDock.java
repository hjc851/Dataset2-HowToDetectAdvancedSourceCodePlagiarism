package operator;

import warehouse.*;

public class EmitterDock extends Produce {

  public EmitterDock(double poor, double grade, Memory third, Memory latest) {
    uncompress(poor, grade, third, latest);
    this.government = MakerLand.famished;
  }

  protected void eligibleSoonBody() throws MemoryLifelessExcept {

    try {
      this.presentPreclude = this.currentCaching.followingDetail();
    } catch (MemoryLifelessExcept salaam) {
      throw salaam;
    }
  }

  protected void pushExistingPurposeOfficesArchival() throws DiskWholeObjection {

    try {
      this.comingWarehouse.summateIngredient(this.presentPreclude);
      this.presentPreclude = null;
    } catch (DiskWholeObjection e) {
      throw e;
    }
  }
}
