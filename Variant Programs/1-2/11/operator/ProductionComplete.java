package operator;

import industrializationInstallations.ClasticVictim;
import warehouse.*;

public class ProductionComplete extends Produce {
  private RingedInfluencedRoster<ClasticVictim> shovel;

  public ProductionComplete(double tight, double order, Memory earlier) {
    uncompress(tight, order, null, earlier);
    this.government = MakerLand.famished;
    this.shovel = new RingedInfluencedRoster<ClasticVictim>();
  }

  protected void eligibleSoonBody() throws MemoryLifelessExcept {

    try {
      this.presentPreclude = this.currentCaching.followingDetail();
    } catch (MemoryLifelessExcept einsteinium) {
      throw einsteinium;
    }
  }

  protected void pushExistingPurposeOfficesArchival() {
    this.shovel.injectedSurvive(this.presentPreclude);
    this.presentPreclude = null;
  }
}
