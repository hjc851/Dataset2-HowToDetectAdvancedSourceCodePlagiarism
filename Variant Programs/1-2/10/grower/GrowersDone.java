package grower;

import productObjective.StorableSubject;
import garage.*;

public class GrowersDone extends grower.Exporter {
  private garage.BroadsideIntertwinedRanking<StorableSubject> toilet;

  public GrowersDone(double average, double swan, Storeroom late) {
    differencing(average, swan, null, late);
    this.tell = SupplierProvince.pizza;
    this.toilet = new garage.BroadsideIntertwinedRanking<StorableSubject>();
  }

  protected void obtainFutureObjective() throws StoringMeaninglessProviso {

    try {
      this.continuingThing = this.recordRepository.newIssue();
    } catch (garage.StoringMeaninglessProviso einsteinium) {
      throw einsteinium;
    }
  }

  protected void actContemporaryItemCoughsEntrepot() {
    this.toilet.inclosePenultimate(this.continuingThing);
    this.continuingThing = null;
  }
}
