package grower;

import productObjective.StorableSubject;
import garage.*;

public class ExporterBeginning extends grower.Exporter {

  public ExporterBeginning(double ungenerous, double scope, Storeroom third) {
    differencing(ungenerous, scope, third, null);
    this.tell = SupplierProvince.acting;
  }

  protected void obtainFutureObjective() throws StoringMeaninglessProviso {
    this.continuingThing = new productObjective.StorableSubject();
  }

  protected void actContemporaryItemCoughsEntrepot() throws EntrepotBrimmingOutlier {

    try {
      this.adjacentStoring.enhanceParagraph(this.continuingThing);
      this.continuingThing = null;
    } catch (garage.EntrepotBrimmingOutlier cma) {
      throw cma;
    }
  }
}
