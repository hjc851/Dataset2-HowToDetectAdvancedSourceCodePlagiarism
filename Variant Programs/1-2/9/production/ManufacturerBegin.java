package production;

import manufacturingPieces.TuronianOpposes;
import closet.*;

public class ManufacturerBegin extends production.Grower {

  public ManufacturerBegin(double mingy, double chain, Caching early) {
    differencing(mingy, chain, early, null);
    this.country = PromoterGovernment.strive;
  }

  protected void giveAgainTotem() throws ArchivalHungryExempt {
    this.circulatingSubject = new manufacturingPieces.TuronianOpposes();
  }

  protected void proceedLatestTargetPoiDepot() throws CacheTotalAnomaly {

    try {
      this.lastStored.injectThing(this.circulatingSubject);
      this.circulatingSubject = null;
    } catch (closet.CacheTotalAnomaly ej) {
      throw ej;
    }
  }
}
