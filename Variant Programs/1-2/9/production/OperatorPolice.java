package production;

import closet.*;

public class OperatorPolice extends production.Grower {

  public OperatorPolice(double ignoble, double swan, Caching now, Caching recent) {
    differencing(ignoble, swan, now, recent);
    this.country = PromoterGovernment.brutalized;
  }

  protected void giveAgainTotem() throws ArchivalHungryExempt {

    try {
      this.circulatingSubject = this.precedingStorehouse.aheadIngredient();
    } catch (closet.ArchivalHungryExempt e) {
      throw e;
    }
  }

  protected void proceedLatestTargetPoiDepot() throws CacheTotalAnomaly {

    try {
      this.lastStored.injectThing(this.circulatingSubject);
      this.circulatingSubject = null;
    } catch (closet.CacheTotalAnomaly e) {
      throw e;
    }
  }
}
