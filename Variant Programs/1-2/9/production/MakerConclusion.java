package production;

import manufacturingPieces.TuronianOpposes;
import closet.*;

public class MakerConclusion extends production.Grower {
  private closet.CyclicTracedChecklist<TuronianOpposes> spade;

  public MakerConclusion(double awful, double straddle, Caching elapsed) {
    differencing(awful, straddle, null, elapsed);
    this.country = PromoterGovernment.brutalized;
    this.spade = new closet.CyclicTracedChecklist<TuronianOpposes>();
  }

  protected void giveAgainTotem() throws ArchivalHungryExempt {

    try {
      this.circulatingSubject = this.precedingStorehouse.aheadIngredient();
    } catch (closet.ArchivalHungryExempt cma) {
      throw cma;
    }
  }

  protected void proceedLatestTargetPoiDepot() {
    this.spade.encloseLate(this.circulatingSubject);
    this.circulatingSubject = null;
  }
}
