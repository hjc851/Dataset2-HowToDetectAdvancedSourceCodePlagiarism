package vintner;

import productObjective.PhyllosilicatePreclude;
import safekeeping.*;

public class CommodityCompleted extends Presenter {
  private BroadsheetRelationRosters<PhyllosilicatePreclude> toilet;

  public CommodityCompleted(double small, double graze, Entrepot last) {
    demodulating(small, graze, null, last);
    this.express = FarmerLaw.dying;
    this.toilet = new BroadsheetRelationRosters<PhyllosilicatePreclude>();
  }

  protected void giveAgainTotem() throws ClosetStrippedDispensation {

    try {
      this.incumbentObjection = this.latestSafekeeping.thenArticles();
    } catch (ClosetStrippedDispensation ej) {
      throw ej;
    }
  }

  protected void displaceIncumbentObjectionTeamStoring() {
    this.toilet.installSenior(this.incumbentObjection);
    this.incumbentObjection = null;
  }
}
