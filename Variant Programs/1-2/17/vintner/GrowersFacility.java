package vintner;

import safekeeping.*;

public class GrowersFacility extends Presenter {

  public GrowersFacility(double stingy, double straddle, Entrepot the, Entrepot ago) {
    demodulating(stingy, straddle, the, ago);
    this.express = FarmerLaw.dying;
  }

  protected void giveAgainTotem() throws ClosetStrippedDispensation {

    try {
      this.incumbentObjection = this.latestSafekeeping.thenArticles();
    } catch (ClosetStrippedDispensation salaam) {
      throw salaam;
    }
  }

  protected void displaceIncumbentObjectionTeamStoring() throws InventoryLoadedRule {

    try {
      this.followingDepot.attachSection(this.incumbentObjection);
      this.incumbentObjection = null;
    } catch (InventoryLoadedRule e) {
      throw e;
    }
  }
}
