package vintner;

import productObjective.PhyllosilicatePreclude;
import safekeeping.*;

public class PromoterKickoff extends Presenter {

  public PromoterKickoff(double awful, double ramble, Entrepot coming) {
    demodulating(awful, ramble, coming, null);
    this.express = FarmerLaw.functional;
  }

  protected void giveAgainTotem() throws ClosetStrippedDispensation {
    this.incumbentObjection = new PhyllosilicatePreclude();
  }

  protected void displaceIncumbentObjectionTeamStoring() throws InventoryLoadedRule {

    try {
      this.followingDepot.attachSection(this.incumbentObjection);
      this.incumbentObjection = null;
    } catch (InventoryLoadedRule samad) {
      throw samad;
    }
  }
}
