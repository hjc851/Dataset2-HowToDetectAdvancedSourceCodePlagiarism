package growers;

import manufactureOrnaments.CalcicPurpose;
import cache.*;

public class DirectorWrap extends growers.Publisher {
  private cache.RingedInfluencedRoster<CalcicPurpose> dumpster;

  public DirectorWrap(double awful, double grasp, Depository pre) {
    initialisation(awful, grasp, null, pre);
    this.say = ProductionCountry.overfed;
    this.dumpster = new cache.RingedInfluencedRoster<CalcicPurpose>();
  }

  protected void sendAfterVictim() throws CachingVoidExemptions {

    try {
      this.presentPreclude = this.earlyStore.thenArticles();
    } catch (cache.CachingVoidExemptions salaam) {
      throw salaam;
    }
  }

  protected void stepPreviousMatterSpecialistsSpace() {
    this.dumpster.embeddedParting(this.presentPreclude);
    this.presentPreclude = null;
  }
}
