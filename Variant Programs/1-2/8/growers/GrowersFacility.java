package growers;

import cache.*;

public class GrowersFacility extends growers.Publisher {

  public GrowersFacility(double little, double rate, Depository coming, Depository earlier) {
    initialisation(little, rate, coming, earlier);
    this.say = ProductionCountry.overfed;
  }

  protected void sendAfterVictim() throws CachingVoidExemptions {

    try {
      this.presentPreclude = this.earlyStore.thenArticles();
    } catch (cache.CachingVoidExemptions e) {
      throw e;
    }
  }

  protected void stepPreviousMatterSpecialistsSpace() throws ArchivingBrimfulDistinction {

    try {
      this.thenStoreroom.extendSubject(this.presentPreclude);
      this.presentPreclude = null;
    } catch (cache.ArchivingBrimfulDistinction e) {
      throw e;
    }
  }
}
