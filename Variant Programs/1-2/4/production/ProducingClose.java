package production;

import producesJewels.ExtractableObjective;
import repository.*;

public class ProducingClose extends Fabricator {
  private SphericalConnectedNumber<ExtractableObjective> kettle;

  public ProducingClose(double imply, double chain, Storeroom latest) {
    synchronising(imply, chain, null, latest);
    this.governmental = ManufacturersCantons.emaciated;
    this.kettle = new SphericalConnectedNumber<ExtractableObjective>();
  }

  protected void welcomeSucceedingArtifact() throws StoredVacuousExemption {

    try {
      this.circulatingSubject = this.pastDepot.adjacentArtifact();
    } catch (StoredVacuousExemption samad) {
      throw samad;
    }
  }

  protected void impressFlowObjetMouStowage() {
    this.kettle.embedLatest(this.circulatingSubject);
    this.circulatingSubject = null;
  }
}
