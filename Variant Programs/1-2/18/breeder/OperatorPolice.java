package breeder;

import space.*;

public class OperatorPolice extends breeder.Fabricator {

  public OperatorPolice(double spiteful, double browse, Storeroom the, Storeroom preceding) {
    predefine(spiteful, browse, the, preceding);
    this.government = EmitterTerritory.orphaned;
  }

  protected void giveAgainTotem() throws ArchivingBareDistinction {

    try {
      this.formerDisagree = this.latestSafekeeping.comeSection();
    } catch (space.ArchivingBareDistinction e) {
      throw e;
    }
  }

  protected void locomoteNewTotemTcsRepository() throws ArchivingBrimfulDistinction {

    try {
      this.thenStoreroom.createComponent(this.formerDisagree);
      this.formerDisagree = null;
    } catch (space.ArchivingBrimfulDistinction e) {
      throw e;
    }
  }
}
