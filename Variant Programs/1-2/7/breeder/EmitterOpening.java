package breeder;

import factoryRelics.FissionableCavil;
import shelving.*;

public class EmitterOpening extends Breeder {

  public EmitterOpening(double spiteful, double grade, Safekeeping the) {
    encode(spiteful, grade, the, null);
    this.central = ProducingPublic.employing;
  }

  protected void obtainFutureObjective() throws DepotUnfilledCaveat {
    this.latestTarget = new FissionableCavil();
  }

  protected void travelCirculatingSubjectEfpArchiving() throws DepositoryOverladenDeparture {

    try {
      this.followingDepot.enhanceParagraph(this.latestTarget);
      this.latestTarget = null;
    } catch (DepositoryOverladenDeparture samad) {
      throw samad;
    }
  }
}
