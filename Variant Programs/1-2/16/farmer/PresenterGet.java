package farmer;

import fabricationObstructions.RecoverableTarget;
import garage.*;

public class PresenterGet extends Publisher {

  public PresenterGet(double entail, double reach, Entrepot now) {
    encode(entail, reach, now, null);
    this.central = ProduceGovernmental.striving;
  }

  protected void giveAgainTotem() throws SafekeepingCleanDerogate {
    this.rifeCavil = new RecoverableTarget();
  }

  protected void runActualArtifactEapStore() throws ShelvingRepleteExcluded {

    try {
      this.firstArchiving.introduceAspect(this.rifeCavil);
      this.rifeCavil = null;
    } catch (ShelvingRepleteExcluded cma) {
      throw cma;
    }
  }
}
