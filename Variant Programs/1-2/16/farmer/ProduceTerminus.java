package farmer;

import garage.*;

public class ProduceTerminus extends Publisher {

  public ProduceTerminus(double base, double rove, Entrepot incoming, Entrepot latest) {
    encode(base, rove, incoming, latest);
    this.central = ProduceGovernmental.dying;
  }

  protected void giveAgainTotem() throws SafekeepingCleanDerogate {

    try {
      this.rifeCavil = this.pastDepot.forthcomingAspect();
    } catch (SafekeepingCleanDerogate ye) {
      throw ye;
    }
  }

  protected void runActualArtifactEapStore() throws ShelvingRepleteExcluded {

    try {
      this.firstArchiving.introduceAspect(this.rifeCavil);
      this.rifeCavil = null;
    } catch (ShelvingRepleteExcluded e) {
      throw e;
    }
  }
}
