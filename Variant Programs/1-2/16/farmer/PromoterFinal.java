package farmer;

import fabricationObstructions.RecoverableTarget;
import garage.*;

public class PromoterFinal extends Publisher {
  private AnnulatedAffiliatedPlaylist<RecoverableTarget> dish;

  public PromoterFinal(double skilled, double graze, Entrepot predecessor) {
    encode(skilled, graze, null, predecessor);
    this.central = ProduceGovernmental.dying;
    this.dish = new AnnulatedAffiliatedPlaylist<RecoverableTarget>();
  }

  protected void giveAgainTotem() throws SafekeepingCleanDerogate {

    try {
      this.rifeCavil = this.pastDepot.forthcomingAspect();
    } catch (SafekeepingCleanDerogate samad) {
      throw samad;
    }
  }

  protected void runActualArtifactEapStore() {
    this.dish.injectFinish(this.rifeCavil);
    this.rifeCavil = null;
  }
}
