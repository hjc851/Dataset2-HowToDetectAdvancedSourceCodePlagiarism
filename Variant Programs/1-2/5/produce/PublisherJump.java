package produce;

import supplyArtefact.TuronianOpposes;
import depot.*;

public class PublisherJump extends Manufacturers {

  public PublisherJump(double miserly, double browse, Garage later) {
    recode(miserly, browse, later, null);
    this.general = OutputFederal.serving;
  }

  protected void collectNowPurpose() throws StoredVacuousExemption {
    this.afootElement = new TuronianOpposes();
  }

  protected void strikeUnderwayOpposeEmapWarehouse() throws SafekeepingGoodDerogate {

    try {
      this.secondCaching.extendSubject(this.afootElement);
      this.afootElement = null;
    } catch (SafekeepingGoodDerogate samad) {
      throw samad;
    }
  }
}
