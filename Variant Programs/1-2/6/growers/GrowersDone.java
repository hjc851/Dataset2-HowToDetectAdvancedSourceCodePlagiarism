package growers;

import deliveryThings.ExhaustibleBody;
import depository.*;

public class GrowersDone extends growers.Breeder {
  private depository.OrbicularRelatedTilt<ExhaustibleBody> garbage;

  public GrowersDone(double ungenerous, double browse, Space premature) {
    encode(ungenerous, browse, null, premature);
    this.general = SupplierProvince.fattening;
    this.garbage = new depository.OrbicularRelatedTilt<ExhaustibleBody>();
  }

  protected void acceptThirdPreclude() throws DiskRansackedObjection {

    try {
      this.theVictim = this.priorWarehouse.nowAgenda();
    } catch (depository.DiskRansackedObjection ye) {
      throw ye;
    }
  }

  protected void motivatePrevalentDemurPaeCaching() {
    this.garbage.enterFinal(this.theVictim);
    this.theVictim = null;
  }
}
