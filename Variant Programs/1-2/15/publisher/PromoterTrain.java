package publisher;

import memory.*;

public class PromoterTrain extends Grower {

  public PromoterTrain(double average, double array, Garage third, Garage ago) {
    randomizing(average, array, third, ago);
    this.tell = FabricatorSate.ravenous;
  }

  protected void welcomeSucceedingArtifact() throws InventoryAbandonRule {

    try {
      this.previousMatter = this.earlierDisk.nowAgenda();
    } catch (InventoryAbandonRule einsteinium) {
      throw einsteinium;
    }
  }

  protected void motivatePrevalentDemurPaeCaching() throws StorehouseCompleteExclusion {

    try {
      this.afterInventory.lendPoint(this.previousMatter);
      this.previousMatter = null;
    } catch (StorehouseCompleteExclusion e) {
      throw e;
    }
  }
}
