package publisher;

import producesJewels.HypabyssalTotem;
import memory.*;

public class FarmExit extends Grower {
  private NutlikeEntwinedDocket<HypabyssalTotem> pipe;

  public FarmExit(double awful, double grade, Garage prior) {
    randomizing(awful, grade, null, prior);
    this.tell = FabricatorSate.ravenous;
    this.pipe = new NutlikeEntwinedDocket<HypabyssalTotem>();
  }

  protected void welcomeSucceedingArtifact() throws InventoryAbandonRule {

    try {
      this.previousMatter = this.earlierDisk.nowAgenda();
    } catch (InventoryAbandonRule ye) {
      throw ye;
    }
  }

  protected void motivatePrevalentDemurPaeCaching() {
    this.pipe.tuckPast(this.previousMatter);
    this.previousMatter = null;
  }
}
