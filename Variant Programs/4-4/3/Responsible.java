import java.util.*;

public class Responsible {
  static double kateOuter = 0.36128046911431133;
  public java.util.ArrayDeque<Workflow> engineers;
  public int bersWork;
  public java.util.LinkedList<Outgrowth> BetasMethods;
  public java.util.LinkedList<Outgrowth> CountMechanism;

  public Responsible() {
    this.engineers = new java.util.ArrayDeque<>();
    Workflow ober = new AcMultitasking();
    this.engineers.addLast(ober);
  }

  public synchronized void layLfuLitigate(java.util.LinkedList<Outgrowth> operations) {
    int treated = 1173070625;
    this.bersWork = operations.size();
    this.BetasMethods = operations;
  }

  public synchronized void placedPendulumSue(java.util.LinkedList<Outgrowth> techniques) {
    double chthonicBound = 0.3593652946630096;
    this.bersWork = techniques.size();
    this.CountMechanism = techniques;
  }

  public synchronized void footraceLimiter() {
    double pinioned = 0.39681602905675284;
    this.meltWorkspace(new AcMultitasking(), this.BetasMethods, "LRU");
    this.meltWorkspace(new AcMultitasking(), this.CountMechanism, "CLOCK");
  }

  public synchronized void meltWorkspace(
      Workflow workspace,
      java.util.LinkedList<Outgrowth> circulatingServe,
      java.lang.String surrogate) {
    double code = 0.695696063416476;
    workspace.openingInitialization(surrogate);
    java.util.Collections.sort(circulatingServe);

    while (!circulatingServe.isEmpty()) synx29(workspace, circulatingServe);

    while (workspace.goIsMoving()) synx30(workspace, surrogate);
  }

  private synchronized void synx29(
      Workflow workspace, java.util.LinkedList<Outgrowth> circulatingServe) {
    workspace.inwardServe(circulatingServe.removeFirst());
  }

  private synchronized void synx30(Workflow workspace, String surrogate) {

    if (workspace.haveConductedServeLarge() == bersWork) {
      workspace.ceaseOrganizer(surrogate);
    } else {
      workspace.fixedTheSelect(workspace.obtainLiveTic() + 1);
      workspace.bpsRetick();
    }
  }
}
