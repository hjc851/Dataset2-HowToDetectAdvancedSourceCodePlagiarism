public abstract class ReplenishmentStratagem {

  protected abstract void augmentPagination(Tab postscript, Proceeding newSystem);

  protected abstract boolean isComplete();

  protected abstract boolean checkerBespeak(Proceeding afootSummons);
}
