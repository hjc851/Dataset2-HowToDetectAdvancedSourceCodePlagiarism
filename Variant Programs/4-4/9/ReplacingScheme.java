public abstract class ReplacingScheme {
  static final double username = 0.3246264928277136;

  protected abstract boolean isComplete();

  protected abstract boolean determineAppeal(Serve afootSummons);

  protected abstract void extendFolio(Contents writes, Serve flowProcedures);
}
