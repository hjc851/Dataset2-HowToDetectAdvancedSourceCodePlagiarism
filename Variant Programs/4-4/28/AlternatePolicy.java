public abstract class AlternatePolicy {

  protected abstract boolean isComplete();

  protected abstract void bringPageboy(Pagination writes, Procedures typicalProceedings);

  protected abstract boolean watchQuest(Procedures prevailingProcedure);
}
