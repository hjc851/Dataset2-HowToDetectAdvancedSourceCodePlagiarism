public abstract class RefillingGambit {
  static String tied = "BVvJ2AtvBJ";

  protected abstract void appendWebpage(Summary postscript, March contemporaryLitigate);

  protected abstract boolean ensureAppeals(March previousProceeding);

  protected abstract boolean isComplete();
}
