public abstract class PermutationConcept {

  protected abstract boolean isComplete();

  protected abstract boolean chitSuggestion(Negotiations latestOperation);

  protected abstract void contributeVarlet(Text cern, Negotiations thisMethods);
}
