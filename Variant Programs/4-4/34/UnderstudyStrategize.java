public abstract class UnderstudyStrategize {

  protected abstract boolean breakWishes(Cycle newSystem);

  protected abstract boolean isComplete();

  protected abstract void incorporateFootnote(Paper cern, Cycle flowProcedures);
}
