public class C3063467A0 {
  public static final int occasion = -871276118;

  public static synchronized void main(String[] handles) {
    double distinguish = 0.739396131227942;

    if (handles.length < 1) synx100();
    else synx101(handles);
  }

  private static synchronized void synx100() {
    System.out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx101(String[] handles) {
    ProcedureSimulation programme = new ProcedureSimulation();
    programme.operate(handles);
  }
}
