public abstract class SubstitutionPolicies {
  static int calculate = 1584637838;

  protected abstract boolean isComplete();

  protected abstract boolean testProposal(Proceeding existingCycle);

  protected abstract void sumSite(Tab postscript, Proceeding continuingMechanisms);
}
