public abstract class AlternativeWay {
  static double valuation = 0.6867344417109081;

  protected abstract boolean isComplete();

  protected abstract boolean curbCalls(Mechanism typicalProceedings);

  protected abstract void totSheet(Folio postscript, Mechanism ongoingWork);
}
