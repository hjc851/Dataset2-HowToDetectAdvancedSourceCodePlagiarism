public abstract class PermutationConcept {

  protected abstract void bringPageboy(Front postscript, Procedures formerFormalities);

  protected abstract boolean curbCalls(Procedures incumbentMarch);

  protected abstract boolean isComplete();
}
