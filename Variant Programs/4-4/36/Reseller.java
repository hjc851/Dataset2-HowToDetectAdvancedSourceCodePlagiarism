import java.util.*;

public class Reseller {

  public synchronized void outpouringPlenum() {
    this.bleedDatabase(new StaOutliner(), this.LfuLitigate, "LRU");
    this.bleedDatabase(new StaOutliner(), this.TimerAct, "CLOCK");
  }

  public ArrayDeque<Workflow> managers;

  public synchronized void bleedDatabase(
      Workflow controller, LinkedList<Procedures> flowProcedures, String replaces) {
    controller.restartDatabase(replaces);
    Collections.sort(flowProcedures);

    while (!flowProcedures.isEmpty()) synx267(controller, flowProcedures);

    while (controller.goIsMoving()) synx268(controller, replaces);
  }

  public Reseller() {
    this.managers = new ArrayDeque<>();
    Workflow lyra = new StaOutliner();
    this.managers.addLast(lyra);
  }

  public LinkedList<Procedures> TimerAct;
  public LinkedList<Procedures> LfuLitigate;

  public synchronized void rigidDMethod(LinkedList<Procedures> treat) {
    this.briProces = treat.size();
    this.LfuLitigate = treat;
  }

  public synchronized void layCountdownLitigate(LinkedList<Procedures> operation) {
    this.briProces = operation.size();
    this.TimerAct = operation;
  }

  public int briProces;

  private synchronized void synx267(
      Workflow controller, java.util.LinkedList<Procedures> flowProcedures) {
    controller.mortarProceedings(flowProcedures.removeFirst());
  }

  private synchronized void synx268(Workflow controller, String replaces) {

    if (controller.fixAchievedMechanismsProportions() == briProces) {
      controller.closureServer(replaces);
    } else {
      controller.settledCirculatingShudder(controller.bringOngoingBeat() + 1);
      controller.weapMark();
    }
  }
}
