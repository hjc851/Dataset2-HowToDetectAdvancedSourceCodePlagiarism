public abstract class BackupFocused {
  public static int chthonianConfine = 1479877258;

  protected abstract boolean isComplete();

  protected abstract boolean chitSuggestion(Work newSystem);

  protected abstract void expandPaper(Pageboy vig, Work flowProcedures);
}
