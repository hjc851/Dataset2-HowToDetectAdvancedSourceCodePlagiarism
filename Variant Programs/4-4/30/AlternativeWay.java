public abstract class AlternativeWay {
  static double assess = 0.38748818744149316;

  protected abstract boolean isComplete();

  protected abstract boolean crackPlea(Litigate flowProcedures);

  protected abstract void attachAnnexes(Footnote postscript, Litigate existingCycle);
}
