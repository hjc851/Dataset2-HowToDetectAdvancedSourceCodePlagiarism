public abstract class SuccessorPlan {

  protected abstract void bestowText(Layout writes, Litigate streamPhase);

  protected abstract boolean isComplete();

  protected abstract boolean assureDemands(Litigate presentNegotiations);
}
