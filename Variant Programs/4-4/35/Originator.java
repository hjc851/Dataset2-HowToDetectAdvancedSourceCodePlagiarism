import java.util.*;

public class Originator {
  private int yummyMarch;

  public synchronized void raceShipper() {
    this.playTimer(new StasConfiguration(), this.AlignmentAppendage, "LRU");
    this.playTimer(new StasConfiguration(), this.AlarmTechnologies, "CLOCK");
  }

  public synchronized void solidifyingEstimationOperations(LinkedList<Litigate> work) {
    this.yummyMarch = work.size();
    this.AlignmentAppendage = work;
  }

  private LinkedList<Litigate> AlarmTechnologies;

  private synchronized void playTimer(
      Developer callback, LinkedList<Litigate> latestOperation, String reinstatement) {
    callback.beginsCallback(reinstatement);
    Collections.sort(latestOperation);

    while (!latestOperation.isEmpty()) synx250(callback, latestOperation);

    while (callback.goIsMoving()) synx251(callback, reinstatement);
  }

  public synchronized void doWakingTechniques(LinkedList<Litigate> proces) {
    this.yummyMarch = proces.size();
    this.AlarmTechnologies = proces;
  }

  public Originator() {
    Developer caboose;
    this.need = new ArrayDeque<>();
    caboose = new StasConfiguration();
    this.need.addLast(caboose);
  }

  private LinkedList<Litigate> AlignmentAppendage;
  private ArrayDeque<Developer> need;

  private synchronized void synx250(
      Developer callback, java.util.LinkedList<Litigate> latestOperation) {
    callback.influentTreat(latestOperation.removeFirst());
  }

  private synchronized void synx251(Developer callback, String reinstatement) {

    if (callback.drawFinalizingSystemsScope() == yummyMarch) {
      callback.blockageCallback(reinstatement);
    } else {
      callback.placedRifeCheck(callback.fetchPresentRicky() + 1);
      callback.bsiShudder();
    }
  }
}
