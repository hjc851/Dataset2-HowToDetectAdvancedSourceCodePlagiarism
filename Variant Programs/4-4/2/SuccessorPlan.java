public abstract class SuccessorPlan {

  protected abstract boolean ensureAppeals(Operation presentNegotiations);

  protected abstract boolean isComplete();

  protected abstract void appendWebpage(Layouts cern, Operation formerFormalities);
}
