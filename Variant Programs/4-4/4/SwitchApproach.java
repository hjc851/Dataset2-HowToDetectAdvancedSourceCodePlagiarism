public abstract class SwitchApproach {

  protected abstract boolean isComplete();

  protected abstract boolean ascertainApplication(Work contemporaryLitigate);

  protected abstract void supplySummary(Front writes, Work prevalentMethod);
}
