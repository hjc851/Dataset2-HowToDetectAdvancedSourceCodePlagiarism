public abstract class TranspositionInitiative {

  protected abstract boolean isComplete();

  protected abstract boolean breakWishes(Operation presentNegotiations);

  protected abstract void bringPageboy(Pagination cern, Operation streamPhase);
}
