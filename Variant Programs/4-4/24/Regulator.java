import java.util.*;

public class Regulator {
  public LinkedList<Litigate> DialMethodologies;
  public int nbrTreat;

  public synchronized void goOrganizer(
      Compiler spreadsheet, LinkedList<Litigate> rifeSue, String replacing) {
    spreadsheet.runDebugging(replacing);
    Collections.sort(rifeSue);

    while (!rifeSue.isEmpty()) synx148(spreadsheet, rifeSue);

    while (spreadsheet.goIsMoving()) synx149(spreadsheet, replacing);
  }

  public LinkedList<Litigate> BetasMethods;
  public ArrayDeque<Compiler> available;

  public synchronized void playSalesperson() {
    this.goOrganizer(new DiddleProgramming(), this.BetasMethods, "LRU");
    this.goOrganizer(new DiddleProgramming(), this.DialMethodologies, "CLOCK");
  }

  public synchronized void bentAlarmTechnologies(LinkedList<Litigate> work) {
    this.nbrTreat = work.size();
    this.DialMethodologies = work;
  }

  public Regulator() {
    this.available = new ArrayDeque<>();
    Compiler a = new DiddleProgramming();
    this.available.addLast(a);
  }

  public synchronized void arrangeBetasMethods(LinkedList<Litigate> march) {
    this.nbrTreat = march.size();
    this.BetasMethods = march;
  }

  private synchronized void synx148(Compiler spreadsheet, java.util.LinkedList<Litigate> rifeSue) {
    spreadsheet.electWork(rifeSue.removeFirst());
  }

  private synchronized void synx149(Compiler spreadsheet, String replacing) {

    if (spreadsheet.fixAchievedMechanismsProportions() == nbrTreat) {
      spreadsheet.ceaseOrganizer(replacing);
    } else {
      spreadsheet.preparePrevalentScore(spreadsheet.fetchPresentRicky() + 1);
      spreadsheet.weapMark();
    }
  }
}
