public abstract class FallbackStrategist {

  protected abstract boolean chitSuggestion(Litigate actualAct);

  protected abstract boolean isComplete();

  protected abstract void extendFolio(Folio cern, Litigate newSystem);
}
