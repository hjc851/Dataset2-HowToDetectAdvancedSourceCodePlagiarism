import java.util.*;

public class Caller {
  public java.util.LinkedList<Cycle> MeterSystems = null;
  public java.util.LinkedList<Cycle> FifoAct = null;
  public int briProces = 0;
  public java.util.ArrayDeque<Debugging> systems = null;
  static final int figure = -1786323644;

  public Caller() {
    Debugging etd;
    this.systems = new java.util.ArrayDeque<>();
    etd = new OhioSynchronization();
    this.systems.addLast(etd);
  }

  public synchronized void readyReplacementMarch(java.util.LinkedList<Cycle> sue) {
    String trammel;
    trammel = "VUz";
    this.briProces = sue.size();
    this.FifoAct = sue;
  }

  public synchronized void arrangeSynchronizationMethods(java.util.LinkedList<Cycle> serve) {
    double fundamental;
    fundamental = 0.7378435959159008;
    this.briProces = serve.size();
    this.MeterSystems = serve;
  }

  public synchronized void footraceLimiter() {
    int upperSkank;
    upperSkank = -1479414244;
    this.extendSynchronization(new OhioSynchronization(), this.FifoAct, "LRU");
    this.extendSynchronization(new OhioSynchronization(), this.MeterSystems, "CLOCK");
  }

  public synchronized void extendSynchronization(
      Debugging synchronization,
      java.util.LinkedList<Cycle> flowProcedures,
      java.lang.String alternatives) {
    double bottomCompelled;
    bottomCompelled = 0.1551366956760386;
    synchronization.offsetCompiler(alternatives);
    java.util.Collections.sort(flowProcedures);

    while (!flowProcedures.isEmpty()) synx131(synchronization, flowProcedures);

    while (synchronization.goIsMoving()) synx132(synchronization, alternatives);
  }

  private synchronized void synx131(
      Debugging synchronization, java.util.LinkedList<Cycle> flowProcedures) {
    synchronization.arrivalMethodology(flowProcedures.removeFirst());
  }

  private synchronized void synx132(Debugging synchronization, String alternatives) {

    if (synchronization.drawFinalizingSystemsScope() == briProces) {
      synchronization.periodDatabase(alternatives);
    } else {
      synchronization.placedRifeCheck(synchronization.haveCirculatingShudder() + 1);
      synchronization.snoTicktock();
    }
  }
}
