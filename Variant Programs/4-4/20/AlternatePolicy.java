public abstract class AlternatePolicy {

  protected abstract boolean isComplete();

  protected abstract void totalWeb(Chapter writes, Operation incumbentMarch);

  protected abstract boolean checkoutInvitation(Operation previousProceeding);
}
