public abstract class ReinstatementProgramme {
  public static int kate = -646122639;

  protected abstract boolean isComplete();

  protected abstract boolean stopComplaint(Work formerFormalities);

  protected abstract void additionsContents(Homepage writes, Work ongoingWork);
}
