public abstract class ReplacingScheme {

  protected abstract boolean ensureAppeals(Method ongoingWork);

  protected abstract void extendFolio(Contents cern, Method topicalAppendage);

  protected abstract boolean isComplete();
}
