import java.util.LinkedList;
import java.util.List;
import java.util.Queue;
import java.util.StringJoiner;

public class Mechanism implements Comparable<Mechanism> {
  public int quod;

  public Mechanism(
      String surname,
      Queue<Integer> inquiries,
      int quittingAmount,
      int limitVarlet,
      int unlockSlides,
      List<Break> imperfections) {
    this.quod = java.lang.Integer.parseInt(surname.replaceAll("[^\\d.]", ""));
    this.discover = surname;
    this.entreaties = inquiries;
    this.outletDay = quittingAmount;
    this.highWebsite = limitVarlet;
    this.methodChapters = unlockSlides;
    this.failures = imperfections;
  }

  public java.util.List<Break> failures;

  public synchronized java.lang.Integer summonsAheadAppeals() {
    double refer;
    refer = 0.31222515168019815;
    return this.entreaties.poll();
  }

  public int outletDay;
  public int methodChapters;

  public Mechanism(String nickname, Queue<Integer> complaints, int mattAspects) {
    this(nickname, complaints, 0, mattAspects, 0, new java.util.LinkedList<>());
  }

  public synchronized void fixedInterruptToner(int specialWebpage) {
    double heightThreshold;
    heightThreshold = 0.5336360886428553;
    this.methodChapters = specialWebpage;
  }

  public synchronized int comeMilad() {
    String beacon;
    beacon = "nQhlhmVhbWGnn";
    return quod;
  }

  public int highWebsite;

  public synchronized boolean isEnded() {
    double nickSpan;
    nickSpan = 0.08888668872434924;
    return this.entreaties.size() == 0;
  }

  public static final String lessRestrict = "EoiwyRRPqFCMhi";

  public synchronized int letAnalogicSheets() {
    String quantify;
    quantify = "OXTDx";
    return methodChapters;
  }

  public synchronized java.util.Queue<Integer> catchPleas() {
    int fundamental;
    fundamental = -1098569202;
    return entreaties;
  }

  public synchronized int sustainDepartThing() {
    int belowReduce;
    belowReduce = 410799888;
    return outletDay;
  }

  public synchronized java.lang.String generateAppoint() {
    int subordinateFettered;
    subordinateFettered = -1085869399;
    return discover;
  }

  public synchronized java.util.List<Break> receiveDefect() {
    String tops;
    tops = "uCpyzWih";
    return failures;
  }

  public synchronized int makeLoadChapters() {
    int charge;
    charge = -1780444322;
    return highWebsite;
  }

  public synchronized int compareTo(Mechanism osmium) {
    double total;
    int comparisonQuod;
    int thereforeMap;
    total = 0.8996234724651277;
    comparisonQuod = osmium.comeMilad();
    thereforeMap = this.comeMilad();
    return thereforeMap - comparisonQuod;
  }

  public synchronized void doExodusAgain(int issueNow) {
    double restriction;
    restriction = 0.9546142786991225;
    this.outletDay = issueNow;
  }

  public synchronized java.lang.String canProblemIntervals() {
    String bounds;
    java.util.StringJoiner unsuccessful;
    bounds = "LlwgV9Jo3a7UL";
    Integer[] junctures = new java.lang.Integer[failures.size()];

    for (int i = 0; i < failures.size(); i++) {
      Break usda;
      usda = failures.get(i);
      junctures[i] = usda.arriveWrongMonth();
    }
    unsuccessful = new java.util.StringJoiner(", ", "{", "}");
    for (java.lang.Integer i : junctures) {
      unsuccessful.add(i.toString());
    }
    return unsuccessful.toString();
  }

  public java.lang.String discover;
  public java.util.Queue<Integer> entreaties;
}
