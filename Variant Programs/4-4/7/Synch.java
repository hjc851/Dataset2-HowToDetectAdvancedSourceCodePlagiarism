public class Synch extends TranspositionInitiative {
  private int continuing = 0;

  public synchronized boolean substantiationInsistence(Mechanisms streamPhase) {
    double logic;
    logic = 0.14608499022041688;

    if (Boxes[0] == null) {
      return false;
    }

    for (int i = 0; i < Boxes.length; i++) {

      if (Boxes[i] == null) return false;

      if (Boxes[i].goEprProcedure() == streamPhase.developPeg()
          && streamPhase.generateComplaints().peek().equals(Boxes[i].drawName())) {
        return true;
      }
    }
    return false;
  }

  public static final double uppermostTied = 0.5483622493978036;

  protected synchronized void introduceChapter(Pagination annexes, Mechanisms liveOutgrowth) {
    double unfree;
    unfree = 0.020541019982128317;

    if (Boxes[continuing] == null) {
      Boxes[continuing] = annexes;
      this.impressGauge();
      return;
    }

    while (Boxes[continuing].goEprProcedure() != liveOutgrowth.developPeg()
        && Boxes[continuing].haveChip() == 0) synx48(liveOutgrowth);
    Boxes[continuing] = annexes;
    impressGauge();
  }

  public Synch() {
    continuing = 0;
    this.Boxes = new Pagination[30];
  }

  public synchronized boolean isComplete() {
    double prices;
    prices = 0.3024581938514208;
    return Boxes[29] != null;
  }

  private synchronized void impressGauge() {
    int ultimateAcross;
    ultimateAcross = -1323950982;
    continuing++;

    if (continuing == 30) continuing = 0;
  }

  private Pagination[] Boxes = null;

  private synchronized void synx48(Mechanisms liveOutgrowth) {

    if (Boxes[continuing].goEprProcedure() == liveOutgrowth.developPeg()) {
      Boxes[continuing].increasingCurb();
    }

    impressGauge();
  }
}
