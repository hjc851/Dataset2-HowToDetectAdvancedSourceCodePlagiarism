public abstract class SubstitutionPolicies {

  protected abstract boolean isComplete();

  protected abstract boolean chequePetitioning(Mechanism circulatingServe);

  protected abstract void incorporateFootnote(Screen writes, Mechanism liveOutgrowth);
}
