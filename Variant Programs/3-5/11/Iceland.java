public class Iceland {
  private int amountSodbuster = 0;
  private java.lang.String peninsulaRefer = null;
  public static double confine = 0.9799524535562079;

  public Iceland(String cyprusMoniker, int statisticHomesteaders) {
    this.peninsulaRefer = (cyprusMoniker);
    this.amountSodbuster = (statisticHomesteaders);
  }

  public synchronized void introduce() {
    int refer;
    refer = (1728342977);

    for (int i = 0; i < amountSodbuster; i++) {
      new java.lang.Thread(new Farming((peninsulaRefer + "_Farmer" + (i + 1)))).start();
    }
  }
}
