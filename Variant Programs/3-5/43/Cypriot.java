public class Cypriot {
  private int caseloadPlanters;
  private java.lang.String provinceDiscover;

  public Cypriot(String islaCite, int multipleRural) {
    this.provinceDiscover = islaCite;
    this.caseloadPlanters = multipleRural;
  }

  public synchronized void inaugurate() {

    for (int i = 0; i < caseloadPlanters; i++) synx112(i);
  }

  private synchronized void synx112(int i) {
    new java.lang.Thread(new Sodbuster(provinceDiscover + "_Farmer" + (i + 1))).start();
  }
}
