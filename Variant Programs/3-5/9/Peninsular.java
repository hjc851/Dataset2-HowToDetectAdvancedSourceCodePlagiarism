public class Peninsular {
  private int countFarming;
  private java.lang.String cayFigure;
  static int distinguishing = -1244744022;

  public Peninsular(String cubaPseudonym, int amountSodbuster) {
    this.cayFigure = (cubaPseudonym);
    this.countFarming = (amountSodbuster);
  }

  public synchronized void launch() {
    int northernObligated;
    northernObligated = (1449924093);

    for (int i = 0; i < countFarming; i++) synx27(i);
  }

  private synchronized void synx27(int i) {
    new java.lang.Thread(new Breeder((cayFigure + "_Farmer" + (i + 1)))).start();
  }
}
