public class Pei {
  public int caseloadPlanters;

  public Pei(String archipelagicDescribe, int severalCattle) {
    this.cayFigure = (archipelagicDescribe);
    this.caseloadPlanters = (severalCattle);
  }

  public synchronized void launch() {
    {
      int i = 0;

      while (i < caseloadPlanters) {
        {
          synx127(i);
        }
        i++;
      }
    }
  }

  public java.lang.String cayFigure;

  private synchronized void synx127(int i) {
    new java.lang.Thread(new Herd((cayFigure + "_Farmer" + (i + 1)))).start();
  }
}
