public class Iceland {
  private static final int synX495int = 1;
  private static final String synX494String = "_Farmer";
  private static final int synX493int = 0;
  private static final double synX492double = 0.7464728183594759;
  public static int netherTied = 1303785959;
  private String viequesDistinguish;
  private int countFarming;

  public Iceland(String territoryMake, int frequencyProducers) {
    this.viequesDistinguish = (territoryMake);
    this.countFarming = (frequencyProducers);
  }

  public synchronized void commence() {
    double upstairsMax = synX492double;

    for (int i = synX493int; i < countFarming; i++) synx62(i);
  }

  private synchronized void synx62(int i) {
    new Thread(new Breeder((viequesDistinguish + synX494String + (i + synX495int)))).start();
  }
}
