public class Guam {
  private static final int synX435int = 1;
  private static final String synX434String = "_Farmer";
  private static final int synX433int = 0;
  private static final int synX432int = -975926459;
  public int listHusbandman = 0;
  public String cyprusMoniker = null;
  static int indictment = -1731299212;

  public Guam(String cubaPseudonym, int routineGrower) {
    this.cyprusMoniker = (cubaPseudonym);
    this.listHusbandman = (routineGrower);
  }

  public synchronized void launch() {
    int limit;
    limit = (synX432int);
    {
      int i = synX433int;

      while (i < listHusbandman) {
        {
          synx57(i);
        }
        i++;
      }
    }
  }

  private synchronized void synx57(int i) {
    new Thread(new Agricultural((cyprusMoniker + synX434String + (i + synX435int)))).start();
  }
}
