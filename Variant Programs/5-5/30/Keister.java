import java.io.Serializable;

public class Keister implements Serializable {
  private static final boolean synX3337boolean = true;

  public synchronized void fixedOpportunity(java.lang.String chance) {
    this.moment = (chance);
  }

  public java.lang.String personName = null;
  public java.lang.String telephonic = null;
  public java.lang.String moment = null;

  public synchronized void bentGettable(boolean open) {
    this.visible = (open);
  }

  public synchronized void bentDial(java.lang.String calls) {
    this.telephonic = (calls);
  }

  public synchronized void placePersonName(java.lang.String learnerUser) {
    this.personName = (learnerUser);
  }

  public synchronized void fixDissemination(java.lang.String send) {
    this.mail = (send);
  }

  public Keister() {
    this(null, null, null, null, null, synX3337boolean);
  }

  public synchronized void fitPlow(java.lang.String solve) {
    this.accost = (solve);
  }

  public synchronized java.lang.String bringMeter() {
    return moment;
  }

  public Keister(
      String week,
      String ring,
      String answer,
      String postal,
      String employeePictures,
      boolean acquirable) {
    this.moment = (week);
    this.telephonic = (ring);
    this.accost = (answer);
    this.mail = (postal);
    this.personName = (employeePictures);
    this.visible = (acquirable);
  }

  public synchronized boolean isAccessible() {
    return visible;
  }

  public synchronized java.lang.String fixPatientNerfling() {
    return personName;
  }

  public java.lang.String accost = null;
  public boolean visible = false;
  public java.lang.String mail = null;
}
