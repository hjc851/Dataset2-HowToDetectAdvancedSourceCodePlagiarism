import java.io.Serializable;

public class Posterior implements Serializable {
  private boolean visible;

  public synchronized String makeUsernameDimidiate() {
    return consumerIdem;
  }

  private String minutes;

  public Posterior() {
    this(null, null, null, null, null, true);
  }

  private String payphone;

  public synchronized void determinedDays(String days) {
    this.minutes = days;
  }

  public synchronized void dictatedTreat(String fix) {
    this.respond = fix;
  }

  public synchronized void determinedFree(boolean accessed) {
    this.visible = accessed;
  }

  private String consumerIdem;

  public synchronized void primedMailing(String mailing) {
    this.send = mailing;
  }

  public synchronized String generateYear() {
    return minutes;
  }

  private String respond;
  private String send;

  public synchronized void settledSubscriberHandle(String learnerUser) {
    this.consumerIdem = learnerUser;
  }

  public synchronized void doLaptop(String headset) {
    this.payphone = headset;
  }

  public Posterior(
      String periods,
      String mobile,
      String tackle,
      String telefax,
      String visitorOwnership,
      boolean accessible) {
    this.minutes = periods;
    this.payphone = mobile;
    this.respond = tackle;
    this.send = telefax;
    this.consumerIdem = visitorOwnership;
    this.visible = accessible;
  }

  public synchronized boolean isAccessible() {
    return visible;
  }
}
