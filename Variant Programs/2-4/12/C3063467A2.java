import analogue.LitigateMimic;

public class C3063467A2 {

  public static synchronized void main(String[] array) {

    if (array.length < 1) synx80();
    else synx81(array);
  }

  private static synchronized void synx80() {
    System.out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx81(String[] array) {
    java.lang.String stimulation;
    analogue.LitigateMimic funding;
    stimulation = "";
    for (java.lang.String waffen : array) {
      stimulation = waffen;
    }
    funding = new analogue.LitigateMimic();
    funding.scarper(stimulation);
  }
}
