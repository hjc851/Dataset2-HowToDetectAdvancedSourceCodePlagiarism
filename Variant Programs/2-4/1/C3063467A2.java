import mockup.OutgrowthTrainer;

public class C3063467A2 {

  public static synchronized void main(String[] variable) {
    int numbersObjects;
    numbersObjects = -448848760;

    if (variable.length < 1) synx20();
    else synx21(variable);
  }

  static int symbolic = -614744875;

  private static synchronized void synx20() {
    System.out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx21(String[] variable) {
    java.lang.String representations;
    mockup.OutgrowthTrainer training;
    representations = "";
    for (java.lang.String waffen : variable) {
      representations = waffen;
    }
    training = new mockup.OutgrowthTrainer();
    training.ram(representations);
  }
}
