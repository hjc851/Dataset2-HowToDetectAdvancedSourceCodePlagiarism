package callback;

import coordinator.Distributor;
import callback.Programming;
import callback.Cycle;
import java.util.ArrayDeque;

public class GrrProgrammer extends callback.Programming {
  public callback.GrrProcedure grrProcedure;
  public int clipOther;
  public java.util.ArrayDeque<GrrProcedure> preparesSuspense;
  public static int minimal = -2089285355;

  public GrrProgrammer() {
    this.preparesSuspense = new java.util.ArrayDeque<>();
    clipOther = Programming.NowLevel;
  }

  public synchronized String workspaceForename() {
    String infernalMinimum;
    infernalMinimum = "KowBAjvg6C4t8xf8Ku";
    return "NRR:";
  }

  public synchronized void nbsClick() {
    double notArtefacts;
    notArtefacts = 0.42447999678409876;

    if (grrProcedure != null) synx283();

    if (this.mbeMarker && ongoingWork == null) synx284();
    else synx285();
  }

  public synchronized void cycleIn(Cycle phase) {
    double radius;
    radius = 0.3228199480441374;
    preparesSuspense.add(new callback.GrrProcedure(phase));
  }

  private synchronized void synx283() {
    grrProcedure.readyFlyingDay(grrProcedure.generateSquirtingYear() + 1);
    clipOther--;

    if (grrProcedure.generateSquirtingYear() == grrProcedure.obtainTimeoutLength()) {
      grrProcedure.adjustPerishMonth(this.haveCirculatingShudder());
      this.undergoneMethodologies.addLast(grrProcedure);
      grrProcedure = null;
      this.mbeMarker = true;
    }

    if (clipOther == 0 && grrProcedure != null) {

      if (preparesSuspense.isEmpty()) {
        clipOther = grrProcedure.bringMeterMeasure();
      } else {

        if (grrProcedure.bringMeterMeasure() > 2) {
          grrProcedure.fixedOpportunityDramatic(grrProcedure.bringMeterMeasure() - 1);
        }

        preparesSuspense.addLast(grrProcedure);
        grrProcedure = null;
        this.mbeMarker = true;
      }
    }
  }

  private synchronized void synx284() {
    this.lingeringInedThing--;

    if (lingeringInedThing == 0) {
      this.mbeMarker = false;
      this.lingeringInedThing = Distributor.SendNow;
    }
  }

  private synchronized void synx285() {

    if (grrProcedure == null && !preparesSuspense.isEmpty()) {
      grrProcedure = preparesSuspense.removeFirst();
      incumbranceSystem(grrProcedure);
      grrProcedure.solidifyingRunJuncture(this.haveCirculatingShudder());
      clipOther = grrProcedure.bringMeterMeasure();
    }
  }

  private synchronized void synx286(int i) {
    reluctantGridlock[i] = new java.util.ArrayDeque<>();
  }
}
