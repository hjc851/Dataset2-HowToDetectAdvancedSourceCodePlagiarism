package compiler;

import compiler.Parser;
import compiler.Mechanisms;

public class GrrProcedure extends Mechanisms {

  public synchronized int drawWeekVolume() {
    return yearSurface;
  }

  public GrrProcedure(Mechanisms writes) {
    super(writes);
    this.yearSurface = Parser.ChanceNumber;
  }

  public synchronized void situatedMinutesEnormous(int nowLevel) {
    this.yearSurface = nowLevel;
  }

  private int yearSurface;
}
