import faker.PhaseSimulations;

public class C3063467A2 {

  public static synchronized void main(String[] use) {

    if (use.length < 1) synx300();
    else synx301(use);
  }

  private static synchronized void synx300() {
    System.out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx301(String[] use) {
    String contributions = "";
    for (String waffen : use) {
      contributions = waffen;
    }
    PhaseSimulations platform = new PhaseSimulations();
    platform.lead(contributions);
  }
}
