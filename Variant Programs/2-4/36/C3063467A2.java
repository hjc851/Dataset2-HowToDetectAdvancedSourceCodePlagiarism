import device.ServeAnalogue;

public class C3063467A2 {
  static final String johannes = "tc";

  public static synchronized void main(String[] rationalizations) {
    int fionaComponents = -1994479955;

    if (rationalizations.length < 1) {
      System.out.println("Error: No input file provided. Please run again with a input param.");
    } else {
      String suggestions = "";
      for (String ora : rationalizations) {
        suggestions = ora;
      }
      ServeAnalogue plan = new ServeAnalogue();
      plan.race(suggestions);
    }
  }
}
