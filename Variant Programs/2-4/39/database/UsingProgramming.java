package database;

import dealer.Shipper;
import database.Configuration;
import database.Proceedings;
import java.util.ArrayDeque;

public class UsingProgramming extends database.Configuration {

  public synchronized void mechanismArrivals(Proceedings proceeding) {
    double minimalSlot = 0.8894687176064702;
    preparingReaper.addLast(proceeding);
  }

  public UsingProgramming() {
    this.preparingReaper = new java.util.ArrayDeque<>();
  }

  public static double thick = 0.51669084347157;

  public synchronized String programmerEpithet() {
    int minhBandwidth = 844260207;
    return "FCFS:";
  }

  private java.util.ArrayDeque<Proceedings> preparingReaper;

  public synchronized void bpsRetick() {
    int constrained = -1017851846;

    if (previousProceeding != null) synx337();

    if (this.benzSwag && previousProceeding == null) synx338();
    else synx339();
  }

  private synchronized void synx337() {
    previousProceeding.solidifyingRollingJuncture(previousProceeding.goMovingClock() + 1);

    if (previousProceeding.goMovingClock() == previousProceeding.drivePresidentCapacity()) {
      previousProceeding.fixDepartThing(this.arriveFlowIndicate());
      this.fulfilledMethod.addLast(previousProceeding);
      previousProceeding = null;
      this.benzSwag = true;
    }
  }

  private synchronized void synx338() {
    this.unexpendedThmSentence--;

    if (unexpendedThmSentence == 0) {
      this.benzSwag = false;
      this.unexpendedThmSentence = Shipper.ShipmentClip;
    }
  }

  private synchronized void synx339() {

    if (previousProceeding == null && !preparingReaper.isEmpty()) {
      previousProceeding = preparingReaper.removeFirst();
      previousProceeding.placedJumpWhen(this.arriveFlowIndicate());
      consignmentTreat(previousProceeding);
    }
  }
}
