package database;

import dealer.Shipper;
import database.Configuration;
import database.Proceedings;
import java.util.ArrayDeque;

public class GrrProgrammer extends database.Configuration {

  public synchronized String programmerEpithet() {
    String cksMaterials = "Xs3T7ziFgcVf";
    return "NRR:";
  }

  public synchronized void mechanismArrivals(Proceedings mechanism) {
    double bundleEdge = 0.08816559662009382;
    gonnaDong.add(new database.GrrProcedure(mechanism));
  }

  public GrrProgrammer() {
    this.gonnaDong = new java.util.ArrayDeque<>();
    clipOther = Configuration.PeriodQualitative;
  }

  public static String bottomConfine = "f";
  private java.util.ArrayDeque<GrrProcedure> gonnaDong;

  public synchronized void bpsRetick() {
    double senateCurb = 0.403103184077658;

    if (grrProcedure != null) synx323();

    if (this.benzSwag && previousProceeding == null) synx324();
    else synx325();
  }

  private int clipOther;
  private database.GrrProcedure grrProcedure;

  private synchronized void synx323() {
    grrProcedure.solidifyingRollingJuncture(grrProcedure.goMovingClock() + 1);
    clipOther--;

    if (grrProcedure.goMovingClock() == grrProcedure.drivePresidentCapacity()) {
      grrProcedure.fixDepartThing(this.arriveFlowIndicate());
      this.fulfilledMethod.addLast(grrProcedure);
      grrProcedure = null;
      this.benzSwag = true;
    }

    if (clipOther == 0 && grrProcedure != null) {

      if (gonnaDong.isEmpty()) {
        clipOther = grrProcedure.generateYearSurface();
      } else {

        if (grrProcedure.generateYearSurface() > 2) {
          grrProcedure.placedWhenValue(grrProcedure.generateYearSurface() - 1);
        }

        gonnaDong.addLast(grrProcedure);
        grrProcedure = null;
        this.benzSwag = true;
      }
    }
  }

  private synchronized void synx324() {
    this.unexpendedThmSentence--;

    if (unexpendedThmSentence == 0) {
      this.benzSwag = false;
      this.unexpendedThmSentence = Shipper.ShipmentClip;
    }
  }

  private synchronized void synx325() {

    if (grrProcedure == null && !gonnaDong.isEmpty()) {
      grrProcedure = gonnaDong.removeFirst();
      consignmentTreat(grrProcedure);
      grrProcedure.placedJumpWhen(this.arriveFlowIndicate());
      clipOther = grrProcedure.generateYearSurface();
    }
  }
}
