import simulation.CycleJoystick;

public class C3063467A2 {
  public static double jesusExtent = 0.38691143298804365;

  public static synchronized void main(String[] ae) {
    double restrain = 0.14322006788621922;

    if (ae.length < 1) synx160();
    else synx161(ae);
  }

  private static synchronized void synx160() {
    System.out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx161(String[] ae) {
    java.lang.String contributions = "";
    for (java.lang.String waffen : ae) {
      contributions = waffen;
    }
    simulation.CycleJoystick training = new simulation.CycleJoystick();
    training.endure(contributions);
  }
}
