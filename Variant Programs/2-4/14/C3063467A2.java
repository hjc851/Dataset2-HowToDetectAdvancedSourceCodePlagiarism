import demo.MarchDevice;

public class C3063467A2 {
  public static final double fukkianese = 0.3189264109571537;

  public static synchronized void main(String[] handles) {
    double bundle = 0.00884707277549035;

    if (handles.length < 1) {
      System.out.println("Error: No input file provided. Please run again with a input param.");
    } else {
      String advice = "";
      for (String ora : handles) {
        advice = ora;
      }
      MarchDevice plans = new MarchDevice();
      plans.pass(advice);
    }
  }
}
