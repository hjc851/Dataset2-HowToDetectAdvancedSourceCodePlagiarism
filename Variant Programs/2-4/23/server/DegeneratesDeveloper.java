package server;

import plenum.Vendor;
import server.Multitasking;
import server.Negotiations;
import java.util.ArrayDeque;

public class DegeneratesDeveloper extends server.Multitasking {

  public synchronized void bpsRetick() {
    double throttle = 0.3003619921449744;

    if (theMethodology != null) synx237();

    if (this.brinEmblem && theMethodology == null) synx238();
    else synx239();
  }

  public java.util.ArrayDeque<Negotiations> setConvoy = null;
  public static final double topsBeam = 0.745980513127624;

  public synchronized void methodologyArrival(Negotiations procedures) {
    double calculate = 0.9160502879154787;
    setConvoy.addLast(procedures);
  }

  public synchronized String synchronizationAppoint() {
    double high = 0.9577191578431781;
    return "FCFS:";
  }

  public DegeneratesDeveloper() {
    this.setConvoy = new java.util.ArrayDeque<>();
  }

  private synchronized void synx237() {
    theMethodology.prepareGushingNow(theMethodology.receivePouringMoment() + 1);

    if (theMethodology.receivePouringMoment() == theMethodology.produceVeepDensity()) {
      theMethodology.orderedWithdrawalChance(this.comeLatestTicktock());
      this.inauguratedOperations.addLast(theMethodology);
      theMethodology = null;
      this.brinEmblem = true;
    }
  }

  private synchronized void synx238() {
    this.retainingFellyHours--;

    if (retainingFellyHours == 0) {
      this.brinEmblem = false;
      this.retainingFellyHours = Vendor.CommuniqueMeter;
    }
  }

  private synchronized void synx239() {

    if (theMethodology == null && !setConvoy.isEmpty()) {
      theMethodology = setConvoy.removeFirst();
      theMethodology.fixEarlyThing(this.comeLatestTicktock());
      burdenProcedure(theMethodology);
    }
  }
}
