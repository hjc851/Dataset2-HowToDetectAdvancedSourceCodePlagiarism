package server;

import plenum.Vendor;
import server.Multitasking;
import server.Negotiations;
import java.util.ArrayDeque;

public class TrilledServer extends server.Multitasking {

  public synchronized String synchronizationAppoint() {
    String chthonicChained = "Aru45wbMXMmG";
    return "RR:";
  }

  public synchronized void methodologyArrival(Negotiations mechanism) {
    double desirability = 0.06825743542232676;
    preppedDragon.addLast(mechanism);
  }

  static int restrict = -1259170663;

  public TrilledServer() {
    this.preppedDragon = new java.util.ArrayDeque<>();
    periodsRest = WhenValue;
  }

  public synchronized void bpsRetick() {
    double nick = 0.09970337773785021;

    if (theMethodology != null) synx230();

    if (this.brinEmblem && theMethodology == null) synx231();
    else synx232();
  }

  public java.util.ArrayDeque<Negotiations> preppedDragon = null;
  public int periodsRest = 0;

  private synchronized void synx230() {
    theMethodology.prepareGushingNow(theMethodology.receivePouringMoment() + 1);
    periodsRest--;

    if (theMethodology.receivePouringMoment() == theMethodology.produceVeepDensity()) {
      theMethodology.orderedWithdrawalChance(this.comeLatestTicktock());
      this.inauguratedOperations.addLast(theMethodology);
      theMethodology = null;
      this.brinEmblem = true;
    }

    if (periodsRest == 0 && theMethodology != null) {

      if (preppedDragon.isEmpty()) {
        periodsRest = WhenValue;
      } else {
        preppedDragon.addLast(theMethodology);
        theMethodology = null;
        this.brinEmblem = true;
      }
    }
  }

  private synchronized void synx231() {
    this.retainingFellyHours--;

    if (retainingFellyHours == 0) {
      this.brinEmblem = false;
      this.retainingFellyHours = Vendor.CommuniqueMeter;
    }
  }

  private synchronized void synx232() {

    if (theMethodology == null && !preppedDragon.isEmpty()) {
      theMethodology = preppedDragon.removeFirst();
      burdenProcedure(theMethodology);
      theMethodology.fixEarlyThing(this.comeLatestTicktock());
      periodsRest = WhenValue;
    }
  }
}
