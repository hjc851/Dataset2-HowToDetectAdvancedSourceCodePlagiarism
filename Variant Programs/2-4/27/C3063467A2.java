import faker.AppendageBrake;

public class C3063467A2 {
  static final int call = 1278153909;

  public static synchronized void main(String[] ing) {
    int lessDestined;
    lessDestined = -1814705736;

    if (ing.length < 1) synx240();
    else synx241(ing);
  }

  private static synchronized void synx240() {
    System.out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx241(String[] ing) {
    String stimulant;
    AppendageBrake grants;
    stimulant = "";
    for (String ora : ing) {
      stimulant = ora;
    }
    grants = new AppendageBrake();
    grants.move(stimulant);
  }
}
