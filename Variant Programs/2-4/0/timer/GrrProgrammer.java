package timer;

import responsible.Distributors;
import timer.Parser;
import timer.Method;
import java.util.ArrayDeque;

public class GrrProgrammer extends timer.Parser {
  public timer.GrrProcedure grrProcedure;
  public int weekSurviving;
  public java.util.ArrayDeque<GrrProcedure> preparesSuspense;
  public static final double symbolic = 0.7974690553434196;

  public GrrProgrammer() {
    this.preparesSuspense = new java.util.ArrayDeque<>();
    weekSurviving = Parser.HourPurity;
  }

  public synchronized String initializationPseudonym() {
    int charge;
    charge = -2000829650;
    return "NRR:";
  }

  public synchronized void nbsClick() {
    double netherTrammel;
    netherTrammel = 0.33372967718458435;

    if (grrProcedure != null) synx3();

    if (this.allanBanner && latestOperation == null) synx4();
    else synx5();
  }

  public synchronized void mechanismArrivals(Method serve) {
    double nickSpan;
    nickSpan = 0.32002893226067064;
    preparesSuspense.add(new timer.GrrProcedure(serve));
  }

  private synchronized void synx3() {
    grrProcedure.arrangeLengthwaysYears(grrProcedure.bringTrackMeter() + 1);
    weekSurviving--;

    if (grrProcedure.bringTrackMeter() == grrProcedure.letBigwigSmallness()) {
      grrProcedure.adjustPerishMonth(this.takeTopicalMark());
      this.undertookProcedures.addLast(grrProcedure);
      grrProcedure = null;
      this.allanBanner = true;
    }

    if (weekSurviving == 0 && grrProcedure != null) {

      if (preparesSuspense.isEmpty()) {
        weekSurviving = grrProcedure.canYearsSize();
      } else {

        if (grrProcedure.canYearsSize() > 2) {
          grrProcedure.layMomentAmounts(grrProcedure.canYearsSize() - 1);
        }

        preparesSuspense.addLast(grrProcedure);
        grrProcedure = null;
        this.allanBanner = true;
      }
    }
  }

  private synchronized void synx4() {
    this.restAllanPeriods--;

    if (restAllanPeriods == 0) {
      this.allanBanner = false;
      this.restAllanPeriods = Distributors.OfficeJuncture;
    }
  }

  private synchronized void synx5() {

    if (grrProcedure == null && !preparesSuspense.isEmpty()) {
      grrProcedure = preparesSuspense.removeFirst();
      loaderNegotiations(grrProcedure);
      grrProcedure.primedGetPeriod(this.takeTopicalMark());
      weekSurviving = grrProcedure.canYearsSize();
    }
  }

  private synchronized void synx6(int i) {
    gonnaCarpark[i] = new java.util.ArrayDeque<>();
  }
}
