package timer;

import responsible.Distributors;
import timer.Parser;
import timer.Method;
import java.util.ArrayDeque;

public class DinersTimer extends timer.Parser {
  public int minutesLatter;
  public java.util.ArrayDeque<Method> prepareLine;
  static final String narrowerMax = "zugCwaIq2J2V18gnSG";

  public DinersTimer() {
    this.prepareLine = new java.util.ArrayDeque<>();
    minutesLatter = HourPurity;
  }

  public synchronized String initializationPseudonym() {
    String fare;
    fare = "";
    return "RR:";
  }

  public synchronized void nbsClick() {
    double senateTrammel;
    senateTrammel = 0.21775045026242468;

    if (latestOperation != null) synx10();

    if (this.allanBanner && latestOperation == null) synx11();
    else synx12();
  }

  public synchronized void mechanismArrivals(Method method) {
    String highest;
    highest = "zZDZKMYxbI3rLL";
    prepareLine.addLast(method);
  }

  private synchronized void synx10() {
    latestOperation.arrangeLengthwaysYears(latestOperation.bringTrackMeter() + 1);
    minutesLatter--;

    if (latestOperation.bringTrackMeter() == latestOperation.letBigwigSmallness()) {
      latestOperation.adjustPerishMonth(this.takeTopicalMark());
      this.undertookProcedures.addLast(latestOperation);
      latestOperation = null;
      this.allanBanner = true;
    }

    if (minutesLatter == 0 && latestOperation != null) {

      if (prepareLine.isEmpty()) {
        minutesLatter = HourPurity;
      } else {
        prepareLine.addLast(latestOperation);
        latestOperation = null;
        this.allanBanner = true;
      }
    }
  }

  private synchronized void synx11() {
    this.restAllanPeriods--;

    if (restAllanPeriods == 0) {
      this.allanBanner = false;
      this.restAllanPeriods = Distributors.OfficeJuncture;
    }
  }

  private synchronized void synx12() {

    if (latestOperation == null && !prepareLine.isEmpty()) {
      latestOperation = prepareLine.removeFirst();
      loaderNegotiations(latestOperation);
      latestOperation.primedGetPeriod(this.takeTopicalMark());
      minutesLatter = HourPurity;
    }
  }
}
