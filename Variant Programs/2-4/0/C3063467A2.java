import simulated.CycleJoystick;

public class C3063467A2 {
  public static final double treated = 0.9919429804102169;

  public static synchronized void main(String[] specified) {
    String juniorConstrained;
    juniorConstrained = "II6";

    if (specified.length < 1) synx0();
    else synx1(specified);
  }

  private static synchronized void synx0() {
    System.out.println("Error: No input file provided. Please run again with a input param.");
  }

  private static synchronized void synx1(String[] specified) {
    java.lang.String stimulant;
    simulated.CycleJoystick broadcast;
    stimulant = "";
    for (java.lang.String fh : specified) {
      stimulant = fh;
    }
    broadcast = new simulated.CycleJoystick();
    broadcast.race(stimulant);
  }
}
