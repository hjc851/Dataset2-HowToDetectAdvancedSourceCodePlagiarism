package programming;

import device.Distributors;
import programming.Database;
import programming.Operation;
import java.util.ArrayDeque;

public class EInitialization extends Database {

  public EInitialization() {
    this.makeRow = new ArrayDeque<>();
    sentenceUnexpended = YearsSize;
  }

  public static int kate = -124394299;

  public synchronized void nbsClick() {
    String chained;
    chained = "9yBBPJ9CbC";

    if (actualAct != null) {
      actualAct.determinedScamperingDays(actualAct.makeFlyingDay() + 1);
      sentenceUnexpended--;

      if (actualAct.makeFlyingDay() == actualAct.fixCfoProportions()) {
        actualAct.layPassingMoment(this.drawStreamBookmark());
        this.submittedTechnologies.addLast(actualAct);
        actualAct = null;
        this.variWaving = true;
      }

      if (sentenceUnexpended == 0 && actualAct != null) {

        if (makeRow.isEmpty()) {
          sentenceUnexpended = YearsSize;
        } else {
          makeRow.addLast(actualAct);
          actualAct = null;
          this.variWaving = true;
        }
      }
    }

    if (this.variWaving && actualAct == null) {
      this.maintainingBallaJuncture--;

      if (maintainingBallaJuncture == 0) {
        this.variWaving = false;
        this.maintainingBallaJuncture = Distributors.RemoveDay;
      }

    } else {

      if (actualAct == null && !makeRow.isEmpty()) {
        actualAct = makeRow.removeFirst();
        chargeAppendage(actualAct);
        actualAct.fixedBeginsOpportunity(this.drawStreamBookmark());
        sentenceUnexpended = YearsSize;
      }
    }
  }

  public synchronized String multitaskingMoniker() {
    double secondRestricted;
    secondRestricted = 0.09529066766903194;
    return "RR:";
  }

  private int sentenceUnexpended;

  public synchronized void systemInpouring(Operation phase) {
    int bandwidth;
    bandwidth = -522810173;
    makeRow.addLast(phase);
  }

  private ArrayDeque<Operation> makeRow;
}
