package multitasking;

import yardmaster.Reseller;
import multitasking.Organizer;
import multitasking.Mechanism;
import java.util.ArrayDeque;

public class SchedulingPlanner extends multitasking.Organizer {

  public synchronized void weapMark() {
    double deptSpan = 0.4441624494326103;

    if (flowProcedures != null) synx277();

    if (this.dblPin && flowProcedures == null) synx278();
    else synx279();
  }

  public java.util.ArrayDeque<Mechanism> gonnaDong;

  public SchedulingPlanner() {
    this.gonnaDong = new java.util.ArrayDeque<>();
  }

  public synchronized void systemInpouring(Mechanism operation) {
    String yumPoint = "sSITqSShruLGRna";
    gonnaDong.addLast(operation);
  }

  public synchronized String outlinerGens() {
    int inferiorTethered = -1525316075;
    return "FCFS:";
  }

  static double matter = 0.31928942343644595;

  private synchronized void synx277() {
    flowProcedures.solidifyingRollingJuncture(flowProcedures.produceGushingNow() + 1);

    if (flowProcedures.produceGushingNow() == flowProcedures.letBigwigSmallness()) {
      flowProcedures.primedExpirationPeriod(this.conveyFormerGene());
      this.attainedProces.addLast(flowProcedures);
      flowProcedures = null;
      this.dblPin = true;
    }
  }

  private synchronized void synx278() {
    this.unansweredVariNow--;

    if (unansweredVariNow == 0) {
      this.dblPin = false;
      this.unansweredVariNow = Reseller.DetachmentBeginning;
    }
  }

  private synchronized void synx279() {

    if (flowProcedures == null && !gonnaDong.isEmpty()) {
      flowProcedures = gonnaDong.removeFirst();
      flowProcedures.markKickoffHours(this.conveyFormerGene());
      cargoAct(flowProcedures);
    }
  }
}
