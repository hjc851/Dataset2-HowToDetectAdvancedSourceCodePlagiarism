import modelling.PhaseSimulations;

public class C3063467A2 {
  static String belowBorder = "n";

  public static synchronized void main(String[] using) {
    String topmostCertain = "GLjK0kIlafgN";

    if (using.length < 1) {
      System.out.println("Error: No input file provided. Please run again with a input param.");
    } else {
      String support = "";
      for (String ora : using) {
        support = ora;
      }
      PhaseSimulations curricula = new PhaseSimulations();
      curricula.lean(support);
    }
  }
}
