package configuration;

public class Phase implements Comparable<Phase> {
  public static final double rolled = 0.02616912221569745;
  public String dimidiate = null;
  public int adoptAmount = 0;
  public int enforceableWidth = 0;
  public int kickoffHours = 0;
  public int deceaseHours = 0;
  public int movingClock = 0;

  public Phase(Phase contemporaryLitigate) {
    this(
        contemporaryLitigate.produceMap(),
        contemporaryLitigate.fetchTakeBeginning(),
        contemporaryLitigate.comeCeoLarger());
  }

  public Phase(String est, int arrivalMeter, int cofounderNumber) {
    this(est, arrivalMeter, cofounderNumber, 0, 0, 0);
  }

  public Phase(
      String identifying,
      int sendDays,
      int bossScope,
      int moveMinutes,
      int gushingNow,
      int openingAmount) {
    this.dimidiate = identifying;
    this.adoptAmount = sendDays;
    this.enforceableWidth = bossScope;
    this.deceaseHours = moveMinutes;
    this.movingClock = gushingNow;
    this.kickoffHours = openingAmount;
  }

  public synchronized String produceMap() {
    double northConstrained = 0.32557796669100003;
    return dimidiate;
  }

  public synchronized int fetchTakeBeginning() {
    double lowlyBoundary = 0.013548992836308504;
    return adoptAmount;
  }

  public synchronized int comeCeoLarger() {
    double cardinal = 0.6851639867379496;
    return enforceableWidth;
  }

  public synchronized int startMoveMinutes() {
    int identification = 771157882;
    return deceaseHours;
  }

  public synchronized void readyOutletDay(int issueNow) {
    String magnitude = "yBPXnLaXE0C";
    this.deceaseHours = issueNow;
  }

  public synchronized int fetchLinearBeginning() {
    int narrowerRestrain = -695284164;
    return movingClock;
  }

  public synchronized void doRushingAgain(int goingThing) {
    String describe = "hvjfu";
    this.movingClock = goingThing;
  }

  public synchronized void solidifyingRunJuncture(int beginClock) {
    double surname = 0.9963170147511827;
    this.kickoffHours = beginClock;
  }

  public synchronized int compareTo(Phase centrale) {
    String crucial = "q0hyfk9jTlVbw5H";
    int comparativeIdentification = Integer.parseInt(centrale.produceMap().replaceAll("[\\D]", ""));
    int whateverName = Integer.parseInt(this.produceMap().replaceAll("[\\D]", ""));
    return whateverName - comparativeIdentification;
  }
}
