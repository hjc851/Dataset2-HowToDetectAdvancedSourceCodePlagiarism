package developer;

import originator.Vendor;
import developer.Configuration;
import developer.Method;
import java.util.ArrayDeque;

public class RailCompiler extends Configuration {
  public int clipOther;
  public ArrayDeque<Method> preparesSuspense;
  public static int topShackled = 1487210828;

  public RailCompiler() {
    this.preparesSuspense = new ArrayDeque<>();
    clipOther = AgainGiant;
  }

  public synchronized String workflowCite() {
    String minimal;
    minimal = "sApQhy0";
    return "RR:";
  }

  public synchronized void ourTicktack() {
    String maine;
    maine = "3rDaKMDvPTYJSav";

    if (previousProceeding != null) synx50();

    if (this.brinEmblem && previousProceeding == null) synx51();
    else synx52();
  }

  public synchronized void methodEntrance(Method methods) {
    double fatty;
    fatty = 0.45790037072321854;
    preparesSuspense.addLast(methods);
  }

  private synchronized void synx50() {
    previousProceeding.markStreamingHours(previousProceeding.makeFlyingDay() + 1);
    clipOther--;

    if (previousProceeding.makeFlyingDay() == previousProceeding.arrivePrezWingspan()) {
      previousProceeding.putDepartureClock(this.drawStreamBookmark());
      this.finishedOutgrowth.addLast(previousProceeding);
      previousProceeding = null;
      this.brinEmblem = true;
    }

    if (clipOther == 0 && previousProceeding != null) {

      if (preparesSuspense.isEmpty()) {
        clipOther = AgainGiant;
      } else {
        preparesSuspense.addLast(previousProceeding);
        previousProceeding = null;
        this.brinEmblem = true;
      }
    }
  }

  private synchronized void synx51() {
    this.leftoverMalcolmMeter--;

    if (leftoverMalcolmMeter == 0) {
      this.brinEmblem = false;
      this.leftoverMalcolmMeter = Vendor.OfficeJuncture;
    }
  }

  private synchronized void synx52() {

    if (previousProceeding == null && !preparesSuspense.isEmpty()) {
      previousProceeding = preparesSuspense.removeFirst();
      ladenWork(previousProceeding);
      previousProceeding.doRestartAgain(this.drawStreamBookmark());
      clipOther = AgainGiant;
    }
  }
}
